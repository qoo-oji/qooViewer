# Changelog

このプロジェクトの変更履歴です。フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に準拠しています。

## [Unreleased]

### 追加

- **環境設定「外観」に「アプリ全体」→「外観モード」を追加しました。** macOSのシステム設定とは別に、qooViewerだけをライト外観・ダーク外観に固定できます(システムに従う/ライト/ダーク、既定は「システムに従う」=これまでどおりの動作)。選んだ瞬間に切り替わり、ウインドウ・パネル・メニュー・ダイアログまでアプリ全体に反映されます。ビューアの背景色はこれまでどおり別の設定で、外観モードでは変わりません。
- **サイドパネルの「リソース」モードの「この本」に、「7z の展開バッファ」の行を追加しました。** 7z/cb7 の本を開いている間、7z を読むためのライブラリがメモリに抱え続けている展開済みデータの大きさ(上限の見積り)を表示します。これまではどの項目にも載らず、「説明のつかないメモリ」としてしか見えていませんでした。7z を含まない本では表示されません。

### 変更

- **環境設定「リセット」の「すべて削除」は、削除の実行をアプリの終了時に行うようになりました。** 操作の手順と結果はこれまでと同じです(確認 →「今すぐ終了」→ 次回起動時はまっさらな状態)。開いたままの本が終了までのわずかな間に保存データへ書き込み、消したはずのデータの断片が残ることがないようにするためです。
- **サイドパネル下段の「本の中身」の一覧は、環境設定「一般」の「並び順」に関わらず、常に本のページ順(実際にページを送る順)で並ぶようになりました。** これまでは「フォルダを上に」などの設定が下段にも及び、ページを送る順と一覧の並びが食い違っていました。「並び順」は上段のフォルダブラウザだけに適用されます(設定の ⓘ にもその旨を書き添えました)。

### 修正

- **細工された・壊れた PDF を開いた瞬間にアプリごと落ちることがあった問題を修正しました。** 画像の大きさとして異常な値が書かれている PDF で起きていました。そのような画像は無視して、ページ自体はこれまでどおり表示します。
- **ページ一覧でサムネイルを一度に大量に読み込むと、メモリが一時的に数GB膨らむことがあった問題を修正しました。** PNG などの本を、大きな画面でサムネイルを小さくして開いた場合に起きやすく、環境設定「表示中のサムネイルの拡大画像を先読み」がONだとさらに増えていました。同時に読み込む枚数を抑えるだけなので、読み込みの速さは変わりません。
- **「本ごとの保存データの削除」で、いま開いている本のデータを消すと、その本のウインドウでページを送ったときにアプリが落ちることがあった問題を修正しました。** 環境設定「一般」の「データを保存しておく本の数」による自動整理も、開いている本は対象にしないようにしました(代わりに次に古い本が整理されます)。
- **入れ子の書庫(書庫の中の書庫)を持つ本で、索引と実際の大きさが食い違う細工された書庫が、上限を超えて一時ファイルを書き続ける問題を修正しました。** 展開しながら大きさを数え、上限を超えた時点で打ち切ります。
- **右クリックの「本の書き出し」で同名ファイルの確認が出ている最中に「タブを閉じる」や赤い閉じるボタンでウインドウを閉じると、書き出しが裏で止まったままメモリを抱え続けていた問題を修正しました。** ウインドウを閉じた時点で書き出しを中止します。
- **拡大鏡(ルーペ)用に見開きを1枚に結合した画像を、ルーペを使っていないピンチ拡大中にも作っていた問題を修正しました。** ルーペを表示している間だけ作り、消したときに手放すようにしました。高解像度のスキャンの見開きでは、ピンチ拡大中のメモリが数百MB減ります。ルーペの動作はこれまでと同じです。
- **画像フォルダの本で、極端に大きなファイルや異常な寸法を持つ画像が1枚あるだけで巨大なメモリ確保が走ることがあった問題を修正しました。** 書庫の本と同じ上限で、そのような画像は「読めなかったページ」として扱います。
- **ウインドウを閉じたあとも、マウスやクリックの監視が内部に残ることがあった問題を修正しました。** 「ブックマーク・レイアウトの編集」ウインドウ(開き直すたびに本1冊ぶんの読み込み資源も残っていました)、各種の検索・絞り込み欄、本のウインドウが対象です。
- **サイドパネルの幅や上下の分割、「ブックマーク・レイアウトの編集」の列幅を調整するハンドルにカーソルを乗せたまま画面が切り替わると、リサイズ用のカーソルが戻らなくなることがあった問題を修正しました。**
- **フォルダの中に書庫が並んだ本を開いている途中で「キャンセル」しても、いま読んでいる書庫1つぶんの処理が終わるまで止まらなかった問題を修正しました。**
- **環境設定「画像の表示」の「単ページ扱いの閾値(横÷縦)」を変えても、表示中の見開きの組み合わせが次のページ送りまで変わらなかった問題を修正しました。** 変えた瞬間に表示中のページを組み直します。
- **環境設定「閲覧中の動作」の「スライドショー」の「間隔」を、スライドショーの再生中に変えても、止めて始め直すまで効かなかった問題を修正しました。** 次のページ送りから新しい間隔になります。
- **環境設定「閲覧中の動作」の「プレビューの大きさ」を変えたあと、同じサムネイルのプレビューが前の大きさで読み込んだ画像を引き伸ばしたまま表示されることがあった問題を修正しました。** ページ一覧・サイドパネルのページモード・「ブックマーク・レイアウトの編集」・書き出しウインドウのすべてが対象です。
- **環境設定「キャッシュ」の「メモリに残しておくページ画像」と「メモリに残しておく入れ子の書庫」の変更が、1回前の値で効いていた問題を修正しました。** 例えば 1000MB → 900MB → 800MB と変えると、800MB にした時点でようやく 900MB が効いていました。
- **ホバー表示中のサイドパネルの右クリックから「サイドパネルを隠す」を外したり、環境設定で「サイドパネルを有効にする」を切ったりしたあと、ツールバーとプログレスバーの自動表示がカーソルをウインドウの外へ出すまで効かなくなることがあった問題を修正しました。**
- **書き出しウインドウの「書き出しオプション…」が、環境設定「レイアウト」で既定値(画像ファイルの連番リネーム・除外ページを含める)を変えても、ウインドウを開き直したときに前回の値のままだった問題を修正しました。** ウインドウを開くたびに設定の既定値から始まります。
- **環境設定「一般」の「表示言語」が、環境設定ウインドウの中身にしか効いていなかった問題を修正しました。** 本のウインドウの中身とタイトル、環境設定や各種ウインドウのタイトル、ダイアログや通知の文言も、選んだ言語で表示されるようになります。メニューバーと macOS が出すダイアログは、次回 qooViewer を起動したときから切り替わります(「表示言語」の ⓘ にも書き添えました)。
  - 本を開けなかったときや書き出しに失敗したときのエラー文、お気に入りメニューの「(お気に入りなし)」など、これまで OS の言語で表示されていた文言も表示言語に従います。
  - 別の言語から「システムに従う」へ戻したとき、ウインドウの中身が再起動まで直前の言語のまま残っていた点も直しました。選んだ瞬間に切り替わります。

## [1.39] - 2026-09-02

### 追加

- **環境設定「外観」に「プログレスバーのサムネイル」を追加しました。** プログレスバーにカーソルを合わせたときに並ぶサムネイルの見た目を調整できます。**既定値はこれまでの見た目とまったく同じ**です。
  - **サムネイルの数**(3〜15枚、既定9枚)。サムネイルはバーの幅いっぱいに並ぶため、**減らすほど1枚が大きくなります**。
  - **サムネイルの下の表示**(ファイル名とページ番号/ページ番号/ファイル名/表示しない、既定はファイル名とページ番号)。枚数を増やすとファイル名は潰れて読めなくなるため、出す情報を選べるようにしました。カーソル位置のページ番号だけは、この設定に関わらず常に表示されます。
  - **文字の大きさ**(8〜20pt、既定10pt)。
  - **カーソル位置以外を暗くする**(既定ON=これまでどおり)。OFFにすると、すべてのサムネイルが同じ明るさで並びます。暗いページの中身まで読み取りたいときに使います。
  - **カーソル位置の強調色**(アクセントカラー/赤/黄/緑/白/黒/カスタム、既定アクセントカラー)と**強調の太さ**(1〜8pt、既定3pt)。カーソル位置のサムネイルの枠・光彩・ページ番号に使われます。
- **いま開いているページのサムネイルに、白い破線の枠が付くようになりました。** これまではカーソル位置にしか印が無く、そこから何ページ動かすことになるのかが分かりませんでした。カーソル位置(実線)とは線の種類が違うので、強調色に何を選んでいても見分けられます。

### 変更

- **プログレスバーのサムネイルが、その本のページの縦横比に合わせて表示されるようになりました。** これまでは決まった形の枠に収めていたため、多くの漫画ではサムネイルの左右に黒い余白が残っていました。ページ一覧と同じく、最初に読み込めた数ページの中央値に合わせます。
- **プログレスバーのサムネイルが鮮明になりました。** これまでは表示する大きさに関わらず一定の低い解像度で読み込んでいたため、少しぼやけていました。
- **環境設定「カーソルを合わせたページをプレビュー」を、「閲覧中の動作」から「外観」→「プログレスバーのサムネイル」へ移しました。** 上記の見た目の設定と同じ場所にまとめるためです。設定した内容はそのまま引き継がれます。

## [1.38] - 2026-09-01

### 変更

- **ツールバーの「現在の表示を基準に自動でレイアウト」ボタンのアイコンを変えました。** 左右に分かれた四角のアイコンは「見開き表示に切り替えるボタン」に見えてしまっていたため、自動で処理することを表す杖のアイコンにしました。ボタンの位置と動作はこれまでどおりです。

### 修正

- **zip/cbz のファイル名の文字化けが直るようになりました。** これまでもファイル名の文字コードを自動判定していましたが、判定が当たらず化けたままになることがほとんどでした。
  - ファイル名を1つずつ判定していたため、`表紙.jpg` `扉.jpg` のような短い名前では手がかりが足りず、日本語(Shift_JIS/CP932・EUC-JP)の本でも大半が化けたままでした。書庫の中のファイル名をまとめて1回で判定するようにしたため、短い名前しかない本でも正しく表示されます。
  - 韓国語(CP949)・中国語(GBK・Big5)のファイル名は、これまで事実上まったく直せていませんでした。日本語と同じように直ります。
  - UTF-8 で書かれているのに、その目印が付いていない書庫も正しく読めるようになりました。
  - 1つの書庫の中に日本語と韓国語のように複数の文字コードが混ざっている場合は、どちらか一方に揃えて読むため、もう一方は文字化けしたままになります(UTF-8 との混在は両方とも正しく読めます)。また、ファイル名が1つしか入っていない書庫では、手がかりが足りず直せないことがあります。

## [1.37] - 2026-08-31

### 追加

- **環境設定「一般」に「ページ順」の項目を追加しました。** 「並び順をFinderに揃える」を**ON**にすると、本のページとサイドパネル下段の一覧が、Finderの表示順と同じ名前順になります。**既定はOFF(これまでどおりの並び)**です。
  - これまでは文字コード順で並べていたため、`_` で始まる名前や、`-` と `_` が混ざった名前、大文字と小文字が混ざった名前を含む本で、Finderの表示順とページ順がずれていました(先頭が `Com` か `com` かで並びが丸ごと変わる、という食い違い方をしていました)。ONにするとこれが解消します。
  - 切り替えると、**開いている本もその場で並び替わります**(開き直す必要はありません)。サイドパネルの一覧、「ブックマーク・レイアウトの編集」ウインドウ、書き出しウインドウも同時に追従します。
  - **ブックマークと読書位置は、並び順を変えても同じページ(画像)を指したままです。** ページ番号は新しい並びで振り直されます。シークレットウインドウで開いた本も同じページを指します(保存データには何も書き込まず、そのウインドウでの表示とジャンプだけを新しい並びに合わせます)。
  - **レイアウトを設定した本は、そのレイアウトを作ったときの並び順のままになります。** どのページが見開きで組になるかは並び順で決まるため、並びが変わると見た目が崩れてしまうからです。新しい並び順に合わせたい本は、「ブックマーク・レイアウトの編集」ウインドウの「ページ順を初期化する」で1冊ずつ合わせられます(レイアウトは削除されませんが、見開きの組み合わせは変わることがあります。作り直したい場合は「現在の表示を基準に自動でレイアウト」を実行してください)。
  - EPUBとPDFは、ファイル自身が持つページ順に従うため、この設定の影響を受けません。
  - CBZの書き出しがComicInfo.xmlに書き込むページ番号は、この設定に関わらず常にFinderと同じ名前順で振ります(書き出したファイルの中身が、アプリ側の表示設定に左右されないようにするためです)。ComicInfo.xmlのブックマークを取り込むときも、ページ番号を同じ名前順として解釈します。

### 変更

- **サイドパネル下段(本の中身ブラウザ)の並びが、本のページ順そのものになりました。** これまでは一覧を名前順に並べ直していたため、ページを並べ替えた本ではビューアの順と食い違っていました。除外したページや、本のページに含まれない画像は、一覧の末尾に名前順でまとまります。

### 修正

- **画像を直接開いた本で、右クリックの「本の書き出し」がグレーアウトせず、選べてしまっていたのを修正しました。** そのまま書き出すと、複数の画像を開いていても**1枚目だけが入った1ページのファイル**が、エラーも警告も出ずに出来上がっていました。マニュアルに記載のとおり、グレーアウトするようになります。

## [1.36] - 2026-08-31

### 修正

- **見開き表示中のページに「レイアウト情報を削除する」を実行すると、見開きが単ページ表示に崩れることがあったのを修正しました。** 自動レイアウトなどでページ単位のレイアウト情報を持つ本で、右開きなら画面左(2番目に読む側)のページの情報を削除したときに起きていました。画面右のページ→画面左のページの順に両方の情報を削除した場合も、2回目の削除で同じように崩れていました。左右どちらのページを、どの順序で削除しても、表示中の見開きはそのまま維持されます。
- **「ブックマーク・レイアウトの編集」ウインドウで読み方向を変更しても、その本を開いているビューアに反映されなかったのを修正しました。** これまでは本を開き直すまで反映されませんでした。編集ウインドウを開いたままでも、変更した時点でビューアの表示が切り替わります(見開き表示の強制も同様です)。
  - 読み方向を切り替えても、各ページの「見開き右/見開き左」(画面上の位置)はこれまでどおり書き換わりません。逆の読み方向のときに自動レイアウトした本などでは、切り替えた時点でその食い違い(空白ページの挿入など)がそのまま見えます。これは本を開き直した場合と同じ表示です。

## [1.35] - 2026-08-30

### 修正

- **メニューバーのメニューを開いている最中に、その中身がアプリ側の都合で作り直されて描画が崩れることがあったのを修正しました**(表示メニュー・ウインドウメニューの崩れとして報告)。アプリ内部の状態が変わるたびにメニューバー全体が組み立て直される構造そのものをやめ、メニューを開いている間に起きた変化は、閉じた直後にまとめて反映するようにしました。
  - きっかけになっていたのは「メニューを開いたまま待っている間に起きること」全般です。たとえば: 読み込みに時間のかかる本(低速な外付けボリュームやネットワーク上の本など)の読み込み完了、サイドパネルのリソースモニタの毎秒の更新(計測中でなくても、パネルを表示しているだけで起きていました)、書き出しが終わった直後の保存データの削除と「書き出したあとの動作」、スライドショーが最後のページに達したときの「次の本へ」「本を閉じる」など。
  - メニューを閉じている間の動作は何も変わりません。リソースモニタのグラフは、メニューを開いている間もこれまでどおり毎秒動き続けます。
- **見開き表示している本を右クリックから書き出したあと、その見開きが一時的に単ページ表示になっていたのを修正しました。** 環境設定「レイアウト」でその形式の「保存データ」を「削除する」にしている場合に起きていました。書き出しが終わって保存データを消した時点で、画面に出ていた見開きが片方だけの表示になり、ページを送り直すと見開きに戻る、という見え方でした。表示だけの問題で、書き出したファイルの中身にも、保存データの削除そのものにも影響はありません。
  - 同じ理由で、コントラスト補正を切り替えた直後にも見開きが単ページ表示になることがあったため、あわせて修正しています。

## [1.34] - 2026-08-30

### 追加

- **ツールバーの本のタイトルの右に、現在位置(「(7 / 24)」)を表示するようにしました。** 「現在のページ / 総ページ数」で、タイトルより一段小さく添えて表示します。総ページ数はページレイアウトで除外(非表示)にしたページを除いた数で、プログレスバーの表示と同じ数え方です。見開き表示中でも数字は1つです。
- **画像を右クリックしたコンテキストメニューに「本の書き出し」を追加しました。** サブメニューの「EPUBとして書き出す」「PDFとして書き出す」「CBZとして書き出す」から、**いま開いている本をその場で書き出せます**。メニューバー「File」の3つの書き出しウインドウと違い、**レイアウト・ブックマーク・メタデータを1つも持っていない本でも書き出せます**(あの一覧に載る条件は関係ありません)。
  - 保存先を決めていない場合は、**まず保存先フォルダを選ぶパネル**が開きます(前回その形式で選んだフォルダが初期位置です)。選ぶと**小さなシート**が続けて開き、書き出しウインドウと同じくタイトル・著者名・カバー画像・書き出しオプションを確かめてから「書き出す」を押します。タイトル・著者名の初期値も、カバー画像の既定(本の実質的な先頭ページ)も、書き出しウインドウと同じです。カバー画像はEPUB・CBZのときだけ出ます(シークレットウインドウではグレーアウトします)。
  - 環境設定で**保存先**を決めてあると、パネルもシートも出ず、何も尋ねずに書き出します(進捗シートが出るだけです)。書き出しが終わっても完了の確認は出ず、そのまま下記の「書き出したあと」の動作へ進みます。
  - 同名のファイルが既にある場合だけは、書き出しウインドウと同じく上書きするかを確認します。
  - **読み方向と見開き/単ページは、いま画面に見えているとおりに書き出されます。** ツールバーやメニューで切り替えたまま書き出せば、その状態がEPUB/PDFに入ります(本ごとの設定として保存していない場合でも反映されます)。
  - 画像を直接開いた本(Finderで画像ファイルを選んで開いた「その場限りの本」)ではこのメニューは選べません。
- **環境設定「本」グループに「レイアウト」画面を追加しました。** ページレイアウトの自動計算と、本を1冊書き出すときの既定の振る舞いをまとめてあります。
  - **レイアウトの保存データが無い本を開いたときに、本全体を自動レイアウトできます。** 「何もしない(既定)/1ページ目を単ページに/1ページ目を見開きに」から選べます。計算の中身は「現在の表示を基準に自動でレイアウト」と同じで、起点が本の1ページ目になります。**すでにレイアウトの保存データがある本には何もしません**(EPUB・PDFは初めて開いた時点でファイル側のレイアウトを読み込むため、これに含まれます)。
  - **形式(EPUB/PDF/CBZ)ごとに、保存先を決めておけます。** 「毎回確認(既定)/保存先を設定」から選べます。「保存先を設定」を選ぶとフォルダを選ぶダイアログが開き、以降その形式の書き出しは、右クリックからでも File メニューの書き出しウインドウからでも、すべてそのフォルダへ行きます(書き出しウインドウの「書き出しを開始」ボタンもフォルダを尋ねなくなり、ボタンの「…」が消えます)。
  - **形式ごとに、書き出しオプションの既定値を決めておけます**(画像ファイルの連番リネーム・除外ページを含めるか・CBZのVolume要素)。ここで決めた値が、書き出しウインドウと右クリックの書き出しシートを開いた直後の値になります。それぞれの画面のトグルはこれまでどおり残っていて、その1回限りの変更として使えます。
  - **形式ごとに、書き出しが終わった本の保存データと履歴を削除できます**(それぞれ「何もしない(既定)/削除」)。保存データはページレイアウト・ブックマーク・メタデータ・読書位置が対象で、**お気に入りは残ります**。この削除は取り消せません。
  - **書き出したあとの動作を決めておけます。** 「何もしない(既定)/次の本の1ページ目へ/次の本へ/本を閉じる/ウェルカム画面へ戻る/毎回確認」から選べます。意味は環境設定「閲覧中の動作」の「最後のページで」と同じです。
  - これらを組み合わせると、**新しい本を開く(自動でレイアウトされる)→ 右クリックから書き出す → 次の本が開く**、という流れを操作なしで繰り返せます。

### 変更

- **メニューバー「File」の書き出し3項目の名前を、開くウインドウの名前と同じ形に揃えました。** 「EPUBとして書き出す…」→「**EPUBの書き出し…**」(PDF・CBZも同様)。上記の右クリックの「EPUBとして書き出す」(いま開いている本をその場で書き出す)と語形が重なり、「…」の有無でしか区別できなかったためです。動作は変わりません。
- **環境設定「閲覧中の動作」の「最初/最後のページで」を、「最初のページで」と「最後のページで」の2つに分けました。** 最初のページと最後のページで別々の動作を選べます(「最後まで読んだら次の本へ、でも最初のページでは何もしない」のような組み合わせができます)。どちらも既定は「何もしない」で、これまでの設定は分離後の両方へそのまま引き継がれます。ここでいう「前のページへ」「次のページへ」は物語的な向きのことで、右開きの本で画面右のページへ移動する操作なども含みます。
  - **「最初のページで」**は、ループする / 前の本の最後のページへ / 前の本へ / 何もしない / 毎回確認 の5つから選べます。
  - **「最後のページで」**は、ループする / 次の本の最初のページへ / 次の本へ / **本を閉じる** / **ウェルカム画面へ戻る** / 何もしない / 毎回確認 の7つから選べます。「本を閉じる」はこのタブを閉じます(タブが1枚だけならウインドウごと閉じます)。「ウェルカム画面へ戻る」はウインドウを残したまま、本だけ閉じます。
  - **「毎回確認」**を選ぶと、そのつど残りの選択肢がシートで並び、その場で選べます。Escキーやシートの外側のクリックで閉じた場合は「何もしない」と同じ結果になります。
  - **「前の本の最後のページへ」「次の本の最初のページへ」が、名前のとおりに動くようになりました。** これまでは「次の本へ」とまったく同じ動作で、移動先の本を以前どこまで読んでいたか(読書位置)からの再開になっていました。これからは読書位置や環境設定「開始ページ」より優先して、その本の端から読み始めます。

## [1.33] - 2026-08-29

### 追加

- **履歴から、任意の本だけを削除できるようになりました。** これまでは「すべて消去」しかなく、見られたくない1冊だけを取り除くことができませんでした。サイドパネルの「履歴」モードの行と、ウェルカム画面の「最近開いた本」の行を右クリックすると「履歴から削除」が並びます。消えるのは履歴の項目だけで、ファイル自体にも、その本の保存データ(お気に入り・ブックマーク・ページレイアウト設定・メタデータ・読書位置)にも触れません。対象が1件なので確認ダイアログは出ません(もう一度その本を開けば履歴に戻ります)。
- **環境設定「リセット」に「履歴の削除…」を追加しました。** 開いた本の履歴を一覧するウインドウが開き、チェックボックスで複数選んでまとめて削除できます。「保存データの削除…」とまったく同じ形のウインドウです(下記参照)。
- **ページ一覧の上でトラックパッドをピンチすると、サムネイルの大きさを変えられるようになりました。** パネル上部のスライダーへ手を伸ばさずに、見ているその場で大きさを決められます。変わる値はスライダー・環境設定「外観」→「ページ一覧」の「サムネイルのサイズ」と同じもので、スライダーのつまみもその場で動き、指を離した大きさが次に開いたときにも引き継がれます(80〜320ptの範囲で頭打ちになります)。ページ一覧を開いている間は、ピンチで背後のページが拡大されることはありません。
- **ツールバー・プログレスバー・サイドパネル・ページ一覧パネルの空きスペースを右クリックできるようになりました。** ボタンや一覧の無い部分を右クリックすると、その部分に対する操作がその場で行えます。
  - ツールバー・プログレスバー・サイドパネルには「ツールバーを隠す」「プログレスバーを隠す」「サイドパネルを隠す」が並びます(メニューバー「View」の同名の項目と同じ設定で、チェックマークが今の状態を表します)。自動的に隠れている状態でカーソルを近づけて一時的に表示させたときも、同じように右クリックできます。
  - どの部分にも「調整…」が並び、選ぶと環境設定「外観」が**その部分のセクションを先頭に表示した状態**で開きます。色や濃さを変えたいときに、画面を探し回らずに済みます。
  - ページ一覧パネルの余白は「調整…」だけで(隠す設定を持たないパネルのため)、飛び先は「ページ一覧」のセクションです。**右クリックではパネルは閉じません**(余白のクリックで閉じるのは左クリックのときだけです)。
  - ボタンや一覧の行の上で右クリックした場合は、これまでどおりそちらのメニューが優先されます。
- **画像を右クリックしたコンテキストメニューから「ページ一覧を表示」を開けるようになりました。** お気に入りの操作の上に、独立したグループとして並びます。ツールバーのグリッドアイコン・tキー・メニューバー「View」→「Show Page Grid」と同じ動作(表示/非表示のトグル)です。
- **ツールバー・プログレスバー・サイドパネルを隠しているとき、ウインドウの端にカーソルを近づけてから表示されるまでの待ち時間を設定できるようになりました**(0〜2.0秒、0.1秒刻み、既定0.0秒 = これまでどおりすぐに表示)。別のウインドウやメニューバーへカーソルを動かしたいだけなのに、通りすがりで隠していた部分が反応してしまうのを防げます。
  - 環境設定「外観」の、それぞれの面のセクション(「ツールバー」「プログレスバー」「サイドパネル」)にあり、**3つを別々に設定できます**。
  - 待ち時間が経つ前にカーソルを端から離した場合や、メニューバー・他のアプリのウインドウ・ウインドウの外へ抜けた場合は、表示されないまま終わります。
  - 隠れるほうの動作は変わりません(これまでどおりすぐに隠れます)。
  - フルスクリーン中(ツールバー・プログレスバーが常に隠れている状態)にも効きます。
- **ウインドウの背後(デスクトップや他のウインドウ)がうっすら透けるすりガラスを選べるようになりました。** 環境設定「外観」に「ウインドウの背後を透かす」スイッチが加わり、ONにすると、これまで不透明だった部分が背後をぼかして透かすすりガラスになります。
  - 対象は4か所 ―― **隠していない(常に表示の)状態**のツールバー・プログレスバー・サイドパネルと、**ウェルカム画面**です。それぞれの面のセクションで別々にON/OFFできます。
  - **既定はすべてOFFで、設定を変えなければ見た目はこれまでと変わりません。**
  - ONにした面は、同じセクションの「すりガラスの濃さ」「背景色」「背景色の濃さ」「文字の影」に従います(これまで常に表示中のツールバー・プログレスバーには「背景色」しか効きませんでしたが、ONにすると「すりガラスの濃さ」も効くようになります)。
  - 隠しているときに画像の上へ浮かぶ帯・パネルには影響しません(従来どおり、背後のページをぼかすすりガラスのままです)。
- **ウェルカム画面が、環境設定「外観」で見た目を設定できる面に加わりました。** 他の面と同じ4項目(すりガラスの濃さ・背景色・背景色の濃さ・文字の影)を設定できます。効くのは上記の「ウインドウの背後を透かす」をONにしたときだけで、OFF(既定)ならこれまでどおり何も重ねません。

### 変更

- **「本ごとの保存データを削除」ウインドウで、複数の本をまとめて削除できるようになりました。** 各行の左端にチェックボックスが付き、選んだぶんをタイトルバーの「選択した項目を削除…」で一度に削除できます。
  - タイトルバーの「すべて選択」は、そのとき一覧に出ている行だけが対象です(絞り込みや検索で隠れている行の選択には触れません)。これまでの「表示中をすべて削除…」は、「すべて選択」→「選択した項目を削除…」に置き換わりました。
  - **選択は絞り込みや検索を変えても残ります。** 別々の条件で絞り込みながらチェックを積み上げて、最後にまとめて削除できます。今いくつ選んでいるかは常にウインドウ下部に表示され、確認ダイアログにも冊数が出ます。
  - 各行の右端の「削除」ボタンで1冊だけ削除する操作は今までどおりです。
- **「保存データの削除」「履歴の削除」の2つのウインドウを、ほかの一覧ウインドウと同じ形に揃えました。** 「メタデータの編集」「EPUB/PDF/CBZの書き出し」などと同じく、操作するものはタイトルバー(ツールバー)に並び、一覧はその下へスクロールして潜ります。
  - 絞り込み・「すべて選択」・「選択した項目を削除…」・検索欄がタイトルバーへ移りました。検索欄はmacOS標準のもので、ウインドウを狭くすると虫眼鏡のアイコンに畳まれます。
  - **「何件中いくつ」の表示を、ウインドウ下部の中央に統一しました。** これまでウインドウによって中央だったり左右だったりしていた位置を、「メタデータの編集」も含めて揃えています。
  - **列の幅が、一覧に並ぶ文字列やボタンの幅に合わせて自動的に調整されるようになりました。** 「ファイル」列・「保存データ」列・削除ボタンの列が中身に対して広すぎ、ウインドウを広げると一緒に間延びしていたのが直り、そのぶんパスの列を広く使えます。**タイトル行の区切り線をドラッグして列の幅を変えられるようにもなりました**(ほかの一覧ウインドウではできていた操作です)。
  - 右下にあった「閉じる」ボタンを無くしました(独立したウインドウのため、タイトルバーの閉じるボタンか Cmd+W で閉じます)。ウインドウの最小サイズも2つで揃えています。
- **環境設定「リセット」の「整理」を、同じ形の2つのボタンにまとめました。** 「保存データの削除…」と「履歴の削除…」が幅を揃えて並び、説明文も1つにまとまりました。どちらも一覧のウインドウを開くだけで、この画面では何も削除されません。
  - **ウインドウとボタンの名前を、対になるように付け直しました。** 「本ごとの保存データを削除」は「保存データの削除」になり、履歴側は「履歴の削除」です。ほかの一覧ウインドウ(「メタデータの編集」「お気に入りの整理」)と同じ「〜の削除」という形で揃えてあり、環境設定のボタンの名前もウインドウ名と同じにしてあります。
  - 「「最近開いたファイル」の履歴を消去…」ボタンは無くなりました。同じことは「履歴の削除…」のウインドウで「すべて選択」→「選択した項目を削除…」とすれば行えます。履歴をひと息に消す操作は、サイドパネル「履歴」モードのゴミ箱ボタンと、メニューバー「File」→「Open Recent」の末尾に残っています。
- **アプリ全体で、同じものを指す言葉を1つに統一しました。** 同じ機能や同じデータが画面によって別の名前で呼ばれていたのを、次のように揃えています(表示言語が日本語のときの文言です)。
  - **「エクスポート」「出力」→「書き出し」**: 1つの書き出しウインドウの中に「EPUBをエクスポート」「EPUB として書き出すには」「EPUB出力を開始…」「出力オプション…」が同時に出ていました。ウインドウ名は「EPUBの書き出し」、ボタンは「EPUBの書き出しを開始…」「書き出しオプション…」に揃えています。「出力先」も「保存先」になりました。
  - **「インポート」「取り込む」→「読み込み」**、**「消去」「取り除く」→「削除」**。
  - **「読書履歴」→「読書位置」**: 「履歴」という語が「開いた本の履歴」と「本ごとの最後に読んだページ」の2つの意味で使われていたため、後者を「読書位置」と呼び分けます。
  - **「最近開いたファイル」→「最近開いた本」**: フォルダもzip/cbzもPDFもEPUBも、このアプリでは同じ「本」です。履歴に並ぶものの数え方も「件」から「冊」に揃えました。
  - 検索欄を空にするボタンと、履歴の全削除の確認ボタンが、どちらも「クリア」という同じ文言でした。確認ボタンを「削除」に変えて分けています。
- **書庫の中のフォルダや、書庫の中の書庫に入っている画像に、本の中のどこにあるページなのかが表示されるようになりました。** 章ごとに書庫やフォルダへ分けてある本では、どの章にも `001.jpg` から番号が振り直されていることが多く、ファイル名だけでは一覧に同じ名前がいくつも並んでどの章のページか分かりませんでした。ファイル名と一緒に、本の直下から見たそこまでの道順(例: `ch02.zip/ch02`)を、ファイル名より一段控えめな文字で添えます。
  - 表示されるのは、プログレスバーのフィルムストリップ、ページ一覧(サムネイルの下を「ファイル名」にしているとき)、サイドパネルの「ページ」モード、「ブックマーク・レイアウトの編集」、EPUB/CBZ書き出しウインドウの「Cover」列とカバー選択の一覧、およびそれぞれの拡大プレビュー・ツールチップです。
  - **本の直下に置かれている画像には何も付きません。** 中にフォルダを持たない普通のcbz/cbrや、直下に画像を並べただけのフォルダの見た目はこれまでどおりです。
  - **EPUBには付きません。** EPUBの画像は `OEBPS/Images/` のような決まった1か所にまとめて置かれているのが普通で、どのページにも同じ道順が付くだけになり、ページを見分ける役には立たないためです。
  - EPUB・CBZの書き出し中に画像を読み出せなかった場合のエラーも、どのページのことなのかがこの道順で示されるようになりました。
- **EPUB・PDF・CBZの書き出しウインドウの一覧を、macOS標準の表に置き換えました。** タイトル行の区切り線が一覧本体の区切り線とずれていたのが直り、「File Name」列を含むすべての列を、区切り線のドラッグで自由に広げたり狭めたりできるようになりました(Finderのリスト表示と同じ操作です)。
  - タイトル行にあった全選択のチェックボックスは、ウインドウ下部の左端へ「すべて選択」として移りました(動作は同じです)。
- **書き出しオプションを「出力オプション…」ボタンにまとめました。** これまで一覧の下に常時並んでいたチェックボックス類は、このボタン(タイトルバーの右端)を押したときだけポップオーバーで開きます。一覧の下の空きスペースが無くなり、同じウインドウの大きさでより多くの本が一度に見えるようになりました。ボタンの名前と位置は、EPUB・PDF・CBZの3つのウインドウで共通です。
- **「ブックマーク・レイアウトの編集」ウインドウの上部を整理しました。** ボタンの見出しに使っていた文字を減らし、ウインドウを狭くしても操作できるようにしています。
  - 左ペインの**並べ替え**が、「名前(A→Z)」「名前(Z→A)」…と6項目を並べる形から、**基準(名前・追加日時・更新日時)と昇順/降順を別々に選ぶ形**になりました。組み合わせは今までと同じで、選んでいる内容は今までの設定がそのまま引き継がれます。
  - 左ペインの**絞り込みを並べ替えのすぐ左**へ移し、並べ替えは上下矢印のアイコンだけのボタンになりました。絞り込みも「絞り込み」という文字を無くし、**マークを選択中の値の先頭**に付ける形にしています(右ペインの絞り込みも同じ見た目に揃えました)。
  - 右ペインの読み方向のドロップダウンから、添えていた**「読み方向」という見出しを削除**しました(選択肢が「右開き」「左開き」なので、見出しが無くても分かるためです)。あわせて、右ペイン上部の並びを「ページの順序を入れ替える上下矢印(左端)」「一括操作…・読み方向・絞り込み(右端)」に整理しています。
  - **ウインドウの最小幅が、上部のボタンがちょうど収まる幅になりました。** これまでは決め打ちの数値だったため、表示言語によっては「一括操作…」の文字が潰れたり、逆に余白が余ったりしていました。文字を消してもツールチップとVoiceOverでは今までどおり名前が分かります。
- **「メタデータの編集」ウインドウの上部を、macOS標準のツールバーへ載せ替えました。** 検索欄はタイトルバー右端の標準の検索欄になり(ウインドウを狭くすると虫眼鏡のアイコンに畳まれます)、一覧はツールバーの下へスクロールして潜るようになりました。
  - 「ファイル名フォーマットの編集…」「巻数フォーマットの編集…」「除外文字列の編集…」の3つのボタンは、ツールバーの**「フォーマットの編集」**という1つのメニューにまとまりました(開けるダイアログの内容は今までと同じです)。
  - 「95冊 / 157冊にメタデータが登録されています」の表示は、ウインドウ下部の中央に置いた細いバーへ移りました。
  - 右下にあった「閉じる」ボタンを無くしました(独立したウインドウのため、タイトルバーの閉じるボタンか Cmd+W で閉じます)。
  - **各列の幅が、一覧に並ぶ文字列やボタンの幅に合わせて自動的に調整されるようになりました。** 「巻数」列と登録/削除ボタンの列が中身に対して広すぎたのが直り、そのぶんファイル名などの長い列を広く使えます。開いたあとはタイトル行の区切り線のドラッグで自由に変えられます。
- **EPUB・PDF・CBZの書き出しウインドウの上部を、macOS標準のツールバーへ載せ替えました。** 「すべて選択」と「出力オプション…」がタイトルバーへ移り、一覧はツールバーの下へスクロールして潜るようになりました。3つのウインドウとも同じ形です。
  - **「すべて選択」はチェックボックスからボタンになりました。** すべて選択されている間はボタンが点灯します(押すと全選択、点灯中に押すと全選択解除、という動作は今までと同じです)。
  - ウインドウ下部には「キャンセル」と「出力を開始」だけが残りました。並びと Return / Esc の割り当ては今までどおりで、一覧がこのボタン行の下へ透けて潜るようになっています。
- **「ブックマーク・レイアウトの編集」ウインドウの操作ボタンを、タイトルバー(ツールバー)へ移しました。** どちらのペインへの操作かが位置で分かるよう、**左ペインへの操作は左ペインの真上、右ペインへの操作は右ペインの真上**に、それぞれ中央揃えで並びます。
  - 左ペイン側: 本の**絞り込み**と**並べ替え**。検索欄は今までどおり一覧のすぐ上に残ります。
  - 右ペイン側: ページの順序を入れ替える**上下矢印**、**一括操作…**、**読み方向**、ページの**絞り込み**。読み込み中の進捗表示もここに出ます。
  - 2つある絞り込みは、カーソルを合わせたときの説明が「本を絞り込む」「ページを絞り込む」と別々になりました。
  - ボタンをタイトルバーへ移したぶん、**タイトルバーにウインドウ名は表示しなくなりました**(ウインドウ名は「ウインドウ」メニューで確認できます)。
  - ウインドウを狭くしてボタンが並びきらない場合は、macOS標準の「≫」ボタンに畳まれ、そこから選べます。これに伴い、ウインドウの最小幅が「右ペインのボタンがすべて収まる幅」ではなくなり、以前より狭くできるようになりました。
- **「お気に入りの整理」ウインドウの「フォルダを上に表示」と「並べ替え」を、タイトルバー(ツールバー)の右端へ移しました。** 一覧の上に常時並んでいた行が無くなり、そのぶん一覧が広く使えます。
  - 「フォルダを上に表示」はチェックボックスからボタンになりました(ONの間は点灯します。動作は今までと同じです)。
  - **並べ替えが「ブックマーク・レイアウトの編集」ウインドウと同じ形になりました。** 上下矢印のアイコンのボタンを押すと、基準(名前・追加日時・更新日時)と「昇順」「降順」が別々のグループとして並びます(これまでは「名前(A→Z)」…と6項目が平らに並んでいました)。選べる組み合わせと現在の設定は今までと同じです。
- **ブックマークの一括リネームが、独立したウインドウから「ブックマーク・レイアウトの編集」ウインドウ上のシートになりました。** どの本を対象にしているのかが常に元のウインドウと結び付いて分かるようになり、リネームを終える(または「キャンセル」する)まで編集ウインドウの操作に戻れない、という形になりました。
  - 右下に「キャンセル」と「適用」が並びます(以前はウインドウのタイトルバーの閉じるボタンでやめる形でした)。
  - 「最後のブックマーク」の下にあった説明文を削除しました。
  - **プレビューの表示を整えました。** 矢印とリネーム後の名前の左端が縦一列に並ぶようになり、どのブックマークがどう変わるのかが読み取りやすくなりました。
- **環境設定のドロップダウンを、macOS 26 ではシステム標準の見た目にしました。** 値を囲んでいた枠が無くなり、値の文字の右にシェブロンのボタンが付く、macOS 26 のシステム設定と同じ形になります(対象は環境設定の15か所のドロップダウンだけで、チェックボックス・スライダー・色の行は変わりません)。macOS 15 では、押せることが分かるように今までどおり枠線付きで表示されます。
- **一覧を持つウインドウで、タイトルバーと一覧の境目の描き方を揃えました**(macOS 26 以降)。対象は「メタデータの編集」「EPUB/PDF/CBZの書き出し」「ブックマーク・レイアウトの編集」「お気に入りの整理」の各ウインドウです。これらは一覧がタイトルバーの下へスクロールして潜る作りのため、その境目を4つとも同じ描き方に統一しました(見た目の差はごくわずかです)。macOS 15 では従来どおりです。
- **ウェルカム画面の「最近開いた本」「最近のお気に入り」の列幅が、ファイル名の長さとウインドウの幅に合わせて自動的に広がるようになりました。** これまでは幅が固定で、少し長い名前はすぐに「…」で省略され、何の本なのか分からなくなっていました。実際に並んでいるファイル名と、その右に付く形式バッジ(CBZ・EPUB・フォルダなど)の幅を測り、ウインドウに収まる範囲でいっぱいまで広げます。2つの列は必要な幅の比で分け合うので、長い名前が並んでいるほうの列が広くなります。それでも収まりきらない名前は、これまでどおり中央を省略して表示します。
- **使い方マニュアル(MANUAL.md)を更新しました。**
  - 上記の「ブックマーク・レイアウトの編集」ウインドウの変更について、「8. ブックマーク」の「ブックマーク・レイアウトの編集」の、左ペインの絞り込み・並べ替え、右ペイン上部の並びと絞り込みの説明を書き直しました。
  - 書き出しウインドウの上記の変更について、「13. EPUBとして書き出す」の一覧・列幅・オプションの説明を書き直し、「14. PDFとして書き出す」「15. CBZとして書き出す」のオプションの説明も「出力オプション…」ボタンからの開き方に合わせました。
  - ページの場所の表示について、「6. ページ一覧」に「画像が本の中のどこにあるかの表示」の項を追加し、表示される各画面の説明からもそこを参照するようにしました。
  - 上記の待ち時間について、「25. 環境設定」の「すりガラスの面」に項目と「表示までの時間」の項を追加し、ツールバー・プログレスバー・サイドパネル・フルスクリーンの各説明からも参照するようにしました。
  - 上記の右クリックについて、「3. 画面の基本構成」に説明を追加し、「6. ページ一覧」に「パネルの余白の右クリック」の項と、右クリックメニューからの開き方を追加しました。「25. 環境設定」の「外観」からも参照するようにしています。
  - 上記のピンチについて、「6. ページ一覧」の「サムネイルのサイズ・間隔・余白」に説明を追加し、「5. 表示モード」の「ピンチ拡大」と「マウスホイールでのスクロール量」からも棲み分けが分かるようにしました。
  - 上記の「メタデータの編集」ウインドウの変更について、「11. メタデータ」にウインドウの構成の説明を追加し、検索欄・列の幅・推測の規則の開き方の説明を書き直しました。あわせて「7. サイドパネル」の絞り込み欄の説明から、このウインドウの検索欄への参照を分けています。
  - 上記の書き出しウインドウの変更について、「13. EPUBとして書き出す」の「すべて選択」と「出力オプション…」の説明を新しい置き場所に合わせ、「15. CBZとして書き出す」の参照もそれに揃えました。
  - 上記の「ブックマーク・レイアウトの編集」ウインドウの変更について、「8. ブックマーク」の左ペイン・右ペインの操作の説明を、タイトルバーへ移した新しい置き場所に合わせて書き直しました。
  - 上記の「お気に入りの整理」ウインドウの変更について、「9. お気に入り」の並べ替えと「フォルダを上に表示」の説明を、タイトルバーへ移した新しい置き場所と新しい選び方に合わせて書き直しました。
  - 上記の一括リネームの変更について、「8. ブックマーク」の「ブックマークの一括リネーム」を、シートになったことと「キャンセル」「適用」ボタンに合わせて書き直しました。
  - 上記のドロップダウンの見た目について、「25. 環境設定」の冒頭に説明を追加しました。
  - 上記の履歴からの個別削除について、「7. サイドパネル」の「履歴モード」と「9. お気に入り」の「ウェルカム画面での表示」に、右クリックの「履歴から削除」の説明を追加しました。
  - 上記の2つの削除ウインドウについて、「25. 環境設定」の「リセット」を書き直しました。「整理」の2つのボタン、2つのウインドウが共有する形(チェックボックス・すべて選択・選択した項目を削除・検索欄・列幅)、それぞれのウインドウ固有の内容、という構成にしています。
  - 上記の言葉の統一について、マニュアル中で引用しているアプリの文言(「画像のエクスポート」「出力オプション…」「出力先」「読書履歴」「最近開いたファイル」など)を、新しい文言に合わせて書き換えました。「16. 画像のエクスポート」の見出しも「16. 画像の書き出し」になっています。

### 修正

- **「ブックマーク・レイアウトの編集」ウインドウに、青い線が一瞬現れることがあったのを修正しました。** ウインドウを開いた直後や、ウインドウ幅・ペイン幅を変えている間に、タイトルバーのあたりへ横一本の青い線が出たり、右ペインの端に縦一本の青い線が出たりすることがありました(マウスを動かすと消えるものでした)。右ペインの一覧に付いていたフォーカスの枠が、表示位置が決まる前の一瞬の形のまま描かれていたためです。表示だけの問題で、カーソルキーでのページ選択など動作は変わりません。

## [1.32] - 2026-08-26

### 追加

- **本を読み込んでいる間、進み具合とキャンセルボタンが表示されるようになりました。** これまでは読み込みが終わるまで画面に何も出ず、入れ子の書庫を含む大きな本では固まったように見えていました。「10 / 13 個の書庫を読み込みました」のように件数で表示します(中に何冊の書庫が入っているかは開いてみるまで分からないため、割合ではなく件数です)。「キャンセル」ボタンか Esc キーで中止でき、中止した時点で使いかけの一時ファイルも削除されます。一瞬で開き終わる本では表示されません。
- **環境設定「キャッシュ」に「メモリに残しておく入れ子の書庫」を追加しました**(0〜1024MB、既定128MB)。章をまたいで行き来したときの速さと、メモリ・一時ファイルの使用量のバランスを決められます。ZIP形式の書庫だけがメモリに置け、RAR形式・7z形式は一時ファイルになりますが、その量もこの設定から決まります。
- **サイドパネル「リソース」モードの「この本」に「入れ子の書庫」の行を追加しました。** いま開いている入れ子の書庫の数と、そのうちメモリに置いているぶんの大きさが分かります(入れ子の書庫を使っていない本では表示されません)。

### 変更

- **書庫の中に書庫が入っている本の開き方を作り直しました。** これまでは本を開いた瞬間に中の書庫を**すべて**ディスクへ展開し、本を閉じるまで置いたままにしていました。今は必要になった書庫だけをその都度開き、使わなくなったものから順に手放します。
  - 1.2GBの本(75MBの章が16個)での実測で、開いた直後にディスクへ置かれる量が **1200MB → 0MB**(ZIP形式)/ **75MB**(RAR形式・7z形式)になりました。読み進めている間も設定した上限で頭打ちになり、本の大きさに比例して増えることがなくなりました。
  - ZIP形式(.zip / .cbz)の入れ子はメモリ上で開けるため、**一時ファイルを一切作らなくなりました**。
  - 本を閉じたとき・別の本へ移ったときに、使っていたメモリと一時ファイルをその場で解放するようになりました。
  - 読み込み中に一時的に使うメモリのピークも下がりました(同じ本で 100MB → 25MB)。
- **同じ本を2回目以降に開くときの待ち時間が大幅に短くなりました。** 入れ子の書庫を含む本については、前回作ったページの一覧を覚えておき、本体のファイルが更新されていなければそのまま使います(上記の本で 0.85秒 → 0.003秒)。ファイルを差し替えた場合は自動的に読み込み直します。この記録は、シークレットウインドウでは作らず、読みもしません。
- **コンテキストメニュー「情報を見る」の「場所」が、入れ子の書庫の中のページでも意味のあるパスを示すようになりました。** これまでは一時ファイルのパス(`…/tmp/qooViewer-1234/<英数字の羅列>.cbz`)が表示されていました。
- **画面に出る文言の表記を統一しました。** 同じ操作なのに場所によって呼び方が違っていたものを揃えています(「新しいタブ/ウインドウ」→「新規タブ/ウインドウ」、ページレイアウトの「単独ページ」→「単ページ」、Moveメニューの「先頭へ移動」→「最初のページへ移動」、名前変更ダイアログの見出し、本が持っているデータを示すアイコンの説明など)。疑問符・括弧の全角と半角、数字と「冊」のあいだの空白といった細かい揺れも揃えました。動作は変わりません。
- **README と使い方マニュアル(MANUAL.md)を、現在の実装に合わせて更新しました。**
  - READMEの機能一覧に、v1.26〜v1.31 で追加した機能(サイドパネルの「リソース」モード、環境設定の「キャッシュ」画面、一覧の右クリックからの開き方の選択、同じ本を二重に開かない動作、マウスにだけ割り当てられる「タブを閉じる」「ウインドウを閉じる」「qooViewerを終了」、サムネイルの右クリックからのブックマークの付け外し、「本の移動をブラウザの並び順に合わせる」設定、すりガラスの面の「文字の影」)を追記しました。あわせて、以前から使えたのに載っていなかった機能(表示モードの「横幅に合わせる(単ページ)」、トラックパッドのピンチ拡大、2本指スクロールの反転)も追記しています。
  - READMEのページレイアウトの説明が、今は無いメニューバー「Layout」を指していたのを「Edit」に直しました。サイドパネルのモード数も「4モード」から「5モード」に直しています。
  - READMEのビルド手順が、このリポジトリに存在しないフォルダ・ファイルを指していたのを、実際の構成に合わせて書き直しました(ソースコードの場所、アプリアイコンの差し替え方、String Catalog の場所、rarのファイル名が文字化けする場合の対処)。環境設定を「タブ」と呼んでいた箇所も、現在の2ペイン構成に合わせています。
  - マニュアルの「すりガラスの面」の数が、章によって4つと5つで食い違っていたのを5つに揃えました。既定のキー割り当ての一覧に載っていなかった「拡大鏡を表示/非表示」(既定 l キー)を加え、コントラスト補正にもキーを割り当てられることを追記しました。

### 修正

- **細工された書庫・EPUBを開いたときに、アプリが落ちたり大量のメモリを消費したりする問題を修正しました**(セキュリティ監査で発見)。中身のサイズを偽った書庫(ごく小さいファイルが「展開すると数GB」だと申告しているものなど)を開くと、そのページを表示・先読みしようとした時点で異常終了することがありました。展開後の大きさが妥当な範囲を超えるページ・EPUB内部データは読み込まないようにし、正常な本の表示には影響しません。
- **ページの一覧を長くスクロールすると、通り過ぎたページのサムネイルがメモリに溜まり続ける問題を修正しました**(コード監査で発見)。ページ一覧パネル・サイドパネルの「ページ」モード・「ブックマーク・レイアウトの編集」の3つの一覧すべてで、一度表示したサムネイルがキャッシュの上限の外側に残り続け、ページ数の多い本ではGB単位に達することがありました。一定量(一覧ごとに128〜256MB)を超えた時点でまとめて解放します。解放の瞬間もスクロール位置は保たれ、見た目は変わりません。
- **本を切り替えたあとも、直前まで画面に表示していたページ画像がしばらくメモリに残っていた問題を修正しました**(v1.31の修正の取りこぼし。コード監査で発見)。特に拡大鏡(ルーペ)やピンチ拡大を使っていた場合は、その高解像度画像(1枚で数百MBになりうる)も一緒に残っていました。本を切り替えた時点で確実に解放します。
- **「ブックマーク・レイアウトの編集」で左の一覧から別の本を選んだあと、前の本のサムネイルなどのメモリキャッシュがしばらく残ることがある問題を修正しました。** 本を選び替えた時点で解放します。
- **スライドショー中にウインドウを閉じたとき、裏でページ送りの処理がしばらく動き続けることがある問題を修正しました。**

## [1.31] - 2026-08-24

### 追加

- **「次の本へ」「前の本へ」と「同じフォルダのファイルを開く」の並び順を、サイドパネルのフォルダブラウザに合わせられるようになりました**(環境設定「一般」→「本の移動をブラウザの並び順に合わせる」、既定OFF。ユーザー要望)。ONにすると、パネル上部の「並べ替え」ボタンで選んだ基準(名前・サイズ・種類・作成日・変更日)と昇順・降順、および「並び順」(フォルダを上に/名前順に混在)が、そのまま本の移動にも使われます。
  - ONの間、前後の本への移動は「同じ種類の本の間だけ」という絞り込みをやめ、**パネルに見えているとおりの順**でフォルダの本とファイルの本を混ぜて1冊ずつ辿ります。
  - 並べ替えを変えると、本を開き直さなくてもその場で反映されます。
  - この設定はサイドパネルの機能そのものに従属するため、「サイドパネルを有効にする」がOFFの間はグレーアウトし、並び順は名前順のままになります。

### 変更

- **本の並び順の「名前順」が、Finderやアプリ内の他の一覧と同じ規則になりました。** これまで「次の本へ」「前の本へ」と「同じフォルダのファイルを開く」だけが別の規則で並んでおり、大文字で始まる本がすべて小文字で始まる本より先に来ていました。大文字小文字を区別しない、Finderと同じ並びに揃えてあります。
- **サイドパネル「リソース」モードの「この本」の表示を作り直しました。**
  - 「先読み」の1行を、**「先読みページ」**と**「過去のページ」**の2行に分けました。以前はどちらも同じ数字に含めていたため、環境設定が10ページなのに「前 52」のような桁違いの数字が出て、異常のように見えていました。
  - 「先読みページ」の帯は、環境設定「前後に先読みするページ数」の範囲ちょうどを1マス1ページで並べます。**すべてのマスが塗られていれば先読みが追いついている**ことが一目で分かり、数値も「後 10/10 · 前 10/10」と分母がそのまま設定値になります。
  - 帯の並びが**本の読み方向に合わせて反転**するようになりました。右開きの本では、後のページが画面の左側に並びます。
  - **見開き表示のときは、表示中の2ページをまとめて現在のページとして塗ります。** 以前は片方だけを現在として扱っており、隣に並んでいる相方が先読みのマスに紛れていました。
  - 「過去のページ」は、先読みの範囲より外側にメモリへ残っている読み終えたページ数です。ゼロでも数十でも正常です。
  - 「ページ画像」の件数に「ページ」を付けました。
- **サイドパネル機能をOFFにしている間、環境設定「一般」のサイドパネル関連の項目がグレーアウトするようになりました**(パネルの位置・開く/フォルダへ移動をダブルクリックにする・並び順・本の移動をブラウザの並び順に合わせる)。効かない設定が触れるままになっていました。

### 修正

- **同じウインドウで別の本を開いたあと、前の本のページ画像がメモリに残り続けることがある問題を修正しました。** 環境設定「メモリに残しておくページ画像」の上限ぶん(既定300MB、最大2GB)が解放されないままになっていました。本を切り替えた時点で確実に解放します。サイドパネル「リソース」モードの「N冊を表示中」も、実際に表示している本の数を示すようになります。
  - さらにコード監査で、**切り替えの瞬間にちょうどページの読み込みが進行中だった場合**に、解放した後のメモリへ前の本のページが読み込み直されて残ってしまう抜け道が見つかったため、これも塞ぎました。
  - 前の本が書庫(zip/rar/7z)やPDFだった場合に、そのファイル自体を開いたままにしていた点も、切り替えた時点で閉じるようにしました。
- **素早くページを送った直後、表示するページのデコードが先読み中のページの順番待ちの後ろに並び、表示が一拍遅れることがある問題を修正しました。** 表示するページを先読みより優先します。
- **サイドパネル「リソース」モードの「今すぐ更新」を素早く続けて押すと、ディスクの走査が重なって走ることがある問題を修正しました。** 前の走査を打ち切ってから新しい走査を始めます。
- **同じウインドウで別の本を開くと、サイドパネル「リソース」モードの「この本」が前の本の値のまま止まってしまう問題を修正しました。** ページを送っても数値が動かず、パネルのモードを切り替え直すまで直りませんでした。
- **見開き表示のとき、先読みが設定より1ページ少なかった問題を修正しました。** 先読みの範囲を見開きの1ページ目から数えていたため、「前後に先読みするページ数」が10でも、まだ見ていないページは9ページ分しか先読みされていませんでした。
- **環境設定で無効になっている行の項目名が、薄くならずに残っていた問題を修正しました。** 右側のスイッチやポップアップだけが薄くなり、項目名は濃いままだったため、行そのものが無効なのか分かりにくくなっていました。macOSのシステム設定と同じく、行ぜんたいが薄くなります。

## [1.30] - 2026-08-24

### 追加

- **サイドパネルに「リソース」モードを追加しました**(最上部の切り替えボタンの5つ目、折れ線グラフのアイコン)。qooViewer自身のCPU使用率・メモリ・ディスクの読み書きを、Windowsのタスクマネージャのような時間軸のグラフで確認できます。v1.29で修正したようなリソースの過剰消費が再び起きたとき、ユーザー自身が気づけるようにするためのものです。
  - **計測は上部のボタンでON/OFFします。起動時は常にOFF**です。ONの間は、サイドパネルを隠していても別のモードに切り替えていても計測が続き、OFFにすると履歴は消えます。計測はアプリ全体で1つで、どのウインドウからでも切り替えられます。計測自体の負荷はありません(表示中の画面更新に1〜2%程度)。
  - グラフは直近2分(1秒刻み)と直近1時間(10秒平均)を切り替えられます。メモリの値はアクティビティモニタの「メモリ」列と、ディスクの値は「ディスク」タブと同じ数え方です。計測がOFFでも、現在値・起動後のメモリの最大値・起動後のディスクの累計は表示されます。
  - **「この本」**: 開いている本について、「メモリに残しておくページ画像」の上限に対する実際の使用量と枚数、「前後に先読みするページ数」の設定に対して実際に前後何ページが残っているか(帯で図示)、サムネイル・拡大サムネイルのキャッシュの使用量を表示します。
  - **「ディスク上」**: このアプリがディスクに保存しているものの内訳(一時ファイル、サムネイルキャッシュ、データベース、その他、コンテナ全体)を15秒ごとに表示します。
  - **「異常」**: 設定と実際の食い違い(メモリの上限超過が続いている、設定より広く先読みしている、ディスクキャッシュがOFFなのにファイルが残っている、上限を超えている、以前の起動や閉じた本の一時ファイルが残っている)を検出して一覧表示します。一瞬だけの超過など、正常な動作は異常として扱いません。
  - 「文字の影」(環境設定「外観」)は、グラフの枠・折れ線・バー・帯を含むこのモードのすべての部品に効きます。

### 変更

- **検索欄・絞り込み欄のキーボードフォーカスが、欄の外をクリックするだけで外れるようになりました**(ユーザー要望)。サイドパネルの絞り込み欄を一度クリックするとフォーカスが残り続け、ショートカットのつもりで押したキーが絞り込みの文字として吸われていました。パネルの余白・一覧の行・画像表示エリアなど、同じウインドウのどこをクリックしても外れます(クリック自体は通常どおり効きます)。Return または Esc でも外れます。絞り込みの文字は消えません(消すには右端の×)。
  - 「ブックマーク・レイアウトの編集」(矢印キーでの移動が検索欄に吸われていました)、「メタデータの編集」、「ライブラリの整理」の検索欄も同じ動きになります。

### 修正

- **本を開いた直後に、メモリの使用量が一時的に設定の数倍まで跳ね上がっていた問題を修正しました**(リソースモニタで発見)。「前後に先読みするページ数」のぶんのページを一斉にデコードしていたため、1枚のデコード中に最終的な大きさの数倍を使う一時的なメモリが枚数ぶん重なっていました(例: 先読み10ページ・大判のページの本で、落ち着いた状態の580MBに対し起動直後は2.4GB)。デコードを同時に2枚までに絞り、同じ例で1.1GBに収まるようになりました。
  - 先読みは現在のページに近い順に行うようにしました。表示のための読み込みは先読みより先に処理されるので、ページ送りの体感は変わりません。
- **メモリの上限いっぱいまでページが溜まった状態でページを送ると、先読みしたばかりの隣のページが先に追い出され、遠くの古いページが残ることがあった問題を修正しました**(リソースモニタで発見。「前 18・後 1」のような偏りになっていました)。ページ画像・サムネイルのメモリキャッシュを、最後に見てから最も時間が経ったものから順に追い出す方式に改めました。いま読んでいる場所の前後が常に残り、読み終えて遠ざかったページから消えます。
  - あわせて、上限を超えた分はその場で追い出すようになりました(これまでは一時的に上限を超えることがありました)。メモリが逼迫したときは、キャッシュを半分まで、さらに逼迫すればすべて解放します。
- **「画面内に収める」モードで、画像の上でマウスホイールを回すとサイドパネルのフォルダ一覧まで一緒にスクロールしてしまう問題を修正しました**(ユーザー報告)。このモードにはページをスクロールするための領域が無いため、ホイールの転送先を探す処理がウインドウの中の別の一覧(フォルダ一覧)を見つけてしまっていました。転送先は、カーソルの位置を実際に覆っているページの領域に限るようにしました。矢印キーでのページ送りはもともと影響を受けていません。
  - 同じ仕組みを使うドラッグスクロール(画像をドラッグして動かす操作)とスクロールバーの判定にも、同じ制限を掛けてあります。
- **「ツールバーを隠す」がONのとき、「横幅に合わせる」「横幅に合わせる(単ページ)」「拡大縮小しない」の表示モードでツールバーが隠れると、タイトルバーが消えて画像がウインドウの最上端まで広がっていた問題を修正しました**(ユーザー報告)。ページをスクロールする表示領域がウインドウの上端に接すると、macOSの仕様でタイトルバーの下まで広がって透明にしてしまうためでした。タイトルバーは常に表示されます。

## [1.29] - 2026-08-23

### 追加

- **環境設定に「キャッシュ」画面を追加しました**(サイドバーの「詳細」の先頭)。メモリに残しておくページ画像の量と、ページのサムネイルをディスクに保存しておく機能のON/OFF・その上限をここで決められます。
  - **メモリの使用量に直結する設定をこの画面に集めました。** 「前後に先読みするページ数」を「画像の表示」から、「表示中のサムネイルの拡大画像を先読み」を「外観」→「ページ一覧」から、それぞれ「先読み」セクションへ移しています。設定の値はそのまま引き継がれます。各画面の「初期設定に戻す」の担当も移動先に合わせてあります。
  - 「メモリに残しておくページ画像」(100〜2000MB、既定300MB)。読んでいるページの前後をデコード済みのままメモリに残しておく量の上限で、開いている本1冊あたりの値です。大きくすると前後へ戻ったときのページ送りが速くなり、小さくするとメモリの使用量が減ります。本を表示したまま変えられます。
  - 「ページのサムネイルをディスクに保存する」(既定OFF)。ONにすると、一度表示したページのサムネイルを保存しておき、同じ本を開き直したときの表示が速くなります。特に「ブックマーク・レイアウトの編集」ウインドウや、外付けドライブ・ネットワークボリューム上の本で効きます。
  - 「最大容量」(50〜2000MB、既定200MB)。この容量を超えそうになると、最後に見てから最も時間が経ったサムネイルから順に削除されます。上限を下げたときは、その場で削除されます。
  - 「現在の使用量」に、いま実際に使っている容量が表示されます。「サムネイルのキャッシュを今すぐ削除」でいつでも空にできます。
  - 削除されるのはサムネイルだけで、お気に入り・ブックマーク・読書履歴・レイアウト設定・メタデータには影響しません。本のファイル自体にも一切触れません。

### 変更

- **サイドパネルのフォルダブラウザで、画像しか入っていないフォルダをクリックしたとき、中へ移動せずその場で本として開くようになりました。** 一覧は今の階層のまま、そのフォルダの行が選択色になります(右クリック→「開く」と同じ動きです)。これまでは中へ移動し、空の一覧に「このフォルダの画像を表示しています。」と表示していましたが、親の一覧で行が強調されているほうが、いまどの本を見ているか・隣にどの本があるかが分かりやすいためです(ユーザーからの提案)。
  - 履歴・お気に入り・Finderから本を開いたときや、画像ファイルを直接開いたときの見え方(親の一覧に留まって本の行を強調)と同じになり、辿り着き方による見え方の違いが減ります。
  - 「開く」と同じ扱いなので、「最近開いたファイル」にも残ります。
  - サブフォルダがあるフォルダはこれまでどおり中へ移動します(直下に画像があればその表示も行い、履歴には残りません)。
- **サイドパネルを隠す設定のときのフォルダブラウザの挙動を、常時表示のときと揃えました。** これまでは、カーソルをウインドウの端に近づけてパネルが表示されるたびに、今開いている本の親フォルダへ戻されていました(フォルダブラウザで奥へ移動しても、パネルが隠れた時点で失われていました)。移動した場所は本を切り替えない限り保たれ、本を開いたり切り替えたりしたときだけ本の場所へ移ります。
- **メモリの使用量を大幅に減らしました。** 表示したページの画像は、画面から外れた後もその数倍のメモリを占め続けていました(例: 4233×6050のフルカラーの本を8ページ送ると、アプリ全体で1GBを超えていました)。ページ画像の持ち方を改め、同じ操作で約600MB、白黒の本なら約200MBに収まるようになりました。
  - 「メモリに残しておくページ画像」の上限が、実際のメモリの使用量そのものになりました。これまでは上限として機能しておらず、実際には設定の3倍近くまで膨らんでいました。
  - 本を閉じたり別の本に替えたりしたときに、それまでの本のぶんのメモリが即座に解放されるようになりました。以前は解放したはずのメモリがアクティビティモニタの数字から減らないまま残っていました。
  - サムネイルにカーソルを合わせたときの拡大プレビューは、ポップオーバーの大きさに合わせた解像度(最大1600px)で読み込むようになりました。見た目は変わりません。以前はページ本体と同じ原寸の画像(1枚あたり最大47MB)を使っており、「表示中のサムネイルの拡大画像を先読み」をONにすると画面内のセルの数だけそれが並んでいました。
  - 白黒のページは、カラーの1/4のメモリで保持するようになりました。同じ上限でも、白黒の本のほうが多くのページを残せます。
- **ページのサムネイルのディスクへの保存を、既定でOFFにしました。** これまでは上限200MBで自動的に保存し続けており、ON/OFFの手段も、どれだけ使っているのかを確認する手段もありませんでした(数日の利用で数百MBに達していたというご報告をいただきました)。
  - **このバージョンを初めて起動したとき、これまでに溜まっていたサムネイルは自動的に削除されます。** 引き続き使いたい場合は、環境設定「キャッシュ」でONにしてください。
  - OFFにしている間は、サムネイルの保存も読み込みも行いません。表示が速くなる効果は無くなりますが、動作や見た目はこれまでと変わりません。
  - サムネイルにカーソルを合わせたときの拡大プレビューの画像は、ディスクには保存しません(メモリにだけ残します)。1枚あたり数MBになるため、保存するとすぐ上限に達し、本来残しておきたい小さなサムネイルのほうが先に消えてしまうためです。

### 修正

- **書庫の中の書庫(入れ子の書庫)を開いたときに展開される一時ファイルが、アプリを終了しても残り続けていた問題を修正しました。** 本を開いたままqooViewerを終了するたびに、展開した書庫(数百MB〜1GB超)がアプリの一時フォルダに溜まり続けていました(11日間で8.4GBに達していた例があります)。
  - 終了時にその起動ぶんの一時ファイルを削除するようにしました。
  - 起動時に、以前の起動(クラッシュや強制終了を含む)が残した一時ファイルを自動的に片付けるようにしました。**このバージョンを初めて起動した時点で、これまでに溜まっていたぶんも削除されます。**

## [1.28] - 2026-08-23

### 追加

- 本の一覧で本を右クリックしたときに、開き方を直接選べるようになりました。「新規ノーマルウインドウで開く」「新規シークレットウインドウで開く」「新規タブで開く」の3つです。
  - 追加した場所は、サイドパネルのフォルダブラウザ・履歴・お気に入りの各一覧と、「お気に入りの編集」「ブックマーク・レイアウトの編集」ウインドウの本の一覧です。どこでも同じ並びで表示されます。
  - 「新規ノーマルウインドウ」「新規シークレットウインドウ」は、今のウインドウがどちらであっても、また環境設定「シークレットモードで起動」の設定に関わらず、選んだとおりの性質のウインドウで開きます。
  - 「新規タブ」だけは今のウインドウの性質をそのまま引き継ぎます(シークレットウインドウからならシークレットのタブになります)。1つのタブバーに記録の残るタブと残らないタブが混在すると、どちらを読んでいるのか見分けられなくなるためです。
  - サイドパネルのフォルダブラウザには、あわせて「開く」も追加しました。フォルダの行では、直下に画像がある(=そのフォルダ自体が1冊の本になる)ときだけこれらの項目が表示されます。
- サイドパネルのお気に入り・ブックマークを、行の右クリックから直接リネーム・削除できるようになりました。これまでは「お気に入りの編集」「ブックマーク・レイアウトの編集」ウインドウを開く必要がありました。
  - お気に入りの本の行に「名前を変更」「お気に入りから削除」、お気に入りのフォルダの行に「名前を変更」「削除」、ブックマークの行に「名前を変更」「ブックマークを削除」が並びます。
  - **削除の確認ダイアログの有無は、各編集ウインドウでの扱いに合わせてあります。** お気に入り(本・フォルダとも)は確認してから削除し、ブックマークは確認せずその場で削除します。フォルダの確認には、中のサブフォルダとお気に入りもまとめて削除される旨を表示します。
  - 「名前を変更」で変わるのは一覧に表示される名前だけで、実体のファイル名・フォルダ名は変わりません(これまでの編集ウインドウでのリネームと同じです)。
  - シークレットウインドウと、画像を直接開いた本を表示しているウインドウでは、これらの項目はグレーアウトします(記録を残さないウインドウの約束どおりです)。
- ページのサムネイルを右クリックしたときに、そのページをブックマークに追加・削除できるようになりました。
  - 使えるのはページ一覧パネル・サイドパネルの「ページ」モード・「本の中身ブラウザ」の3箇所で、いずれも同じ内容です。
  - 既にブックマークが付いているページでは、項目の文言が「このページをブックマークから削除」に変わります。文言そのものが今の状態を表すので、ページ一覧パネルのようにしおりの印が無い場所でも見分けられます。
  - 対象は右クリックしたそのページ1枚だけです。見開き表示中でも相方のページには触れません(環境設定「見開き表示中のブックマーク」の対象ページの設定も関係しません)。
  - シークレットウインドウと、画像を直接開いた本ではグレーアウトします。
- ページ一覧パネルの上部、サムネイルサイズのスライダーの右に、拡大プレビューの表示/非表示を切り替えるスイッチを追加しました。カーソルを合わせると「拡大プレビューの表示」というツールチップが出ます。
  - 環境設定「外観」→「ページ一覧」の「カーソルを合わせたら拡大プレビューを表示」と同じ設定で、どちらから変えても即座に両方へ反映されます。読みながらその場で切り替えられます。
- マウス操作に「タブを閉じる」「ウインドウを閉じる」「qooViewerを終了」を割り当てられるようになりました。環境設定「マウス」の一覧の末尾にあります。
  - **既定ではどれにも何も割り当てていません。** 取り消しの効かない操作なので、使いたい場合にご自身で割り当ててください。
  - 「タブを閉じる」はそのタブ1枚だけを確認なしで閉じます。「ウインドウを閉じる」は赤い閉じるボタンと同じで、同じウインドウのタブをすべて閉じ、複数のタブが開いていれば設定に従って確認ダイアログが出ます。
  - この3つは**マウス専用**です。キーボードには macOS 標準の Cmd+W・Cmd+Q があるため、環境設定「キーボード」の一覧には出ません。
- 環境設定「閲覧中の動作」→「サムネイルプレビュー」に「プレビューの大きさ」(440〜800pt、既定440pt)を追加しました。拡大プレビューの一辺の長さを変えられます。
  - 「表示までの時間」と同じく、ページ一覧・サイドパネルのページモード・ブックマーク・レイアウトの編集・書き出しウインドウのすべてに効きます。
  - 最小値が既定値と同じ440ptなのは、サムネイル(最大320pt)よりプレビューが小さくなる逆転を防ぐためです。
  - 表示の大きさが変わるだけで、読み込む画像は元から原寸のため、大きくしても読み込みは重くなりません。

### 変更

- **ページ一覧パネルにしか効かない設定を、すべて環境設定「外観」→「ページ一覧」に集めました。** 環境設定「閲覧中の動作」から次の3つが移動しています。
  - 「カーソルを合わせたら拡大プレビューを表示」「表示中のサムネイルの拡大画像を先読み」(移動前は「サムネイルプレビュー」の中)
  - 「ホイール1ノッチのスクロール行数」(移動前は「ページ一覧」という独立したセクション)
  - これまでは1つの見出しの下に、ページ一覧だけに効く項目と、サイドパネル・ブックマーク編集・書き出しウインドウにも効く項目が混ざっていて、どれがどこに効くのか分かりませんでした。**効く範囲が同じものだけが1つの見出しの下に並ぶ**ようにしています。
  - 「閲覧中の動作」→「サムネイルプレビュー」に残るのは、すべての場所に共通の「表示までの時間」と「プレビューの大きさ」だけです。
  - 設定の値そのものは引き継がれます。設定し直す必要はありません。
- **同じ本を、同じ種類のウインドウ・タブで二重に開かないようになりました。** 既に開いている場合は、新しく開く代わりにそのウインドウ/タブが手前に出ます(表示していたページはそのままで、先頭に戻ったりはしません)。
  - これまで「新しいウインドウで開く」「新しいタブで開く」でだけ効いていた動作が、履歴・お気に入り・フォルダブラウザ・ウェルカム画面の一覧・「開く…」パネル、Finderやドックから渡された本など、本を開く操作全般に広がりました。
  - ノーマルウインドウとシークレットウインドウは別々に数えます。シークレットウインドウで開いている本をノーマルウインドウで開こうとした場合は、これまでどおり新しく開きます(逆も同じです)。
  - 前後の本への移動、フォルダブラウザでフォルダを移動したときの自動表示、「このフォルダの画像をすべて開く」は対象外です。ページ送りの延長や、そのウインドウの中で完結する操作のため、これまでどおり今のウインドウの表示が切り替わります。

### 修正

- 環境設定の各画面にある「初期設定に戻す」ボタンが、拡大プレビューの表示/先読みとホイールのスクロール行数について、**移動前の画面の担当のままになっていた**問題を修正しました。「外観」のボタンでこの3つが戻り、「閲覧中の動作」のボタンでは戻らなくなります(それぞれの項目が並んでいる画面と一致します)。

## [1.27] - 2026-08-23

### 修正

- 環境設定「すべてのウインドウを閉じたときにqooViewerを終了する」がOFFのとき、ウインドウをすべて閉じた状態でFinderやDockアイコンへのドロップから本を開くと、空の小さなウインドウが一瞬表示されてから消えていた問題を修正しました。本のウインドウだけが開くようになりました。
- 同じ状態でDockアイコンをクリックしても何も開かなかった問題を修正しました。ウェルカム画面のウインドウが開きます。

## [1.26] - 2026-08-22

### 追加

- 環境設定「外観」の各面に「文字の影」を追加しました(なし / 1〜5、既定は「なし」)。
  - 背景色を濃くすると、文字やアイコンの色と近くなって読めなくなることがあります(ライト外観で背景色を黒・濃さ100%にすると黒地に黒、ダーク外観で白・濃さ100%にするとその逆)。1以上にすると、文字とアイコンに反対色の細い輪郭が付き、背後が何色でも読めるようになります。輪郭はぼけず、文字そのものの色も変わりません。
  - 数字は輪郭の太さで、1がいちばん細く5がいちばん太くなります。面ごとに個別に設定でき、既定の「なし」ではこれまでの見た目とまったく同じです。
  - 検索欄・スライダー・サムネイル画像には付きません。ページ一覧パネルのサムネイルサイズのスライダーだけは、つまみが背景に溶けないよう、輪郭の代わりに薄い溝が付きます。
  - コンテキストメニュー・システムのダイアログ・ポップオーバーは macOS が不透明に描いているため、そもそもこの設定の影響を受けません。
  - 選択中のモードボタンと選択行のハイライトにも縁が付きます。背景色をアクセントカラーに近い色にしたときに、どれが選ばれているのか分からなくなるのを防ぎます。

### 変更

- サイドパネルのフォルダブラウザでフォルダを移動すると、**そのフォルダに画像があればその場で表示する**ようになりました。
  - 行のクリックだけでなく、「戻る」「進む」「1階層上へ」でも同じように切り替わります。フォルダブラウザで今いる場所と、表示している本が常に一致します。
  - 画像とサブフォルダが同居しているフォルダでも、一覧にはサブフォルダが残るので、そのまま奥へ進めます。
  - これまでは、画像だけが入っているフォルダへ移動すると一覧が空になり、何も起きないように見えていました(この一覧は画像ファイルを行として表示しないため)。空になる場合は、代わりにその理由を表示するようにしました。
  - **この移動で表示した本は「最近開いたファイル」に残りません。** 目的の本を探して通り抜けただけのフォルダで履歴が埋まらないようにするためです。読書位置・ブックマーク・お気に入り・ページレイアウトはこれまでどおり記録されるので、同じフォルダへ戻れば続きから読めます。

### 修正

- 画像を直接開いた状態から、その画像が入っているフォルダを開いたときに、サイドパネル下段(本の中身ブラウザ)で今表示しているページがハイライトされなかった問題を修正しました。ページを1つ送ると付いていましたが、開いた直後から付くようになりました。
- 環境設定「閲覧中の動作」のスライドショーの「間隔」で、スライダーの目盛りが細かすぎて1本の直線に見えていた問題を修正しました。目盛りは0.5秒刻みになり、区切りのいい秒数へドラッグで合わせられます。0.1秒単位の調整は、これまでどおり右側の⬆⬇で行えます。

## [1.25] - 2026-08-22

### 追加

- 画像ファイルをそのまま開けるようになりました。Finderで複数選択した画像は、まとめて1冊の本として開きます。
  - ウェルカム画面へのドラッグ&ドロップ、Dockアイコンへのドロップ、Finderの右クリック→「このアプリケーションで開く」、「開く…」パネルのいずれからでも開けます。
  - 対応する画像は、これまでフォルダ・書庫の中で読めていたものと同じです(JPEG / PNG / GIF / BMP / WebP / HEIC / TIFF / AVIF)。
  - ページの並びはファイル名順です(名前に含まれる数字は数値として比べます)。フォルダをまたいで選んだ場合は、フォルダごとにまとまった上で名前順になります。選んだ順序や、Finderでの並び順には影響されません。
  - 画像1枚だけを渡した場合は、その1枚だけの本になります。同じフォルダにある他の画像は含めません(フォルダ全体を開きたい場合は、これまでどおりフォルダ自体を開いてください)。
  - 複数枚を開いているときは、ウインドウのタイトルに「先頭の画像名 (3枚)」のように枚数を表示します。
  - **画像を直接開いた本については、アプリ側の記録を一切残しません。** 読書位置・ページレイアウト・ブックマーク・お気に入り・メタデータ・「最近開いたファイル」のいずれにも記録されず、閉じれば何も残りません(シークレットウインドウで開いた本と同じ扱いです)。これらの登録・編集を行うメニューやボタンはグレーアウトします。一覧の表示やページの移動は通常どおり行えます。
  - 「開く…」「新しいウインドウで開く…」「新しいタブで開く…」のパネルで複数選択ができるようになりました。画像以外を複数選んだ場合は、これまでどおり最初の1つだけを開きます。
- 本を表示している最中のウインドウにも、ファイル/フォルダをドラッグ&ドロップして別の本を開けるようになりました。これまではウェルカム画面(まだ何も開いていない画面)にしかドロップできませんでした。
  - サイドパネルの上を含め、ウインドウのどこへ落としても開きます。ドラッグ中はウインドウの縁が強調されます。
  - 落とした本は、そのウインドウの内容を置き換えます。
- メニューバー「File」に「このフォルダの画像をすべて開く」を追加しました。画像を直接開いているときだけ使えます。
  - 今表示しているページと同じフォルダにある画像をすべて集めて、通常のフォルダの本として開き直します。開き直したあとも、それまで見ていたページがそのまま表示されます。
  - 開き直した後は普通のフォルダの本なので、読書位置・ブックマーク・お気に入りなども通常どおり記録されます。
  - そのフォルダへのアクセスをまだ許可していない場合は、その場で許可を求めるパネルが表示されます。
- 環境設定に「外観」画面を追加しました。アプリの見た目に関する設定をここにまとめています。
  - ページ一覧パネル・ツールバー・プログレスバー・サイドパネル、およびそれ以外の浮かぶ表示(「情報を見る」パネル・操作のフィードバック・拡大率の表示)の「すりガラス」を、面ごとに個別に調整できます。項目は「すりガラスの濃さ」「背景色」「背景色の濃さ」の3つです。
  - 既定値では、これまでの見た目とまったく同じです。3つの組み合わせで「今より透けさせる」「好きな色に染める」「すりガラスをやめて単色にする」のいずれもできます。
  - ツールバーとプログレスバーは、「表示」メニューで隠す設定にしているとき(およびフルスクリーン中)だけ画像の上に浮かぶため、「すりガラスの濃さ」はそのときにだけ効きます。背景色は常に効きます。
  - コンテキストメニューやシステムのダイアログは macOS が描いているため、色を変えられません。
  - ビューアの「背景色」(旧「画像の表示」)と、ページ一覧の見え方に関する設定(旧「閲覧中の動作」)も、この画面へ移しました。
- ページ一覧(サムネイルグリッド)の見え方を細かく設定できるようにしました(環境設定「外観」)。
  - サムネイルの下に出す情報を「ページ番号」「ファイル名」「表示しない」から選べます。文字の大きさも変えられます。
  - 表示中のページを示す枠の色を、アクセントカラー・赤・黄・緑・白・黒・カスタムから選べます。
- ページ一覧の上でマウスホイールを1ノッチ回したときに動く行数を設定できるようにしました(環境設定「閲覧中の動作」、0.5〜5.0行、0.1行刻み)。トラックパッドのスクロールはこれまでどおりです。
- 環境設定「一般」に「シークレットモードで起動」を追加しました。ONにすると、**どの経路で本を開いても既定でシークレットウインドウ**になります(通常起動・Finderからの起動・Dockアイコンへのドロップ・ウインドウへのドロップ・「開く…」パネル・履歴やお気に入りから開く場合)。
  - 記録を残したいときは、ファイルメニューの「新規ノーマルウインドウ」から開いてください。このウインドウと、そこから派生したタブ/ウインドウだけが通常のウインドウとして扱われます。
  - この設定がONの間は、「前回読んでいた本を開き直す」は記録が増えないため使えなくなります。
- ファイルメニューに「新規ノーマルウインドウ」(⌘N)を追加しました。「新規シークレットウインドウ」(⇧⌘N)と合わせて、設定に関わらず常に両方が並びます。
- Dockアイコンの右クリックメニューから「新規ノーマルウインドウ」「新規シークレットウインドウ」を開けるようにしました。
- サイドパネルとページ一覧の右クリックメニューを追加しました。
  - フォルダブラウザ・履歴・お気に入りの行から「Finderで開く」が使えます。お気に入りは「開く」も選べます。
  - 本の中身ブラウザ・ページモード・ページ一覧のサムネイルからも「Finderで開く」が使えます。書庫やPDFの中のページは、中の画像がFinderからは見えないため、その書庫/PDFファイル自体を表示します。
  - その代わり、書庫やPDFの中のページには「画像をエクスポート…」が出ます。表示していないページも1枚だけ書き出せます。
  - ブックマークの行からは「このページへ移動」「ブックマークを編集…」が使えます。
  - メニューを開いている間は、対象の行が枠で囲まれて強調されます(Finderのリスト表示と同じ見え方です)。
- 「最近開いたファイル」の履歴を消去できるようにしました。ファイルメニューの「最近開いたファイルを開く」の末尾、サイドパネルの履歴モードのゴミ箱ボタン、環境設定「リセット」の3か所から行えます。ファイル自体には触れません。
- 環境設定の各画面に「初期設定に戻す」ボタンを追加しました(「フォルダのアクセス権」「リセット」を除く)。その画面の設定だけが戻り、ほかの画面やお気に入り・ブックマーク・読書履歴には影響しません。
  - 「一般」の「データを保持する本の数」と「履歴の保存件数」だけは戻しません。この2つは値を下げると、保存済みの読書履歴や「最近開いたファイル」がその場で消えてしまうためです(戻したい場合はスライダーを直接動かしてください)。

### 変更

- サイドパネル下段(本の中身ブラウザ)を、画像を直接開いた本でも表示するようにしました。開くときに渡した画像が、そのままページ順で並びます。
- サイドパネル上段(フォルダブラウザ)は、画像を直接開いているとき、その画像が入っているフォルダの1つ上を表示し、画像の入ったフォルダ自体をハイライトするようになりました。フォルダブラウザは画像ファイルを一覧に出さないため、画像の入ったフォルダをそのまま表示すると中身が空に見えてしまうためです。
- ページ一覧(サムネイルグリッド)をクリックで閉じる条件を、次の3つだけに限定しました。
  - サムネイルをクリックする(そのページへ移動して閉じます)
  - パネル内の余白をクリックする
  - パネルの外側に見えている画像表示エリアをクリックする
  - ツールバー・プログレスバー・タイトルバー・メニューバー・サイドパネル・環境設定などの別ウインドウ・他のアプリを操作しても閉じなくなりました。サイドパネルで次の本を探したり、環境設定でサムネイルの大きさを調整したりしている間、パネルは開いたままになります。
- スライドショーの間隔を0.1秒刻みで設定できるようにしました(0.5〜30秒)。スライダーの右の⬆⬇で細かく詰められます。
- サイドパネル上段の「並べ替え」ボタンを、「Finderで開く」ボタンの左隣へ移動しました。
- 環境設定「リセット」の「すべてのお気に入り・ブックマーク・レイアウト設定・読書履歴を削除」で、「最近開いたファイル」の履歴と「最後に開いていた本」の記録も削除するようにしました。
- 環境設定の「初期設定に戻す」ボタンの名前を、すべての画面で同じにしました(以前は画面によって「このモードの設定を初期状態に戻す」など表記が違っていました)。何が戻るのかの違いは、項目名の右のⓘで説明します。

### 修正

- PDFの本で「このページをエクスポート」を実行すると、保存先を選んだあとに必ず「画像を読み込めませんでした」となり、書き出せなかった不具合を修正しました。
  - PDFのページに埋め込まれている画像を取り出して書き出します。JPEGならそのまま、可逆形式ならPNGとして保存し、ファイル名の拡張子も実際の形式に合わせます。
- EPUB/PDF/CBZの各エクスポートウインドウで、ファイル名の列が狭いままファイル名が「…」で省略され、逆にインジケータの左側に広い余白ができていた不具合を修正しました。
  - 一覧の内容に合わせて列の幅とウインドウの幅を自動調整する動作が、実際には一度も働いていませんでした。
  - ウインドウを広げたぶんはファイル名の列が受け取るようになり、右側に何の列でもない余白が残らなくなりました。

## [1.24] - 2026-08-22

### 追加

- 「一番右のページへ」「一番左のページへ」を追加しました。読み方向を見て飛び先を決めるので、右開き・左開きのどちらでも名前どおりに動きます(右開きなら一番右が先頭ページ、左開きなら一番右が最終ページ)。
  - 既定では option+→ / option+← と、option+クリック(画面の右側/左側)に割り当てています。
  - これまで option+→ / option+← に割り当てていた「最初のページへ」「最後のページへ」は、読み方向に関わらず物語的な先頭/末尾へ飛ぶ操作としてそのまま残ります(既定は home / end)。メニューバー「Move」→「Move to First / Move to Last」もこちらのままです。
- マウスのドラッグジェスチャー(マウスジェスチャー)を追加しました。ボタンを押したまま画面をなぞって離すと、その向きに応じた操作を行います。
  - 上下左右の4方向を、左ボタン・中ボタンそれぞれについて、修飾キー(shift/option)と組み合わせて割り当てられます。
  - 30ポイント以上を1秒以内に動かしたときに成立します。それに満たない動きは、これまでどおりクリックとして扱われます。
  - 既定では何も割り当てていません。環境設定「マウス」から好きな操作に割り当ててください。
  - 画面をスクロールできる表示モードでは、画像を掴んで動かす操作が優先されます。画像が動く余地が無かったとき(端に着いていた、画像が画面に収まっている)だけジェスチャーになります。

### 変更

- マウスの割り当てを、キーボードと同じ「操作ごとにトリガーを割り当てる」形に作り直しました。
  - 1つの操作に複数のトリガーを割り当てられるようになりました。
  - トリガーに**ボタン**(左/中)と**修飾キー**(shift/option)を指定できるようになりました。これまでは「画面の左側をクリック」「画面の右側をクリック」「ホイールを上へ回す」「ホイールを下へ回す」の4つだけでした。
  - これまで何も起きなかった**中ボタン(ホイールクリック)**が使えるようになり、既定で「拡大鏡を表示/非表示」を割り当てました。
  - クリックの位置に「画面全体」を選べるようになりました。左右どちらをクリックしても同じ操作にしたい場合に、2件に分けずに済みます。同じボタン・修飾キーで左右と画面全体の両方に割り当てた場合は、左右を指定した方が優先されます。
  - 既定に「shift+クリックで1ページだけ移動」を追加しました。
  - マウスに割り当てられる操作は、ページ移動・本の移動と、拡大鏡・ページ一覧・実寸表示・スライドショーに絞りました。レイアウト・ブックマーク・お気に入り・表示モードの切り替えは対象外です。
  - これまでのマウスの割り当ては、そのまま引き継がれます。
- 修飾キー付きのホイール操作(option+ホイール)は、「表示モード別の操作」の「スクロールできるとき」の設定に関わらず、常に割り当てた操作を行うようになりました。素のホイールはこれまでどおりです。
- shift+ホイールは割り当ての対象外にしました。macOSがホイール操作の縦横を入れ替えてしまうため、向きを正しく判定できないためです。
- 環境設定の「キーとマウス」を、「キーボード」と「マウス」の2つの画面に分けました。
  - この変更のため、環境設定を次に開いたときだけ、最後に開いていた画面ではなく「一般」から始まります。
- 「ページ一覧を表示」を表示/非表示のトグルにしました。開いたときと同じキー・マウス操作でそのまま閉じられます。
  - これまではキーを押しても閉じず、パネルの外側をクリックするか、ページを選ぶしかありませんでした。
  - マウスジェスチャーを割り当てている場合は、パネルの上でそのジェスチャーを行っても閉じます。
- アップデートで操作が増えたとき、その既定のキー/トリガーが空いていれば自動的に割り当てるようにしました。すでに別の操作へ割り当てているキーを奪ったり、自分で外したキーを戻したりはしません。

### 修正

- キー割り当ての保存データに、いまのバージョンに無い操作が1つでも残っていると、**割り当てが丸ごと初期設定に戻ってしまう**不具合を修正しました。古いバージョンから使い続けている場合に、自分で変更したキー割り当てが反映されないことがありました。
  - 「ブックマークに追加」「お気に入りに追加」が追加/削除のトグルへ統合される前に保存された割り当ては、統合後の操作へ読み替えるようにしました。同じキーでこれまでどおり使えます。

## [1.23] - 2026-08-21

### 追加

- サイドパネル(ブラウザモード)上段のフォルダブラウザに、並べ替えの設定を追加しました。
  - 「Finderで表示」ボタンの隣のボタンから、名前・サイズ・種類・作成日・変更日を基準に、昇順・降順で並べ替えられます。設定は記憶され、次回起動時も引き継がれます。
  - フォルダをまとめて上に表示するかどうかは、これまでどおり環境設定「一般」の「並び順」に従います(降順にしてもフォルダは上に表示されたままです)。
  - フォルダのサイズはFinderと同じく計算しないため、サイズで並べたときのフォルダ同士は名前順に並びます。
- PDFとして書き出すときに、読み方向(右開き/左開き)と、本ごとに指定した見開き/単ページも埋め込むようになりました。
  - 書き出したPDFをqooViewerで開き直すと、これらの設定が復元されます。プレビュー.appやAcrobatなど、この指定を尊重するソフトでも同じ並びで表示されます。
  - これに伴い、PDF書き出しウインドウの「見開き・読み方向の設定は失われます」という警告を削除しました。
  - ページごとのレイアウト(このページだけ単独で表示する、見開きの左右どちらに置く、といった個別指定)は、PDFにこれを表す仕組みが無いため、引き続き書き出されません。

### 変更

- フォルダブラウザの名前順の並びを、Finderと同じ並び方(大文字・小文字や全角・半角を区別せず、数字は数値として比べる)に変更しました。以前は大文字で始まる名前がすべて小文字で始まる名前より先に並んでいました。
- PDFへのシリーズ名・巻数の埋め込み方を、qooViewer独自の形式からCalibre互換のXMPメタデータに変更しました。
  - 以前はPDFのキーワード欄へ`series:シリーズ名, series_index:巻数番号`という独自の書き方で埋め込んでいましたが、これはqooViewer以外のどのソフトも解釈できず、他のアプリからは意味の分からないタグが1つ付いているようにしか見えませんでした。
  - 新しい形式では、CalibreやKavitaなど、この形式に対応した書庫サーバ/管理ソフトからそのままシリーズ名・巻数として読み取れます。タイトル・著者名も同時に埋め込まれます。
  - キーワード欄には何も書き込まなくなりました。
  - 読み込み側は、Calibreで編集したPDFのシリーズ名・巻数も読み取れるようになりました。以前のバージョンのqooViewerが書き出した独自形式のPDFも、これまでどおり読み取れます。

### 修正

- Calibreが書き出したEPUB/PDFの巻数が「3.50」のように余分な0付きで取り込まれることがある不具合を修正しました。「3.5」として取り込まれます。
- macOS 26 で、メニューバーのメニューを開いている最中にアプリが落ちることがある不具合を、v1.22で直しきれていなかった経路についても修正しました。
  - 大きな書庫を開いた直後にメニューを開いたまま待っていると、最初の見開きの読み込みが終わった時点で「画像のエクスポート」や「レイアウト」の項目が組み替わり、落ちることがありました。同じフォルダのファイル一覧・最近開いたファイル・ブックマーク一覧も、それぞれ読み込みが終わった時点で同じことが起こりえました。
  - メニューを開いている間は、これらの内容を一切変えないようにしました(閉じたあとに最新の状態へ更新されます)。
- ページ送りのアニメーション中に、プログレスバーのクリックなどで別のページへ移動すると、ビューアが操作を受け付けなくなることがある不具合を修正しました。
- PDFを拡大鏡やピンチ操作で拡大したときに、ページの実際の解像度を超えて描画し、必要以上にメモリを使っていた問題を修正しました。見た目は変わらず、メモリ使用量だけが減ります。
- 履歴やお気に入りから本を開くたびに、フォルダへのアクセス権に関する資源が少しずつ消費されていた問題を修正しました。長時間使い続けた場合に、ファイルを開けなくなることを防ぎます。
  - 環境設定「フォルダのアクセス権」で許可したフォルダについても、追加した直後にアクセスが有効にならないことがある問題を併せて修正しました。

## [1.22] - 2026-08-21

### 追加

- メニューバー「File」に「新規シークレットウインドウ」(Shift+Cmd+N)を追加しました。このウインドウ(とそこから開いた新しいウインドウ/タブ)で開いた本の情報は、qooViewerに一切残りません。
  - 「最近開いたファイル」の履歴・最後に開いていた本・読書位置・ブックマーク/お気に入り/レイアウト/メタデータの登録と自動取り込み・ディスク上のサムネイルキャッシュのいずれも保存されません。
  - シークレットウインドウがアクティブな間は、履歴(ウェルカム画面・サイドパネル・「最近開いたファイル」メニュー)とウェルカム画面の「最近のお気に入り」を表示せず、お気に入り/ブックマーク/レイアウト/メタデータの追加・編集の項目は無効になります。登録済みデータの閲覧(ブックマークへのジャンプ等)はできます。
  - 見つからないお気に入りを開こうとしたときの「お気に入りから削除」も、シークレットウインドウでは選べません(保存データへの変更にあたるため)。
  - EPUBの目次・PDFのアウトライン・ComicInfo.xmlのブックマークや書誌メタデータ、読み方向の指定など、ファイル自身に含まれる情報はそのウインドウの中だけで利用できます。
  - FinderやDockから開いた本は、常に通常のウインドウで開きます。
  - ウインドウのサイズ・位置は「新しいウインドウで開く」で開く通常のウインドウと同じです(元のウインドウと同じ大きさで、右下へ少しずらして開きます)。
- Dockのアプリアイコンへフォルダをドロップして開けるようにしました(これまではzip/cbz・rar/cbr・7z/cb7・PDF・EPUBファイルのみ受け付けていました)。
- ページ一覧(サムネイルグリッド)のサムネイルの大きさを変えられるようにしました。
  - パネル上部のスライダーで80〜320ptの範囲で滑らかに拡大縮小でき、列数は大きさに合わせて自動的に変わります(これまでは5列×120pt固定でした)。値は記憶されます。
  - サムネイルの解像度はセルの大きさ(と画面の倍率)に追従するので、大きく拡大してもぼやけません。
  - 環境設定「閲覧中の動作」に「ページ一覧」セクションを追加し、サムネイルのサイズ・横/縦の間隔・ウインドウの端からパネルまでの左右/上下の余白(%)を調整できるようにしました。
- サムネイルの拡大プレビューを調整できるようにしました(環境設定「閲覧中の動作」→「サムネイルのプレビュー」)。
  - ページ一覧の拡大プレビューをOFFにできます(サイドパネルのページモードやブックマーク編集ウインドウのサムネイルには影響しません)。
  - カーソルを合わせてから表示するまでの時間を0〜1秒で変更できます(これまでは0.35秒固定)。ページ一覧・サイドパネル・ブックマーク編集・書き出しウインドウで共通です。
  - 「表示中のサムネイルの拡大画像を先読み」(既定OFF)をONにすると、ページ一覧で見えているサムネイルの原寸画像を先に読み込んでおき、プレビューを即座に表示します。

### 修正

- macOS 26 で、メニューバーのメニューを開いている最中に画面の状態が変わると、アプリが落ちることがある不具合を修正しました。ページ一覧(サムネイルグリッド)を表示したままメニューを開いた場合や、お気に入り/ブックマーク一覧のメニューを開いたときにその内容が更新される場合が該当します。いずれも、メニューを開いている間は画面やメニューの内容を変えず、メニューが閉じたあとに更新するようにしました。
- ページ一覧(サムネイルグリッド)のサムネイル枠を、実際のページの縦横比に合わせるようにしました。以前は固定の枠だったため、縦長のページの左右に常に余白(プレースホルダ)が残り、横の間隔を0にしても詰まりませんでした。枠の比率は複数ページの中央値から決めるので、先頭だけ横長のカバーがあっても列全体が横長に引きずられず、そうしたページはセル内に収めて表示します。
- macOS 15 (Sequoia) で、環境設定のⓘボタンで開く説明のポップオーバーが縦に異常に大きくなり、本文の上に巨大な空白が表示される不具合を修正しました。
- 対応OSの下限を macOS 15.0 に設定しました。

## [1.21] - 2026-08-20

### 修正

- アプリアイコンの四隅が角丸にならず、白い四角として表示されることがある不具合を修正しました。

## [1.20] - 2026-08-20

### 追加

- メニューバー「編集」に「メタデータの編集…」を追加しました(「レイアウトの編集…」の下)。本ごとに著者・タイトル・シリーズ・巻数を登録できます。
  - 一覧には、一度でも開いた本や、お気に入り・ブックマーク・レイアウトを登録した本が並びます。未登録の本には、ファイル名から機械的に推測した値があらかじめ入っており、そのまま、または直してから「登録」を押すと保存されます。
  - 登録済みの行は文字色で見分けられます。「削除」で登録を解除すると、表示はファイル名からの推測値に戻ります。
  - 一度登録した内容は、あとから推測の規則を変更しても影響を受けません。
  - ファイル名はドラッグで範囲を選んでコピーできます(シリーズ名などを切り出して他の欄へ貼り付けられます)。右クリックからはファイル名全体をコピーできます。
  - 推測の規則は、ウインドウ上部の3つのボタンから編集できます。いずれも既定の内容に戻す「リセット」を備えています。
    - ファイル名フォーマット: `@author`(著者名)・`@title`(タイトル)・`@ignore`(読み飛ばす任意の文字列)を組み合わせて書きます。上から順に照合し、最初に一致したものを使います(ドラッグで優先順位を変更できます)。
    - 巻数フォーマット: タイトルの末尾から巻数を取り出す正規表現と、シリーズ名の分離だけを行う正規表現を、別々に登録できます。全角数字は半角に変換されます。
    - 除外文字列: 照合の前にファイル名から削るノイズの正規表現です(括弧書きの年号、「(完)」などの完結表記)。
- メタデータを登録した本は、ツールバー中央の表示がファイル名から「[著者] タイトル」に変わります(著者が未登録の場合はタイトルのみ)。
- EPUB/PDFを初めて開いたとき、ファイルに埋め込まれているメタデータを読み取って登録するようにしました。
  - EPUBは、タイトル・著者に加え、EPUB3の「belongs-to-collection」「group-position」とCalibre独自の拡張メタデータからシリーズ名・巻数を読み取ります(両方ある場合はCalibre側を優先します)。
  - PDFは、タイトル・著者に加え、キーワードに「series:シリーズ名, series_index:巻数番号」の形式で書かれている場合のみシリーズ名・巻数を読み取ります。
- 環境設定「リセット」に「本ごとの保存データを削除…」を追加しました。検証用に一度開いただけの本など、不要になった保存データを個別に片付けられます。
  - 各行に、本のフルパス・元のファイルが今も存在するか(あり/なし/不明)・お気に入り/ブックマーク/レイアウト/メタデータのどれが登録されているかが表示されます。
  - 「見つからない本のみ」で絞り込んでから「表示中をすべて削除…」でまとめて削除できます。
  - 削除されるのはqooViewerが保存しているデータだけで、ファイル自体には触れません。
  - 「不明」は権限が無くて判定できない場合です。外していたボリューム上の本を「消えた」と誤解して消さないよう、「なし」とは区別しています。
- メニューバー「File」に「Export as CBZ…」を追加しました(「Export as PDF…」の下)。選んだ本をCBZ(ページ画像をそのままzipにまとめた形式)として書き出し、メタデータ・ブックマーク・ページレイアウトをアーカイブ直下の`ComicInfo.xml`として同梱します。ComicRack・Komga・Kavita・YACReaderなど多くのソフトが読み取れる形式です。
  - 対象になる本の条件・一覧の見かた・タイトル/著者名の編集・カバー画像の選択・空き容量チェック・同名ファイルの確認・進捗表示は、EPUB出力と同じです。
  - ページ画像は再エンコードせず、元のバイト列をそのまま格納します(画質は劣化しません)。EPUB出力と違い、WebP・HEIC・BMP・TIFF・AVIFをPNGへ変換することもありません。
  - `ComicInfo.xml`には、タイトル・シリーズ名・巻数(`Number`)・著者(`Writer`と`Penciller`)・ページ数・言語・読み方向(`Manga`)と、ページごとのカバー指定・見開き合成ページ・ブックマーク名・画像の縦横サイズを書き込みます。巻数は「上」「下」のような数値でない値もそのまま書き出せます。
  - 「ファイル名を連番にリネームする」はCBZでは既定ONです。CBZにはページの読み順を記録する仕組みが無く、どのソフトもファイル名の並び順だけでページ順を決めるため、並べ替え・除外を反映するには連番が必要になるためです。
  - 「巻数を ComicInfo の Volume 要素にも書き出す」(既定OFF)を追加しました。Kavitaは`Volume`を巻数として読みますが、Komgaはシリーズ名に連結して巻ごとに別シリーズへ分けてしまうため、使うソフトに合わせて選べるようにしています。
  - 元のファイルが既に`ComicInfo.xml`を持っている場合、qooViewerが扱っていない項目(出版社・あらすじ・発行年・ジャンルなど)はそのまま引き継ぎます。
- `ComicInfo.xml`を含むcbz/cbr/cb7、および`ComicInfo.xml`を直下に置いたフォルダを初めて開いたとき、その内容を読み取って登録するようにしました。EPUB/PDFからの取り込みと同じく、本ごとに1回だけ行われます。
  - タイトル・著者・シリーズ名・巻数をメタデータとして登録します。
  - `Manga`が`YesAndRightToLeft`なら右開き、`No`なら左開きとして読み方向を登録します(`Yes`は「漫画である」ことしか表さないため、読み方向は変更しません)。
  - `Pages`の`Bookmark`属性に書かれたしおりを、ブックマークとして取り込みます(その本にまだブックマークが1件も無い場合のみ)。

### 変更

- EPUB/PDFがファイル自身に持っているレイアウト情報(読み方向・見開き・ページごとの見開き配置)の扱いを変更しました。初めて開いたときに読み取って登録し、以降は登録された内容に従います。
  - 従来はファイル側の指定が常に優先され、読み方向・見開きの切り替えやページレイアウトの編集がすべて無効化(グレーアウト)されていましたが、これらをEPUB/PDFでも自由に操作できるようにしました。
  - EPUBのページごとの見開き配置も取り込むため、EPUBでもページ単位のレイアウトを編集できます。
  - 取り込みは本ごとに1回だけ行われるため、変更した内容が本を開き直すたびに元へ戻されることはありません。
- EPUBの目次・PDFのしおりから自動的に取り込んだブックマークを、「ブックマーク・レイアウトの編集」ウインドウにも表示するようにしました。自分で付けたものと同じく、リネーム・削除・一括リネームの対象になります。
- EPUB/PDFとして書き出せる本の条件を広げました。
  - 元のファイル形式の制限をなくし、zip/cbz・rar/cbr・7z/cb7・PDF・EPUB・フォルダのすべてを対象にしました。
  - ブックマーク・ページレイアウト設定に加えて、メタデータの登録がある本も対象になります。一覧のインジケータにもメタデータを追加しました。
  - タイトル・著者は、メタデータを登録している本では登録内容が使われます。
  - 元がPDFの本をEPUBとして書き出す場合、埋め込まれている画像を取り出します(JPEG形式はそのままjpgに、Flate可逆圧縮形式はpngに変換)。それ以外の形式の画像を含むPDFは書き出せません。ファイル名はページ順の6桁連番になります。
  - 元がPDFの本をPDFとして書き出す場合は、ページをそのまま複製します(画像の形式を問わず、画質は劣化しません)。
- 書き出すEPUB・PDFに、メタデータのシリーズ名と巻数を埋め込むようにしました。EPUBはEPUB3標準とCalibre独自形式の両方に、PDFはキーワードに「series:シリーズ名, series_index:巻数番号」の形式で書き込みます。
- 書き出すEPUBの言語(dc:language)に、表示言語の設定に応じた言語コードを書き込むようにしました。従来の「und」(言語不明)では、Kindle Previewerがエラーとして扱うためです。
- 書き出すEPUBに、Kindleが固定レイアウトの本に必須としているメタデータ(元のページ解像度、ページ送り方向など)を書き込むようにしました。従来はKindle Previewerがエラー「E34002」を出し、変換に失敗していました。
- 書き出すEPUBで、ページのファイル名に「&」や空白が含まれていると正しく参照できていなかったのを修正しました。「&」を含む本はEPUBとして壊れており、Kindleではそのページが空のまま変換されていました(ファイル名の「連番リネーム」がOFFのときに起きていました)。
- WebP・HEIC・BMP・TIFF・AVIF形式のページは、EPUB書き出し時にPNGへ変換するようにしました。これらの形式のままではKindleが変換に失敗するためです。PNGは可逆形式のため画質は劣化しません(ファイルサイズは大きくなることがあります)。
- ライブラリデータのJSONの書き出し・読み込みに「メタデータ」と「メタデータのフォーマット」を追加しました。
  - 「メタデータのフォーマット」はアプリ全体の設定のため、読み込みは「上書き」か「無視」の2択です(既定は「無視」)。書き出し時も既定ではチェックが外れています。
  - メタデータの読み込みだけは対象の本を読み込む必要がないため、元のファイルが今その場に無くても取り込めます。
  - ファイル形式が formatVersion 3 になりました。formatVersion 2 で書き出したファイルもそのまま読み込めます。
- 環境設定ウインドウを、macOSのシステム設定と同じ2ペイン構成(左のサイドバーで画面を選び、右に中身を表示)に作り直しました。項目が増え、横一列のタブでは1つあたりが狭くなってきたためです。
  - サイドバーの8項目は「本」「操作」「詳細」の3つに区切って並べ、区切りごとに色を揃えたアイコンを付けています。
  - 最後に開いていた画面を記憶し、次に環境設定を開いたときも同じ画面から始まります。
  - ウインドウの大きさを変えられるようにしました(従来は固定)。変えた大きさは記憶されます。
- タブの幅に合わせて1語に切り詰めていた画面の名前を、中身が分かる名前に改めました。
  - 「開く」→「本を開く」、「描画」→「画像の表示」、「閲覧」→「閲覧中の動作」、「入力」→「キーとマウス」、「入力2」→「表示モード別の操作」、「アクセス権」→「フォルダのアクセス権」(「一般」「リセット」は変更ありません)
- 環境設定の説明文を整理し、画面に常時表示される文章を大幅に減らしました。
  - 項目名だけで意味が通るように名前を見直し、その言い換えでしかなかった説明文をなくしました。「前回の本を開く」→「前回読んでいた本を開き直す」、「サムネイルプレビュー」→「カーソルを合わせたページをプレビュー」、「自動的にポインタを隠す」→「動かしていないあいだポインタを隠す」など。
  - 項目名に収まらない補足は、項目名の右に付く ⓘ にまとめました。カーソルを合わせると吹き出しで、クリックするとポップオーバーで読めます。
  - ポップアップで選ぶ項目に出ていた、選択中の選択肢の説明もなくしました。
  - 「リセット」画面だけは説明文を残し、文字を大きく・濃くしました。「この操作は元に戻せません」という注意は赤で表示します。
- 環境設定のサイドバーの区切りの見出しと、スライダーの左右に出る下限・上限の数値を、読み取りやすい大きさ・濃さに調整しました。
- ページのサムネイルをディスクに保存しておくようにしました。同じ本を開き直したときや、アプリを再起動した後でも、サムネイルがすぐ表示されます。
  - 「レイアウトの編集」「ブックマーク・レイアウトの編集」ウインドウで本を選び直すたびに待たされることがなくなりました。ページグリッドとプログレスバーのサムネイルにも効きます。
  - 保存先はMacのローカルディスクのため、本体が外付けドライブやネットワークドライブにある場合でも2回目以降は速く表示されます。
  - コントラスト補正の切り替えや、本のファイルを差し替えた場合は自動的に作り直されます。環境設定「リセット」の一括削除でも消去されます。
- 「本ごとの保存データを削除」ウインドウで、ファイルの実在確認を一覧の表示とは別に進めるようにしました。確認が終わるまでは「確認中…」と表示され、上部に進捗が出ます。
- 「ブックマーク・レイアウトの編集」ウインドウで本を切り替えたとき、ページ一覧を先に表示するようにしました。一度開いたことのある本なら、本体の読み込みを待たずにすぐ一覧が出ます。
  - サムネイルは読み込みが済んだ順に埋まっていきます。
  - 読み込みが終わるまでの間は、上部に進捗が表示され、レイアウト列・並べ替え・読み方向の変更・ブックマークの追加は一時的に無効になります。既にあるブックマークの削除・リネームは最初から行えます。
- EPUB出力ウインドウの「Cover」列の表示を速くしました。既定のカバー名を求めるためだけに本を丸ごと読み込んでいたのをやめ、一度開いたことのある本ならすぐ表示されるようにしています。

### 修正

- 細工された`ComicInfo.xml`を含む本を開くと、Mac内の他のファイルの内容が読み取られ、メタデータとして登録されてしまう問題を修正しました。読み取られた内容は、その本をCBZとして書き出すと出力ファイルにも書き込まれるため、共有すると外部へ渡るおそれがありました。`ComicInfo.xml`の解析で外部のファイルを一切参照しないようにしています。
  - 併せて、極端に大きな`ComicInfo.xml`を読み込まないようにしました。圧縮された小さなファイルが展開時に数GBに膨らむ細工で、本を開いただけでアプリが停止することを防ぐためです。
- ブックマーク名やタイトルに特殊な文字が含まれていると、書き出したEPUBが壊れて開けなくなることがあったのを修正しました。
- 他のアプリが書いた`ComicInfo.xml`から、読み方向や表紙の指定を読み取れないことがあったのを修正しました(値の大文字・小文字の違いを問わないようにしています)。
- 本のファイルが差し替えられている疑いがあるとき、「保存済みのレイアウトをそのまま使うか」の確認に答える前に、EPUB/PDFのレイアウト情報が取り込まれてしまうことがあったのを修正しました。取り込みは確認に答えた後に行います。
- 元がPDFの本をEPUBとして書き出すときに、画像が埋め込まれているのに「埋め込み画像がありません」として書き出せないことがあったのを修正しました。他の形式から変換されたPDFで、ページの中身がひとまとまりに包まれている場合に起きていました。
- 読み込めないページがある本をEPUBとして書き出すと、そのページだけが欠けた壊れたEPUBが出来上がることがあったのを修正しました。書き出し自体を中止し、どのページで失敗したかを表示します。
- 「メタデータの編集」ウインドウを開いたときに、本の件数が多いとしばらく操作を受け付けなくなっていたのを修正しました。ファイル名からの推測を表示とは別に進めるようにし、推測が終わるまでは進捗表示を出します。推測の規則を変更したときも同様です。
- ライブラリデータのJSONからメタデータを読み込むとき、件数が多いと極端に時間がかかっていたのを修正しました。1件ずつ保存していたのをまとめて保存するようにしています。
- 削除・移動されたファイルが「最近開いた本」の履歴に残り続けることがあったのを修正しました。選んだ項目が見つからない場合は、その場で履歴から取り除くようにしています。
- 履歴・お気に入りの実在確認の最中に外付け・ネットワークドライブを接続しても、一覧にすぐ反映されないことがあったのを修正しました。
- 「本ごとの保存データを削除」で「表示中をすべて削除」を実行したとき、本の件数が多いと長く操作を受け付けなくなっていたのを修正しました。
  - 削除自体を本1冊ずつではなくまとめて行うようにしました。
  - 1冊削除するたびに、残りすべての本のファイルの実在確認をやり直していたのをやめ、まだ確認していない本だけを対象にするようにしました。外付け・ネットワークドライブ上の本が多いほど効果があります。
  - 確認が重なったときに、古い確認結果が新しい結果を上書きしたり、確認中なのに進捗表示だけ消えたりすることがあったのも併せて修正しました。
- EPUB出力・PDF出力ウインドウを開いたときに、対象になりうる本が多いとしばらく操作を受け付けなくなっていたのを修正しました。元のファイルが今も存在するかの確認を一覧の表示とは別に進め、その間は読み込み中の表示を出します。外付け・ネットワークドライブ上の本が多いほど効果があります。
- メタデータだけを登録した本が、EPUB出力・PDF出力の一覧に出てこないことがあったのを修正しました。EPUB/PDFを単体で開いてメタデータが自動的に取り込まれた本が、これに当てはまっていました。
- 巻数に「上」「下」のような数字以外を登録している本を書き出すと、EPUB/PDFに不正な巻数が書き込まれていたのを修正しました。数値として読める場合にのみ書き込むようにしています(書き込まれない場合もシリーズ名は書き込まれます)。
  - EPUBの巻数の項目は数値しか受け付けないため、従来はEPUBの検証ツールがエラーとして扱う状態でした。PDF側も、書き込んだ巻数をqooViewer自身が読み戻せない状態でした。
- メニューバーのメニューを直接クリックしたとき、メニューの中身が正しく表示されないことがあったのを修正しました。
  - 「ウインドウ」メニューから「画面全体に表示」「中央に配置」「移動とサイズ変更」などの標準項目が丸ごと消える、「編集」メニューで項目名が「…」で省略された狭い幅になる、本来使えないはずのカット/コピー/ペーストが使えるように見える、といった症状が出ていました。
  - 別のメニューを開いた状態からカーソルを移動した場合は正常に表示されるため、直接クリックしたときだけ起きていました。
  - メニューを開く直前に、履歴・お気に入り・ブックマークの実在確認をまとめて行っており、その待ち時間にmacOSのメニューの組み立てが間に合っていませんでした。メニューの表示中はファイルを一切読まないようにしました。
  - 履歴やお気に入りに外付けドライブ・ネットワークドライブ上のファイルが含まれている場合に起きやすい症状でした。
- ウェルカム画面と「お気に入りの整理」ウインドウの表示が、お気に入りの数や保存先の速さに左右されてもたつくことがあったのを修正しました。実在確認の結果を覚えておき、表示のたびにファイルを読み直さないようにしました。
- 「本ごとの保存データを削除」ウインドウを開いたときに、保存データを持つ本が多いとしばらく操作を受け付けなくなることがあったのを修正しました。
- 本を1冊開くたびに、履歴の件数に比例した待ち時間が発生していたのを解消しました。
- 本を開くとき、本体が外付け・ネットワークドライブ上にあると、ファイル情報の確認のために画面の描画が止まることがあったのを解消しました。
- 本を開くたびに、ページ一覧の保存が終わるまで表示を待たされていたのを解消しました。保存は表示とは別に進めます。
- 「ブックマーク・レイアウトの編集」ウインドウでブックマークを追加したときに、本体が外付け・ネットワークドライブ上にあると一瞬操作を受け付けなくなることがあったのを修正しました。
- ディスクに保存したサムネイルの読み書きが1件ずつ順番待ちになっており、複数の本・ウインドウを同時に開いていると表示が遅れることがあったのを修正しました。
  - 保存容量の点検(起動後に一度だけ行われます)の最中に、他のサムネイルの表示が止まることもなくなりました。
- EPUB出力・PDF出力ウインドウの一覧で、右端のインジケータの縦の並びが行によってずれていたのを修正しました。
- 書き出し先に元の本と同じフォルダを選び、同じ名前のファイルへ上書きしようとすると、元のファイルが失われることがあったのを修正しました(cbzをcbzとして書き出し直す場合など)。書き出しはいったん一時ファイルに対して行い、最後まで成功したときだけ本来の名前へ置き換えるようにしています。
- 書き出しの途中でキャンセルしたり失敗したりしたときに、出力先に中途半端なファイルが残らなくなりました。

## [1.14] - 2026-08-18

### 追加

- トラックパッドのピンチイン・ピンチアウトで、表示中のページを無段階に拡大できるようにしました。どの拡大縮小モードでも使えます。
  - 拡大の中心はマウスカーソルの位置です。カーソルを置いた場所が動かないように拡大されるので、読みたいところへ寄っていけます。
  - 拡大していない状態が100%で、それより小さくは縮小しません。上限は環境設定「描画」タブの「ピンチ拡大の上限」で変更できます(既定400%)。
  - 「画面内に収める」で拡大した場合も、画面からはみ出した部分をスクロールして読めます。
  - ページが変わったとき、拡大縮小モード・見開き/単ページ・読み方向を切り替えたとき、esc キーを押したときに解除されます。倍率は記憶されません。
  - トラックパッドの2本指ダブルタップ(スマートズーム)で、200%と等倍を切り替えられます。
  - 拡大中は画面表示用よりも高解像度の画像データに自動的に切り替わるため、画面をそのまま引き伸ばすよりも精細に表示されます。
  - 拡大中はマウスホイール・2本指スクロールがスクロール専用になり、読んでいる最中に意図せずページが送られることはありません。横方向のフリックによるページ送りも同様に止まります。
  - ピンチ操作中は、現在の倍率が画面上部に一時的に表示されます。
- 環境設定「閲覧」タブに「2本指スクロールを反転」を追加しました(既定OFF)。トラックパッドの2本指スクロールで画像が動く向きを、上下・左右とも逆にします。
  - macOSの「ナチュラルなスクロール」と違い、qooViewerのトラックパッド操作だけが対象です。物理的なマウスホイールの向きは変わりません。
  - 向きに操作を割り当てている部分(ホイールの上/下、フリックの左右によるページ送り)の向きは変わりません。そちらを入れ替えたい場合は「入力」「入力2」タブで割り当てを変更してください。
  - ONのときは、端まで来たときの跳ね返り(ゴムのように少し伸びる動き)がなくなり、可動範囲でぴたりと止まります。

### 変更

- 環境設定「描画」タブの「最大拡大率」(従来100〜500%)と「拡大鏡の倍率」(従来150〜400%)について、指定できる範囲をどちらも100〜800%に広げました。既定値は従来どおりです。

## [1.13] - 2026-08-18

### 追加

- ビューアの背景色に、好きな色を指定できる「カスタム」を追加しました。環境設定「描画」タブの「背景色」で「カスタム」を選ぶと、色を指定するダイアログが開きます。
  - ダイアログの上段はカラーパレットで、押すとその色が下段のRGBに読み込まれます。下段では赤・緑・青をスライダーまたは数値(0〜255)の直接入力で調整でき、調整中の色をプレビューと16進表記で確認できます。
  - 「元に戻す」でダイアログを開いた時点の色に戻せます。OKで確定、キャンセルで破棄され、キャンセルした場合は「背景色」の選択もダイアログを開く前の状態に戻ります。
  - 指定した色を変更したいときは「カスタム」をもう一度選んでください。プリセットの色に切り替えても指定した色は保存されたままです。

## [1.12] - 2026-08-17

### 追加

- 拡大縮小モードに「横幅に合わせる(単ページ)」を追加しました。見開きのページや、見開きのままスキャンされた横長のページを、片ページぶんが画面幅いっぱいになる大きさまで拡大し、左右にスクロールしながら片側ずつ読めます。
  - 画像を切り分ける機能ではありません。ページ番号も総ページ数も変わりません。縦長のページなど半分にする意味が無い内容では、「横幅に合わせる」と同じ表示になります。
  - 新しいページを表示したときは、読み方向に応じた読み始め側(右開きなら右上、左開きなら左上)から表示されます。
- 拡大縮小モードを直接選ぶキーボードショートカットを追加しました(画面内に収める=⌘1、横幅に合わせる=⌘2、横幅に合わせる(単ページ)=⌘3、拡大縮小しない=⌘4)。従来どおり f キーで順に切り替えることもできます。
- 画面をスクロールできる表示モード向けの操作を追加しました。「1画面分進む+次のページ」は、1画面分スクロールし、横にも余地があれば1画面分横へ移り、それも尽きたら次のページへ進みます。ほかに「1画面分下へ/上へ」「ページの先頭へ/末尾へスクロール」を追加しています。
- スクロールできる表示モードで、画像をドラッグして動かせるようにしました(掴んでいる間はカーソルが手の形になります)。
- 少しずつスクロールする操作を追加しました。既定では ↑↓←→ に割り当てており、1回あたりの移動量は「入力2」タブの「スクロールの量」で変更できます(既定20pt、表示モードごと)。
  - 従来は「1画面分」単位の送りしか無く、とくに「拡大縮小しない」で横に長い画像を見るとき、キーボードから横方向へ動かす手段がありませんでした。
- 環境設定に「入力2」タブを追加しました。表示モードごとに、キーボード・マウスの割り当てとマウスホイールの動作を別々に設定できます。
  - 「入力」タブの割り当てが基本になり、「入力2」で設定した項目だけがそのモードで上書きされます。設定していない項目は「入力」タブの割り当てがそのまま働きます。
  - 表示モードによって意味が変わる操作(スクロール系とページ送り系)だけを扱います。ブックマークやお気に入りなど、どのモードでも同じように働く操作は「入力」タブにまとまっています。
  - マウスホイールは「スクロールのみ(既定)」「スクロール(端で横へ)」「スクロール+ページ送り」「ページ送りのみ」から選べます。「スクロール+ページ送り」にすると、ホイールだけで本を最後まで読み進められます。
- サイドパネルをウインドウの右側にも表示できるようにしました。環境設定「一般」タブの「パネルの位置」で左右を選べます(既定は左)。
  - 常時表示・「サイドパネルを隠す」のどちらの状態にも反映されます。隠す設定の場合は、マウスカーソルを近づけてパネルを表示させる端も、選んだ側に切り替わります。
  - パネルの幅を調整するドラッグ位置(ビューアとの境界)や、「ページ」モードでサムネイルにカーソルを合わせたときの拡大プレビューが出る向きも、合わせて左右が入れ替わります。

### 変更

- 画面をスクロールできる表示モード(横幅に合わせる/横幅に合わせる(単ページ)/拡大縮小しない)でも、画面クリックでページを読み進められるようにしました。従来は「画面内に収める」のときだけでした。
  - 既定では1画面分スクロールし、スクロールしきったら次のページへ進みます。読み方向に応じて、画面の一方をクリックすると進み、もう一方をクリックすると戻ります。
  - 画像をドラッグして動かした場合は、指を離してもページは送られません。
- 自動的に隠す設定にしているサイドパネル・ツールバー・プログレスバーが、マウスカーソルをウインドウの外(他のアプリのウインドウ・デスクトップ・メニューバーなど)へ移動したときにも隠れるようにしました。従来は、ウインドウの中へカーソルが戻るまで表示されたままになることがありました。

## [1.11] - 2026-08-14

### 変更

- サイドパネルの「ページ」モードの表示を見直しました。
  - 左からページ番号・サムネイル・ファイル名の順に並ぶようにしました(従来はサムネイルとページ番号のみで、ファイル名は表示されませんでした)。
  - サムネイルにカーソルを合わせると、拡大プレビュー(ファイル名付き)が表示されるようになりました。大きさはページ一覧(サムネイルグリッド)や「ブックマーク・レイアウトの編集」ウインドウの拡大プレビューと揃えてあります。
- サイドパネルを隠す設定にしている場合、マウスカーソルを左端に近づけてパネルを表示した時点で、上段(フォルダブラウザ)・下段(本の中身ブラウザ)とも、現在の本/ページの行まであらかじめスクロールされた状態で表示されるようにしました(従来は一覧の先頭から表示され、次にページを送るまでスクロールされませんでした)。
- サイドパネルの上段(フォルダブラウザ)で、同じフォルダ内の別の本に切り替えた場合も、新しくハイライトされた行まで自動的にスクロールするようにしました。
- サイドパネルの「ページ」モードで、ページレイアウトの除外/ページ順の変更によって一覧の中身が変わった場合も、現在のページの行まで自動的にスクロールするようにしました。
- ツールバーとプログレスバーのボタンのデザインを、サイドパネルのボタンと同じ、枠のない一回り大きいアイコンに統一しました(従来は macOS 標準の枠付きボタンでした)。

### 修正

- ファイル・メモリ・内部の監視処理といったリソースを「確保したまま解放していない」箇所がないか、ソースコード全体を対象に調査し、見つかった問題をまとめて修正しました。以下がその内訳です。
- PDF書き出しで、書き出せるページが1枚も無かった(すべてのページの画像を読み込めなかった)場合に、書き出し先と同名の既存ファイルが削除されたうえ、中身の壊れたPDFだけが残ってしまう不具合を修正しました。この場合、既存のファイルはそのまま残ります。
- ウインドウを閉じた後も内部の監視や読み込み処理が残り続けることがある問題を数か所修正しました。アプリを起動したまま長時間使い続けたときの安定性が向上します。
  - ウインドウを閉じた後に、別のウインドウの位置・サイズが「メインウインドウの位置・サイズ」として誤って記憶され、次回起動時に意図しない位置・サイズで開いてしまうことがある不具合を修正しました。
  - EPUB書き出しウインドウの一覧を表示・スクロールするたびに、本のファイルへのアクセス権が解放されずに積み重なっていく問題を修正しました。
- 本をたくさん開いたときに、動作が次第に重くなりメモリ使用量が増え続ける問題を修正しました。
  - 本を閉じても、その本の画像キャッシュ・読み込み処理・開いたままのファイルが内部に残り続け、開いた冊数ぶん積み上がっていました。ブックマークやページレイアウトの変更が、本をたくさん開いた後ほど遅くなる原因にもなっていました。
  - お気に入り・履歴から本を開くたびに、ファイルへのアクセス権を確保したまま解放していませんでした(増え続けると、最終的にファイルを開けなくなるおそれがありました)。
  - 同じウインドウで本を切り替えるたびに、ウインドウ内部の処理が一段ずつ増えていきました。
- ページを素早く送ったとき、もう表示されないと分かっているページの画像読み込みが最後まで実行されていた問題を修正しました。素早いページ送りへの反応が改善します。
- 大量の書庫ファイルを含む本(フォルダ内に多数の zip/rar などが並んでいる本や、書庫の中に書庫が入れ子になっている本)を読み進めたときに、開いたままのファイルが際限なく増えていた問題を修正しました。

## [1.10] - 2026-08-13

### 追加

- サイドパネルを追加しました。ウインドウ左端に常時表示され、最上部のボタンで「ブラウザ」「ブックマーク」「履歴」「ページ」の4つのモードをワンクリックで切り替えられます(ボタンはアイコンのみの表示で、カーソルを合わせるとモード名が表示されます。選んだモードは次回起動時にも記憶されます)。
  - ブラウザモードでは、上段に現在のファイル/フォルダと同じ場所にある一覧(本を開いていないときはボリューム一覧)、下段に今開いている本自身の中身を表示します。
  - 上段からファイル/フォルダを選んで本を開いたり、フォルダを移動したりできます。パネルを開くと、現在の本(または1階層上へ移動した場合は直前にいたフォルダ)が自動的にハイライト表示されます。アクセス権の無いフォルダは、その場で許可できます。
  - 下段では、本の内部(フォルダ・書庫の中身。書庫の中に書庫が入っている場合を含む)をたどり、画像をダブルクリックしてそのページへジャンプできます。
  - ブックマークモードでは、上段にお気に入りをフォルダ階層のままツリー表示し、下段に今開いている本のブックマーク一覧を表示します。お気に入りをクリックするとその本を開き、ブックマークをクリックするとそのページへジャンプします(今表示中のページのブックマークは強調表示されます)。
  - ブックマークモードの上段・下段それぞれに、「今の本をお気に入りに追加」「今のページをブックマークに追加」ボタンと、それぞれの編集ウインドウを開くボタンがあります。
  - 履歴モードでは、過去に開いた本(ファイルメニューの「Open Recent」と同じ一覧)を新しい順に表示します。本を読んでいる最中でも一覧として確認でき、クリックでそのまま開けます。
  - ページモードでは、今開いている本の全ページをサムネイル付きの縦1列で表示します。クリックでそのページへジャンプでき、ページを送るたびに現在のページへ自動的にスクロールします(ブックマークのあるページにはしおりのアイコンが付きます)。ページ一覧(サムネイルグリッド)と違い、ジャンプしても閉じないため出しっぱなしにして使えます。
  - ブラウザモードの上段・下段、および履歴モードには絞り込み用の入力欄があり、名前の一部を入力して目的の項目を素早く探せます。
  - 表示メニューの「Hide Side Panel」で、ツールバー・プログレスバーと同様に普段は隠しておき、マウスカーソルをウインドウ左端に近づけたときだけ表示する動作に切り替えられます。
  - パネル右端をドラッグして幅を、上段・下段の仕切り線をドラッグして上下の表示比率を調整できます(幅は次回起動時にも記憶され、上下の比率はモードごとに別々に記憶されます)。
  - 環境設定「一般」タブの「サイドパネルを有効にする」をOFFにすると、サイドパネル自体とメニューバー「View」→「Hide Side Panel」を非表示にできます。
- 「最近開いたファイル」の履歴として保持する件数を、環境設定「一般」タブの「履歴の保存件数」で変更できるようにしました(10〜200件、既定30件。従来は10件固定でした)。ファイルメニューの「Open Recent」と、サイドパネルの「履歴」モードの両方に反映されます(ウェルカム画面の一覧は従来どおり最大10件です)。
- 書庫の直下(またはその中で見つかった、入れ子になった書庫の中)に画像が無く、書庫ファイルしか無い場合でも、その中身を統合して1冊の本として開けるようにしました。
- 拡大鏡機能を追加しました。マウスカーソル位置を中心に画像の一部を円形に拡大表示します。
  - ON/OFFは既定 l キー(環境設定「入力」タブで変更可能)、メニューバー「View」→「Loupe」、右クリックメニューの「Loupe」から。OFFは同じ操作の再実行、画像上での左クリック・右クリック、または esc キーでも行えます。
  - 拡大率・レンズのサイズは環境設定「描画」タブの「拡大鏡」セクションで調整できます。
  - 表示中は、ツールバー・プログレスバー・サイドパネルの自動表示(マウスを画面端に近づけたときの一時表示)が無効になります。
  - 画面表示用の画像よりも高解像度の画像データを参照して拡大するため、画面をそのまま引き伸ばすよりも精細に表示されます。
  - 見開き表示中は、左右のページの境目をまたいでも途切れず拡大できます。
- コントラスト補正機能を追加しました。紙の黄ばみなどでコントラストが甘くなった古いスキャン本を、きっちりした白黒に見えるよう補正して表示します。
  - メニューバー「View」→「Contrast Correction」で本ごとにON/OFFを切り替えます(既定OFF)。設定は本単位で記憶され、次回その本を開いたときも同じ状態で表示されます。
  - カラーのページ(口絵・カラーイラスト等)は自動的に判定され、適用されません。
  - 拡大鏡で表示される画像にも同じ補正が反映されます。
- 「ブックマーク・レイアウトの編集」ウインドウの左ペイン(本の一覧)・右ペイン(ページの一覧)で、カーソルキーの上下から選択を移動できるようにしました。

### 変更

- 内部処理のリファクタリングを行いました(二重に実行されていた処理、不要なデータベースの読み書き、重複していた実装の整理)。機能面の変更はありません。

### 修正

- 「ブックマーク・レイアウトの編集」ウインドウのもたつきを改善しました。
  - 一覧の項目をクリックしても、まれに選択が反応しないことがある問題を修正しました。
  - 本を切り替えたとき、ページ一覧をスクロールしたとき、ウインドウをリサイズしたときの引っかかりを軽減しました。
  - ブックマークやページレイアウトを追加・変更・削除したときの反応を速くしました(登録している本が多いほど効果があります)。
- メニューやドロップダウンを開くたびに一瞬引っかかることがある問題を修正しました(編集ウインドウに限らず、アプリ全体が対象です)。
- ブックマークのリネーム欄などのテキストフィールドで、Cmd+Aによる全選択ができなかった不具合を修正しました。
- ブックマークがある本を表示している状態で「ブックマークの編集」を呼び出しても、その本が選択状態にならなかった(一覧がスクロールされていた場合は選択行が画面外のままになることもあった)不具合を修正しました。

## [1.05] - 2026-08-12

### 変更

- ページ一覧(サムネイルグリッド)を、パネルの外側であればビューア画面に限らずどこをクリックしても閉じるようにしました(タイトルバー・メニューバー・他のウインドウ・他のアプリを含みます)。
- 「情報を見る」の表示形式を、サブメニューからビューアウインドウ内のパネル表示に変更しました(値の位置が揃うようになり見やすくなりました)。
  - 情報を「一般情報」(種類・サイズ・場所・名前・作成日・変更日)と「詳細情報」(解像度・色空間・カラープロファイル・アルファチャンネル)に整理しました。
  - 見開き表示中にどちらのページの情報かが分かるよう、対象ページのサムネイルを表示するようにしました。

### 修正

- 「ブックマーク・レイアウトの編集」ウインドウで、行(左ペインのファイル名、右ペインのページ)をクリックしてから選択状態(ハイライト)になるまで、わずかにラグを感じることがある不具合を修正しました。
- 「お気に入りの編集」ウインドウのフォルダ・お気に入り項目についても、同様のラグを修正しました。
- qooViewerを起動したままウインドウをすべて閉じた状態から本を開き直したときに、以下の不具合を修正しました。
  - 他のアプリから外部ビューアとして本を開こうとすると、ウインドウが一瞬表示されてすぐ閉じてしまい、本が表示されないことがある。
  - 他のアプリ経由で本を閉じた直後に再び開こうとすると、ウインドウがフリーズする(応答しなくなる)ことがある。
  - 「最近開いたファイル」または「お気に入り」から本を開いても、何も起こらない(ウインドウが開かない)ことがある。
  - 続けて別の本を開くと、既存のウインドウを再利用せず毎回新しいウインドウで開いてしまうことがある。
  - ウインドウの位置・サイズを調整しても、ウインドウを閉じて開き直したときや、同じウインドウ内で別の本に切り替えたときに、調整前の位置・サイズに戻ってしまうことがある。

## [1.04] - 2026-08-11

### 修正

- ページ一覧(サムネイルグリッド)を表示している間、マウスカーソルを動かしても自動非表示が解除されず、隠れたまま戻らなくなることがある不具合を修正しました。
- ページ一覧(サムネイルグリッド)のページ番号が見づらかったため、白色で表示するようにしました。

## [1.03] - 2026-08-11

### 追加

- PDFのブックマーク・見開きレイアウト対応を強化しました(EPUBと同様の扱い)。
  - PDFに埋め込まれているしおり(アウトライン)を、本を開いたときに自動的にブックマークとして取り込むようになりました(この本にまだブックマークが無い場合のみ)。
  - PDFに読み方向(左開き/右開き)や見開き表示の指定が埋め込まれている場合、それをEPUBと同じように優先して反映するようになりました。この場合、読み方向・見開き表示の切り替えや、ページレイアウトの編集は行えません(常にPDF側の指定が優先されます)。
  - どちらも、該当する情報を持たないPDFにはこれまでと同じく影響しません。
- PDF形式で書き出す機能を追加しました(フォルダ / zip・cbz / rar・cbr / 7z・cb7形式、かつブックマークもしくはページレイアウトを設定していること。EPUB出力と同じ対象条件)。ファイルメニューの「EPUBとしてエクスポート…」の下に「PDFとしてエクスポート…」が追加されます。
  - ブックマークをPDFのしおり(アウトライン)として、タイトル・著者名をPDFのメタデータとして埋め込みます。
  - EPUB出力と異なり、カバー画像の指定機能はありません。
  - PDF形式には見開き・読み方向のレイアウト情報を保存する手段が無いため、これらの設定は書き出しに含まれません(出力ウインドウにその旨の案内を表示します)。
- 「ブックマーク・レイアウトの編集」ウインドウに、ダブルクリックでの操作を追加しました。
  - 左ペインでファイル名をダブルクリックすると、その本を開きます。
  - 右ペインでページ番号・サムネイル・余白など(ボタンやドロップダウンを除く部分)をダブルクリックすると、そのページへジャンプします。
- ファイルメニューとコンテキストメニューに「Finderで開く」を追加しました。フォルダの本はそのフォルダを、ファイルの本(zip・cbz等)は親フォルダを開いてそのファイルを選択した状態で、Finderを開きます。
- コンテキストメニューに「情報を見る」を追加しました。見ているページのファイル名・フォーマット・解像度・色情報・ファイルサイズをサブメニューで確認できます。

### 変更

- ページ一覧(サムネイルグリッド)の閉じ方を変更しました。独立したウインドウ風の表示ではなく、ビューア画面に重ねて表示するパネルになり、パネルの外側(ビューア画面)をクリックすると閉じるようになりました。「閉じる」ボタンと表示順を反転する機能は廃止しました(サムネイルをクリックしてページへ移動した場合は、これまでどおり自動的に閉じます)。
- 環境設定の視認性を改善しました。
  - 選択肢を選ぶ項目に枠線を付け、クリックして選べる場所であることが分かるようにしました。
  - 設定項目の名前を短くし、説明文を項目の下に移しました。項目名が途中で折り返さなくなり、説明も読みやすくなりました。
  - 選択肢を選ぶ項目では、いま選んでいる選択肢の説明が項目のすぐ下に表示されるようになりました。選び直すと説明も切り替わります。
  - 「開く」タブの「Finderから開いたとき」「お気に入りを開くとき」を、同じ設定であることが分かるように1つのセクションにまとめました。
  - スライダーの項目で、現在の値を右側にまとめて表示するようにしました。
  - 環境設定ウインドウを少し広げました。
  - タブ名を短くしました(「キー・マウス操作」→「入力」)。英語表示では、幅が不揃いだったタブが揃うようになりました。
  - 「リセット」タブの見出しに警告アイコンを表示し、削除される内容を箇条書きにしました。
  - 「アクセス権」タブに、フォルダの許可がなぜ必要かの説明を追加しました。
  - 一部の設定項目名・選択肢名の文言を整理しました。

## [1.02] - 2026-08-10

### 変更

- アプリアイコンを更新しました。

## [1.01] - 2026-08-09

### 追加

- ビューアが表示している画像をそのままファイルとして書き出せる「画像のエクスポート」機能を追加しました。ファイルメニュー、およびコンテキストメニューの「画像のエクスポート」から使えます。
  - 単一ページ表示中は、そのページをエクスポートできます。
  - 見開き表示中は、右または左のページを個別にエクスポートするか、見開き全体を1枚の画像に結合してエクスポートできます(結合時、左右の高さが異なる場合は低い方に合わせて調整されます)。
  - 保存先のフォルダとファイル名を指定するウインドウが開きます。

### 変更

- UIを調整しました。
  - 独立したメニューだった「お気に入り」「レイアウト」の操作を、標準の「編集」メニューにまとめました。
  - お気に入り・ブックマークの追加/削除ボタンの文言を整理しました。
  - 「ブックマーク・レイアウトの編集」ウインドウに、ブックマークとレイアウト設定をまとめて削除できる項目を追加しました。
  - ブックマークの名前変更欄を開いたときに、既存の文字列が自動的に選択された状態になるようにしました。
  - お気に入り・ブックマーク・ページレイアウト設定の書き出し結果に、スキップした本がある場合はその一覧を表示するようになりました。
  - 「表示順を初期化」を「ページ順を初期化する」という文言に変更しました。
  - ブックマークの一括リネームウインドウで、表紙を割り当てる設定の文言をより分かりやすく変更しました。

### 修正

- EPUB出力ウインドウの一覧に、元のファイル/フォルダを削除済みの本が表示されてしまう不具合を修正しました。
- 横長画像と縦長画像が混在する本を見開き表示でページ送りすると、本来表示されるべきページが飛ばされてしまうことがある不具合を修正しました。
- 見開き表示中に「1枚だけ前の画像へ」を使うと、本来は隣のページと組になって見開き表示されるはずが、片方だけが単独ページとして表示されてしまうことがある不具合を修正しました。
- お気に入り・ブックマーク・ページレイアウト設定をJSONファイルに書き出す際、ブックマークだけファイルの移動・リネーム追跡用の情報が含まれないことがある不具合を修正しました(お気に入り・ページレイアウト設定は以前から含まれていました)。この情報が無い既存のブックマーク・お気に入り・ページレイアウト設定も、書き出し時に自動的に補われるようになりました。
- 日本語ローカライズを一部修正しました。

## [1.00] - 2026-08-04

### 追加

- ページレイアウト設定機能を追加しました。
  - 読み方向・見開き/単ページ表示・ページ順序を、本全体またはページ単位で指定できます。
  - メニューバー・コンテキストメニュー・ツールバーの「レイアウト」から操作できます。
  - 変更を適用する範囲(このページだけ/これ以降/本全体など)を選べます。
  - 見開き/単ページ表示を自動で判定する「自動レイアウト」機能を追加しました。
- 従来の「ブックマークの編集」ウインドウを、「ブックマーク・レイアウトの編集」ウインドウに置き換えました。
  - 「お気に入りの編集」と同じ2ペイン構成で、本を開いていなくても、すべての本のブックマークとページレイアウト設定を横断的に一覧・検索・編集できます。
  - 右ペインに、ページ番号の列と、スクロールしない列見出し・列の区切り線を追加しました。
  - ファイル名の表示に対応形式(zip/cbz・rar/cbr・7z/cb7・PDF・EPUBなど)を示すバッジを常時表示するようにしました。
  - ブックマークの一括リネーム機能を追加しました。
- お気に入り・ブックマーク・ページレイアウト設定をJSONファイルとして書き出し/読み込みできる機能を追加しました(ファイルメニューから)。
- 選択した本をEPUB形式で書き出せる機能を追加しました。
  - 書き出し時のカバー画像は、本に含まれるページから選び直せます。
  - 本に含まれない画像ファイルを、専用のカバーとして追加することもできます。
  - 書き出すタイトル・著者名を、本ごとにその場で編集できるようになりました。ウインドウを開いた時点で、ファイル名から判定できる場合は自動的に入力されます。
  - 一覧に元のファイル名を表示する「File Name」列を追加しました。
  - タイトル行の列区切り線をドラッグして、各列の幅を個別に調整できるようになりました。ウインドウを開いた時点の内容に応じて、初期の列幅・ウインドウ幅も自動調整されます。
- 見開き表示中にツールバー/お気に入りメニューからブックマークを追加する際の挙動(読み方向に基づく既定側のページに固定するか、毎回どちらのページか尋ねるか)を選べる設定を環境設定「開く」タブに追加しました。
- ブックマーク追加/削除ボタン、お気に入り編集ウインドウのゴミ箱ボタンにツールチップを追加しました。
- 「現在の表示を基準に自動でレイアウトする」操作にキーボードショートカットを追加しました(既定: e キー。環境設定「キー・マウス操作」タブから変更できます)。

### 変更

- 「ブックマーク・レイアウトの編集」ウインドウの右ペインを整理しました。
  - ページの並べ替え・除外設定が、開いているビューアにも即座に反映されるようになりました。
  - 除外したページは一覧の最後尾にファイル名順で表示され、除外を解除すると元の位置へ自動的に戻るようになりました。
  - 下部にあった各種ボタンを「一括操作」メニューにまとめ、ツールバー上へ移動しました。
- EPUB出力ウインドウの選択方法を変更しました。
  - チェックボックス列をタイトル行に統合し、タイトル行のチェックボックスで全選択/全選択解除できるようにしました(従来の「選択」メニュー・「全選択解除」ボタンは廃止しました)。
  - 「連番リネーム」設定の既定をOFFに変更しました。
- レイアウト変更を「このページだけ」に適用した場合、見開きの相方ページの設定は書き換えないようにしました。
- 見開き表示での左右のラベル表記が、読み方向に応じて画面上の左右と一致するように変更しました。
- サムネイルにカーソルを合わせたときのプレビュー画像を大きくしました。
- メニューバーの「レイアウト」メニューを「お気に入り」の右に配置しました。
- 環境設定ウインドウに「開く」タブを追加し、「一般」タブにあった「本を再度開いたとき」「Finderから開いたとき」「お気に入りを開くとき」「見開き表示でのブックマーク追加」の設定を移動しました。
- お気に入り・ブックマーク・ページレイアウト設定を付けた後でも、対象のファイル/フォルダを同一ボリューム内でリネーム・移動すれば、登録内容がそのまま引き継がれるようになりました(ボリュームを跨いだ移動は非対応)。
- お気に入り・ブックマーク・ページレイアウト設定のJSON書き出し形式を変更しました。以前のバージョンで書き出したJSONファイルは読み込めません。
- 「同じフォルダのファイルを開く」「最近開いたファイルを開く」などのメニュー、お気に入り一覧、ウインドウタイトルでの本の名前表示を、「ファイル名 (形式)」のようなバッジ付き表記から、拡張子を含むファイル名(例:「タイトル.cbz」)をそのまま表示する形に変更しました。
- 「ブックマークの一括リネーム」ウインドウの表示を整理しました。
  - 「表紙に固定の名前を付ける」を「先頭ページに固定で表紙を割り当てる」という文言に変更しました。
  - 連番の設定項目を、上から「番号前の文字列」「連番開始番号」「番号後の文字列」の順に並べ替えました。
- JSONファイルのインポート処理を高速化しました。ブックマーク・ページレイアウト設定・お気に入りの件数が多いライブラリでは、インポートにかかる時間が大幅に短くなります。
- 「ブックマーク・レイアウトの編集」ウインドウでページを並べ替える際の速度を改善しました。見開き左/右のレイアウトを多くのページに指定している本ほど効果があります。
- 「ブックマークの一括リネーム」の実行速度を改善しました。ブックマークの件数が多い本ほど効果があります。
- 環境設定「キー・マウス操作」タブのキーボード操作一覧の並び順を、機能ごとにまとまるよう整理しました(ページ送り→表示・レイアウト→ページ一覧→ブックマーク→お気に入り→スライドショー・実寸表示→本の移動)。

### 削除

- 「ブックマーク・レイアウトの編集」ウインドウの右ペイン下部にあった「現在のページを追加」ボタンを削除しました。

### 修正

- お気に入り・ブックマーク・ページレイアウト設定関連の不具合を修正しました。
  - お気に入り・ブックマーク・ページレイアウト設定が、まれに一部または全部消えてしまうことがある不具合。
  - 「ブックマーク・レイアウトの編集」ウインドウで、同じブックマークの名前を続けて2回リネームしようとすると、2回目の入力欄が空欄になってしまう不具合。
  - 「お気に入りの編集」ウインドウで、同じフォルダ名/お気に入り名を続けて2回リネームしようとすると、2回目の入力欄が空欄になってしまう不具合。
  - レイアウトやブックマークを編集した本が、「EPUB出力」ウインドウの一覧にすぐには反映されず、アプリを再起動しないと表示されない不具合。
  - 環境設定「リセット」タブの全削除が、保存データの状態によっては一部を削除しきれないことがある不具合。
  - 「ブックマークの一括リネーム」ウインドウで、先頭ページに割り当てられる表紙用ブックマークの名前が「表紙」ではなく「カバー画像」になってしまう不具合。
  - 「ブックマークの一括リネーム」ウインドウのプレビュー欄が先頭6件しか表示されず、それ以降のブックマークの結果を確認できない不具合。
- ページレイアウト機能の不具合を修正しました。
  - コンテキストメニューからレイアウトを変更すると、見開き表示中に意図しないページに適用されてしまうことがある不具合。
  - 「ブックマーク・レイアウトの編集」ウインドウでレイアウトを変更すると、指定した適用範囲を無視して本全体に適用されてしまう不具合。
  - 設定を保存する際に、無関係な他のページの設定が消えてしまうことがある不具合。
  - 「見開き左」を「このページだけ」に指定すると、隣接するページにも誤って設定が反映されてしまう不具合。
  - 起動直後は見開き状態が「レイアウト」メニューに反映されない不具合。
  - 自動レイアウトの計算結果が本の途中までしか正しく広がらない不具合。
  - EPUBの巻頭・巻末ページで見開き指定をしても単一ページとして扱われてしまう不具合。
  - 見開きの末尾にある単独ページが、戻る方向のページ送りで飛ばされてしまう不具合。
- 「ブックマーク・レイアウトの編集」ウインドウの不具合を修正しました。
  - お気に入りから開いた際、右ペインの絞り込みが正しく機能しない不具合。
  - 右ペインでページをドラッグ&ドロップで並べ替えられない不具合、除外したページがあると並べ替えられない不具合。
  - ページを並べ替えると、ブックマークが元のページ(ファイル)に正しく追従しない不具合。
  - 読み方向のドロップダウンが、実際に使われている読み方向ではなく常に「既定」と表示されてしまう不具合。
  - 列の区切り線がタイトル行と各行とでずれて表示される不具合(EPUB出力ウインドウにも同様の列区切り線を追加しました)。
  - ブックマーク追加の「＋」ボタンが機能しない不具合。
- EPUB関連の不具合を修正しました。
  - 出力時、出力先の空き容量判定が正しく行われない不具合。
  - JSONファイルをインポートすると、ブックマークが本来とは異なるページに紐づいてしまう不具合。
  - EPUBファイル由来のブックマーク・レイアウト情報が、通常の「ブックマーク・レイアウトの編集」ウインドウに混じって表示されてしまう不具合。
- 書き出したEPUBをApple Booksで開いたときの不具合を修正しました。
  - 本ごとに読み方向を上書き設定していない場合、既定の読み方向がEPUBに明示的に書き出されず、右開きの本でも左開きとして認識されてしまう不具合。
  - カバー画像が正しく認識されず、表示されない不具合。
- 見た目・ローカライズの不具合を修正しました。
  - EPUB出力ウインドウ、ライブラリのインポート/エクスポートウインドウなど、一部のウインドウタイトルが表示言語を日本語にしていても英語のまま表示されてしまう不具合。
  - インポート/エクスポートウインドウで、内容が少ないときにウインドウ下側にだけ大きな余白ができてしまう見た目の不具合。
  - 環境設定の一部の項目が、表示言語を日本語にしていても英語のまま表示されてしまう不具合。
- 他のウインドウへフォーカスを移すためにクリックしただけで、意図せずページが送られてしまう不具合を修正しました。
- ウインドウをDockに最小化したままFinderから本を開くと、ウインドウが最小化された状態のままになってしまう不具合を修正しました。
- ファイルメニューの「同じフォルダのファイルを開く」「最近開いたファイルを開く」に表示されるファイル名が拡張子を除いた名前のみで、同名のcbz/epubなど拡張子違いの同じ本が並ぶと見分けが付かない不具合を修正しました(お気に入りメニューなど他の一覧と同様、拡張子を示すバッジを名前の末尾に表示するようにしました)。
- 「お気に入りに追加」ウインドウで、新規フォルダの作成ボタンがツールバーの中に埋もれて見つけにくい問題を修正しました。ウインドウ左下に、「Cancel」「Add」と同じ行で表示するようにしました。
- EPUB出力ウインドウの列見出し「File Name」「Author」が、表示言語を日本語にしていても英語のまま表示されてしまう不具合を修正しました。

## [0.99] - 2026-07-29

### 追加

- お気に入り機能を追加しました。
- EPUB(固定レイアウトの画像ベースのコミックEPUB)に対応しました。
- 数字キーでページジャンプするキーボードショートカットを追加しました。
- お気に入り・ブックマークを追加/削除すると、画面下部に「「〜〜」をお気に入りに追加しました」のような確認メッセージが表示されるようになりました。

### 変更

- ブックマークの編集機能をリニューアルしました。
- お気に入り関連のキーボードショートカットの既定値を、対応するブックマーク操作のショートカットにOptionキーを組み合わせたものに変更しました。環境設定「キー・マウス操作」タブでの並び順も、ブックマーク関連の項目のすぐ下にお気に入り関連の項目が並ぶよう整理しました。
- お気に入り・ブックマークの追加/削除を、メニューバー・コンテキストメニュー・ツールバーのどこからでも1つのボタンで行えるようにしました。登録済みかどうかに応じて「追加」「削除」が自動的に切り替わり、ツールバーのボタンは登録済みのとき色が反転して一目で見分けられます。
- 見開き表示中、左右どちらか一方のページにブックマークが付いていれば「登録済み」として表示されるようになりました。削除操作を行うと、見開き両方にブックマークが付いている場合は両方まとめて削除され、確認メッセージにも両方のページ名が表示されます。

### 削除

- ツールバーから、ブックマーク一覧・お気に入り一覧を表示するボタンを削除しました(一覧はメニューバー・コンテキストメニューから引き続き開けます)。
- 環境設定「キー・マウス操作」タブから「お気に入り一覧を表示」の項目を削除しました(お気に入り一覧はメニューバー・コンテキストメニューのサブメニューから引き続き開けます)。

### 修正

- 既に開いている本を「新しいウインドウで開く」「新しいタブで開く」(Finderから開いたときに、環境設定「Finderから開いたとき」が「新しいタブ/ウインドウで開く」になっている場合を含む)で開こうとすると、同じ本をもう1つ開いてしまっていた不具合を修正しました。既にその本を開いているウインドウ/タブがある場合は、新しく開き直さずそのウインドウ/タブをアクティブにするようになりました。
- 環境設定「キー・マウス操作」タブでキーボードショートカットを追加しようとした際、既に別の操作へ割り当て済みのキーを選べてしまう(かつ選ぶとその操作から無言で奪ってしまう)不具合を修正しました。既に使用されているキーを選んだ場合は、どの操作に割り当て済みかを示す警告を表示し、追加を拒否するようになりました。
- 「ツールバーを隠す」設定をONにしているとき、隠れているツールバーを表示させようとカーソルを上端に近づけても、ツールバーの上のほうにしか反応せず、少し下(ボタンの下半分など)にカーソルを動かすと隠れてしまう不具合を修正しました。ツールバーの表示領域全体でカーソルに反応するようになりました。タブバー表示中も同様に修正されています。
- プログレスバーのサムネイルプレビュー下に表示されるファイル名・ページ番号が見づらかった問題を修正しました。文字を少し大きくし、ファイル名の背景に色を付けることで、白っぽいページの上でも読みやすくなりました。
- 「現在のページをブックマークから削除」の操作が機能していなかった不具合を修正しました。
- ウェルカム画面の「最近開いたファイル」「最近お気に入りに追加したファイル」をダブルクリックで開くと、2回目のクリックが開いた本のページ送りとして誤認識され、意図せず1ページ送られてしまうことがある不具合を修正しました。

## [0.98] - 2026-07-27

### 追加

- 環境設定「閲覧」タブに「プログレスバーにカーソルを合わせたときにサムネイルプレビューを表示する」設定を追加しました。OFFにすると、プログレスバーのホバー時にサムネイル・ファイル名を含むフィルムストリップを表示せず、カーソル位置に対応するページ番号だけを表示するシンプルな表示になります(サムネイルの読み込みも行われなくなります)。既定はONで、従来通りの表示です。

### 変更
- 「起動時に前回開いていた本を自動的に開く」設定の挙動を改善しました。復元前にそのファイルが削除されていないか、前回開いたときから内容が変わっていないかを確認するようになりました。

### 修正

- タブバーの「＋」ボタンで新しいタブ(ウェルカム画面)を開こうとすると、一瞬表示されてすぐ閉じてしまい、開けなかった不具合を修正しました。
- トラックパッドで2本指スワイプによるページ送りを行った際、意図せず複数ページ分送られてしまったり、逆方向にページが送られてしまったりする不具合を修正しました。
- 「トラックパッドでのページ送りはフリックを使用する」設定(旧称「トラックパッドのフリックをホイールのスクロールとして扱う」)をONにしているとき、トラックパッドの2本指での縦スクロールがページ送りに反応してしまう不具合を修正しました。この設定がONのときは、左右へのフリックのみでページ送りされ、縦スクロールは無視されます。物理的なマウスホイールでのページ送りには影響ありません。

### 削除

- 環境設定ウインドウの各項目下に表示されていた説明文を削除しました

## [0.97] - 2026-07-26

### 追加

- タブバーやウインドウのタイトルに、開いている本のファイル名が表示されるようになりました。複数のタブ/ウインドウを開いていても、どれが何の本を開いているか見分けられます。

### 修正

- 見開き表示で、左右のページの元画像の解像度が異なる場合に、解像度が低い方のページが小さいまま表示され、両ページの高さが揃わないことがある不具合を修正しました。
- Finderで、すでに本を開いている状態のまま別の本を開いたとき(「現在の本を閉じて新しい本を開く」設定の場合)に、使われない空のウインドウ(ウェルカム画面)がもう1つ余分に開いてしまう不具合を修正しました。
- アプリを正常な手順で終了しなかった場合に、次回起動時に前回のウインドウ構成が復元されなくなりました。毎回、通常どおり単一のウインドウから起動します。
- ウインドウのサイズ・位置が、次回起動時に正しく記憶・復元されるようになりました。
- 「新しいウインドウで開く」で本を開いたとき、新しいウインドウが元のウインドウとはっきり見分けられるよう、少しずらして表示されるようになりました(画面からはみ出す場合は自動的に調整されます)。

## [0.96] - 2026-07-26

### 追加
- スクロールホイールを素早く回転させたとき、通過するページも一瞬ずつめくりながら 表示してから止まったページへ着地するようになりました。

### 修正
- 矢印キー(←→↑↓、shift+←→)によるページ送りなどのショートカットが、まったく 反応しない不具合を修正しました。
- マウスの機種によっては、スクロールホイールを1ノッチ回しただけで2ページ分 進んでしまうことがある不具合を修正しました。
- 「横幅に合わせる」「拡大縮小しない」表示モードで、画像内をスクロールする 操作が、意図せずページ送りとしても反応してしまうことがある不具合を修正しました。
- 見開き表示中に矢印キーやボタンでページを送った際、ごく短い時間だけ 異なるページが一瞬表示されてしまうことがある不具合を修正しました。

## [0.95] - 2026-07-26

初版リリース。
