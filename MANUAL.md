# qooViewer 使い方マニュアル

qooViewer の各機能・設定項目の詳しい使い方をまとめたマニュアルです。導入方法(ビルド手順)は [README.md](./README.md)、変更履歴は [CHANGELOG.md](./CHANGELOG.md) を参照してください。

## 目次

1. [はじめに](#1-はじめに)
2. [本を開く](#2-本を開く)
3. [画面の基本構成](#3-画面の基本構成)
4. [ページ送りの操作](#4-ページ送りの操作)
5. [表示モード](#5-表示モード)
6. [ページ一覧(サムネイルグリッド)](#6-ページ一覧サムネイルグリッド)
7. [ブックマーク](#7-ブックマーク)
8. [お気に入り](#8-お気に入り)
9. [スライドショー](#9-スライドショー)
10. [実寸表示ウインドウ](#10-実寸表示ウインドウ)
11. [前後の本への移動・同じフォルダのファイルを開く](#11-前後の本への移動同じフォルダのファイルを開く)
12. [複数ウインドウ・タブ](#12-複数ウインドウタブ)
13. [本の続きから再開する](#13-本の続きから再開する)
14. [フルスクリーン表示](#14-フルスクリーン表示)
15. [環境設定](#15-環境設定)
16. [サンドボックスとアクセス権について](#16-サンドボックスとアクセス権について)
17. [表示言語の切り替え](#17-表示言語の切り替え)
18. [トラブルシューティング](#18-トラブルシューティング)

---

## 1. はじめに

qooViewer は macOS 15 (Sequoia) 以降で動作する漫画ビューアです。本棚(ライブラリ)画面は持たず、フォルダやファイルを直接選んで開くシンプルな作りになっています。

対応する形式は次の通りです。

- 画像を含むフォルダ
- zip / cbz
- rar / cbr
- 7z / cb7
- PDF
- EPUB(固定レイアウトの、画像ベースのコミックEPUBのみ。文章主体のリフロー型EPUB、および
  DRM付きEPUBには対応していません)

フォルダ・アーカイブ内の画像は JPEG(jpg/jpeg)、PNG、GIF、BMP、WebP、HEIC、TIFF(tif/tiff)、AVIF に対応しています。

EPUBを開いた場合、そのEPUBが読み方向(右開き/左開き)・見開き/単ページ表示・ページの見開き内
配置を明示していれば、qooViewer側の設定より優先してそれに従って表示され、対応するツールバー・
メニューバー・右クリックメニューの項目はグレーアウトして操作できなくなります(詳しくは
[5. 表示モード](#5-表示モード)を参照)。

---

## 2. 本を開く

以下のいずれかの方法で本を開けます。

- ツールバーの「開く…」ボタン、またはメニューバー「File」→「Open…」(Cmd+O)からファイル/フォルダ選択パネルで選ぶ
- ウインドウへフォルダ/ファイルをドラッグ&ドロップする(ウェルカム画面・ビューア画面のどちらでも可)
- メニューバー「File」→「Open Recent」(最近開いたファイルを開く)から、過去に開いたことのある本を選ぶ(最大10件、新しい順)
- メニューバー「File」→「Open File in Same Folder」(同じフォルダのファイルを開く)から、今開いている本と同じフォルダ内の別の本を選ぶ
- Finder で対応拡張子のファイルをダブルクリックする、または右クリック→「このアプリケーションで開く」で qooViewer を選ぶ(Finder から開けるようにする設定は README の「Finderから直接開けるようにする」を参照)

「File」→「Open…」は今のウインドウの内容を置き換えて開きます。別のウインドウ・タブとして開きたい場合は「Open in New Window…」「Open in New Tab…」を使います。

---

## 3. 画面の基本構成

ビューア画面は上から順に、次の3つの要素で構成されています。

- **ツールバー**: ページ/ブック移動ボタン、本のタイトル、お気に入り追加・お気に入り一覧・ブックマーク一覧・ブックマーク追加・ページ一覧・スライドショーの各ボタン
- **画像表示エリア**: 現在のページ(見開き時は2枚)を表示する領域。右クリックでコンテキストメニューが開きます
- **プログレスバー**: 画面下部に表示される、本全体に対する現在位置を示すバー。クリック/ドラッグで任意のページへ直接ジャンプできるほか、バーにカーソルを合わせるとホバー位置付近のページのサムネイルをフィルムストリップ(横一列のサムネイル帯、最大9枚)で確認できます。バーの両端のボタンは、それぞれ最初のページ/最後のページへ移動します

環境設定「閲覧」タブの「プログレスバーにカーソルを合わせたときにサムネイルプレビューを表示する」(既定ON)をOFFにすると、このフィルムストリップの代わりに、カーソル位置に対応するページ番号(「現在のページ数 / 総ページ数」)だけを表示するシンプルな表示になります。この場合、サムネイルの読み込みも一切行われません。

ツールバーの各ボタンは、メニューバーからも同じ操作を呼び出せます(フルスクリーン中でツールバーが隠れているときに便利です)。

環境設定「一般」タブの表示メニュー相当の項目(メニューバー「View」→「Hide Toolbar」「Hide Progress Bar」)をONにすると、ウインドウ表示中でもツールバー・プログレスバーが自動的に隠れるようになります(フルスクリーン中は設定に関わらず常に自動的に隠れます)。自動的に隠れている間は、マウスカーソルを画面の上端または下端に近づけると、半透明の状態で一時的に表示されます。

---

## 4. ページ送りの操作

### キーボード

環境設定「キー・マウス操作」タブで自由に変更できます(1つの操作に複数のキーを割り当て可能)。既定の割り当ては次の通りです。

| 操作 | 既定のキー |
| --- | --- |
| 左へ移動(Move Left) | ← |
| 右へ移動(Move Right) | → |
| 次へ移動(Move to Next、読み方向に関わらず常に「次」) | z / space |
| 前へ移動(Move to Previous、読み方向に関わらず常に「前」) | x / shift+space |
| 1ページだけ左へ移動 | shift+← |
| 1ページだけ右へ移動 | shift+→ |
| 最初のページへ | home / option+→ |
| 最後のページへ | end / option+← |
| 割合位置へジャンプ(0=先頭ページ、9=全ページ数の90%、10%刻み) | 0〜9 |
| ページ一覧を表示 | t |
| ブックマークを追加 | a |
| 次のブックマークへ | c |
| 前のブックマークへ | d |
| ブックマークの編集… | b |
| 次の本へ | control+c |
| 前の本へ | control+d |
| スライドショー開始/停止 | g |
| 見開き/単ページ切替 | s |
| 右開き/左開き切替 | r |
| 表示モード切替(画面内に収める→横幅に合わせる→拡大縮小しないの順で循環) | f |
| 左ページを実寸表示 | q |
| 右ページを実寸表示 | w |

「左へ移動」「右へ移動」は画面上の位置(左右)を基準にした操作で、読み方向(右開き/左開き)によって実際には「次」「前」どちらの意味になるかが変わります。一方「次へ移動」「前へ移動」は読み方向に関わらず常に同じ意味になります。マウスホイールのように、位置と無関係に「進む/戻る」という一定の操作感が欲しい場面はこちらを使います。

キーを追加しようとしたとき、そのキーが既に別の操作に割り当て済みだった場合は、割り当てられず「このショートカットは既に使用されています」という警告とともに、現在どの操作に割り当てられているかが表示されます。別の操作へ付け替えたい場合は、先にその操作側で×ボタンを押してキーを外してから、あらためて追加してください。

### マウス

環境設定「キー・マウス操作」タブの「Mouse」セクションで、画面クリックとホイールの4つのトリガーそれぞれに操作を割り当てられます(1トリガーにつき1操作)。既定は次の通りです。

| トリガー | 既定の操作 |
| --- | --- |
| 画面の左側をクリック | 左へ移動 |
| 画面の右側をクリック | 右へ移動 |
| ホイールを上へ回す | 前へ移動 |
| ホイールを下へ回す | 次へ移動 |

画面クリックでのページ送りは、表示モードが「画面内に収める」のときだけ有効です(「横幅に合わせる」「拡大縮小しない」では、クリック/ドラッグは画像のスクロール操作として扱われます)。

### トラックパッド

環境設定「閲覧」タブの「トラックパッドでのページ送りはフリックを使用する」(既定ON)をONにすると、トラックパッドの「ページ間をスワイプ」ジェスチャー(2本指、または3本指/4本指。指の本数は Mac 本体の「システム設定」→「トラックパッド」の設定に従います)による左右のフリックが、上表のホイールと同じ操作(既定では前へ移動/次へ移動)として扱われます。このときトラックパッドの2本指の縦スクロールはページ送りとして反応しません(完全に無視されます)。OFFにすると、フリックによる特別な扱いはせず、2本指スクロールは画像のスクロール操作になります。物理的なマウスホイールでのページ送りは、この設定に関わらず常に有効です。

---

## 5. 表示モード

### 拡大縮小モード

環境設定「描画」タブ、またはツールバー/メニューバーの表示モード切替(f キー)で3つから選べます。

| モード | 内容 |
| --- | --- |
| 画面内に収める(既定) | 縦横とも画面に収まるように拡大縮小する。クリックでのページ送りが使える唯一のモード |
| 横幅に合わせる | 横幅基準で拡大縮小し、縦が画面より長ければ縦スクロールする |
| 拡大縮小しない | 原寸のまま表示し、縦横ともスクロールする |

「新しく開く本の初期表示モード」(環境設定「描画」タブ)で、初めて開く本に最初に適用するモードを指定できます。一度開いた本は、次に開いたときも前回選んだモードがそのまま復元されます。

### 見開き/単ページ

sキー、ツールバーのメニュー、メニューバー「View」→「Spread」で切り替えます。見開き表示中でも、横長画像(横÷縦が環境設定「描画」タブの「単ページ表示にする横長画像のしきい値」以上、既定1.0)は自動的に単ページ表示になります。見開きのページの組み合わせ(奇数/偶数ペア)がずれてしまった場合は、「1ページだけ左/右へ移動」(既定 shift+←/shift+→)で調整できます。

### 読み方向

rキー、メニューバー「View」→「Right-to-Left」で右開き(日本式)/左開き(欧米式)を切り替えます。右開きのときは、プログレスバーの進行方向や「左へ/右へ移動」の意味も含め、画面全体が右から左へ読む前提の並びに切り替わります。

### EPUBを開いている場合の自動反映

EPUBが読み方向・見開き/単ページを明示している場合、上記の切り替え操作は本を開いている間だけ無効化(グレーアウト)され、EPUB側の指定に固定されます。また、そのEPUBが特定のページを見開きの左右どちらに配置するかを個別に指定している場合、見開き表示中はそのページの組み合わせを含む「1ページだけ左/右へ移動」も無効化されます。プログレスバーやページ一覧から直接ジャンプした場合も、指定された組み合わせがずれないよう自動的に着地位置が調整されます。指定が無い項目(または通常のフォルダ/zip/cbz/rar/cbr/7z/cb7/PDF)は、これまで通り自由に操作できます。

---

## 6. ページ一覧(サムネイルグリッド)

ツールバーのグリッドアイコン、tキー、メニューバー「View」→「Show Page Grid」で開きます。全ページのサムネイルを一覧表示し、クリックでそのページへジャンプします。右上のボタンで並び順(正順/逆順)を切り替えられます。

---

## 7. ブックマーク

ブックマークは、メニューバーでは「Favorites」メニューにお気に入りとまとめて表示されます(お気に入りが上のグループ、ブックマークが下のグループです。お気に入り自体については[8. お気に入り](#8-お気に入り)を参照)。

- **追加**: ツールバーのブックマークアイコン(塗りつぶし)、aキー、メニューバー「Favorites」→「Add Current Page to Bookmarks」、右クリックメニュー「Add Current Page to Bookmarks」、または後述の「ブックマークの編集」ウインドウ右ペイン下部の「Add Current Page」ボタン
- **一覧・編集**: ツールバーのブックマークアイコン(枠線)、bキー、メニューバー「Favorites」→「Edit Bookmarks…」。「お気に入りの整理」と同じ独立したウインドウが、左ペインに本の一覧、右ペインに選択中の本のブックマーク一覧を表示する2ペイン構成で開きます(下記参照)。メニューバーからは、本を1つも開いていない状態でも(「Edit Favorites…」と同様に)常に開けます
- **次/前のブックマークへジャンプ**: 既定 c / d キー(下記の並べ替え設定に関わらず、常にページ番号順で判定されます)
- メニューバー「Favorites」→「Bookmark List」、および右クリックメニューの「Bookmark List」には、今開いている本のブックマーク一覧がサブメニュー形式で表示され、クリックでジャンプできます

ブックマークは本ごと(ファイル/フォルダのパスごと)に保存されます。ブックマークを追加すると「ページ N」という名前が自動で付きますが、後から自由に名前を変更できます。既にブックマークを付けているページで再度追加しようとした場合は、重複して追加されません(既存のブックマークがそのまま残ります)。

### ブックマークの編集

「ブックマークの編集」ウインドウは、「お気に入りの整理」ウインドウと同じ操作感の2ペイン構成です。

- 左ペインには、ブックマークを1件以上持つすべての本が一覧表示されます(「今開いている本だけ」という制限はありません)。今その本を開いているウインドウ/タブがある場合は、本のアイコンが塗りつぶし表示になり、名前が太字になり、「Now Reading」というバッジが付きます(クリックしても本を開けない他の行とはっきり見分けられるようにするためです)。ウインドウを開いた時点で、今読んでいる本にブックマークがあれば、その行が自動的に選択されます。本の名前はこの左ペインにのみ表示されます(ウインドウのタイトルバーや右ペインには重複表示しません)。ペイン幅が狭く名前が省略表示されている場合は、行の上にカーソルを合わせるとフルネームがツールチップで表示されます。左ペインの幅自体も、ウインドウを開いた時点で登録されている本の名前の長さに応じて自動的に調整されます(名前が長い本が多いほど広く開きます。右ペインが窮屈になる場合はウインドウ全体の幅を広げます。一度手動でリサイズすると、次回以降はそのサイズが記憶されます)。
- 右ペインには、左ペインで選んだ本のブックマーク一覧が表示されます。行末のゴミ箱ボタン、または右クリックメニューの「Delete」で削除、右クリックメニューの「Rename」で名前を変更できます。これらは、その本を今開いているかどうかに関わらず行えます(開いている場合は、ビューワー側の表示にも即座に反映されます)。ブックマーク名が省略表示されている場合は、行にカーソルを合わせるとフルネームがツールチップで表示されます。クリックした行は左ペインと同じ見た目で選択状態が強調表示されます(その本を今開いていない場合、クリックしてもページへはジャンプしませんが、選択状態の表示だけは行われます)。
- ブックマークをダブルクリックすると、その本を今開いているウインドウ/タブがある場合に限り、該当ページへジャンプします(開いていない本を新たに開いてジャンプする機能はありません)。シングルクリックはページへのジャンプを伴わない選択のみです。
- 右ペイン上部の「Sort By」メニューから、名前(A→Z / Z→A)・追加日時(古い順/新しい順)・更新日時(古い順/新しい順)の6種類で並べ替えられます(既定は名前の昇順。項目名・挙動は[8. お気に入り](#8-お気に入り)の「お気に入りの整理」画面と共通です。更新日時は、ブックマークの名前を変更した日時です)。この並べ替えは一覧画面の表示だけに影響し、次/前のブックマークへジャンプする操作(c/dキー)は常にページ番号順のまま変わりません。
- 右ペイン下部の「Add Current Page」ボタンから、左ペインで選んでいる本の現在のページをブックマークとして追加できます。その本を今開いているウインドウ/タブがある場合にのみ有効です。
- ブックマークを1件も登録していない場合は、その旨を案内する画面が表示され、「Add Current Page to Bookmarks」ボタンから直接追加できます。今読んでいる本が無い場合はこのボタンは無効になります。追加すると、その場で2ペイン構成の画面に切り替わり、今読んでいる本と追加したブックマークがそのまま表示されます。

---

## 8. お気に入り

ブックマーク(本の中の特定ページの目印)とは別に、本そのものをフォルダ階層で整理して登録・管理できる「お気に入り」機能があります。メニューバーでは「Favorites」メニューの上側のグループに表示されます([7. ブックマーク](#7-ブックマーク)も参照)。

### 登録

- ツールバーの星アイコン、メニューバー「Favorites」→「Add Current Book to Favorites…」、右クリックメニュー「Add Current Book to Favorites…」、またはキーボードショートカット(環境設定「キー・マウス操作」タブで割り当て。既定では未割り当て)から登録できます。
- 登録先のフォルダを選ぶシートが開きます。「Favorites (Top Level)」(フォルダ分けせずお気に入りの一番上の階層に直接置く)か、既存のフォルダから選ぶか、右上の「New Folder…」からその場で新しいフォルダを作成して選べます。
- 同じ本を、既に登録済みのフォルダへもう一度登録しようとした場合は、確認なしで登録内容を上書きします。既に別のフォルダへ登録されている場合は、どのフォルダに登録済みかを表示した上で、そこにも追加するか確認します。
- 登録できる件数は全フォルダ合計で999件、フォルダの階層は最大3階層までです。上限に達した状態で登録・新規フォルダ作成を実行しようとすると、件数・階層のどちらの上限に達したかを示すエラーが表示されます。

### 一覧・開く

- メニューバー「Favorites」→「Favorites List」、ツールバーの星(丸)アイコン、右クリックメニューの「Favorites List」、またはキーボードショートカットから、フォルダ階層のままサブメニュー形式で一覧を表示できます(ツールバーのボタンからの一覧も、メニューバーと同じくホバーでサブフォルダが展開します)。
- 一覧から項目をクリックすると開きます。既に本を表示している状態でお気に入りを開いたときの挙動(現在の本を閉じて開く/新しいタブで開く/新しいウインドウで開く)は、環境設定「一般」タブの「お気に入りを開くとき」で設定します(Finderから開いたときの設定と同じ考え方です)。まだ本を表示していない状態(ウェルカム画面)から開く場合は、この設定に関わらず常にそのウインドウでそのまま開きます。
- 登録した本のファイル/フォルダが見つからない場合(移動・削除された場合)は、その旨を表示した上で、お気に入りから削除するか確認するダイアログが表示されます。

### お気に入りの編集

メニューバー「Favorites」→「Edit Favorites…」から、専用のウインドウでお気に入りを整理できます。

- 左側にフォルダツリー、右側に選択中のフォルダの中身(サブフォルダ+お気に入り)を表示する2ペイン構成です。左側のフォルダ名が省略表示されている場合は、カーソルを合わせるとフルネームがツールチップで表示されます。また左ペインの幅は、ウインドウを開いた時点で登録されているフォルダ名の長さに応じて自動的に調整されます(ブックマーク編集画面と同様。一度手動でリサイズすると、次回以降はそのサイズが記憶されます)。
- 右側のフォルダをダブルクリックしてさらに奥のフォルダへ移動すると、左側のフォルダツリーも現在のフォルダまで自動的に展開され、該当する行が強調表示されます(今どの階層にいるかが常に左ペインで分かるようにするためです)。右側の各項目(フォルダ・お気に入り)にカーソルを合わせると、左ペインと同様にフルネームがツールチップで表示されます。また、右側の項目をクリックすると、左ペインと同じ見た目でその項目が選択状態として強調表示されます。
- 新規フォルダの作成: サイドバー下部の「New Folder」ボタン、または空白部分を右クリックして「New Folder」を選びます。
- フォルダの名前変更・削除、お気に入りの削除は、それぞれの項目を右クリックするか、行末のゴミ箱ボタンから行います(フォルダの削除は、中のサブフォルダ・お気に入りもすべて連鎖して削除されるため、確認ダイアログが表示されます)。
- ドラッグ&ドロップで、フォルダ・お気に入りを別のフォルダへ移動できます。
- 詳細ペイン下部の「Add Current Book」ボタンから、今読んでいる本を、選択中のフォルダへ直接登録できます(ブックマーク編集画面の「Add Current Page」と位置・見た目を揃えています)。既に別のフォルダへ登録済みの場合の確認、件数上限に達した場合のエラー表示は、[登録](#登録)の項と同じです。本を1つも開いていない場合はこのボタンは無効になります。
- 詳細ペインでお気に入りをダブルクリックすると、その本を開きます(シングルクリックは選択のみで、開きません)。開き方(そのまま/新しいタブ/新しいウインドウ)は、環境設定「一般」タブの「お気に入りを開くとき」に従います(ビューワーの「お気に入り一覧」から開く場合と同じです)。今読んでいる本のウインドウが1つも無い場合は、常に新しいウインドウで開きます。
- 登録した本の実体ファイルが見つからない項目は、グレー表示で区別されます。
- 並べ替え: 詳細ペイン上部の「Sort By」メニューから、名前(A→Z / Z→A)・追加日時(古い順/新しい順)・更新日時(古い順/新しい順)の6種類から選べます(既定は名前の昇順)。追加日時はフォルダ作成/お気に入り登録の日時、更新日時はお気に入りが別フォルダへ移動した日時、またはフォルダ直下でお気に入り・サブフォルダの追加/移動/削除があった日時です(フォルダ自身がよそへ移動されただけでは更新されません)。
- 「Folders on Top」(既定ON)をOFFにすると、並べ替え基準に関わらずフォルダとお気に入りを常に分けて表示するのをやめ、両者を区別せず並べ替え基準だけで混在させて1つの並びにします。
- ここで選んだ並べ替え・表示順の設定は、メニューバー「Favorites」→「Favorites List」、ツールバーの「Favorites List」サブメニューにもそのまま反映されます。

### ウェルカム画面での表示

何も開いていない状態(ウェルカム画面)では、「最近開いたファイル」と「最近お気に入りに追加したファイル」がそれぞれ最大10件表示され、クリックで直接開けます(表示前に実際にファイルが存在するか確認済みのものだけが表示されます)。それぞれ環境設定「一般」タブから個別にON/OFFできます(既定はON)。

---

## 9. スライドショー

ツールバーの再生アイコン、gキー、メニューバー「View」→「Slideshow」で開始/停止します。ページを自動的にめくる間隔は環境設定「閲覧」タブの「Interval」(1〜30秒、既定5秒)で調整できます。最後のページまで到達すると自動的に停止します。

---

## 10. 実寸表示ウインドウ

見開き表示中の左右いずれかのページを、原寸大の別ウインドウで表示します。既定では左ページが q、右ページが w に割り当てられています。変更したい場合は環境設定「キー・マウス操作」タブから「左ページを実寸表示」「右ページを実寸表示」に好きなキーを割り当ててください。

---

## 11. 前後の本への移動・同じフォルダのファイルを開く

ツールバーの上下矢印ボタン、既定 control+c(次)/control+d(前)キー、メニューバー「Move」→「Go to Previous/Next Book」で、今開いている本と同じフォルダ内にある前後の本へ直接移動できます(サブフォルダは対象外)。移動先は現在の本と同じ種類(アーカイブ/PDFファイル同士、またはフォルダ同士)に絞られ、ファイル名の自然順で並びます。

メニューバー「File」→「Open File in Same Folder」では、種類を問わず同じフォルダ内のすべての本を一覧でき、選んで開けます。

最初/最後のページを超えてページ送りしようとしたときの動作は、環境設定「閲覧」タブの「Behavior at the first/last page」で設定します。

| 選択肢 | 内容 |
| --- | --- |
| 何もしない(既定) | 最初/最後のページで止まる |
| ループする | 同じ本の最初/最後のページへループする |
| 次の本の最初のページへ | 次(前)の本を開き、必ず最初のページから読む |
| 次の本へ | 次(前)の本を開く(その本の続きから。記憶がなければ最初のページから) |

サンドボックスの制約により、フォルダを開いた場合はこれらの機能がそのまま使えますが、アーカイブ/PDFファイル単体を直接開いた場合は、あらかじめそのフォルダへのアクセスを許可しておく必要があります(詳しくは[16. サンドボックスとアクセス権について](#16-サンドボックスとアクセス権について)を参照)。

---

## 12. 複数ウインドウ・タブ

メニューバー「File」→「Open in New Window…」「Open in New Tab…」から、新しいウインドウ/タブで別の本を開けます。タブバーの「+」ボタンからも、何も開いていない新しいタブ(ウェルカム画面)を追加できます。

「新しいウインドウで開く」「新しいタブで開く」(Finderから別の本を開こうとしたときに、下記の設定が「新しいタブ/ウインドウで開く」になっている場合を含む)で選んだ本が、既に別のウインドウ/タブで開かれている場合は、同じ本をもう1つ開くのではなく、その本を開いているウインドウ/タブがそのままアクティブになります。

環境設定「一般」タブの「Finderから開いたとき」(既に qooViewer が本を表示している状態で、Finder から別の本を開こうとしたときの動作)は次から選べます。

| 選択肢 | 内容 |
| --- | --- |
| 現在の本を閉じて新しい本を開く(既定) | 同じウインドウで新しい本に置き換える |
| 新しいタブで開く | 現在のウインドウに新しいタブとして開く |
| 新しいウインドウで開く | 新しいウインドウとして開く |

複数のタブが開いているウインドウを、赤い閉じるボタンまたはメニューバー「Window」→「Close Window」で閉じようとすると、環境設定「一般」タブの「複数のタブが開いているウインドウを閉じるときに確認する」(既定ON)がONの場合、確認ダイアログが表示されます。Cmd+W やタブ自体の×ボタンでは、この確認は表示されません。

環境設定「一般」タブの「すべてのウインドウを閉じたときにqooViewerを終了する」(既定OFF)をOFFのままにしておくと、ウインドウをすべて閉じても他の多くの Mac アプリ同様 Dock に常駐したままになります。

---

## 13. 本の続きから再開する

qooViewer は本ごとに、最後に表示していたページ・表示モード(見開き/単ページ)・読み方向・拡大縮小モードを自動的に記憶します。以前開いたことのある本を再度開いたときの動作は、環境設定「一般」タブの「本を再度開いたとき」で設定します。

| 選択肢 | 内容 |
| --- | --- |
| 前回表示したページから再開する(既定) | そのまま前回の続きから表示する |
| 常に最初から表示する | 前回どこまで読んでいたかに関わらず、常に最初のページから |
| 前回最後のページを表示した場合は最初から表示する | 最後まで読み終えた本だけ、開き直すとまた最初から読める |
| 問い合わせる | そのつど「前回表示したページから再開しますか?」と確認する |

また「起動時に前回開いていた本を自動的に開く」(環境設定「一般」タブ、既定OFF)をONにすると、次回 qooViewer を起動したときに、前回終了時にアクティブだった画面が表示していた本を自動的に開きます。ファイルが削除・移動されていたり、前回開いたときから中身が変わっていたりする場合は、安全のため自動では開かず、ウェルカム画面のまま起動します。

なお、本ごとの読書状態・ブックマークを保持しておく本の数には上限があり、環境設定「一般」タブの「データを保持する本の数」(50〜2000冊、既定500冊)で調整できます。上限を超えると、最後に読んだ時刻が古い本のデータから自動的に削除されます。

---

## 14. フルスクリーン表示

メニューバー「View」→「Enter Full Screen」(macOS標準)、右クリックメニューの「Full Screen」、またはウインドウ右上の緑色のボタンでフルスクリーンにできます。フルスクリーン中はツールバー・プログレスバーが自動的に隠れ、画像を画面いっぱいに表示します。マウスカーソルを画面の上端/下端に近づけると、半透明の状態で一時的に再表示されます。

環境設定「一般」タブの「起動時にフルスクリーンにする」(既定OFF)をONにすると、起動直後に自動的にフルスクリーンになります。

---

## 15. 環境設定

環境設定ウインドウ(Cmd+,)は6つのタブに分かれています。

### 一般

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| 表示言語 | アプリの表示言語(システムに従う/日本語/English) | システムに従う |
| 起動時に前回開いていた本を自動的に開く | [13.](#13-本の続きから再開する)参照 | OFF |
| 起動時にフルスクリーンにする | [14.](#14-フルスクリーン表示)参照 | OFF |
| 本を再度開いたとき | [13.](#13-本の続きから再開する)参照 | 前回表示したページから再開する |
| Finderから開いたとき | [12.](#12-複数ウインドウタブ)参照 | 現在の本を閉じて新しい本を開く |
| お気に入りを開くとき | [8.](#8-お気に入り)参照。Finderから開いたときと同じ3択(現在の本を閉じて開く/新しいタブ/新しいウインドウ) | 現在の本を閉じて新しい本を開く |
| すべてのウインドウを閉じたときにqooViewerを終了する | [12.](#12-複数ウインドウタブ)参照 | OFF |
| 複数のタブが開いているウインドウを閉じるときに確認する | [12.](#12-複数ウインドウタブ)参照 | ON |
| データを保持する本の数 | [13.](#13-本の続きから再開する)参照(50〜2000冊) | 500冊 |
| ウェルカム画面に最近開いたファイルを表示 | [8.](#8-お気に入り)参照(最大10件) | ON |
| ウェルカム画面に最近のお気に入りを表示 | [8.](#8-お気に入り)参照(最大10件) | ON |

### 描画

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| 最大拡大率 | 画像が画面より小さいとき、最大何%まで拡大して表示するか(100〜500%) | 200% |
| 画像の補間品質 | 拡大縮小時のなめらかさ(高品質/標準/低品質(軽量)) | 高品質 |
| 背景色 | ビューアの背景色(黒/ダークグレー/白/セピア) | 黒 |
| 新しく開く本の初期表示モード | [5.](#5-表示モード)参照 | 画面内に収める |
| 単ページ表示にする横長画像のしきい値(横÷縦) | [5.](#5-表示モード)参照(0.5〜3.0) | 1.0 |
| 先読みするページ数(前後各) | 現在のページの前後何ページ分を事前に読み込んでおくか(0〜10)。多いほどページ送りが速くなる代わりにメモリを多く使う | 3ページ |

### 閲覧

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| 最初/最後のページを超えたときの動作 | [11.](#11-前後の本への移動同じフォルダのファイルを開く)参照 | 何もしない |
| トラックパッドでのページ送りはフリックを使用する | [4.](#4-ページ送りの操作)参照 | ON |
| プログレスバーにカーソルを合わせたときにサムネイルプレビューを表示する | [3.](#3-画面の基本構成)参照。OFFにするとページ番号だけのシンプルな表示になる | ON |
| スライドショーの間隔 | [9.](#9-スライドショー)参照(1〜30秒) | 5秒 |
| しばらく操作がないときマウスカーソルを自動的に隠す | 一定時間操作がないとマウスカーソルを自動的に隠す | ON |
| 非表示にするまでの時間 | 上記がONのときの待ち時間(0.5〜10秒) | 2.0秒 |

### キー・マウス操作

[4. ページ送りの操作](#4-ページ送りの操作)を参照してください。「初期設定に戻す」ボタンで、キー・マウスの割り当てをすべて既定値に戻せます。

### アクセス権

[16. サンドボックスとアクセス権について](#16-サンドボックスとアクセス権について)を参照してください。

### リセット

お気に入り・ブックマーク・読書履歴(本ごとの最後に読んだページ・表示設定)は、すべて同じ1つのデータファイルに保存されています。アプリの更新などでこのファイルが互換性を失い、開けなくなることに備えた緊急避難用のタブです。

- 「すべてのお気に入り・ブックマーク・読書履歴を削除…」ボタンから、これらすべてを完全に削除できます。非常に強力な(元に戻せない)操作のため、実行前に必ず確認ダイアログが表示されます。誤ってクリックしてしまっても、確認ダイアログで「Cancel」を選べば何も削除されません。
- 削除を実行すると、完了を知らせるダイアログの後、qooViewerは自動的に終了します。次に起動したときは、まっさらな状態から始まります。
- 万一、アプリ自体が起動できなくなるほどデータの互換性が失われた場合は、この画面を開くこともできません。その場合は、起動時に代わりに案内ダイアログが表示され、そこで削除するかどうかを選べます(「削除して続行」を選ぶと、上記と同じ内容が削除された上でそのまま起動します。「削除せずに終了」を選ぶと、データを残したままqooViewerが終了します)。

---

## 16. サンドボックスとアクセス権について

qooViewer はサンドボックス化されたアプリのため、ユーザーが選択パネルやドラッグ&ドロップで直接選んだファイル/フォルダにしかアクセスできません。フォルダを開いた場合はその配下がまとめて許可されるため問題ありませんが、**アーカイブ(zip/cbz・rar/cbr・7z/cb7)やPDFファイル単体を直接開いた場合**、[11.](#11-前後の本への移動同じフォルダのファイルを開く)の「同じフォルダのファイルを開く」「前の本/次の本」機能は、まだアクセス許可のない兄弟ファイルを読もうとするため一覧が空になります。

これを避けるには、環境設定「アクセス権」タブの「フォルダを追加…」から、漫画ファイルを置いているフォルダ(ホームフォルダ、外部ボリューム、ドライブのルートフォルダなど、どこでも指定できます)へのアクセスをあらかじめまとめて許可しておきます。一度許可すれば、以後そのフォルダ配下では次回起動後も含めて常に正しく機能します(セキュリティスコープ付きブックマークとして保存されるため)。

今開いている本のフォルダだけをその場で許可したい場合は、メニューバー「File」→「Open File in Same Folder」→「Grant Access to This Folder…」からも許可できます。

「最近開いたファイルを開く」機能は、開いた時点でアクセス許可を自動的に保存するため、この設定をしなくても次回起動後に問題なく開けます。

環境設定「アクセス権」タブでは、許可済みフォルダの一覧確認・取り消し(「−」ボタン)もできます。

---

## 17. 表示言語の切り替え

環境設定「一般」タブの「表示言語」で、システムに従う/日本語/Englishを切り替えられます。macOS本体の言語設定とは独立して切り替えられ、メニューバー・ツールバー・環境設定画面などアプリ全体の表示に反映されます。

**注意**: ファイルが開けなかった場合などの一部のエラーメッセージは、アプリ内の表示言語設定ではなく、Macのシステム言語設定に従って表示されます(実装上の意図的な簡略化です)。

---

## 18. トラブルシューティング

**Q. 「同じフォルダのファイルを開く」「前の本/次の本」が空になる/機能しない**

A. アーカイブ/PDFファイルを単体で直接開いた場合に起きる、サンドボックスの制約による既知の挙動です。[16. サンドボックスとアクセス権について](#16-サンドボックスとアクセス権について)を参照し、あらかじめフォルダへのアクセスを許可してください。

**Q. 古い日本語のzipファイルで、ファイル名が文字化けする**

A. zip/cbz内のファイル名の文字コードは自動判定していますが、判定しきれない場合があります。判定精度に関する不具合と思われる場合はお知らせください。

**Q. トラックパッドでスワイプしても反応しない/意図しない方向にページが送られる**

A. 環境設定「閲覧」タブの「トラックパッドでのページ送りはフリックを使用する」の設定と、Mac本体の「システム設定」→「トラックパッド」の「ページ間をスワイプ」の指の本数設定(2本指/3本指/4本指)の組み合わせを確認してください。詳細は[4. ページ送りの操作](#4-ページ送りの操作)を参照してください。

**Q. キーボードショートカットが反応しない**

A. 割り当てたいキーが、環境設定「キー・マウス操作」タブで実際に割り当てられているか確認してください。command キーを含む組み合わせや、Mission Control・入力ソース切替など macOS 標準の操作と衝突する組み合わせ(control+矢印キーなど)は選択肢に含まれていません。

**Q. 起動時に前回のウインドウが復元されない**

A. アプリを正常な手順で終了しなかった場合(強制終了など)は、次回起動時に前回のウインドウ構成は復元されず、通常どおり単一のウインドウから起動します。
