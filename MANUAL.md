# qooViewer 使い方マニュアル

qooViewer の各機能・設定項目の詳しい使い方をまとめたマニュアルです。導入方法(ビルド手順)は [README.md](./README.md)、変更履歴は [CHANGELOG.md](./CHANGELOG.md) を参照してください。

## 目次

1. [はじめに](#1-はじめに)
2. [本を開く](#2-本を開く)
3. [画面の基本構成](#3-画面の基本構成)
4. [ページ送りの操作](#4-ページ送りの操作)
5. [表示モード](#5-表示モード)
6. [ページ一覧(サムネイルグリッド)](#6-ページ一覧サムネイルグリッド)
7. [サイドパネル](#7-サイドパネル)
8. [ブックマーク](#8-ブックマーク)
9. [お気に入り](#9-お気に入り)
10. [ページレイアウト](#10-ページレイアウト)
11. [メタデータ](#11-メタデータ)
12. [ライブラリデータの書き出し・読み込み(JSON)](#12-ライブラリデータの書き出し読み込みjson)
13. [EPUBの書き出し](#13-epubの書き出し)
14. [PDFの書き出し](#14-pdfの書き出し)
15. [CBZの書き出し](#15-cbzの書き出し)
16. [画像の書き出し](#16-画像の書き出し)
17. [Finderで開く/情報を見る](#17-finderで開く情報を見る)
18. [スライドショー](#18-スライドショー)
19. [実寸表示ウインドウ](#19-実寸表示ウインドウ)
20. [拡大鏡](#20-拡大鏡)
21. [前後の本への移動・同じフォルダのファイルを開く](#21-前後の本への移動同じフォルダのファイルを開く)
22. [複数ウインドウ・タブ](#22-複数ウインドウタブ)
23. [本の続きから再開する](#23-本の続きから再開する)
24. [フルスクリーン表示](#24-フルスクリーン表示)
25. [環境設定](#25-環境設定)
26. [サンドボックスとアクセス権について](#26-サンドボックスとアクセス権について)
27. [表示言語の切り替え](#27-表示言語の切り替え)
28. [トラブルシューティング](#28-トラブルシューティング)

---

## 1. はじめに

qooViewer は macOS 15 (Sequoia) 以降で動作する漫画ビューアです。本棚(ライブラリ)画面は持たず、フォルダやファイルを直接選んで開くシンプルな作りになっています。

対応する形式は次の通りです。

- 画像を含むフォルダ
- zip / cbz
- rar / cbr
- 7z / cb7
- PDF
- EPUB(固定レイアウトの、画像ベースのコミックEPUBのみ。文章主体のリフロー型EPUB、および
  DRM付きEPUBには対応していません)

フォルダ・アーカイブ内の画像は JPEG(jpg/jpeg)、PNG、GIF、BMP、WebP、HEIC、TIFF(tif/tiff)、AVIF に対応しています。

ファイル自身が読み方向(右開き/左開き)・見開き/単ページ表示・ページの見開き内配置を明示して
いる場合(EPUB、指定を持つPDF、`ComicInfo.xml` を含むcbz/cbr/cb7)は、その本を初めて開いた
ときにその指定を読み取り、qooViewer側の設定として登録します。以降の表示は登録された内容に
従い、読み方向・見開き・ページごとのレイアウトはどの形式の本でも自由に変更できます(詳しくは
[5. 表示モード](#5-表示モード)を参照)。
どの形式の本に対しても、読み方向・見開き/単ページ・ページ順序を自分で指定できる
「ページレイアウト」機能があります([10. ページレイアウト](#10-ページレイアウト)を参照)。

---

## 2. 本を開く

以下のいずれかの方法で本を開けます。

- ツールバーの「開く…」ボタン、またはメニューバー「File」→「Open…」(Cmd+O)からファイル/フォルダ選択パネルで選ぶ
- ウインドウへフォルダ/ファイルをドラッグ&ドロップする(ウェルカム画面・ビューア画面のどちらでも可。サイドパネルの上を含め、ウインドウのどこへ落としても開きます。ドラッグ中はウインドウの縁が強調されます)
- Dock の qooViewer アイコンへフォルダ/ファイルをドロップする
- メニューバー「File」→「Open Recent」(最近開いた本)から、過去に開いたことのある本を選ぶ(新しい順。保持件数は環境設定「一般」の「履歴の保存件数」で変更できます。既定30件)
- サイドパネルの「履歴」モードから選ぶ([7. サイドパネル](#7-サイドパネル)参照。「Open Recent」と同じ一覧を、本を読んでいる最中でも一覧として表示できます)
- メニューバー「File」→「Open File in Same Folder」(同じフォルダのファイルを開く)から、今開いている本と同じフォルダ内の別の本を選ぶ
- Finder で対応拡張子のファイルをダブルクリックする、または右クリック→「このアプリケーションで開く」で qooViewer を選ぶ(Finder から開けるようにする設定は README の「Finderから直接開けるようにする」を参照)
- サイドパネルのフォルダブラウザから、ファイル/フォルダをたどって選ぶ([7. サイドパネル](#7-サイドパネル)参照)
- 画像ファイルそのものを開く。上記のドロップ・パネル・Dock・Finder のどの方法でも開けます(下記参照)

「File」→「Open…」は今のウインドウの内容を置き換えて開きます。別のウインドウ・タブとして開きたい場合は「Open in New Window…」「Open in New Tab…」を使います。

メニューバー「File」には、この他に、お気に入り・ブックマーク・ページレイアウト設定をJSONファイルで
書き出し/読み込みする「Import…」「Export…」([12. ライブラリデータの書き出し・読み込み(JSON)](#12-ライブラリデータの書き出し読み込みjson)参照)、
今表示しているページの画像を書き出す「画像の書き出し」サブメニュー([16. 画像の書き出し](#16-画像の書き出し)参照)、
選択した本をEPUB形式で書き出す「EPUBの書き出し…」([13. EPUBの書き出し](#13-epubの書き出し)参照)、
その下のPDF形式で書き出す「PDFの書き出し…」([14. PDFの書き出し](#14-pdfの書き出し)参照)、
さらにその下のCBZ形式で書き出す「CBZの書き出し…」([15. CBZの書き出し](#15-cbzの書き出し)参照)、
今開いている本をFinderで表示する「Show in Finder」があります。

「Open Recent」「Open File in Same Folder」の各サブメニュー、お気に入り一覧、ウインドウのタイトルバーでは、本の名前を拡張子付きのファイル名/フォルダ名(例:「タイトル.cbz」)でそのまま表示します。

### ページの並び順

本のページは**ファイル名順**に並びます(フォルダの本ならサブフォルダの中まで再帰的に集めた上で、フォルダごとにまとまった順。書庫の本なら書庫の中のパス順です)。

環境設定「一般」→「ページ順」→「**並び順をFinderに揃える**」で、2通りから選べます。**既定はOFF**です。

- **OFF(既定)**: 文字コード順。大文字で始まる名前が、小文字で始まる名前よりすべて先に来ます(例:`Cover.jpg` → `Img02.jpg` → `img01.jpg`)。`_` で始まる名前は、大文字で始まる名前より後、小文字で始まる名前より前に入ります。
- **ON**: **Finderと同じ名前順**。数字は数値として比べ(`2.jpg` は `10.jpg` より前)、大文字・小文字や全角・半角は区別せず、記号もシステムの照合順序に従います。

多くの本では、どちらを選んでも並びは変わりません。違いが出るのは、先頭に `_` が付く名前、`-` と `_` が混ざった名前、大文字と小文字が混ざった名前を含む本だけです。

- この設定は、本のページのほか、サイドパネル下段の本の中身ブラウザ、複数の画像をまとめて1冊として開いたときの並びに効きます。サイドパネル上段のフォルダブラウザは、この設定に関わらず常にFinderと同じ名前順です([7. サイドパネル](#7-サイドパネル)参照)。
- **切り替えると、開いている本もその場で並び替わります。** 本を開き直す必要はありません。表示していたページはそのまま表示され続けます。
- **ブックマークと読書位置は、並び順を変えても同じページ(画像)を指したままです。** ページ番号は新しい並びで振り直されます。
- **ページごとのレイアウトを設定した本は、そのレイアウトを作ったときの並び順のままになります。** 見開きの組み合わせ(どのページとどのページが並ぶか)は並び順で決まるため、並びが変わるとレイアウトの見た目が崩れてしまうからです。新しい並び順に合わせたい本は、「ブックマーク・レイアウトの編集」ウインドウの「Reset Page Order」(ページ順を初期化する)([8. ブックマーク](#8-ブックマーク)参照)で1冊ずつ合わせられます。合わせた場合は次のようになります。

  - **レイアウト**: 削除されません。各ページの設定(単ページ/見開き左・右/除外)はそのまま残ります。ただし、どのページ同士が見開きで組むかは並び順で決まるため、組み合わせは変わります。作り直すには「現在の表示を基準に自動でレイアウト」([10. ページレイアウト](#10-ページレイアウト)参照)を実行してください。
  - **ブックマーク・読書位置**: 同じページ(画像)を指したままです。ページ番号だけが振り直されます。
- EPUBとPDFは、ファイル名ではなくファイル自身が持つページ順に従うため、この設定の影響を受けません([10. ページレイアウト](#10-ページレイアウト)参照)。
- CBZの書き出しが `ComicInfo.xml` に書き込むページ番号は、この設定に関わらず常にFinderと同じ名前順で振られます(書き出したファイルの中身が、アプリ側の表示設定に左右されないようにするためです)。

### 読み込み中の表示

入れ子になった書庫を含む本(書庫の中に章ごとの書庫が入っているものなど)は、ページの一覧を作るために中の書庫を1つずつ開く必要があり、大きな本や外付けドライブ・ネットワークボリューム上の本では時間がかかることがあります。読み込みに時間がかかっているときは、画面の中央に進み具合と「キャンセル」ボタンが表示されます。

- 読み込みが一瞬で終わる本では表示されません(本を開くたびに画面がちらつかないよう、少し待ってから出るようにしてあります)。
- 進み具合は「10 / 13 個の書庫を読み込みました」のように**件数**で表示します。中に何冊の書庫が入っているかは開いてみるまで分からないため、割合(パーセント)では表示していません。読み進むにつれて分母のほうも増えていきます。下には、いま読み終えた書庫の名前が出ます。
- 「キャンセル」を押すか Esc キーを押すと読み込みを中止し、直前まで開いていた本の表示に戻ります(何も開いていなければウェルカム画面に戻ります)。中止した時点で、そこまでに使った一時ファイルもすべて削除されます。
- **同じ本を2回目以降に開くときは、この一覧作りをまるごと省略します。** 前回の結果を覚えておき、本体のファイルが更新されていなければそのまま使うためです。ファイルを差し替えた場合は自動的に読み込み直します。この記録はシークレットウインドウでは作らず、読みもしません([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照)。

### 画像ファイルを直接開く

フォルダや書庫に入れずに、画像ファイルそのものを開くこともできます。ウインドウへのドラッグ&ドロップ、Dock の qooViewer アイコンへのドロップ、Finder の右クリック→「このアプリケーションで開く」、「開く…」パネルのいずれからでも開けます。対応する画像の種類は、フォルダや書庫の中で読めるものと同じです(JPEG / PNG / GIF / BMP / WebP / HEIC / TIFF / AVIF)。

- **Finder で複数選択した画像は、まとめて1冊の本になります。** ページの並びはファイル名順です(上記「ページの並び順」参照。既定では名前に含まれる数字は数値として比べるので、`2.jpg` は `10.jpg` より前になります)。フォルダをまたいで選んだ場合は、フォルダごとにまとまった上で名前順に並びます。選んだ順序や Finder での表示順には影響されません。
- 複数枚を開いているときは、ウインドウのタイトルに「先頭の画像名 (3枚)」のように枚数が表示されます。
- **画像1枚だけを渡した場合は、その1枚だけの本になります。** 同じフォルダにある他の画像は含まれません。渡したものがそのまま本になる、という一貫した動きにしてあります。フォルダ全体を読みたい場合は、これまでどおりフォルダ自体を開いてください。1枚だけ開いた後からフォルダ全体へ切り替えることもできます(下記「このフォルダの画像をすべて開く」参照)。
- **画像を直接開いた本については、アプリ側の記録を一切残しません。** 読書位置・ページレイアウト・ブックマーク・お気に入り・メタデータ・「最近開いた本」のいずれにも記録されず、ウインドウを閉じれば何も残りません([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)のシークレットウインドウと同じ扱いです)。そのため、これらの登録・編集を行うメニューやボタンはグレーアウトします。一覧の表示、ページの移動、見開き・読み方向・表示モードの切り替えは通常どおり行えます(切り替えた内容はそのウインドウを閉じるまで有効です)。
  - 開いた直後の読み方向・見開き・拡大縮小モードは、通常の本を初めて開いたときと同じ初期値になります(読み方向の初期値は、初回起動時にシステムの言語設定から一度だけ決まります。日本語なら右開き、それ以外は左開き)。
- 一度に1冊としてまとめられる画像は1000枚までです。

### このフォルダの画像をすべて開く

メニューバー「File」→「このフォルダの画像をすべて開く」は、**画像を直接開いているときだけ**使えます。今表示しているページと同じフォルダにある画像をすべて集めて、**通常のフォルダの本として開き直します**。開き直したあとも、それまで見ていたページがそのまま表示されます。

開き直した後は普通のフォルダの本なので、読書位置・ブックマーク・お気に入り・ページレイアウトなども通常どおり記録され、前後の本への移動も使えるようになります。

そのフォルダへのアクセスをまだ許可していない場合は、その場で許可を求めるパネルが表示されます([26. サンドボックスとアクセス権について](#26-サンドボックスとアクセス権について)参照)。

---

## 3. 画面の基本構成

ビューア画面は上から順に、次の3つの要素で構成されています。

- **ツールバー**: ページ/ブック移動ボタン、本のタイトルと現在位置(「(7 / 24)」のように、タイトルの右に一段小さく「現在のページ / 総ページ数」を表示します。総ページ数は除外(非表示)にしたページを除いた数で、見開き表示中でも数字は1つです)、「現在の表示を基準に自動でレイアウトする」ボタン([10. ページレイアウト](#10-ページレイアウト)参照)、ブックマーク追加/削除・お気に入り追加/削除(いずれも登録状態に応じて動作とアイコンが自動的に切り替わる1つのトグルボタン。登録済みのときは塗りつぶしアイコン+背景色反転で強調表示されます)・ページ一覧・スライドショーの各ボタン
- **画像表示エリア**: 現在のページ(見開き時は2枚)を表示する領域。右クリックでコンテキストメニューが開きます
- **プログレスバー**: 画面下部に表示される、本全体に対する現在位置を示すバー。クリック/ドラッグで任意のページへ直接ジャンプできるほか、バーにカーソルを合わせると、ホバー位置付近のページのサムネイルが横一列に並びます(既定9枚)。カーソル位置のページは枠・光彩・ページ番号(「現在のページ / 総ページ数」)で強調され、**いま開いているページには白い破線の枠**が付きます。各サムネイルの下にはファイル名とページ番号が付きます。書庫の中のフォルダ・書庫の中の書庫にある画像では、サムネイルの**上**に本の中での場所も表示されます(サムネイルとファイル名の高さを揃えたままにするため、この行だけ上に伸びます。[画像が本の中のどこにあるかの表示](#画像が本の中のどこにあるかの表示)参照)。バーの両端のボタンは、それぞれ最初のページ/最後のページへ移動します

このサムネイルの見え方は、環境設定「外観」→「パネル」→「プログレスバー」のページの「サムネイル」で調整できます(並べる枚数、下に何を書くか、文字の大きさ、カーソル位置以外を暗くするかどうか、カーソル位置の強調色と太さ。[25. 環境設定](#25-環境設定)の「プログレスバーのサムネイル」参照)。同じページの「カーソルを合わせたページをプレビュー」(既定ON)をOFFにすると、サムネイルの代わりに、カーソル位置に対応するページ番号(「現在のページ数 / 総ページ数」)だけを表示するシンプルな表示になります。この場合、サムネイルの読み込みも一切行われません。

ツールバーの各ボタンは、メニューバーからも同じ操作を呼び出せます(フルスクリーン中でツールバーが隠れているときに便利です)。

メニューバー「View」→「Hide Toolbar」「Hide Progress Bar」をONにすると、ウインドウ表示中でもツールバー・プログレスバーが自動的に隠れるようになります(フルスクリーン中は設定に関わらず常に自動的に隠れます)。自動的に隠れている間は、マウスカーソルを画面の上端または下端に近づけると、半透明の状態で一時的に表示されます。一時的に表示されている状態でマウスカーソルをウインドウの外(他のアプリのウインドウ・デスクトップ・メニューバーなど)へ移動すると、その時点で隠れます。

カーソルを近づけてから実際に表示されるまでの待ち時間は、環境設定「外観」→「パネル」の「ツールバー」「プログレスバー」それぞれのページにある「表示までの時間」(0〜2.0秒、既定0.0秒=すぐに表示)で、別々に設定できます。別のウインドウやメニューバーへカーソルを動かす途中で反応してほしくない場合に使います([25. 環境設定](#25-環境設定)の「表示までの時間」参照)。

この3つの要素の左右どちらかの隣には、既定で常時表示される「サイドパネル」があります(既定は左側。環境設定で右側にも変更できます)。詳しくは[7. サイドパネル](#7-サイドパネル)を参照してください。

**ツールバー・プログレスバー・サイドパネルは、ボタンや一覧の無い空きスペースを右クリックしても操作できます。** それぞれ「ツールバーを隠す」「プログレスバーを隠す」「サイドパネルを隠す」(メニューバー「View」の同名の項目と同じ設定です。チェックマークが今の状態を表します)と、「調整…」が並びます。「調整…」を選ぶと環境設定「外観」がその部分のページを開いた状態で開くので、色や濃さをその場で調整できます。自動的に隠れている状態でカーソルを近づけて一時的に表示させたときも、同じように右クリックできます。ボタンや一覧の行の上で右クリックした場合は、これまでどおりそちら側のメニュー(あれば)が優先されます。ページ一覧パネルの余白にも同じ「調整…」があります([6. ページ一覧](#6-ページ一覧サムネイルグリッド)参照)。

ツールバー・プログレスバー・サイドパネル・ページ一覧パネル、それ以外の浮かぶ表示(「情報を見る」パネル・操作のフィードバック・拡大率の表示)、およびウェルカム画面の背景色と「すりガラス」の濃さは、環境設定「外観」→「パネル」の一覧から面を選んで、この6つの面ごとに個別に変えられます。文字とアイコンに輪郭を付ける「文字の影」も面ごとに設定できます。隠していない(常に表示の)状態のツールバー・プログレスバー・サイドパネルとウェルカム画面には、ウインドウの背後(デスクトップや他のウインドウ)をうっすら透かす「ウインドウの背後を透かす」スイッチもあります(既定OFF)([25. 環境設定](#25-環境設定)の「外観」参照)。

---

## 4. ページ送りの操作

### キーボード

環境設定「キーボード」で自由に変更できます(1つの操作に複数のキーを割り当て可能)。既定の割り当ては次の通りです。

| 操作 | 既定のキー |
| --- | --- |
| 左へ移動(Move Left) | ← |
| 右へ移動(Move Right) | → |
| 次へ移動(Move to Next、読み方向に関わらず常に「次」) | z / space |
| 前へ移動(Move to Previous、読み方向に関わらず常に「前」) | x / shift+space |
| 1ページだけ左へ移動 | shift+← |
| 1ページだけ右へ移動 | shift+→ |
| 最初のページへ | home |
| 最後のページへ | end |
| 一番右のページへ | option+→ |
| 一番左のページへ | option+← |
| 割合位置へジャンプ(0=先頭ページ、9=全ページ数の90%、10%刻み) | 0〜9 |
| ページ一覧を表示 | t |
| ブックマークに追加/削除(トグル) | a |
| 次のブックマークへ | c |
| 前のブックマークへ | d |
| ブックマークの編集… | b |
| お気に入りに追加/削除(トグル) | option+a |
| お気に入りの編集… | option+b |
| 次の本へ | control+c |
| 前の本へ | control+d |
| スライドショー開始/停止 | g |
| 拡大鏡を表示/非表示 | l |
| 見開き/単ページ切替 | s |
| 右開き/左開き切替 | r |
| 表示モード切替(画面内に収める→横幅に合わせる→横幅に合わせる(単ページ)→拡大縮小しないの順で循環) | f |
| 現在の表示を基準に自動でレイアウトする | e |
| 左ページを実寸表示 | q |
| 右ページを実寸表示 | w |

「左へ移動」「右へ移動」は画面上の位置(左右)を基準にした操作で、読み方向(右開き/左開き)によって実際には「次」「前」どちらの意味になるかが変わります。一方「次へ移動」「前へ移動」は読み方向に関わらず常に同じ意味になります。マウスホイールのように、位置と無関係に「進む/戻る」という一定の操作感が欲しい場面はこちらを使います。

端まで飛ぶ操作にも同じ2種類があります。

- **「一番右のページへ」「一番左のページへ」**は画面上の位置を基準にします。右開きの本では一番右が先頭ページ、左開きの本では一番右が最終ページです。矢印キーや画面の左右クリックのように、**向きそのものを指し示す操作**に向きます(既定で option+→ / option+← と option+クリックに割り当てています)。
- **「最初のページへ」「最後のページへ」**は読み方向に関わらず物語的な先頭/末尾です。home / end のように向きを表さないキーや、メニューバー「Move」→「Move to First / Move to Last」がこちらにあたります。

「ブックマークに追加/削除」「お気に入りに追加/削除」は、それぞれ現在のページ/本が登録済みかどうかに応じて自動的に「追加」「削除」のどちらかとして働く1つの操作です(詳しくは[8. ブックマーク](#8-ブックマーク)・[9. お気に入り](#9-お気に入り)を参照)。お気に入り側の既定キーは、対応するブックマーク側のキーに option を組み合わせたもの(a→option+a、b→option+b)にしてあります。なお、文字キーは shift の有無を区別しない仕様のため(shift+a は a と同じ操作を実行します)、区別が必要なお気に入り側には shift ではなく option を使っています。

「ブックマークの編集…」(bキー)は、実際には「ブックマーク・レイアウトの編集」ウインドウを開きます([8. ブックマーク](#8-ブックマーク)参照)。メニューバー「Edit」内のレイアウトのグループのうち、ページごとの表示状態(単ページ固定/見開き右固定/見開き左固定/除外)を変更する各項目にはキーボードショートカットは割り当てられていません。「現在の表示を基準に自動でレイアウトする」(Auto-Layout Based on Current View)だけは e キーが割り当てられています([10. ページレイアウト](#10-ページレイアウト)参照)。

以前ツールバーにあった「お気に入り一覧を表示」ボタンは廃止されており、対応するキーボードショートカットの割り当ても選べなくなっています。お気に入り一覧は、メニューバー「Edit」→「Favorites List」、または右クリックメニューの「Favorites List」から表示してください([9. お気に入り](#9-お気に入り)参照)。

拡大縮小モードは、f キーで順に切り替えるほかに、⌘1〜⌘4 で直接選ぶこともできます(⌘1=画面内に収める、⌘2=横幅に合わせる、⌘3=横幅に合わせる(単ページ)、⌘4=拡大縮小しない)。これらはメニューバー「View」の表示モード切替に表示される固定のショートカットで、環境設定では変更できません(環境設定で割り当てられるキーは command を含まないものに限られます)。

キーを追加しようとしたとき、そのキーが既に別の操作に割り当て済みだった場合は、割り当てられず「このショートカットは既に使用されています」という警告とともに、現在どの操作に割り当てられているかが表示されます。別の操作へ付け替えたい場合は、先にその操作側で×ボタンを押してキーを外してから、あらためて追加してください。

### マウス

環境設定「マウス」で、キーボードと同じく**操作ごとにトリガーを割り当てます**(1つの操作に複数のトリガーを割り当て可能)。トリガーは次の組み合わせで表します。

- **クリック**: ボタン(左/中) × 位置(画面の左側/画面の右側/画面全体) × 修飾キー(なし/shift/option/shift+option)
- **ドラッグ**: ボタン(左/中) × 方向(左へ/右へ/上へ/下へ) × 修飾キー(なし/shift/option/shift+option)
- **ホイール**: 向き(↑/↓) × 修飾キー(なし/option)

既定の割り当ては次の通りです。

| 操作 | 既定のトリガー |
| --- | --- |
| 左へ移動 | 左ボタン・画面の左側 |
| 右へ移動 | 左ボタン・画面の右側 |
| 1ページだけ左へ移動 | shift + 左ボタン・画面の左側 |
| 1ページだけ右へ移動 | shift + 左ボタン・画面の右側 |
| 一番右のページへ | option + 左ボタン・画面の右側 |
| 一番左のページへ | option + 左ボタン・画面の左側 |
| 拡大鏡を表示/非表示 | 中ボタン・画面全体 |
| 前へ移動 | ホイール↑ |
| 次へ移動 | ホイール↓ |

位置に「画面全体」を選ぶと、左右どちらをクリックしても同じ操作になります。同じボタン・同じ修飾キーで「画面の左側/右側」と「画面全体」の両方に割り当てた場合は、**左右を指定した方が優先**されます(たとえば「中ボタン・画面全体」に拡大鏡を、「中ボタン・画面の左側」に別の操作を割り当てると、左半分では後者、右半分では拡大鏡になります)。

ドラッグ(マウスジェスチャー)には既定の割り当てがありません。使いたい場合は、環境設定「マウス」で好きな操作に割り当ててください。

割り当てられる操作は、ページ移動・本の移動と、拡大鏡・ページ一覧・実寸表示・スライドショー、および下記の「閉じる/終了」に絞ってあります。レイアウト・ブックマーク・お気に入り・表示モードの切り替えは、キーボードとメニューから操作してください。

**閉じる/終了の3つ**(「タブを閉じる」「ウインドウを閉じる」「qooViewerを終了」)は、**マウスにだけ**割り当てられます(キーボードには macOS 標準の Cmd+W・Cmd+Q があるため、環境設定「キーボード」の一覧には出ません)。**既定ではどれにも何も割り当てていません。**取り消しの効かない操作なので、使いたい場合にご自身で割り当ててください。

| 操作 | 内容 |
| --- | --- |
| タブを閉じる | そのタブ1枚だけを閉じます。確認は出ません。タブが1枚だけのときはウインドウが閉じます |
| ウインドウを閉じる | 赤い閉じるボタン・メニューバー「ウインドウ」→「ウインドウを閉じる」と同じで、同じウインドウのタブをすべて閉じます。複数のタブが開いていれば、環境設定「一般」の設定に従って確認ダイアログが出ます([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照) |
| qooViewerを終了 | アプリケーションを終了します |

最後のウインドウを閉じたときにアプリごと終了するかどうかは、環境設定「一般」の「最後のウインドウを閉じたら終了」に従います。

次の組み合わせは macOS 標準の操作と重なるため、選択肢に含まれていません。

- **右ボタン / control+クリック**: 常にコンテキストメニューを開きます
- **command**: メニューバーのショートカットと衝突します
- **shift+ホイール**: macOSがホイール操作の縦横を入れ替えてしまうため、ホイールでは shift を使えません(クリックでは使えます)

トリガーを追加しようとしたとき、そのトリガーが既に別の操作に割り当て済みだった場合は、割り当てられず「このトリガーは既に使用されています」という警告が表示されます(キーボードと同じ扱いです)。

#### ドラッグ(マウスジェスチャー)

ボタンを押したまま画面をなぞって離すと、その向きに応じた操作を行います。

- 押した点から離した点までの移動量が、**縦横どちらかで30ポイントを超えている**こと
- 押してから離すまでが**1秒以内**であること
- 向きは移動量の大きい方の軸で決まります(上下左右の4方向のみ。斜めや、L字などの複数ストロークは扱いません)

30ポイントに満たない動きは、これまでどおりクリックとして扱われます(手ぶれでページ送りができなくなることはありません)。逆に30ポイントを超えたストロークは、割り当てが無くても**クリックにはなりません**(意図して大きく動かしたものをページ送りとして扱わないため)。

ドラッグは、クリックと違って画面の左右を区別しません。左右で動作を変えたい場合は、割り当てる操作の側で「左へ移動」「右へ移動」のような画面位置基準の操作を選んでください。

#### スクロールできる表示モードでの扱い

画面をスクロールできる表示モード(「横幅に合わせる」「横幅に合わせる(単ページ)」「拡大縮小しない」)では、クリックの割り当ては下記の「表示モード別の操作」の設定が優先されます。修飾キーの付かないホイールの扱いも「表示モード別の操作」で決まります。ただし**修飾キー付きのホイール(option+ホイール)は「スクロールできるとき」の設定を通さず、常に割り当てられた操作を行います**。

これらのモードでは、**画像をドラッグして動かせます**(掴んでいる間はカーソルが手の形になります)。ドラッグして動かした場合、指を離してもクリックとはみなされないため、意図せずページが送られることはありません。スクロールバーの上をクリック/ドラッグした場合も、ページ送りではなくスクロールバーの操作になります。

このため、**画像が実際に動いたかどうか**でドラッグの意味が決まります。動いたならスクロール、動く余地が無かった(端に着いていた、画像が画面に収まっている)ならマウスジェスチャーです。「画面内に収める」で表示しているときは掴んで動かす余地がないので、ドラッグは常にジェスチャーになります。

### 表示モードごとの設定(環境設定「表示モード別の操作」)

「キーボード」「マウス」の割り当てはどの表示モードでも同じように働きます。それに対し、画面をスクロールできる表示モードでは操作の意味が変わってくるため、モードごとの上書きを「表示モード別の操作」で設定できます。

上部の「設定する表示モード」で対象(横幅に合わせる/横幅に合わせる(単ページ)/拡大縮小しない)を選び、その下でキーボード・マウス・マウスホイールを設定します。ここで設定していない項目は、「キーボード」「マウス」の割り当てがそのまま働きます。マウスはクリックのトリガーだけを扱います(このモードでのホイールの扱いは下記「スクロールできるとき」が決まり、ドラッグは表示モードによって意味が変わらないため)。表示モードによって意味が変わる操作(スクロール系とページ送り系)だけを扱うため、ブックマークやお気に入りなどはここには出てきません。

既定の割り当ては次の通りです(3つのモードとも共通)。

| 操作 | 既定のキー/トリガー |
| --- | --- |
| 1画面分進む+次のページ | space |
| 1画面分戻る+前のページ | shift+space |
| 1画面分+左へ移動 | 左ボタン・画面の左側 |
| 1画面分+右へ移動 | 左ボタン・画面の右側 |
| 1画面分下へ | page down |
| 1画面分上へ | page up |
| 下へスクロール | ↓ |
| 上へスクロール | ↑ |
| 左へスクロール | ← |
| 右へスクロール | → |
| ページの先頭へスクロール | home |
| ページの末尾へスクロール | end |

「1画面分進む+次のページ」は、まず1画面分スクロールし、下端まで来たら読み方向へ1画面分だけ横へ移り(そのとき縦は先頭へ戻ります)、横にも余地が無くなったら次のページへ進みます。「横幅に合わせる(単ページ)」で右半分を読み終えたら左半分の先頭へ移る、という読み進め方はこの動作によるものです。

「1画面分+左へ移動」「1画面分+右へ移動」は、上記の動作を画面上の左右で指定するものです。読み方向により、進む/戻るのどちらになるかが変わります(「左へ移動」「右へ移動」と同じ考え方)。既定では画面の左右をクリックしたときに割り当てられているため、右開きの本では画面の左をクリックすると読み進み、右をクリックすると戻ります。

「上/下/左/右へスクロール」は1画面分ではなく少しずつ動かす操作です。1回あたりの移動量は「1回のスクロール量」(既定20pt、5〜200pt)で変更できます。とくに「拡大縮小しない」で横に長い画像を見るときは、これがキーボードから横方向へ動かす手段になります(←→は「キーボード」ではページ送りに割り当てられているため、この画面で上書きしています)。

マウスホイールの動作は「スクロールできるとき」で選びます。

| 選択肢 | 動作 |
| --- | --- |
| スクロールのみ(既定) | ホイールはスクロールするだけで、ページは送らない |
| スクロール(端で横へ) | 端まで来たら1画面分横へ移動する。ページは送らない |
| スクロール+ページ送り | 端まで来たら横へ移動し、それも尽きたらページを送る |
| ページ送りのみ | ホイールはスクロールに使わず、常に「マウス」でホイールに割り当てた操作を行う |

スクロールできないとき(「画面内に収める」で表示しているとき)は、この選択に関わらず「マウス」でホイールに割り当てた操作を行います。また、修飾キー付きのホイール(option+ホイール)は、この選択に関わらず常に割り当てられた操作を行います。

「初期設定に戻す」を押すと、選んでいる表示モードについてこの画面のすべての設定(キーボード・マウス・1回のスクロール量・マウスホイール)が初期設定に戻ります(ほかの表示モードや「キーボード」「マウス」には影響しません)。

### トラックパッド

環境設定「閲覧中の動作」の「トラックパッドのフリックでページを送る」(既定ON)をONにすると、トラックパッドの「ページ間をスワイプ」ジェスチャー(2本指、または3本指/4本指。指の本数は Mac 本体の「システム設定」→「トラックパッド」の設定に従います)による左右のフリックが、上表のホイールと同じ操作(既定では前へ移動/次へ移動)として扱われます。このときトラックパッドの2本指の縦スクロールはページ送りとして反応しません(完全に無視されます)。OFFにすると、フリックによる特別な扱いはせず、2本指スクロールは画像のスクロール操作になります。物理的なマウスホイールでのページ送りは、この設定に関わらず常に有効です。

環境設定「閲覧中の動作」の「2本指スクロールを反転」(既定OFF)をONにすると、トラックパッドの2本指スクロールで**画像が動く向き**が、上下・左右とも逆になります。macOSの「システム設定」→「トラックパッド」→「ナチュラルなスクロール」と違い、qooViewerのトラックパッド操作だけが対象なので、物理的なマウスホイールの向きは変わりません。

- 向きに操作を割り当てている部分(ホイールの上/下、フリックの左右によるページ送り)の向きは変わりません。そちらを入れ替えたい場合は「マウス」「表示モード別の操作」で割り当てを変更してください。
- ONのときは、端まで来たときの跳ね返り(ゴムのように少し伸びる動き)がなくなり、可動範囲でぴたりと止まります。

---

## 5. 表示モード

### 拡大縮小モード

環境設定「画像の表示」、またはツールバー/メニューバーの表示モード切替(f キー、または ⌘1〜⌘4)で4つから選べます。

| モード | 内容 |
| --- | --- |
| 画面内に収める(既定) | 縦横とも画面に収まるように拡大縮小する。スクロールする余地が無いモード |
| 横幅に合わせる | 表示中の内容**全体**の横幅を画面幅に合わせ、縦が画面より長ければ縦スクロールする |
| 横幅に合わせる(単ページ) | 表示中の内容の**半分**の横幅を画面幅に合わせる。左右にスクロールしながら片側ずつ読む |
| 拡大縮小しない | 原寸のまま表示し、縦横ともスクロールする |

「横幅に合わせる(単ページ)」は、見開きのページや、見開きのままスキャンされた横長のページを、片ページぶんが画面幅いっぱいになる大きさで読むためのモードです。「横幅に合わせる」との違いは「表示中の内容の全体の横幅を合わせるか、半分の横幅を合わせるか」の一点だけで、画像を切り分けているわけではありません(ページ番号も総ページ数も変わりません)。縦長のページなど半分にする意味が無い内容では、自動的に「横幅に合わせる」と同じ表示になります。判定には環境設定「画像の表示」の「単ページ扱いの閾値(横÷縦)」を使います。

新しいページを表示したときは、読み方向に応じた読み始め側(右開きなら右上、左開きなら左上)から表示されます。

「既定の表示モード」(環境設定「画像の表示」)で、初めて開く本に最初に適用するモードを指定できます。一度開いた本は、次に開いたときも前回選んだモードがそのまま復元されます。

### ピンチ拡大(トラックパッド)

トラックパッドのピンチイン・ピンチアウトで、表示中のページを無段階に拡大できます。上の4つのどのモードでも使えます。

- **拡大の中心**はマウスカーソルの位置です。カーソルを置いた場所が動かないように拡大されるので、読みたいところへ寄っていけます(カーソルが画像表示エリアの外にある場合は画面の中央が中心になります)。
- 拡大していない状態が100%で、**それより小さくは縮小しません**。上限は環境設定「画像の表示」の「トラックパッドのピンチ拡大の上限」で変更できます(既定400%。100%にするとピンチ拡大が無効になります)。
- 「画面内に収める」で拡大した場合も、画面からはみ出した部分を2本指スクロール・ドラッグ・キーボードでスクロールして読めます。
- **解除する**: ピンチインで100%まで戻すか、esc キーを押します。ページが変わったとき、拡大縮小モード・見開き/単ページ・読み方向を切り替えたときにも自動的に解除されます。倍率は記憶されないので、次に開いたときは常に等倍から始まります。
- トラックパッドの**2本指ダブルタップ**(スマートズーム)で、200%と等倍を切り替えられます。
- 拡大中は、画面表示用よりも高解像度の画像データに自動的に切り替わるため、画面をそのまま引き伸ばすよりも精細に表示されます([20. 拡大鏡](#20-拡大鏡)と同じ仕組みです)。
- 拡大中はマウスホイール・2本指スクロールがスクロール専用になります(「表示モード別の操作」の「マウスホイール」の設定に関わらず、端まで来てもページは送られません)。横方向のフリックによるページ送りも止まります。拡大を解除すれば元の設定どおりに戻ります。
- ピンチ操作中は、現在の倍率が画面上部に一時的に表示されます。
- **ページ一覧パネルを開いている間、その上でのピンチはサムネイルの大きさを変える操作になります**([6. ページ一覧](#6-ページ一覧サムネイルグリッド)参照)。背後のページが拡大されることはありません。

### 見開き/単ページ

sキー、ツールバーのメニュー、メニューバー「View」→「Spread」で切り替えます。見開き表示中でも、横長画像(横÷縦が環境設定「画像の表示」の「単ページ扱いの閾値(横÷縦)」以上、既定1.0)は自動的に単ページ表示になります。見開きのページの組み合わせ(奇数/偶数ペア)がずれてしまった場合は、「1ページだけ左/右へ移動」(既定 shift+←/shift+→)で調整できます。

この切り替えは「読んでいる間の一時的な表示モード」です。特定のページだけを常に単ページ/見開きの左右いずれかに固定したり、ページの表示順序自体を変えたりしたい場合は、[10. ページレイアウト](#10-ページレイアウト)の機能を使います。

### 読み方向

rキー、メニューバー「View」→「Right-to-Left」で右開き(日本式)/左開き(欧米式)を切り替えます。右開きのときは、プログレスバーの進行方向や「左へ/右へ移動」の意味も含め、画面全体が右から左へ読む前提の並びに切り替わります。

### EPUB/PDF/ComicInfo.xml を開いている場合の自動反映

EPUBまたはPDFが読み方向・見開き/単ページを明示している場合、その本を初めて開いたときにその指定を読み取り、ページレイアウトの設定として登録します([11. メタデータ](#11-メタデータ)のEPUB/PDF/ComicInfo.xmlからの取り込みと同じ考え方です)。EPUBが特定のページを見開きの左右どちらに配置するかを個別に指定している場合(この個別指定はEPUBのみで、PDFにはありません)、その指定もページごとの表示状態として一緒に登録されます。

以降の表示は登録された内容に従います。読み方向・見開きの切り替えや、ページごとのレイアウトの変更は、EPUB/PDFであっても自由に行えます。取り込みは本ごとに1回だけ行われるため、変更した内容が次にその本を開いたときにファイル側の値へ戻されることはありません。

cbz/cbr/cb7(および画像フォルダ)の場合は、直下に `ComicInfo.xml` があれば、その `Manga` の値から読み方向を読み取ります。`YesAndRightToLeft` なら右開き、`No` なら左開きとして登録します(`Yes` は「漫画である」ことしか表しておらず方向は未指定のため、読み方向は変更しません)。CBZには見開き/単ページの指定に当たる項目が無いため、そちらは読み取りません([15. CBZの書き出し](#15-cbzの書き出し)参照)。

なお、見開きの左右を明示的に指定したページを表示している間は、その組み合わせが崩れないよう「1ページだけ左/右へ移動」だけは無効化されます(これはEPUB由来かどうかに関わらず、自分で指定したページでも同じです)。プログレスバーやページ一覧から直接ジャンプした場合も、指定された組み合わせがずれないよう自動的に着地位置が調整されます。

### コントラスト補正

紙の黄ばみなどでコントラストが甘くなった古いスキャン本を、きっちりした白黒に見えるよう補正して表示する機能です。メニューバー「View」→「Contrast Correction」で本ごとにON/OFFを切り替えます(既定OFF)。設定は本単位で記憶され、次回その本を開いたときも同じ状態で表示されます。環境設定「キーボード」から好きなキーを割り当てることもできます(既定では割り当てていません)。

カラーのページ(口絵・カラーイラストなど)は自動的に判定され、この設定がONの本でも補正は適用されません。拡大鏡([20. 拡大鏡](#20-拡大鏡))で表示される画像にも同じ補正が反映されます。

---

## 6. ページ一覧(サムネイルグリッド)

ツールバーのグリッドアイコン、tキー、メニューバー「View」→「Show Page Grid」、画像表示エリアを右クリックしたコンテキストメニューの「ページ一覧を表示」で開きます。全ページのサムネイルを一覧表示し、クリックでそのページへジャンプします。

**「ページ一覧を表示」は表示/非表示のトグルです。**開いたときと同じ操作でそのまま閉じられます ― tキー(割り当てを変えていればそのキー)をもう一度押す、割り当てたマウス操作をもう一度行う、のいずれでも閉じます。マウスジェスチャーを割り当てている場合は、パネルの上でそのジェスチャーを行っても閉じます(パネルがビューアのほぼ全面を覆うため)。

独立したウインドウではなく、ビューア画面の中央に重ねて表示されるパネルです。パネルの大きさは、画像表示領域から環境設定「外観」→「パネル」→「ページ一覧パネル」→「サムネイル」の「左右の余白」「上下の余白」(それぞれ片側の%)を引いたもので、列数はその幅とサムネイルのサイズ・間隔から自動的に決まります(ウインドウの大きさを変えると列数も変わります)。

**クリックで閉じるのは、次の3つの場合だけです。**

- サムネイルをクリックする(そのページへ移動して閉じます)
- パネル内の余白(サムネイルとサムネイルの隙間、見出しの周辺など)をクリックする(何もせず閉じます)
- パネルの外側に見えている**画像表示エリア**をクリックする(何もせず閉じます)

ツールバー・プログレスバー・タイトルバー・メニューバー・サイドパネル・環境設定などの別ウインドウ・他のアプリを操作しても閉じません。サイドパネルで次に開く本を探したり、環境設定でサムネイルの大きさを調整したりしている間、パネルは開いたままになります。なお、パネルを表示している間はツールバー・プログレスバーのボタンは押せません(先にパネルを閉じてください)。

### サムネイルの右クリック

サムネイルを右クリックすると、次の項目が使えます(サイドパネルの「ページ」モード・「本の中身ブラウザ」の右クリックと同じ内容です)。

- **このページをブックマークに追加 / このページをブックマークから削除**: そのページのブックマークを付け外しします。既に付いているページでは文言が「削除」に変わるので、**項目の文言そのものが今の状態を表します**。対象は右クリックしたそのページ1枚だけで、見開き表示中でも相方のページには触れません。結果は画面下部に通知で表示されます。シークレットウインドウと、画像を直接開いた本ではグレーアウトします([8. ブックマーク](#8-ブックマーク)参照)。
- **Finderで開く**: そのページの画像ファイルをFinderで表示します。書庫(zip/cbz・rar/cbr・7z/cb7・EPUB)やPDFの中のページの場合は、中の画像はFinderからは見えないため、**その書庫/PDFファイル自体**を表示します。
- **画像を書き出す…**: 書庫・PDFの中のページのときだけ表示されます。そのページの画像を1枚のファイルとして書き出します([16. 画像の書き出し](#16-画像の書き出し)参照)。フォルダの本の画像はFinderで実物を選択できるため、この項目は出ません。

読みながら出しっぱなしにできるページ一覧が欲しい場合は、サイドパネルの「ページ」モード([7. サイドパネル](#7-サイドパネル)参照)を使ってください。こちらはジャンプしても閉じず、現在のページに合わせて自動的にスクロールします。

### パネルの余白の右クリック

サムネイルの無い余白を右クリックすると、「調整…」だけのメニューが開きます。選ぶと環境設定「外観」が「ページ一覧パネル」のページを開いた状態で開きます。このページには、パネルの「背景」(すりガラスの濃さ・背景色・文字の影)と「サムネイル」(サイズ・間隔・余白・下の表示・枠の色など)の両方が並んでいます。

**右クリックではパネルは閉じません。** 余白のクリックで閉じるのは左クリックのときだけです。パネルを開いたまま見た目を調整できます(上記のとおり、環境設定ウインドウを操作してもパネルは閉じません)。

### サムネイルのサイズ・間隔・余白

パネル上部(見出しの右)のスライダーで、サムネイルの大きさ(80〜320pt、既定120pt)をその場で変えられます。大きさに合わせて列数も自動的に変わります。値は記憶され、環境設定「外観」→「パネル」→「ページ一覧パネル」→「サムネイル」の「サムネイルのサイズ」と共通です。

**サムネイルの上でトラックパッドのピンチイン・ピンチアウトをしても、同じように大きさを変えられます。** スライダーへ手を伸ばさずに、見ているその場で「もっと大きく/もっと詰めて」を決められます。変わる値はスライダー・環境設定とまったく同じもので、上のスライダーのつまみもその場で動き、指を離した大きさがそのまま次に開いたときにも引き継がれます。80pt・320ptの端まで来るとそれ以上は変わりません。ページ一覧を開いている間は、ピンチで背後のページが拡大されることはありません([5. 表示モード](#5-表示モード)の「ピンチ拡大」参照)。

スライダーの右にあるスイッチで、下記の**拡大プレビューの表示/非表示をその場で切り替えられます**(カーソルを合わせると「拡大プレビューの表示」というツールチップが出ます)。環境設定「外観」→「パネル」→「ページ一覧パネル」→「サムネイル」の「カーソルを合わせたら拡大プレビューを表示する」と同じ設定で、どちらから変えても即座に両方へ反映されます。

同じ環境設定の「横の間隔」「縦の間隔」(0〜40pt、既定10pt)でサムネイル同士の間隔を、「左右の余白」(既定10%)「上下の余白」(既定5%)でウインドウの端からパネルまでの余白を調整できます。

### サムネイルの下の表示・枠の色

環境設定「外観」→「パネル」→「ページ一覧パネル」→「サムネイル」で、見え方をさらに調整できます。

- **サムネイルの下の表示**: 各サムネイルの下に何を書くかを「ページ番号」(既定)「ファイル名」「表示しない」から選べます。「表示しない」にすると文字の分の隙間も無くなり、サムネイルだけが詰まって並びます。ファイル名が長い場合は中間が省略表示されます。
  - 「ファイル名」を選んでいるとき、**書庫の中のフォルダや、書庫の中の書庫に入っている画像は、ファイル名の上に本の中での場所が1行加わります**(下記「[画像が本の中のどこにあるかの表示](#画像が本の中のどこにあるかの表示)」参照)。
- **文字の大きさ**: 上記の文字の大きさ(8〜20pt、既定11pt)。「表示しない」を選んでいるときは操作できません。
- **表示中のページの枠の色**: 今表示しているページを示す枠の色を、「アクセントカラー」(既定。macOSのシステム設定「強調表示の色」に追従します)・赤・黄・緑・白・黒・カスタムから選べます。「カスタム」を選ぶと、背景色と同じ色指定ダイアログが開きます([25. 環境設定](#25-環境設定)参照)。

パネル自体の背景色・すりガラスの濃さも、同じ環境設定「外観」→「パネル」→「ページ一覧パネル」の「背景」で変えられます([25. 環境設定](#25-環境設定)の「外観」参照)。

### 画像が本の中のどこにあるかの表示

章ごとに書庫やフォルダへ分けてある本では、どの章にも `001.jpg` から番号が振り直されていることがよくあります。ファイル名だけを出していると、一覧に同じ名前がいくつも並び、どの章のページなのか区別がつきません。そこで**書庫の中のフォルダ・書庫の中の書庫に入っている画像には、ファイル名と一緒に「本の直下から見たそこまでの道順」を、ファイル名より一段控えめな文字で添えます**(例: `ch02.zip/ch02`、`vol1.7z/chapters/ch03.cbz`)。フォルダの本を開いた場合も同じで、中の書庫のファイル名が道順の一部として現れます。

- **本の直下に置かれている画像には何も付きません。** 中にフォルダを持たない普通のcbz/cbrや、直下に画像を並べただけのフォルダでは、これまでとまったく同じ表示のままです。
- **EPUBには付きません。** EPUBの画像は `OEBPS/Images/` のような決まった1か所にまとめて置かれているのが普通で、どのページにも同じ道順が付くだけになり、ページを見分ける役には立たないためです。
- 道順が長くて表示幅に収まらない場合は中間が省略されます。カーソルを合わせると、道順とファイル名をつないだ全体がツールチップで表示されます。
- PDFのページには元々ファイル名という単位が無いため(「本のファイル名 (ページ番号)」と表示されます)、道順も付きません。

この表示が出るのは次の場所です。**画像表示エリアと、サイドパネルの「本の中身ブラウザ」には出ません**(後者は階層をたどって見るものなので、今どこにいるかはパネル上部に別途表示されています)。

| 場所 | 参照 |
| --- | --- |
| プログレスバーのサムネイル(「ファイル名」を含む表示のとき) | [3. 画面の基本構成](#3-画面の基本構成) |
| ページ一覧(サムネイルグリッド)のサムネイルの下(「ファイル名」表示のとき)と拡大プレビュー | この章 |
| サイドパネル「ページ」モードの行と拡大プレビュー | [7. サイドパネル](#7-サイドパネル) |
| 「ブックマーク・レイアウトの編集」のサムネイルのツールチップと拡大プレビュー | [8. ブックマーク](#8-ブックマーク) |
| EPUB/CBZの書き出しウインドウの「カバー画像」列と、そこから開くカバー選択の一覧 | [13. EPUBの書き出し](#13-epubの書き出し) |

なお、コンテキストメニューの「情報を見る」では、以前から「場所」の欄に**ディスク上のフルパスに書庫の中の道順をつないだもの**が表示されています([17. Finderで開く/情報を見る](#17-finderで開く情報を見る)参照)。

### マウスホイールでのスクロール量

ページ一覧の上で**物理マウスのホイール**を回したときに、1ノッチで何行分スクロールするかを、環境設定「外観」→「パネル」→「ページ一覧パネル」→「サムネイル」の「ホイール1ノッチのスクロール行数」(0.5〜5.0行、0.1行刻み、既定1.0行)で調整できます。スライダーで大まかに合わせてから、値の右の⬆⬇ボタンで0.1行ずつ詰められます。

トラックパッド(やMagic Mouseなど)のなめらかなスクロールはこの設定の対象外で、これまでどおり指の動きにそのまま追従します。ピンチ(2本指を開く/閉じる)は、スクロールではなくサムネイルの大きさの変更になります(上記「サムネイルのサイズ・間隔・余白」参照)。

### 拡大プレビュー

サムネイルにカーソルを合わせてしばらく待つと、そのページの拡大画像とファイル名がポップオーバーで表示されます(書庫の中のフォルダ・書庫の中の書庫にある画像では、ファイル名の上に本の中での場所も表示されます。上記「[画像が本の中のどこにあるかの表示](#画像が本の中のどこにあるかの表示)」参照)。

**ページ一覧だけに効く設定**は、環境設定「外観」→「パネル」→「ページ一覧パネル」→「サムネイル」にあります(パネルの見た目の設定と同じ場所です)。

- **カーソルを合わせたら拡大プレビューを表示する**(既定ON): OFFにするとページ一覧では拡大プレビューを出しません。サイドパネルのページモードやブックマーク・レイアウトの編集ウインドウのサムネイルは、サイズ調整が無く拡大が無いと何のページか分からなくなるため、この設定に関わらず常に表示されます。**ページ一覧パネル上部のスイッチ**(上記「サムネイルのサイズ・間隔・余白」参照)と同じ設定です
- **表示中のサムネイルの拡大画像を先読み**(既定OFF): ONにすると、ページ一覧で画面に見えているサムネイルの拡大プレビュー用の画像をあらかじめデコードしておき、プレビューを即座に表示します。そのぶんメモリとCPUを使うため、この項目だけは環境設定「キャッシュ」→「先読み」にあります(メモリの使用量に直結する設定はそこにまとめてあります)。上をOFFにしているときは操作できません

**すべての場所に共通の設定**は、環境設定「閲覧中の動作」→「サムネイルプレビュー」にあります。

- **表示までの時間**(0〜1.00秒、既定0.35秒): カーソルを合わせてから表示するまでの待ち時間。ページ一覧・サイドパネルのページモード・ブックマーク・レイアウトの編集・書き出しウインドウのすべてのサムネイルに効きます
- **プレビューの大きさ**(440〜800pt、既定440pt): 拡大プレビューの一辺の長さ。画像はこの正方形に縦横比を保って収まり、下のファイル名もこの幅で折り返します。上と同じくすべての場所に効きます。最小値が440ptなのは、**サムネイルより小さいプレビューを作らせないため**です(サムネイルは最大320ptまで大きくできます)。プレビュー用の画像は、この大きさに画面の倍率を掛けた解像度(最大1600px)でデコードします。Retinaディスプレイでも等倍以上の画素があるので見た目は原寸と変わらず、ページ本体の画像を使うよりはるかに軽く済みます

---

## 7. サイドパネル

ウインドウの左端(または右端)に、フォルダ内のファイル一覧や、今開いている本自身の中身、お気に入り・ブックマークの一覧を確認・操作できるサイドパネルがあります。環境設定「一般」の「サイドパネルを有効にする」(既定ON)をOFFにすると、サイドパネル自体とメニューバー「View」→「Hide Side Panel」がどちらも非表示になります。あわせて、環境設定「一般」のサイドパネル関連の項目(パネルの位置・開く・フォルダへ移動をダブルクリックにする・並び順・本の移動をブラウザの並び順に合わせる)もグレーアウトします。

- **既定の表示**: 常時表示されています。表示されている間は、その分だけツールバー・画像表示エリア・プログレスバーの幅が狭くなります。メニューバー「View」→「Hide Side Panel」をONにすると、ツールバー・プログレスバーの「隠す」設定と同様、普段は隠れていてマウスカーソルをウインドウの端(パネルを表示している側)に近づけると一時的に表示される挙動に切り替わります。この場合、表示中は画像の上に重なって表示され(幅は変わりません)、カーソルがパネルの外へ出るか、ウインドウの外へ出ると自動的に隠れます(クリックでは閉じません)。カーソルを近づけてから表示されるまでの待ち時間は、環境設定「外観」→「サイドパネル」の「表示までの時間」(0〜2.0秒、既定0.0秒=すぐに表示)で変えられます([25. 環境設定](#25-環境設定)の「表示までの時間」参照)。
- **表示する側**: 環境設定「一般」の「パネルの位置」で、ウインドウの左右どちらに表示するかを選べます(既定は左)。常時表示・隠す設定のどちらにも反映され、隠す設定の場合はカーソルを近づける端も選んだ側になります。設定はすべてのウインドウ/タブで共通で、次回起動時にも記憶されます。
- **幅の調整**: パネルとビューアとの境界(左側に表示している場合はパネルの右端、右側の場合は左端)をドラッグすると、幅を調整できます(220〜480pt)。調整した幅は次回起動時にも記憶されます。
- **表示モードの切り替え**: パネル最上部に「ブラウザ」「ブックマーク」「履歴」「ページ」「リソース」の5つの切り替えボタンが並んでいます。押したボタンのモードへワンクリックで直接切り替わります(選択中のモードはボタンが色付きで表示されます)。ボタンはアイコンのみの表示で、カーソルを合わせるとモード名がツールチップで表示されます。選んだモードは次回起動時にも記憶され、すべてのウインドウ/タブで共通です。
- **上下2段**: 「ブラウザ」「ブックマーク」モードは上下2段に分かれており、上段・下段の境界(パネル内の水平の仕切り線)をドラッグすると表示比率を調整できます。比率はモードごとに別々に記憶されます。「履歴」「ページ」「リソース」モードは1つの一覧でパネルの高さいっぱいを使います。
- **絞り込み**: 「ブラウザ」の上段・下段、および「履歴」モードには絞り込み用の入力欄があり、名前の一部を入力するとその文字を含む項目だけが表示されます(大文字小文字は区別しません)。右端の×ボタンで解除できるほか、フォルダや本の中の階層を移動すると自動的に解除されます。入力欄のキーボードフォーカスは、**欄の外(パネルの余白・一覧の行・画像表示エリアなど、ウインドウ内のどこでも)をクリック**するか、Return または Esc を押すと外れます(絞り込みの文字はそのまま残ります)。フォーカスが外れれば、キーボードショートカットが通常どおり効きます。「ブックマーク・レイアウトの編集」「ライブラリの整理」の各ウインドウの検索欄も同じです(「メタデータの編集」ウインドウの検索欄はツールバーの標準の検索欄で、Esc または欄の外のクリックでフォーカスが外れます)。
- **右クリック**: 一覧の各行を右クリックすると、その行に応じた操作(「Finderで開く」など)のメニューが開きます。本を指している行では、開き方を選ぶ「開く」「新規ノーマルウインドウで開く」「新規シークレットウインドウで開く」「新規タブで開く」が共通で並びます([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照)。お気に入り・ブックマークの行では、いちばん下に「名前を変更」と削除の項目が並びます(下記の各モードの説明を参照)。それ以外の内容は各モードの説明を参照してください。メニューが開いている間は、**対象の行が枠で囲まれて**強調されます(Finderのリスト表示で右クリックしたときと同じ見え方です)。どの行に対するメニューなのかが一目で分かり、メニューを閉じると枠も消えます。
- **クリック方法**: 既定ではシングルクリックで開く・移動します。環境設定「一般」の「開く・フォルダへ移動をダブルクリックにする」をONにすると、パネル全体の「開く」「移動する」操作がダブルクリックに統一されます(戻る/進む/1階層上へのボタン操作には影響しません)。この場合、上段のフォルダは「移動する」と「画像フォルダとして本を開く」の2つの意味をダブルクリック1つで表す必要があるため、直下に画像ファイルがあれば本として開き、無ければその中へ移動する、という判定になります。
- **並び順**: 環境設定「一般」の「並び順」で、上段・下段どちらの一覧にも共通で「フォルダを上に」(既定。Finderと同様、フォルダをまとめてファイルより上に表示)、「名前順に混在」(フォルダ・ファイルを区別せず名前順)のどちらかを選べます。上段(フォルダブラウザ)ではこれに加えて、パネル上部の「並べ替え」ボタンで並べ替えの基準(名前・サイズ・種類・作成日・変更日)と昇順・降順を選べます(次の「ブラウザモード 上段: フォルダブラウザ」参照)。こうして決まる上段の並び順は、環境設定「一般」の「本の移動をブラウザの並び順に合わせる」をONにすると、前後の本への移動とメニューバー「File」→「Open File in Same Folder」にも使われます([21. 前後の本への移動・同じフォルダのファイルを開く](#21-前後の本への移動同じフォルダのファイルを開く)参照)。

### ブラウザモード 上段: フォルダブラウザ

現在開いているファイル/フォルダが置かれている、同じフォルダの中身を一覧表示します。本を何も開いていない(ウェルカム画面の)ときは、代わりにマウントされているボリューム(内蔵ディスク・外部ディスクなど)の一覧が表示されます。

- 上部の「戻る」「進む」「1階層上へ」ボタンで移動できます。右端の「Finderで開く」ボタンで、今表示中のフォルダをFinderで開けます(ボリューム一覧を表示中は無効)。
- 「Finderで開く」ボタンの**左隣**にある「並べ替え」ボタン(上下の矢印のアイコン)で、一覧の並べ替え方を変えられます。「名前」「サイズ」「種類」「作成日」「変更日」のいずれかを基準に、「昇順」「降順」を選べます。設定はすべてのウインドウ/タブで共通で、次回起動時にも記憶されます。
  - フォルダをまとめて上に表示するかどうかは、この基準とは別に環境設定「一般」の「並び順」に従います。降順にしてもフォルダは上に表示されたままです。
  - フォルダのサイズは、Finderと同じく中身をたどって計算することはしません。そのため「サイズ」で並べた場合、フォルダ同士は名前順に並びます。
  - 「名前」で並べる場合は、Finderと同じ並び方(大文字・小文字や全角・半角を区別せず、名前に含まれる数字は数値として比べる)になります。
  - この設定は上段のフォルダブラウザにだけ効きます。下段(本の中身ブラウザ)は、本のページ順と一致させるため常にファイル名順のままです。
- 既定では、ファイル(zip/cbz・rar/cbr・7z/cb7・PDF・EPUB)をクリックするとその本を開き、フォルダをクリックすると中身に応じて次のどちらかになります。
  - **画像しか入っていないフォルダ(=それ自体が1冊の本)**: 中へは移動せず、その場で本として開きます。右クリック→「開く」と同じで、一覧は今の階層のまま、そのフォルダの行が選択色になります。いまどの本を見ているか、隣にどの本があるかが一覧で分かるようにするためです(一覧は画像ファイルを行として表示しないので、中へ移動しても空に見えるだけです)。「最近開いた本」にも残ります。
  - **サブフォルダがあるフォルダ**: 中へ移動します。直下に画像もあれば、その画像がそのまま表示されます(一覧にはサブフォルダが残るので、そのまま奥へ進めます)。この移動で表示した本は、目的の本を探して通り抜けただけのフォルダで履歴が埋まらないよう、「最近開いた本」には残りません。読書位置・ブックマーク・お気に入り・ページレイアウトはこれまでどおり記録されます。
- 「戻る」「進む」「1階層上へ」で移動した先の直下に画像があれば、その画像がそのまま表示されます。画像だけのフォルダにこの方法で入った場合は、一覧の代わりに「このフォルダの画像を表示しています。」と表示されます。
- 本を開いたり切り替えたりすると、その本の親フォルダへ移動して本の行が自動的にハイライトされ、スクロールして表示されます。「1階層上へ」で移動した場合は、それまでいたフォルダが同様にハイライトされます。フォルダブラウザで別の場所へ移動した状態は、本を切り替えない限りそのまま保たれます。パネルを隠す設定にしている場合も同じで、パネルが隠れて再び表示されても、移動した場所は変わりません。
  - 画像ファイルを直接開いているときだけは、その画像が入っているフォルダの**1つ上**を表示し、画像の入ったフォルダ自体をハイライトします。この一覧は画像ファイルを表示しないため、画像の入ったフォルダをそのまま表示すると中身が空に見えてしまうためです。ハイライトされたフォルダをクリックすれば、そのままフォルダ全体を1冊として開けます。
- 行を右クリックすると「開く」「新規ノーマルウインドウで開く」「新規シークレットウインドウで開く」「新規タブで開く」「Finderで開く」が使えます([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照)。「Finderで開く」は、フォルダならそのフォルダを開き、ファイルなら親フォルダを開いてそのファイルを選択した状態にします。
  - 開き方の4項目は、その行が1冊の本を指しているときだけ表示されます。ファイルの行には常に表示されます(この一覧には開ける形式のファイルしか並ばないため)。フォルダの行では、**直下に画像があるフォルダ**(=そのフォルダ自体が1冊の本になるもの)にだけ表示されます。奥の本へたどり着くための途中のフォルダには、開くべき本がそもそも無いためです。その場合は「Finderで開く」だけになります。
- サンドボックスの制約でアクセス許可の無いフォルダへ移動しようとすると、その場でアクセスを許可するボタンが表示されます(環境設定の「フォルダのアクセス権」を開く必要はありません)。

### ブラウザモード 下段: 本の中身ブラウザ

今開いている本自身の内部構成(フォルダ本ならその配下、アーカイブ本ならその中身)を一覧表示します。**並び順は常に本のページ順そのもの**です(ページの並べ替えを行った本ではその順で並びます。[2. 本を開く](#2-本を開く)の「ページの並び順」参照)。除外したページや、本のページに含まれない画像は、一覧の末尾に名前順でまとまります。上段と同じく「戻る」「進む」「1階層上へ」ボタンがありますが、この移動は本の中に限られます(本の外へは出られません)。本を開いていないとき、または本がPDF・EPUB形式のときは、この下段は表示されず上段のみになります。

画像ファイルを直接開いている場合は、開くときに渡した画像がそのままページ順で並びます(辿るべき階層が無いため、「戻る」「進む」「1階層上へ」は使いません)。同じフォルダにある他の画像は含まれません。

- フォルダ、および中に入れ子になった書庫ファイル(書庫の中に書庫が入っている場合を含む)をクリックすると、その中へ移動します。
- 画像ファイルをクリックすると、そのページへジャンプします。ネストした書庫の中まで踏み込んで見つかった画像(元々その本のページ一覧には含まれていない場合)をクリックした場合は、そのネストした書庫/フォルダ自体を新しい本として開きます。
- 今表示しているページのファイルは色付きの背景で強調され、ページを送るたびにその行が常に見えるよう自動的にスクロールします(フォルダや入れ子の書庫の境界をまたいだ場合は、表示中の階層自体が切り替わります)。パネルを隠す設定にしている場合も、マウスカーソルをウインドウの端に近づけて表示された時点で、既にその行が見えている状態になります。
- 行を右クリックすると「Finderで開く」が使えます。**書庫の中の画像は、中の画像自体はFinderからは見えないため、その書庫ファイル(=今開いている本)を表示します。**その代わり、書庫の中の画像には「画像を書き出す…」も表示され、その1枚を書き出せます([16. 画像の書き出し](#16-画像の書き出し)参照)。フォルダの本の画像は、実物をFinderで選択できるため「Finderで開く」だけになります。
- 本のページとして特定できた画像の行には、あわせて「このページをブックマークに追加」(付いているページでは「このページをブックマークから削除」)も表示されます。内容はページ一覧パネル・サイドパネルの「ページ」モードの右クリックと同じです([6. ページ一覧](#6-ページ一覧サムネイルグリッド)参照)。

書庫の直下(またはその中で見つかった、入れ子になった書庫の中)に画像が無く、書庫ファイルしか無い場合でも、その中身は自動的に統合されて1冊の本として開けます(例: 書庫の中に章ごとの書庫が複数入っているケース)。

中の書庫は、**そのページまで読み進めたときに初めて開かれます**。最近使ったものだけを手元に残し、使わなくなったものから順に手放すため、本1冊ぶんがまとめてディスクへ展開されることはありません。本を閉じたり別の本へ移ったりした時点で、そのために使っていたメモリと一時ファイルはすべて解放されます(残っている量は[7. サイドパネル](#7-サイドパネル)の「リソース」モードで確認できます)。

### ブックマークモード 上段: お気に入り

お気に入りを、登録したフォルダの階層のままツリー表示します([9. お気に入り](#9-お気に入り)参照)。

- フォルダをクリックすると、その場で開いて中身(サブフォルダ・お気に入り)を表示します。もう一度クリックすると閉じます(この開閉は「開く・フォルダへ移動をダブルクリックにする」設定の影響を受けず、常にシングルクリックです)。
- フォルダの行を右クリックすると「名前を変更」「削除」が使えます。フォルダを削除すると、その中のサブフォルダとお気に入りもまとめて削除されるため、確認ダイアログが表示されます。
- お気に入りをクリックするとその本を開きます。開き方(そのまま開く/新規タブ/新規ウインドウ)は、環境設定「本を開く」の「お気に入りから」に従います。まだ本を表示していない状態(ウェルカム画面)から開く場合は、この設定に関わらず常にそのウインドウでそのまま開きます。
- 上部の「+」ボタンで、今開いている本をお気に入りに追加できます(ツールバーの星アイコンと同じ、登録先フォルダを選ぶシートが開きます)。本を開いていないときは押せません。
- 上部の鉛筆ボタンで、「お気に入りの編集」ウインドウを開きます。
- お気に入りの行を右クリックすると「開く」「新規ノーマルウインドウで開く」「新規シークレットウインドウで開く」「新規タブで開く」([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照)、「Finderで開く」、「名前を変更」「お気に入りから削除」が使えます。削除は元に戻せないため、確認ダイアログが表示されます。
- 「名前を変更」で変えられるのは、この一覧に表示される名前だけです。**実体のファイル名やフォルダ名は変わりません**([9. お気に入り](#9-お気に入り)参照)。
- 複数のお気に入りをまとめて整理したい場合や、別のフォルダへ移動したい場合は、上部の鉛筆ボタンから「お気に入りの編集」ウインドウを開いてください。

### ブックマークモード 下段: ブックマーク一覧

今開いている本のブックマークを、ページ番号順に一覧表示します([8. ブックマーク](#8-ブックマーク)参照)。

- ブックマークをクリックすると、そのページへジャンプします。今表示しているページのブックマークは、色付きの背景と塗りつぶしのアイコンで強調されます。
- 上部の「+」ボタンで、今表示しているページをブックマークに追加できます。本を開いていないときは押せません。
- 上部の鉛筆ボタンで、「ブックマーク・レイアウトの編集」ウインドウを開きます。
- ブックマークの行を右クリックすると「このページへ移動」「ブックマークを編集…」「名前を変更」「ブックマークを削除」が使えます。**ブックマークの削除には確認ダイアログが出ません**(「ブックマーク・レイアウトの編集」ウインドウのゴミ箱ボタンと同じ扱いです)。消してしまった場合は、そのページへ移動して付け直してください。
- 一括リネームなど、複数のブックマークをまとめて扱う操作は「ブックマーク・レイアウトの編集」ウインドウで行います。
- 本を開いていない場合や、ブックマークが1件も無い場合は「(ブックマークなし)」と表示されます。

### 履歴モード

過去に開いた本(ファイルメニューの「Open Recent」と同じ一覧)を新しい順に表示します。本を読んでいる最中でも一覧として確認でき、クリックするとその本を開きます。

- 今開いている本は色付きの背景で強調されます。カーソルを合わせるとファイルのフルパスがツールチップで表示されるため、同名の本が複数ある場合も見分けられます。
- 行を右クリックすると「開く」「新規ノーマルウインドウで開く」「新規シークレットウインドウで開く」「新規タブで開く」「Finderで開く」「履歴から削除」が使えます([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照)。
- **「履歴から削除」で、その1件だけを履歴から消せます。** 見られたくない本だけを取り除きたいときに使います。消えるのは履歴の項目だけで、**ファイル自体にも、その本の保存データ(お気に入り・ブックマーク・ページレイアウト設定・メタデータ・読書位置)にも触れません。**対象が1件なので確認ダイアログは出ません(もう一度その本を開けば履歴に戻ります)。複数まとめて消したい場合は、環境設定「リセット」の「履歴の削除…」を使ってください([25. 環境設定](#25-環境設定)参照)。
- 上部のゴミ箱ボタンで、履歴をすべて削除できます(確認ダイアログが出ます)。削除するとファイルメニューの「Open Recent」からも消えます。**ファイル自体には一切触れません。**履歴が1件も無いときは押せません。同じ操作は、メニューバー「File」→「Open Recent」の末尾の「履歴をすべて削除」と、環境設定「リセット」からも行えます。
- 見出しの右端に現在の件数が表示されます。履歴として保持する件数は、環境設定「一般」の「履歴の保存件数」(10〜200件、既定30件)で変更できます。件数を減らすと、その場で古い履歴から取り除かれます。
- 移動・リネームされたファイルは、移動先を追いかけて表示が新しいパスに更新されます(同じボリューム内での移動・リネームの場合)。
- 削除されたファイルは履歴から自動的に取り除かれます。この確認は、qooViewerが再びアクティブになったときと、ドライブを接続/取り外したときに行われます(一覧を表示するたびには行いません。メニューを開くたびにファイルを読みに行くと、メニューの表示が乱れる不具合の原因になるためです)。
  - そのため、qooViewerを操作している最中に他のアプリからファイルを削除した場合は、一覧にしばらく残ることがあります。その項目を選んで開こうとすると、そこで見つからないことが分かり、履歴から取り除かれます。

### ページモード

今開いている本の全ページを、左から**ページ番号・サムネイル・ファイル名**の順に並べた縦1列で表示します。クリックするとそのページへジャンプします。書庫の中のフォルダ・書庫の中の書庫にある画像では、ファイル名の上に本の中での場所が1行加わります([画像が本の中のどこにあるかの表示](#画像が本の中のどこにあるかの表示)参照)。

- 現在表示中のページは、枠線と色付きの背景で強調されます。ページを送るたびに、その行が常に見えるよう自動的にスクロールします。パネルを隠す設定にしている場合も、マウスカーソルをウインドウの端に近づけて表示された時点で、既に現在のページが見えている状態になります。
- サムネイルにカーソルを合わせると、少し待って拡大プレビュー(ファイル名付き)がポップオーバー表示されます(パネルを左側に表示している場合は右へ、右側の場合は左へ開きます)。大きさは、ページ一覧(サムネイルグリッド)や「ブックマーク・レイアウトの編集」ウインドウの拡大プレビューと同じです。
- ファイル名が長くパネル幅に収まらない場合は、2行に折り返したうえで中間が省略表示されます。カーソルを合わせると、本の中での場所とファイル名をつないだ全体がツールチップで表示されます。
- ページレイアウトで「除外」に設定したページは、この一覧には表示されません([10. ページレイアウト](#10-ページレイアウト)参照)。除外の設定/解除はその場で一覧に反映されます。
- ブックマークが付いているページには、ファイル名の隣にしおりのアイコンが表示されます。
- 見出しの右端に「現在のページ / 総ページ数」が表示されます。
- ページ一覧(サムネイルグリッド、[6.](#6-ページ一覧サムネイルグリッド)参照)と違い、ジャンプしても閉じないため、読みながら出しっぱなしにして使えます。サムネイルは両者で共有されるので、二重に読み込まれることはありません。
- 行を右クリックすると「このページをブックマークに追加」(付いているページでは「このページをブックマークから削除」)と「Finderで開く」が使えます。書庫・PDFの中のページのときは、上の「本の中身ブラウザ」と同じく書庫/PDFファイル自体を表示し、代わりに「画像を書き出す…」が表示されます。内容はページ一覧パネルの右クリックと同じです([6. ページ一覧](#6-ページ一覧サムネイルグリッド)参照)。
- このモードは常にシングルクリックで動作します(「開く・フォルダへ移動をダブルクリックにする」設定の影響を受けません)。

### リソースモード

qooViewer自身がどれだけCPU・メモリ・ディスクを使っているかと、環境設定「キャッシュ」の各設定に対して実際にどれだけ使われているかを、その場で確認できます。「メモリの使用量が増え続けていないか」「一時ファイルが残っていないか」といった、アプリ側の不具合をユーザー自身が把握するためのものです。

- **計測のON/OFF**: 上部の丸いボタンで計測を開始/停止します。**起動時は常にOFF**で、次回起動時にも引き継がれません。ONにすると、「計測中 — HH:MM から」と表示され、**サイドパネルを隠していても、別のモードに切り替えていても**、1秒ごとの計測が続きます。OFFにするとグラフの履歴は消えます。計測はアプリ全体で1つなので、どのウインドウでONにしても他のウインドウの表示も「計測中」になり、どこからでもOFFにできます。
  - 計測自体の負荷は無視できる量です(CPU 0.0%)。このモードを表示している間だけ、画面の更新のために1〜2%程度を使います。
- **このアプリ**: CPU使用率・メモリ・ディスクの3つのグラフ。右端が「いま」で、時間が経つと左へ流れていきます(Windowsのタスクマネージャと同じ見え方です)。見出し右端の「2分」「1時間」をクリックすると、直近2分(1秒刻み)と直近1時間(10秒平均)を切り替えられます。
  - **CPU**: アクティビティモニタと同じ数え方で、複数のコアを使っていれば100%を超えます。
  - **メモリ**: アクティビティモニタの「メモリ」列と同じ値です。下に「起動後の最大」(このアプリを起動してからの最大値)と、「この本のキャッシュ以外」(メモリ全体から、下の「この本」のキャッシュの実使用量を引いた残り。アプリ本体や他に開いている本のぶんです)を表示します。
  - **ディスク**: 物理的な読み込み(オレンジ)と書き込み(紫)の速度です。macOSのファイルキャッシュから読めた分は数えません(アクティビティモニタの「ディスク」タブと同じ)。下に起動後の累計を表示します。
  - 計測がOFFでも、現在値・起動後の最大・累計は表示されます(グラフだけが空になります)。
- **この本**: このウインドウで開いている本のメモリキャッシュの実使用量を、設定の上限に対するバーで表示します。見出し右端には、すべてのウインドウ/タブで開いている本の冊数が出ます(上限は本1冊ごとに適用されます)。
  - **ページ画像**: 「メモリに残しておくページ画像」の上限に対する実使用量とページ数。
  - **先読みページ**: 環境設定「前後に先読みするページ数」の範囲を、いま表示しているページを挟んで1マス1ページで並べた帯です。**すべてのマスが塗られていれば先読みが追いついています**。欠けたマスは、そのページがまだメモリにありません。数値は「後 10/10 · 前 10/10」のように前後それぞれの充填数で、分母がそのまま設定値です(本の先頭や末尾で、その側にページが無いときは省略されます)。
    - 帯の並びは**本の読み方向に合わせて反転**します。右開きの本では、後のページが画面の左側に並びます。
    - **見開き表示のときは、表示中の2ページがまとめて濃いマス**になります。先読みもその2ページの前後に効くため、単ページ表示でも見開き表示でも設定どおりのページ数が先読みされます。
  - **過去のページ**: 先読みの範囲より外側に、現在のページから途切れずにメモリへ残っている読み終えたページ数です。読み終えたページは「メモリに残しておくページ画像」の上限に達するまで残るので、この数がゼロでも数十でも正常です。
  - **サムネイル**(プログレスバー・各一覧用、上限60MB)と**拡大サムネイル**(ページ一覧のグリッドと拡大プレビュー用、上限128MB)。
  - **入れ子の書庫**: この本の中で見つかった書庫のうち、いま開いているものの数と、そのうちメモリに置いているぶんの大きさです(環境設定「メモリに残しておく入れ子の書庫」の上限に対するバー)。件数は形式を問わず数えますが、大きさはZIP形式のぶんだけです ―― RAR形式・7z形式は一時ファイルとして持つため、下の「ディスク上」の「一時ファイル」に出ます。入れ子の書庫を使っていない本では、この行は表示されません。
- **ディスク上**: このアプリがディスクに保存しているものの内訳(`~/Library/Containers` 内)。**一時ファイル**(入れ子の書庫のうち、読むためにファイルへ書き出す必要があったもの。RAR形式・7z形式だけが対象で、ZIP形式はメモリから直接読むためここには出ません)、**サムネイルキャッシュ**(環境設定「キャッシュ」の上限を併記。OFFのときは「(オフ)」)、**データベース**(お気に入り・ブックマーク・読書位置・レイアウト・メタデータ)、**その他**、**コンテナ全体**。15秒ごとに自動で更新されるほか、右端のボタンで今すぐ更新できます。
- **異常**: 設定と実際が食い違っている状態を検出すると、ここに一覧表示されます。何も無ければ「検出されていません」と出ます。各項目にカーソルを合わせると、何が起きているかの説明がツールチップで表示されます。検出するのは次のとおりです。
  - ページ画像 / サムネイル / 拡大サムネイルが、メモリの上限を超えたまま数秒以上続いている
  - 環境設定「前後に先読みするページ数」より広い範囲を先読みしている
  - 「ページのサムネイルをディスクに保存する」がOFFなのに、サムネイルのファイルが残っている
  - サムネイルのディスクキャッシュが「最大容量」を超えている
  - 以前の起動が残した一時ファイルがある(本来は起動時に自動的に削除されます)
  - 本を1冊も開いていないのに、この起動の一時ファイルが残っている(本来は本を閉じたときに削除されます)
  - 一瞬だけ超える・直後に片付く、といった正常な動作は異常として扱いません。
- このモードはシークレットウインドウでも同じように使えます(記録は一切残しません)。

---

## 8. ブックマーク

ブックマークは、メニューバーでは標準の「Edit」メニュー(このアプリにはテキスト編集機能が無いため、既定の取り消す/コピー/ペーストなどの項目は表示されません)の中に、お気に入り・ページレイアウトとまとめて表示されます(上から順に、お気に入りのグループ→区切り線→ブックマークのグループ→区切り線→ページレイアウトのグループ。お気に入り自体については[9. お気に入り](#9-お気に入り)、ページレイアウトについては[10. ページレイアウト](#10-ページレイアウト)を参照)。

EPUBの目次(TOC)、PDFに埋め込まれているしおり(アウトライン)、cbz/cbr/cb7 の `ComicInfo.xml` に書かれたしおり(`Pages` の `Bookmark` 属性)のいずれかを持つ本は、初めて開いたとき(その本にまだブックマークが1件も無い場合のみ)、それらの見出しが自動的にブックマークとして取り込まれます。取り込まれたブックマークは、自分で付けたものと同じ扱いになり、「ブックマーク・レイアウトの編集」ウインドウからリネーム・削除・一括リネームができます(書き出しの対象にもなります)。

- **追加/削除**: ツールバーのブックマークアイコン、aキー、メニューバー「Edit」→「Add This Page to Bookmarks」/「Remove This Page from Bookmarks」、右クリックメニューの同項目から操作できます。今表示しているページ以外を対象にしたい場合は、ページ一覧パネル・サイドパネルの「ページ」モード・「本の中身ブラウザ」でそのページを右クリックしてください([6. ページ一覧](#6-ページ一覧サムネイルグリッド)参照。そちらは常に右クリックした1ページだけが対象で、下記の見開き表示中の対象ページの設定は関係しません)。これらはすべて1つの操作(トグル)にまとまっており、現在のページに付いているかどうかに応じて「追加」「削除」のどちらの動作になるか、およびメニュー・ツールバーの表示文言が自動的に切り替わります。ツールバーのアイコンは、付いているときは塗りつぶし表示+背景色反転で強調され、一目で区別できます。操作すると、画面下部に「「ページ N」をブックマークに追加しました」のように結果を知らせる通知が一時的に表示されます。見開き表示中にツールバー・お気に入りメニュー・キーボードショートカットから追加する場合、クリック位置の情報が無いため、見開きの左右どちらのページを対象にするかは環境設定「本を開く」→「見開き表示中のブックマーク」の「対象ページ」で選べます(既定は読み方向に基づく既定側のページに固定。「毎回確認」にすると、追加のたびにダイアログで左右を選べます)。右クリックのコンテキストメニューからの追加は、常にクリックした側のページが対象になるため、この設定の影響を受けません。
- **一覧・編集**: bキー、メニューバー「Edit」→「Edit Bookmarks…」。「お気に入りの編集」と同じ操作感の独立したウインドウが、左ペインに本の一覧、右ペインに選択中の本のブックマーク・ページレイアウト設定を表示する2ペイン構成で開きます(下記「ブックマーク・レイアウトの編集」参照)。メニューバーからは、本を1つも開いていない状態でも(「Edit Favorites…」と同様に)常に開けます
- **次/前のブックマークへジャンプ**: 既定 c / d キー(下記の並べ替え設定に関わらず、常にページ番号順で判定されます)
- メニューバー「Edit」→「Bookmark List」、および右クリックメニューの「Bookmark List」には、今開いている本のブックマーク一覧がサブメニュー形式で表示され、クリックでジャンプできます
- サイドパネルの「ブックマーク」モードでも、今開いている本のブックマークを一覧表示し、クリックでジャンプできます。追加・編集ウインドウの呼び出しに加え、行を右クリックしての名前の変更・削除もそこから行えます([7. サイドパネル](#7-サイドパネル)参照)

ブックマークは本ごと(ファイル/フォルダのパスごと)に保存されます。ブックマークを追加すると「ページ N」という名前が自動で付きますが、後から自由に名前を変更できます。既にブックマークを付けているページで再度「追加」側の操作が実行された場合(まれに古いキー割り当てなどから発生した場合)も、重複して追加されることはありません(既存のブックマークがそのまま残ります)。

ファイル/フォルダを同一ボリューム内でリネーム・移動した場合、そのファイルをqooViewerで開き直した時点で、ファイルノード(iノード番号)を手がかりにブックマークが自動的に新しいパスへ引き継がれます(ボリュームを跨いだ移動には対応していません)。

### ブックマーク・レイアウトの編集

「メタデータの編集」「EPUB/PDF/CBZの書き出し」「お気に入りの整理」と同じく、このウインドウは操作するボタン類をタイトルバー(ツールバー)に置き、一覧がその下へスクロールして潜る作りです(macOSの標準的なウインドウと同じ形です)。

「ブックマーク・レイアウトの編集」ウインドウは、「お気に入りの編集」ウインドウと同じ操作感の2ペイン構成です。ブックマークだけでなく、[10. ページレイアウト](#10-ページレイアウト)で説明する各本のページレイアウト設定も、このウインドウの右ペインからまとめて編集できます。メニューバー「Edit」→「Edit Layout…」からも、同じウインドウをレイアウト情報側の絞り込みで開けます。

- 左ペインには、ブックマークまたはページレイアウト設定のいずれかを1件以上持つすべての本が一覧表示されます(「今開いている本だけ」という制限はありません)。今その本を開いているウインドウ/タブがある場合は、本のアイコンが塗りつぶし表示になり、名前が太字になり、「Now Reading」というバッジが付きます。ファイル名の横には対応形式(zip/cbz・rar/cbr・7z/cb7・PDF・EPUBなど)を示すバッジが常に表示されます。行の右側には、ブックマーク件数と、ページレイアウト設定を持つ本には専用のアイコンが表示されます。ウインドウを開いた時点で、今読んでいる本にブックマークまたはレイアウト設定があれば、その行が自動的に選択されます。ペイン幅が狭く名前が省略表示されている場合は、行の上にカーソルを合わせるとフルネームがツールチップで表示されます。ファイル名をダブルクリックすると、その本を開きます(開き方は下記「ブックマークをダブルクリックすると〜」の項と同じ規則です)。
- 絞り込みと並べ替えは、タイトルバーの**左ペイン側**(左ペインの真上)に並んでいます。**絞り込み**のドロップダウン(絞り込みのマークが選択中の値の先頭に付きます)から「すべて」「ブックマークがある」「レイアウト情報がある」で本の一覧を絞り込めます。その右隣の上下矢印のアイコンの**並べ替え**ボタンからは、並べ替えの基準(名前・追加日時・更新日時)と「昇順」「降順」を、それぞれ別の項目として選べます(既定は名前の昇順)。左ペインの一覧のすぐ上にある検索欄で、ファイル名を絞り込むこともできます。メニューバー「Edit」→「Edit Bookmarks…」から開いた場合は「ブックマークがある」、メニューバー「Edit」→「Edit Layout…」から開いた場合は「レイアウト情報がある」に初期絞り込みされた状態で開きます。
- 左ペインの各行を右クリックすると、「開く」「新規ノーマルウインドウで開く」「新規シークレットウインドウで開く」「新規タブで開く」でその本を開けるほか([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照)、「Delete All Bookmarks and Layouts…」(ブックマークおよびレイアウトを全削除…)を、その本を選択した状態と同じ扱いで直接実行できます。「開く」はファイル名をダブルクリックした場合と同じ挙動です。新規ウインドウ/タブで開く場合の「今のウインドウ」は、いちばん手前で本を表示しているウインドウを指します(このウインドウ自体は特定の本に属さないためです)。
- 右ペインには、左ペインで選んだ本のページ一覧が、ページ番号・サムネイル・レイアウト・ブックマークの4列で表示されます(列の境界をドラッグすると幅を調整できます)。
  - 一度開いたことのある本は、本体の読み込みを待たずにページ一覧がすぐ表示されます。サムネイルは読み込みが済んだ順に埋まっていきます。読み込みが終わるまでの間、タイトルバーの右ペイン側に進捗が表示され、レイアウト列・並べ替え(ドラッグと上下矢印)・読み方向の変更・ブックマークの追加は一時的に無効になります。既にあるブックマークの削除・リネームは最初から行えます(ブックマークを付ける位置はページ一覧の並びから決まるため、本体の読み込みが終わって並びが確定するまでは追加できないようにしてあります)。その本を初めて開く場合は、従来どおり読み込みの完了を待ってから表示されます。サムネイルにカーソルを合わせると、少し待って拡大プレビュー(ファイル名付き)がポップオーバー表示されます。書庫の中のフォルダ・書庫の中の書庫にある画像では、拡大プレビューにも、サムネイルのツールチップにも本の中での場所が含まれます([画像が本の中のどこにあるかの表示](#画像が本の中のどこにあるかの表示)参照)。行のうち、ボタンやドロップダウンリストではない部分(ページ番号・サムネイル・右側の余白など)をダブルクリックすると、その本のそのページへジャンプします(挙動は下記「ブックマークをダブルクリックすると〜」の項と同じです)。
  - **ページ番号**: 除外されていないページに、実際にビューアで表示される順序の番号が振られます。除外(非表示)にしたページは番号の代わりに「非表示」と表示され、一覧の最後尾へファイル名順で移動します。除外を解除すると、ファイル名から推測される元の位置へ自動的に戻ります。
  - **レイアウト**: そのページの表示状態(単ページ/見開き右/見開き左/除外(非表示)/未設定)を切り替えるドロップダウンです。詳しくは[10. ページレイアウト](#10-ページレイアウト)を参照してください。
  - **ブックマーク**: ブックマークがあれば名前が表示され、ダブルクリックでリネーム、ゴミ箱アイコンで削除できます。無ければ「+」アイコンから追加できます(除外されているページには追加できません)。
  - 行をドラッグ&ドロップすると、ページの表示順序を並べ替えられます(除外中のページがあっても並べ替え可能です)。行を選択し、タイトルバーの右ペイン側にある上下矢印ボタンでも1つずつ順序を入れ替えられます。
- ページの順序を入れ替える上下矢印ボタン・「一括操作…」メニュー・読み方向・絞り込みは、タイトルバーの**右ペイン側**(右ペインの真上)に、この順で並びます。左ペインへの操作(本の絞り込み・並べ替え)とは仕切り線を挟んで左右に分かれているので、どちらのペインへの操作かが位置で分かります。**読み方向**(右開き/左開き。上書きしない場合はその時点で実際に使われている値)は、そのドロップダウンで切り替えます。EPUBやPDFであっても、読み方向・並べ替え・レイアウト列はすべて編集できます(ファイル自身の指定は、初めて開いたときに登録される初期値として扱われます。[10. ページレイアウト](#10-ページレイアウト)参照)。
- 「Bulk Operations…」(一括操作…)メニューから、「Bulk Rename Bookmarks…」(ブックマークを一括リネーム…)、「Delete All Bookmarks…」(ブックマークを全削除…)、「Delete All Layout Info…」(レイアウトを全削除…)、「Delete All Bookmarks and Layouts…」(ブックマークおよびレイアウトを全削除…)、「Reset Page Order」(ページ順を初期化する)を実行できます。「Reset Page Order」は、ドラッグ&ドロップ等で入れ替えたページの並び順を、ファイル名の名前順(環境設定「並び順をFinderに揃える」に従った並び。[2. 本を開く](#2-本を開く)の「ページの並び順」参照)に戻します(カスタムの並び順が無い場合は無効です)。
- タイトルバーの右ペイン側でいちばん右の**絞り込み**のドロップダウン(左ペイン側と同じく、絞り込みのマークが選択中の値の先頭に付きます)から、右ペインの一覧を「すべて」「ブックマークがある」で絞り込めます。ツールチップは左ペイン側が「本を絞り込む」、右ペイン側が「ページを絞り込む」と別々になっています。
- ブックマークをダブルクリックすると、該当ページへジャンプします。その本を今開いていない場合でも、次のようにジャンプ先が自動的に決まります。(1) その本を既に別のウインドウ/タブで開いている場合は、そのウインドウ/タブをアクティブにしてジャンプします。(2) 本を1つも開いていない(ウェルカム画面のみの)場合は、その画面でそのまま開いてジャンプします。(3) ウインドウが1つも無い場合は、新しいウインドウを開いてジャンプします。(4) 他の本を開いている状態で、その本がどこにも開かれていない場合は、今いちばん手前のウインドウ/タブで開いている本を置き換えて開き、ジャンプします。対象のファイル/フォルダが見つからない・アクセスできない場合は、「Could Not Open Book」というアラートが表示されます。
- ブックマーク・レイアウトいずれのデータも1件も登録していない場合は、その旨を案内する画面が表示され、「Add This Page to Bookmarks」ボタンから直接追加できます。今読んでいる本が無い場合はこのボタンは無効になります。

### ブックマークの一括リネーム

タイトルバー右ペイン側の「Bulk Operations…」(一括操作…)メニューの「ブックマークを一括リネーム…」から、選択中の本のブックマークをまとめてリネームするシートを開けます(左ペインの右クリックメニューには一括リネームの項目はなく、「Delete All Bookmarks and Layouts…」のみが直接実行できます)。

- 「最初のページを表紙としてブックマークする」をONにすると、最初のページに(ブックマークが無ければ自動的に追加した上で)「表紙」という名前を割り当てます。
- 「最後のブックマーク」の扱いを、通常のブックマークのまま連番に含めるか、「後書き」「奥付」「おまけ」のいずれかの固定名にするか選べます。
- 表紙・最後のブックマーク(固定名にした場合)を除いた残りのブックマークに、「番号前の文字列」「連番開始番号」「番号後の文字列」を指定して連番を振ります。プレビュー欄(該当するブックマークすべてをスクロールして確認できます)で結果を確認してから、右下の「適用」で適用します。やめる場合は「キャンセル」です。

---

## 9. お気に入り

ブックマーク(本の中の特定ページの目印)とは別に、本そのものをフォルダ階層で整理して登録・管理できる「お気に入り」機能があります。メニューバーでは「Edit」メニュー内のいちばん上のグループに表示されます([8. ブックマーク](#8-ブックマーク)も参照)。

### 登録・削除

- ツールバーの星アイコン、メニューバー「Edit」→「Add This Book to Favorites…」/「Remove This Book from Favorites」、右クリックメニューの同項目、または既定 option+a キーから登録・削除できます。これらはすべて1つの操作(トグル)にまとまっており、現在の本が登録済みかどうかに応じて「追加」「削除」のどちらの動作になるか、およびメニュー・ツールバーの表示文言が自動的に切り替わります。ツールバーのアイコンは、登録済みのときは塗りつぶし表示+背景色反転で強調され、一目で区別できます。
- 未登録の状態から実行すると、登録先のフォルダを選ぶシートが開きます。「Favorites (Top Level)」(フォルダ分けせずお気に入りの一番上の階層に直接置く)か、既存のフォルダから選ぶか、左下の「New Folder…」からその場で新しいフォルダを作成して選べます(「Cancel」「Add」と同じ行に並んでいます)。
- 同じ本を、既に登録済みのフォルダへもう一度登録しようとした場合は、確認なしで登録内容を上書きします。既に別のフォルダへ登録されている場合は、どのフォルダに登録済みかを表示した上で、そこにも追加するか確認します。
- 既に登録済みの状態から実行すると、確認なしで削除されます。複数のフォルダに登録されている場合は、そのすべてから削除されます。
- 登録・削除のいずれの操作でも、画面下部に「「本のタイトル」をお気に入りに追加しました」のように結果を知らせる通知が一時的に表示されます。
- 個別のお気に入りは、サイドパネルの「ブックマーク」モードの行を右クリックしても削除できます([7. サイドパネル](#7-サイドパネル)参照)。こちらは登録済みの本を今開いていなくても操作でき、削除の前に確認ダイアログが表示されます。
- 登録できる件数は全フォルダ合計で999件、フォルダの階層は最大3階層までです。上限に達した状態で登録・新規フォルダ作成を実行しようとすると、件数・階層のどちらの上限に達したかを示すエラーが表示されます。

### 一覧・開く

- メニューバー「Edit」→「Favorites List」、または右クリックメニューの「Favorites List」から、フォルダ階層のままサブメニュー形式で一覧を表示できます(ホバーでサブフォルダが展開します)。ツールバーの一覧表示ボタンは廃止されており、星アイコンは上記の追加/削除トグル専用です。
- 一覧から項目をクリックすると開きます。既に本を表示している状態でお気に入りを開いたときの挙動(現在の本を閉じて開く/新規タブで開く/新規ウインドウで開く)は、環境設定「本を開く」の「お気に入りから」で設定します(Finderから開いたときの設定と同じ考え方です)。まだ本を表示していない状態(ウェルカム画面)から開く場合は、この設定に関わらず常にそのウインドウでそのまま開きます。
- サイドパネルの「ブックマーク」モードでも、同じ内容をフォルダ階層のままツリー表示し、クリックで開けます([7. サイドパネル](#7-サイドパネル)参照)。
- 登録した本のファイル/フォルダを同一ボリューム内でリネーム・移動した場合は、ファイルノード(iノード番号)を手がかりに自動的に追跡され、開こうとした時点で新しい場所・名前へ登録内容が引き継がれます。実際にファイルが削除された場合、またはボリュームを跨いで移動された場合など、追跡できない場合にのみ、その旨を表示した上でお気に入りから削除するか確認するダイアログが表示されます。

### お気に入りの編集

メニューバー「Edit」→「Edit Favorites…」、または既定 option+b キーから、専用のウインドウでお気に入りを整理できます。

- 左側にフォルダツリー、右側に選択中のフォルダの中身(サブフォルダ+お気に入り)を表示する2ペイン構成です。左側のフォルダ名が省略表示されている場合は、カーソルを合わせるとフルネームがツールチップで表示されます。また左ペインの幅は、ウインドウを開いた時点で登録されているフォルダ名の長さに応じて自動的に調整されます(ブックマーク編集画面と同様。一度手動でリサイズすると、次回以降はそのサイズが記憶されます)。
- 右側のフォルダをダブルクリックしてさらに奥のフォルダへ移動すると、左側のフォルダツリーも現在のフォルダまで自動的に展開され、該当する行が強調表示されます(今どの階層にいるかが常に左ペインで分かるようにするためです)。右側の各項目(フォルダ・お気に入り)にカーソルを合わせると、左ペインと同様にフルネームがツールチップで表示されます。また、右側の項目をクリックすると、左ペインと同じ見た目でその項目が選択状態として強調表示されます。
- 新規フォルダの作成: サイドバー下部の「New Folder」ボタン、または空白部分を右クリックして「New Folder」を選びます。
- お気に入りを右クリックすると、「開く」「新規ノーマルウインドウで開く」「新規シークレットウインドウで開く」「新規タブで開く」でその本を開けます([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照)。「開く」はダブルクリックした場合と同じで、環境設定「本を開く」の「お気に入りから」に従います。新規ウインドウ/タブで開く場合の「今のウインドウ」は、いちばん手前で本を表示しているウインドウを指します(このウインドウ自体は特定の本に属さないためです)。
- フォルダの名前変更・削除、お気に入りの削除は、それぞれの項目を右クリックするか、行末のゴミ箱ボタンから行います(フォルダの削除は、中のサブフォルダ・お気に入りもすべて連鎖して削除されるため、確認ダイアログが表示されます)。ゴミ箱ボタンにカーソルを合わせるとツールチップで操作内容が表示されます。
- ドラッグ&ドロップで、フォルダ・お気に入りを別のフォルダへ移動できます。
- 詳細ペイン下部の「Add This Book」ボタンから、今読んでいる本を、選択中のフォルダへ直接登録できます(ブックマーク編集画面の「Add This Page to Bookmarks」と位置・見た目を揃えています)。既に別のフォルダへ登録済みの場合の確認、件数上限に達した場合のエラー表示は、[登録・削除](#登録削除)の項と同じです。本を1つも開いていない場合はこのボタンは無効になります。
- 詳細ペインでお気に入りをダブルクリックすると、その本を開きます(シングルクリックは選択のみで、開きません)。開き方(そのまま/新規タブ/新規ウインドウ)は、環境設定「本を開く」の「お気に入りから」に従います(ビューワーの「お気に入り一覧」から開く場合と同じです)。今読んでいる本のウインドウが1つも無い場合は、常に新しいウインドウで開きます。
- 登録した本の実体ファイルが見つからない項目は、グレー表示で区別されます。
- 並べ替え: タイトルバー右端の上下矢印のアイコンの**並べ替え**ボタンから、並べ替えの基準(名前・追加日時・更新日時)と「昇順」「降順」を、それぞれ別の項目として選べます(既定は名前の昇順。「ブックマーク・レイアウトの編集」ウインドウの並べ替えと同じ形です)。追加日時はフォルダ作成/お気に入り登録の日時、更新日時はお気に入りが別フォルダへ移動した日時、またはフォルダ直下でお気に入り・サブフォルダの追加/移動/削除があった日時です(フォルダ自身がよそへ移動されただけでは更新されません)。
- タイトルバー右端の「フォルダを上に表示」ボタン(既定ON。ONの間は点灯します)をOFFにすると、並べ替え基準に関わらずフォルダとお気に入りを常に分けて表示するのをやめ、両者を区別せず並べ替え基準だけで混在させて1つの並びにします。
- ここで選んだ並べ替え・表示順の設定は、メニューバー「Edit」→「Favorites List」、および右クリックメニューの「Favorites List」サブメニューにもそのまま反映されます。

### ウェルカム画面での表示

何も開いていない状態(ウェルカム画面)では、「最近開いた本」と「最近お気に入りに追加したファイル」がそれぞれ最大10件表示され、クリックで直接開けます(表示前に実際にファイルが存在するか確認済みのものだけが表示されます)。それぞれ環境設定「一般」から個別にON/OFFできます(既定はON)。

「最近開いた本」の行を右クリックすると「履歴から削除」が使え、その1件だけを履歴から消せます(サイドパネルの「履歴」モードの同じ項目と同じ動作です。[7. サイドパネル](#7-サイドパネル)参照)。「最近お気に入りに追加したファイル」の行には右クリックメニューはありません(お気に入りの整理は「お気に入りの編集」ウインドウで行います)。

---

## 10. ページレイアウト

EPUBは、package documentの中で読み方向・見開き/単ページ・ページごとの見開き内配置(左右)を明示できます。「ページレイアウト」機能は、これと同等の指定をqooViewer自身が持つもので、フォルダ・zip/cbz・rar/cbr・7z/cb7・PDF・EPUBのすべてで使えます。

EPUB/PDFがファイル自身にレイアウト情報を持っている場合は、その本を初めて開いたときに読み取って登録し、以降は登録された内容に従います([5. 表示モード](#5-表示モード)参照)。そのため、EPUBのページレイアウトもここから自由に変更できます。

指定できる内容は次の3種類です。

- 本ごとの読み方向の上書き(「ブックマーク・レイアウトの編集」ウインドウの右ペインから設定します。[8. ブックマーク](#8-ブックマーク)参照)
- ページごとの表示状態: 単ページ固定/見開き右固定/見開き左固定/除外(非表示、通常の読書フロー・ページ数・サムネイル一覧から完全にスキップします)
- ページごとの表示順序の並べ替え(「ブックマーク・レイアウトの編集」ウインドウでのドラッグ&ドロップ、または「Reset Page Order」(ページ順を初期化する)で自然順に戻す)

### ページごとの表示状態を変更する

次のいずれかの方法で、ページの表示状態を変更できます。

- メニューバー「Edit」内のレイアウトのグループ。見開き表示中で実際に2ページとも表示されている場合は「Left Page」「Right Page」のサブメニューに分かれ、それぞれ「Set as Single Page」「Set as Spread Right Page」「Set as Spread Left Page」「Set as Excluded (Hidden)」から選べます。既に上書き設定がある場合は「Delete Layout Info」でその設定だけを削除できます。単ページ表示中、または見開きの相方が存在しない場合は、現在のページに対する1つの項目群だけが表示されます。
- 画像表示エリアを右クリックしたコンテキストメニューの「Layout」サブメニュー(内容はメニューバーと同じです)。
- 「ブックマーク・レイアウトの編集」ウインドウの右ペイン「レイアウト」列のドロップダウン。

変更を実行すると、「Apply Layout Change To…」ダイアログでどの範囲に適用するかを選びます。

| 選択肢 | 内容 |
| --- | --- |
| このページだけ更新する | 選んだページ1枚だけに適用します |
| このページを基準に本全体のレイアウトを更新する | 選んだページの設定を基準として、本全体に自動計算で広げます |
| このページより前のページ全体を更新する | 選んだページより前のすべてのページに適用します(先頭ページでは選べません) |
| このページより後のページ全体を更新する | 選んだページより後のすべてのページに適用します(末尾ページでは選べません) |

「レイアウト情報を削除する」(Delete Layout Info)には範囲選択はなく、選んだページの上書き設定だけを直接削除します。

### 自動レイアウト

ツールバーの「現在の表示を基準に自動でレイアウトする」ボタン(杖のアイコン)、メニューバー「Edit」→「Auto-Layout Based on Current View」、またはキーボードショートカット(既定 e キー、環境設定「キーボード」で変更可能)から、今画面に表示されている見開き/単ページの状態を基準に、本全体のページの組み合わせを自動的に再計算できます。本全体を書き換える操作のため、実行前に確認ダイアログが表示されます。除外(非表示)にしたページは計算の対象に含まれません。

### レイアウトの保存データが無い本を開いたとき

環境設定「レイアウト」の「自動レイアウト」で、**レイアウトの保存データを1つも持っていない本を開いたときに、本全体を自動レイアウトする**ように設定できます([25. 環境設定](#25-環境設定)参照)。

| 選択肢 | 内容 |
| --- | --- |
| 何もしない(既定) | これまでどおり。横長判定によるその場の見開き判定だけで表示します |
| 1ページ目を単ページに | 本の1ページ目を単ページとして、そこを起点に本全体を自動レイアウトします |
| 1ページ目を見開きに | 本の1ページ目を見開きの1枚目(2ページ目と組む)として、そこを起点に本全体を自動レイアウトします |

計算の中身は上記の「自動レイアウト」とまったく同じで、起点(基準にするページ)が本の1ページ目になるところだけが違います。以降のページの組み方(横長の画像は常に単独で表示する、端数は末尾へ送る)も同じです。

**すでにレイアウトの保存データがある本には何もしません。** 手で付けた設定を上書きしないためであり、EPUB/PDFのようにファイル自身がレイアウトを持つ本を巻き込まないためでもあります(それらは初めて開いた時点でファイル側の指定が登録されるので、この判定の時点では「レイアウトの保存データを持っている本」になっています)。ファイルの中身が差し替わった疑いがあり、その確認ダイアログを出している間も何もしません。

シークレットウインドウと、画像を直接開いたその場限りの本では動きません(どちらもデータを保存しないためです)。

### EPUB/PDF由来の本を開いている場合

以前のバージョンでは、EPUB、またはファイル自身がレイアウト指定を持つPDFを開いている間、レイアウト関連の項目はすべて無効化(グレーアウト)されていました。現在は、ファイル側の指定は初めて開いたときに登録されるだけの「初期値」として扱われ、その後はどの形式の本でも自由に編集できます。

### ファイルの移動・リネームについて

ブックマークと同様、ページレイアウトの設定もファイルノード(iノード番号)で本を識別しています。対象のファイル/フォルダを同一ボリューム内でリネーム・移動しても、次に開いたときに設定はそのまま引き継がれます(ボリュームを跨いだ移動には対応していません)。

---

## 11. メタデータ

本ごとに「著者」「タイトル」「シリーズ」「巻数」を登録できます。登録した内容は、ツールバーの表示や、EPUB/PDFとして書き出すときの情報に使われます。

メニューバー「Edit」→「メタデータの編集…」(「レイアウトの編集…」の下)から、専用のウインドウを開きます。本を1つも開いていない状態でも、常に開けます。

ウインドウのタイトルバーには「フォーマットの編集」(推測の規則を編集するメニュー)と検索欄が並び、その下が本の一覧、いちばん下に「95冊 / 157冊にメタデータが登録されています」という件数の表示があります。閉じるときはタイトルバーの閉じるボタン(または Cmd+W)を使います。

### 一覧の見かた

一覧には、qooViewerが何らかの情報を保存している本(一度でも開いた本、お気に入り・ブックマーク・ページレイアウト設定・メタデータのいずれかを登録した本)がすべて並びます。左から「ファイル名」「著者名」「タイトル」「シリーズ」「巻数」と、右端に登録/削除のボタンが並びます。ファイル名以外の4項目はその場で編集できます。

- **未登録の本**: 著者・タイトル・シリーズ・巻数には、ファイル名から機械的に推測した値があらかじめ入っています(推測の規則は下記「推測の規則を編集する」を参照)。
  - ウインドウを開いた直後は、この推測が終わるまで一覧の代わりに進捗表示が出ます(推測前の空欄がいったん表示されて後から埋まる、という見え方にならないようにするためです)。本の件数が多いほど時間がかかりますが、その間もツールバーの「フォーマットの編集」や検索欄は操作できます。推測の規則を変更したときも同じように作り直されます。
- **登録**: 表示されている内容をそのまま、または直してから「登録」ボタンを押すと、その内容が保存されます。
- **登録済みの本**: 行の文字色が変わり、ボタンが「削除」に変わります。「削除」を押すと登録が解除され、表示はファイル名からの推測値に戻ります。
- 一度登録した内容は、あとから推測の規則(ファイル名フォーマット・巻数フォーマット・除外文字列)を変更しても影響を受けません。推測はあくまで未登録の本の初期表示に使われるものです。
- **ファイル名のコピー**: ファイル名はドラッグで範囲を選んでコピーできます。シリーズ名などをファイル名から切り出して、他の欄へ貼り付けるのに使えます。右クリックの「ファイル名をコピー」ではファイル名全体をコピーできます。
- **検索**: ツールバー右端の検索欄で、ファイル名・著者・タイトル・シリーズを対象に絞り込めます。ウインドウの幅が狭いときは虫眼鏡のアイコンに畳まれ、押すと欄が開きます。
- **列の幅**: ウインドウを開いた時点の内容(ファイル名・著者名・タイトル・シリーズ・巻数の長さ)に応じて、各列の幅が自動的に調整されます。タイトル行の区切り線をドラッグすれば、そこから個別に変えられます(Finderのリスト表示と同じ操作です)。
- **巻数の欄**: `3`や`10.5`のような数字のほか、「上」「下」のような文字も登録できます。ただしEPUB/PDFとして書き出すときに埋め込まれるのは数値として読める場合だけです(EPUB・PDFのどちらも、巻数の項目が数値しか受け付けないためです)。CBZ(`ComicInfo.xml`)への書き出しにはこの制限はありません。

### 推測の規則を編集する

ツールバーの「フォーマットの編集」から、ファイル名から著者・タイトル・シリーズ・巻数を推測する規則を編集する3つのダイアログ(「ファイル名フォーマットの編集…」「巻数フォーマットの編集…」「除外文字列の編集…」)を開けます。いずれのダイアログにも左下に「リセット」があり、既定の内容に戻せます。各リストの説明は、見出しの右にある ⓘ にカーソルを合わせると読めます。

推測は「除外文字列でノイズを削る」→「ファイル名フォーマットで著者とタイトルに分ける」→「巻数フォーマットでタイトルをシリーズ名と巻数に分ける」の順に行われます。

- **ファイル名フォーマットの編集**: 拡張子を除いたファイル名から、著者名とタイトルを取り出す書式です。`@author`(著者名)、`@title`(タイトル)、`@ignore`(メタデータとして扱わない任意の文字列)の3つを、丸括弧・角括弧・空白などと組み合わせて書きます(例: `(@ignore) [@author] @title (@ignore)`)。上から順に照合し、最初に一致した書式が使われます。ドラッグで並べ替えると優先順位を変えられます。書式の中の空白は「0文字以上の空白」として扱われるため、除外文字列を削った跡に余分な空白が残っていても一致します。
- **巻数フォーマットの編集**: タイトルの末尾から巻数を取り出す書式です。正規表現で書き、2つのリストに分かれています。
  - **巻数抽出用の正規表現**: 最初のかっこ(キャプチャグループ)の中身が巻数になります。全角数字は半角に変換されます。一致した部分より前がシリーズ名になります。
  - **シリーズ名抽出用の正規表現**: 「上巻」「総集編」のように、シリーズ名の分離だけを行い、巻数の欄は空欄のままにするものです。上のリストの後に照合されます。
  - どちらのリストにも末尾で一致しなかった場合、シリーズ・巻数はどちらも空欄になります(1冊完結の本にはシリーズという概念が無いため、タイトルがそのままシリーズ名になることはありません)。
- **除外文字列の編集**: ファイル名フォーマットと照合する前に、ファイル名から削るノイズを正規表現で登録します。既定では、括弧書きの年号(1900〜2099)と、完結を示す括弧書き(`(完結)`・`(完全版)`・`(結)`・`(完)`・`(終)`)が登録されています。

### ツールバーの表示

メタデータを登録した本は、ツールバー中央の表示がファイル名から次のように変わります。

| 登録内容 | ツールバーの表示 |
| --- | --- |
| タイトルと著者 | `[著者] タイトル` |
| タイトルのみ | タイトル |
| 未登録、またはタイトルが空欄 | ファイル名(従来どおり) |

### EPUB/PDF/ComicInfo.xml からの自動的な取り込み

EPUB・PDF、または `ComicInfo.xml` を含むcbz/cbr/cb7・フォルダを初めて開いたとき、ファイル自身に埋め込まれているメタデータを読み取り、自動的に登録します。意味のある値が1つも書かれていない場合は登録されません(未登録のまま、ファイル名からの推測値が表示されます)。

- **EPUB**: タイトル(`dc:title`)・著者(`dc:creator`)に加え、シリーズ名と巻数を次の2種類から読み取ります。両方に書かれている場合はCalibre側を優先します。
  - EPUB3標準の`belongs-to-collection`/`collection-type`/`group-position`
  - Calibre独自の拡張メタデータ(`calibre:series`/`calibre:series_index`)
- **PDF**: PDFに埋め込まれたXMPメタデータからタイトル(`dc:title`)・著者(`dc:creator`)・シリーズ名(`calibre:series`)・巻数(`calibreSI:series_index`)を読み取ります。PDFにはシリーズを表す標準的な項目が無いため、この分野で事実上の標準になっているCalibreの表現に合わせたもので、Calibreで編集したPDFや、Calibre形式に対応した書庫サーバ(Kavitaなど)向けに用意されたPDFがそのまま読めます。qooViewerがPDFを書き出すときも同じ形式で書き込みます。
  - XMPが無い場合や、XMPに書かれていない項目は、PDFの文書情報(Title・Author)から補います。
  - シリーズ名がどちらにも無い場合に限り、キーワード(Keywords)の`series:シリーズ名, series_index:巻数番号`という形式も読み取ります。これはバージョン1.22以前のqooViewerが書き出していた独自形式で、当時書き出したPDFを今も読めるようにするためのものです(現在の書き出しでは使いません)。

- **CBZ(`ComicInfo.xml`)**: `Title`・`Series` に加え、クレジット欄(`Writer` → `Penciller` → `CoverArtist` → `Editor` の順で最初に見つかったもの)を著者として読み取ります。巻数は `Number`、`Number` が空の場合は `Volume` から読み取ります。アーカイブのルート直下の `ComicInfo.xml` を探し、そこに無い場合のみサブフォルダ内も探します。書き出す側の詳細は[15. CBZの書き出し](#15-cbzの書き出し)を参照してください。

取り込みは本ごとに1回だけ行われます。取り込んだあとにこのウインドウで内容を変更しても、次にその本を開いたときにファイル側の値へ戻されることはありません。

### ファイルの移動・リネームについて

ブックマーク・ページレイアウト設定と同様、メタデータもファイルノード(iノード番号)で本を識別しています。対象のファイル/フォルダを同一ボリューム内でリネーム・移動しても、次に開いたときに登録内容はそのまま引き継がれます(ボリュームを跨いだ移動には対応していません)。

---

## 12. ライブラリデータの書き出し・読み込み(JSON)

メニューバー「File」→「Export…」/「Import…」から、お気に入り・ブックマーク・ページレイアウト設定・メタデータを1つのJSONファイルとしてまとめて書き出し/読み込みできます。本を1つも開いていない状態でも、常に開けます。書き出したJSONファイルはqooViewer自身の読み込みにのみ対応しています(他アプリと共有できる形式で書き出したい場合は、[15. CBZの書き出し](#15-cbzの書き出し)の `ComicInfo.xml` を使ってください)。

### 書き出し

「Export…」で開くウインドウで、書き出す種類(お気に入り/ブックマーク/ページレイアウト設定/メタデータ/メタデータのフォーマット)をチェックボックスで選び、「Export Library Data」ボタンから保存先を選んで書き出します。完了すると結果(書き出した件数、対象ファイルが見つからずスキップした本の数など)が表示されます。

「メタデータのフォーマット」は、[11. メタデータ](#11-メタデータ)で編集するファイル名フォーマット・巻数フォーマット・除外文字列のことです。本ごとのデータではなくアプリ全体の設定のため、既定ではチェックが外れています。別のマシンへ移るときなど、自分で育てた書式も一緒に持っていきたい場合にチェックしてください。

### 読み込み

「Import…」で開くウインドウで、まず読み込むJSONファイルを選びます。ファイルに含まれているカテゴリ(お気に入り/ブックマーク/ページレイアウト設定/メタデータ)ごとに、次の3つから取り込み方針を選べます(含まれていないカテゴリのピッカーは操作できません)。

| 選択肢 | 内容 |
| --- | --- |
| Overwrite(上書き) | ファイルに含まれる本については、既存のデータを置き換えます |
| Merge(マージ、既定) | 既存に無いものだけを追加し、既存のデータは変更しません |
| Ignore(無視) | そのカテゴリを取り込みません |

「メタデータのフォーマット」だけは、アプリ全体の設定を丸ごと置き換えるものであるため「Overwrite(上書き)」と「Ignore(無視)」の2択で、既定は「Ignore」です(自分の設定が意図せず置き換わらないようにするためです)。

なお、メタデータの取り込みだけは対象の本を読み込む必要がないため、元のファイルが今その場に無くても取り込めます(外付けボリュームを繋いでいない状態でも取り込めます)。

方針を選んだら「Import Library Data」ボタンで実行します。完了すると、カテゴリごとの取り込み件数、対象ファイルが見つからずスキップした本の数などが表示されます。

### ファイルの識別方法について

書き出されるJSONには、各本のファイルパスに加えてファイルノード(iノード番号)も記録されます。読み込み時は、まずファイルノードが一致する本を優先的に探し、見つからない場合にのみJSON内のファイルパスを頼りに探します。そのため、書き出し後に対象のファイル/フォルダを同一ボリューム内でリネーム・移動していても、正しく本を特定できます(ボリュームを跨いだ移動には対応していません)。JSON内のファイルパスはあくまで参考情報としても残りますが、読み込み時の照合には使われません。

現在の形式は formatVersion 3 です。メタデータとメタデータのフォーマットを追加したことによる更新で、formatVersion 2 で書き出したJSONファイルもそのまま読み込めます(その場合、メタデータの項目が含まれていないだけです)。

さらに古い、iノード情報を持たない formatVersion 1 のJSONファイルとの互換性はありません。読み込めませんので、必要な場合は新しいバージョンで登録し直すか、書き出しをやり直してください。

---

## 13. EPUBの書き出し

> **いま開いている本を1冊だけ書き出したい場合**は、このウインドウを開かずに、画像を右クリックした「本の書き出し」から直接書き出せます。レイアウト・ブックマーク・メタデータを持っていない本でも書き出せます([表示している本をその場で書き出す](#表示している本をその場で書き出す)参照)。

メニューバー「File」→「EPUBの書き出し…」から、ブックマーク・ページレイアウト設定・メタデータのいずれかを持つ本を選んで、固定レイアウトの画像ベースEPUBとして書き出せます。元のファイル形式は zip/cbz・rar/cbr・7z/cb7・PDF・EPUB・フォルダのすべてが対象です。本を1つも開いていない状態でも、常に開けます。

ウインドウを開くと、元のファイルが今も存在する本だけに絞り込んだ一覧が表示されます。この確認が終わるまでは読み込み中の表示になります(外付けドライブやネットワークドライブ上の本が多いと少し時間がかかります)。確認中もウインドウの操作は妨げられません。「PDFの書き出し」「CBZの書き出し」も同じです。

元がPDFの本を書き出す場合は、埋め込まれている画像を取り出します(JPEG形式はそのままjpgに、Flate可逆圧縮形式はpngに変換します)。それ以外の形式の画像を含むPDFは書き出せません。ファイル名はページ順の6桁連番になります。

- 一覧はmacOS標準の表で、「File Name」「タイトル」「Author」「Cover」の4列と、ページレイアウト設定・ブックマーク・メタデータを持っているかを示すアイコンで構成されます。タイトルバー右側の「すべて選択」ボタンで、全選択/全選択解除ができます(すべて選択されている間はボタンが点灯します)。
  - **File Name**: 元のファイル名/フォルダ名(拡張子付き)と、対応形式のバッジをそのまま表示します(編集不可)。
  - **タイトル・Author**: 書き出すEPUBのタイトル(dc:title)・著者名(dc:creator)です。ウインドウを開いた時点で、ファイル名/フォルダ名から次のようなパターンを判定して自動的に入力されます(いずれにも当てはまらない場合はファイル名がそのままタイトルに入り、著者名は空欄になります)。この欄はその場で自由に編集できます。
    - `[著者名] タイトル.拡張子`、`(任意の文字列) [著者名] タイトル (任意の文字列).拡張子` のような、著者名を角括弧で囲む形式(前後の任意の文字列・末尾の追加の角括弧はタイトルの判定から除外されます)
    - `タイトル - 著者名.拡張子` のような、ハイフン区切りの形式
  - **Cover**: 既定では、本の実質的な先頭ページ(除外・並べ替えを反映した後の1ページ目)がカバーとして使われます。カバーのページが書庫の中のフォルダ・書庫の中の書庫にある場合は、ファイル名だけでなく本の中での場所を含めて表示します(選び直すポップオーバーの一覧でも同じです。[画像が本の中のどこにあるかの表示](#画像が本の中のどこにあるかの表示)参照)。一度開いたことのある本は、この既定のカバー名がすぐ表示されます(それ以外の本では、名前が分かるまで「読み込み中…」と表示されます)。この列をクリックすると、カバーを変更するポップオーバーが開きます。「Reset to Default (First Page)」で既定に戻す、本に含まれるページの一覧からサムネイルを見ながら選び直す、「Choose File…」で本に含まれない画像ファイルを専用のカバーとして追加する、のいずれかを選べます。専用ファイルとして追加した画像は、その本のページ一覧やビューアには一切表示されません(カバー画像としてのみ使われます)。
  - タイトル行の各列の区切り線をドラッグすると、列の幅を個別に調整できます(Finderのリスト表示と同じ操作です)。ウインドウを開いた時点の内容(ファイル名・タイトル・著者名の長さ)に応じて、初期の列幅・ウインドウ幅もあらかじめ自動調整されます。
- メタデータにシリーズ名を登録している場合は、EPUB3標準(`belongs-to-collection`/`collection-type`/`group-position`)とCalibre独自形式(`calibre:series`/`calibre:series_index`)の両方に書き込まれます。読み取る側のソフトによって対応している形式が違うためです。巻数は、`3`や`10.5`のように数値として読める場合にのみ書き込まれます(「上」「下」のような文字を登録している場合は、シリーズ名だけが書き込まれ、巻数は省略されます)。
- 書き出しオプションは、タイトルバー右端の「書き出しオプション…」ボタン(歯車のアイコン)を押すと開くポップオーバーの中にあります。EPUBでは「Renumber Image Files Sequentially」(書き出すファイル名を連番にリネームするか)、「Include Excluded Pages」(ページレイアウトで除外(非表示)にしたページも書き出しに含めるか)の2つです。ボタンの名前と位置は、PDF・CBZの書き出しウインドウでも同じです。
  - **ここに出る値は、環境設定「レイアウト」の形式ごとの既定値から始まります**([25. 環境設定](#25-環境設定)参照)。毎回同じ設定にしたい場合は、そちらで決めておけばこのポップオーバーを開かずに済みます。この画面で変えた内容はその1回限りで、既定値そのものは変わりません。
- 右下の「EPUBの書き出しを開始…」ボタンを押すと、保存先フォルダを選ぶパネルが開き、選ぶと書き出しが始まります。環境設定「レイアウト」でEPUBの保存先を決めてある場合はパネルが開かず、そのフォルダへそのまま書き出します(このときボタンの名前は「…」の付かない「EPUBの書き出しを開始」です。[25. 環境設定](#25-環境設定)参照)。保存先の空き容量が、選択した本の合計サイズの1.2倍未満の場合はエラーが表示され、書き出しは行われません。同名のEPUBファイルが既に存在する場合は、スキップ/このファイルを上書き/以降すべてスキップ/以降すべて上書き、を選べる確認ダイアログが表示されます。書き出し中は進捗シートが表示され、いつでもキャンセルできます。完了すると、成功した冊数と、失敗した本があればその理由の一覧が表示されます。
- 読み込めないページが1つでもある本は、そのページを飛ばさずに書き出し自体を失敗させ、理由を表示します(ページの欠けた壊れたEPUBが出来上がるのを防ぐためです)。

### Apple Booksとの互換性

書き出したEPUBをApple Booksで開く際の互換性を高めるため、次の対応をしています。

- **読み方向**: 本ごとに読み方向を上書き設定していない場合でも、環境設定の既定の読み方向(日本語環境では右開きが既定)を明示的にEPUBへ書き出すようにしています。これにより、Apple Booksが読み方向を正しく認識できずに左開きとして扱ってしまうことを防ぎます。
- **カバー画像**: EPUB3の標準的な指定(`properties="cover-image"`)に加えて、EPUB2互換の`<guide>`要素によるカバー参照も併記しています。カバーとして本に含まれない専用ファイルを選んだ場合は、その画像を表示する専用ページも自動生成されます。

---

## 14. PDFの書き出し

メニューバー「File」→「EPUBの書き出し…」の下にある「PDFの書き出し…」から、ブックマーク・ページレイアウト設定・メタデータのいずれかを持つ本を選んで、PDFとして書き出せます(対象・開き方は[13. EPUBの書き出し](#13-epubの書き出し)と同じで、元のファイル形式は zip/cbz・rar/cbr・7z/cb7・PDF・EPUB・フォルダのすべてが対象です)。本を1つも開いていない状態でも、常に開けます。

元がPDFの本を書き出す場合は、埋め込まれている画像を取り出すのではなく、元のページをそのまま複製します。そのため埋め込まれている画像の形式を問わず、画質が劣化することもありません(EPUB書き出しのようなPDFの画像形式の制限はありません)。

- 一覧の列は「File Name」「Title」「Author」の3列です(EPUB書き出しと異なりCover列はありません)。File Nameは編集不可、Title・Authorの自動判定・編集方法はEPUB書き出しと同じで、メタデータを登録している本では登録内容が使われます。右端には、ページレイアウト設定・ブックマーク・メタデータを持っているかを示すアイコンが並びます。
- 「書き出しオプション…」ボタンから開くオプションは「Include Excluded Pages」(ページレイアウトで除外(非表示)にしたページも書き出しに含めるか)のみです(EPUB書き出しにある「Renumber Image Files Sequentially」に相当する項目はありません)。
- ブックマークはPDFのしおり(アウトライン)として、タイトル・著者名はPDFの文書情報として埋め込まれます。
- メタデータにシリーズ名を登録している場合は、Calibre互換のXMPメタデータとして、シリーズ名(`calibre:series`)・巻数(`calibreSI:series_index`)に加えてタイトル(`dc:title`)・著者(`dc:creator`)も埋め込まれます。qooViewerがPDFを読み込むときも同じ形式を解釈しますし、CalibreやKavitaなど、この形式に対応したソフトからもそのまま読めます([11. メタデータ](#11-メタデータ)参照)。巻数は、EPUB書き出しと同じく数値として読める場合にのみ書き込まれます。
- 読み方向(右開き/左開き)と、本ごとに見開き/単ページを指定している場合はその指定も、PDF標準の項目として埋め込まれます。qooViewerで書き出したPDFを開き直すと、これらの設定が復元されます。プレビュー.appやAcrobatなど、この項目を尊重するソフトでも同じ並びで表示されます。
  - ページごとのレイアウト(このページだけ単独で表示する、見開きの左右どちらに置く、といった個別指定)は、PDFにこれを表す仕組みが無いため書き出されません。本全体に対する読み方向・見開きの指定のみが埋め込まれます。
- 右下の「PDFの書き出しを開始…」ボタンの動作(保存先の尋ね方と、環境設定「レイアウト」で保存先を決めてある場合の扱い)、保存先の空き容量チェック、同名ファイルが既にある場合の確認(スキップ/上書き/以降すべてスキップ/以降すべて上書き)、進捗シートとキャンセル、完了後の結果表示は、いずれもEPUB書き出しと同じ挙動です。

---

## 15. CBZの書き出し

メニューバー「File」→「PDFの書き出し…」の下にある「CBZの書き出し…」から、ブックマーク・ページレイアウト設定・メタデータのいずれかを持つ本を選んで、CBZ(ページ画像をそのままzipにまとめた、コミックで広く使われている形式)として書き出せます(対象・開き方は[13. EPUBの書き出し](#13-epubの書き出し)と同じで、元のファイル形式は zip/cbz・rar/cbr・7z/cb7・PDF・EPUB・フォルダのすべてが対象です)。本を1つも開いていない状態でも、常に開けます。

メタデータ・ブックマーク・ページレイアウトは、アーカイブの直下に `ComicInfo.xml` というファイルとして同梱されます。ComicRack 由来の事実上の標準形式で、Komga・Kavita・YACReader など多くのソフトが読み取れます。

- 一覧の列(「File Name」「タイトル」「Author」「Cover」)の見かた、タイトル・著者名の自動判定と編集、カバー画像の選び方、列幅の調整、「すべて選択」ボタンは、いずれもEPUB書き出しとまったく同じです([13. EPUBの書き出し](#13-epubの書き出し)参照)。
- ページ画像は再エンコードせず、元のバイト列をそのまま格納します(画質は劣化しません)。EPUB書き出しのようにWebP・HEIC・BMP・TIFF・AVIFをPNGへ変換することもありません(CBZには対応形式の制限が無く、変換するとかえってファイルサイズが膨らむためです)。JPEG・PNGのように既に圧縮されている画像は、zip側で重ねて圧縮せずそのまま格納します。
- 元がPDFの本を書き出す場合は、EPUB書き出しと同じく埋め込まれている画像を取り出します(JPEG形式はそのままjpgに、Flate可逆圧縮形式はpngに変換します)。それ以外の形式の画像を含むPDFは書き出せません。ファイル名はページ順の6桁連番になります。
- 「書き出しオプション…」ボタンから開くオプションは3つあります。
  - **Renumber Image Files Sequentially**(ファイル名を連番にリネームする): **CBZでは既定ONです**。CBZにはページの読み順を記録する仕組みが無く、どのソフトもアーカイブ内のファイル名の並び順だけでページ順を決めます。そのため、ページの並べ替えや除外を行った本は、連番に振り直さないと書き出した先で元のファイル名順に戻ってしまいます。元のファイル名を残したい場合にのみOFFにしてください。
  - **Include Excluded Pages**: ページレイアウトで除外(非表示)にしたページも書き出しに含めるかどうかです(既定OFF)。
  - **巻数を ComicInfo の Volume 要素にも書き出す**: 既定OFFです(理由は下記「Komga・Kavitaとの互換性」を参照)。
- 右下の「CBZの書き出しを開始…」ボタンの動作(保存先の尋ね方と、環境設定「レイアウト」で保存先を決めてある場合の扱い)、保存先の空き容量チェック、同名ファイルが既にある場合の確認(スキップ/上書き/以降すべてスキップ/以降すべて上書き)、進捗シートとキャンセル、完了後の結果表示は、いずれもEPUB書き出しと同じ挙動です。
- 読み込めないページが1つでもある本は、そのページを飛ばさずに書き出し自体を失敗させ、理由を表示します(ページが欠けると、ページ番号を基準にしているブックマークやカバーの指定がすべてずれるためです)。

### ComicInfo.xml に書き込まれる内容

| ComicInfo の項目 | 書き込まれる内容 |
| --- | --- |
| `Title` | 一覧の「タイトル」列の値 |
| `Series` | メタデータのシリーズ名。未登録の場合はタイトルと同じ値を入れます(シリーズ名が空だと、単巻の本がソフト側でうまくまとまらないことがあるためです) |
| `Number` | メタデータの巻数。「上」「下」のように数値でない値もそのまま書き込めます(EPUB/PDF書き出しと異なり、数値である必要はありません) |
| `Volume` | 「巻数を ComicInfo の Volume 要素にも書き出す」がONで、かつ巻数が整数として読める場合のみ |
| `Writer`・`Penciller` | 一覧の「Author」列の値(同じ値を両方に書き込みます) |
| `PageCount` | 実際に書き出したページ数 |
| `LanguageISO` | 表示言語の設定に応じた言語コード(`ja`・`en` など) |
| `Manga` | 読み方向が右開きなら `YesAndRightToLeft`、左開きなら `Yes`。CBZに読み方向を残せる唯一の項目です |
| `Pages` の各 `Page` | ページごとの情報(下記) |

`Pages` の各 `Page` には、次の情報が書き込まれます。

- `Image`: そのページの番号(0始まり)。アーカイブ内の画像をファイル名順に並べたときの位置です。
- `Type="FrontCover"`: カバーに指定したページに付きます。カバーとして本に含まれない画像ファイルを指定した場合、その画像は先頭のページとして追加されます(EPUB書き出しと異なり、CBZでは「ページではないカバー画像」を持てないためです)。
- `DoublePage="true"`: ページレイアウトで「単ページ」に指定したページに付きます。ComicInfo の `DoublePage` は「1枚の画像に見開き2ページ分が入っている」という意味で、qooViewer の「単ページ」(見開き表示中でも単独で表示するページ)が実質的にこれに当たるためです。
- `Bookmark`: そのページに付けているブックマークの名前。ComicRack・YACReader などでは、これが目次として表示されます。
- `ImageWidth`・`ImageHeight`・`ImageSize`: 画像の縦横のピクセル数とバイト数。

元のファイルが既に `ComicInfo.xml` を持っている場合(他のソフトでタグ付けしたcbzなど)、その内容を土台にして上書きします。**qooViewer が扱っていない項目(出版社・あらすじ・発行年・ジャンル・年齢制限など)は、そのまま引き継がれます。** `LanguageISO` と `Manga` については、元のファイルに明示的な値が書かれていればそちらを優先します(表示言語の設定から決めた値より確かなためです)。ただし読み方向を右開きにしている本には、必ず `YesAndRightToLeft` を書き込みます。

### Komga・Kavitaとの互換性

巻数の扱いだけは、サーバーソフトによって解釈が逆になります。

- **Komga**: `Number` を「シリーズ内での巻/号の位置」として読みます。一方 `Volume` はシリーズ名の一部として扱い、シリーズ名を「タイトル (3)」のように書き換えるため、巻ごとに別のシリーズへ分裂してしまうことがあります。
- **Kavita**: `Volume` を「巻」として読みます。`Number` だけの場合も順序は正しく並びますが、表示は「Chapter 3」のようになります。

そのため既定では `Number` だけを書き込み(Komgaで正しく、Kavitaでも順序は正しくなります)、Kavitaで「巻」として扱わせたい場合にだけ「巻数を ComicInfo の Volume 要素にも書き出す」をONにする、という形にしています。

### 書き出したCBZを開き直すと

qooViewer で `ComicInfo.xml` を含むcbz/cbr/cb7(および `ComicInfo.xml` を直下に置いたフォルダ)を初めて開くと、その内容がDBの初期値として取り込まれます。EPUB/PDFからの取り込みと同じ考え方で、取り込みは本ごとに1回だけです。

- タイトル・著者・シリーズ名・巻数 → メタデータ([11. メタデータ](#11-メタデータ)参照)
- `Manga` が `YesAndRightToLeft` なら右開き、`No` なら左開き → ページレイアウトの読み方向([10. ページレイアウト](#10-ページレイアウト)参照)。`Yes` は「漫画である」ことしか表しておらず方向は未指定のため、読み方向は変更しません。
- `Page` の `Bookmark` → ブックマーク([8. ブックマーク](#8-ブックマーク)参照。その本にまだブックマークが1件も無い場合のみ)

---

### 表示している本をその場で書き出す

**いま開いている本1冊だけを書き出したい場合**は、上の3つのウインドウを開かずに、画像表示エリアを右クリックした「本の書き出し」から直接書き出せます(EPUB・PDF・CBZ共通の説明です)。

- サブメニューに「EPUBとして書き出す」「PDFとして書き出す」「CBZとして書き出す」が並びます。
- **レイアウト・ブックマーク・メタデータを1つも持っていない本でも書き出せます。** 書き出しウインドウの一覧に載る条件(いずれかを持っていること)は、この導線には関係ありません。
- 書き出される内容(ページ順・除外・ブックマーク・メタデータの埋め込み)は、書き出しウインドウから書き出した場合と同じです。
- シートの「保存先」には、直前のパネルで選んだフォルダがアイコンとパスで表示されます。選び直したい場合は「変更…」でもう一度パネルを開けます。
- **シートでは、書き出しウインドウと同じくタイトル・著者名・カバー画像を設定できます**(「毎回確認」のときだけ。保存先を決めてある場合はシート自体が出ません)。
  - **タイトル・著者名**の初期値は書き出しウインドウと同じで、メタデータに登録があればそれを、無ければファイル名/フォルダ名から推測した値が入ります([13. EPUBの書き出し](#13-epubの書き出し)参照)。その場で自由に書き換えられます。
  - **カバー画像**は EPUB・CBZ のときだけ出ます(PDFはカバーの概念を持たないため)。既定は本の実質的な先頭ページで、クリックすると書き出しウインドウのカバー列とまったく同じポップオーバーが開き、本の中のページから選び直したり、本に含まれない画像ファイルを専用のカバーとして追加したりできます。**シークレットウインドウではこの項目はグレーアウトします**(カバーの指定は保存データとして残るため)。
- **読み方向と見開き/単ページだけは、いま画面に見えているとおりに書き出されます。** ツールバーやメニューで切り替えたまま書き出せば、その状態がEPUB/PDFに入ります。本ごとの設定として保存している場合はそちらが優先されます(優先順位は 本ごとの設定 → 画面の表示 → 環境設定の既定値 です)。
  - 書き出しウインドウのほうは本を開いていなくても使えるため、画面の表示という手がかりがありません。そちらでは 本ごとの設定 → 環境設定の既定値 になります。
- 画像を直接開いた本(Finderで画像ファイルを選んで開いた「その場限りの本」。[2. 本を開く](#2-本を開く)参照)では、この項目はグレーアウトします。あの本は渡された画像そのものが中身で、ディスク上の1冊として書き出せる実体を持たないためです。

選んだあとの流れは、環境設定「レイアウト」の**その形式の「保存先」**によって変わります([25. 環境設定](#25-環境設定)参照)。

| 保存先の設定 | 選んだあとの流れ |
| --- | --- |
| 毎回確認(既定) | **まず保存先フォルダを選ぶパネルが開きます**(前回その形式で選んだフォルダが初期位置です)。選ぶと小さなシートが続けて開くので、内容を確かめて「書き出す」を押します。ここでキャンセルするとシートは開かず、何も起きません |
| 保存先を設定 | パネルもシートも出ず、決めておいたフォルダへそのまま書き出します。進捗シートが出るだけです |

保存先は形式ごとの設定で、**右クリックからの書き出しと File メニューの書き出しウインドウの両方に効きます**。「保存先を設定」にしてあれば、書き出しウインドウの「書き出しを開始」ボタンもフォルダを尋ねません。

- **書き出しが終わっても、完了の確認は出ません。** シートは自分で閉じ、そのまま下記の「書き出したあと」の動作へ進みます(繰り返しの流れを途切れさせないためです)。失敗した場合だけ、理由がシートに表示されます。
- **同名のファイルが既にある場合だけは確認します**(スキップ/上書き)。スキップを選んだ場合は書き出していないので、保存データ・履歴の削除も「書き出したあと」の動作も行われません。
- 書き出しの途中経過は進捗シートに表示され、書き出し中は「本の書き出し」メニューが選べなくなります。

#### 書き出したあとの後始末

環境設定「レイアウト」で、形式ごとに次のことを決めておけます([25. 環境設定](#25-環境設定)参照)。**いずれも既定は「何もしない」で、削除は取り消せません。**

| 設定 | 「削除」にしたときに消えるもの |
| --- | --- |
| 保存データ | 書き出した本のページレイアウト・ブックマーク・メタデータ・読書位置。**お気に入りは残ります**(手で登録したものなので) |
| 履歴 | 「最近開いた本」の履歴からその本の項目。本そのものには触れません |

さらに、書き出しが終わったあとの動作を選べます(形式によらず共通の設定です)。

| 選択肢 | 内容 |
| --- | --- |
| 何もしない(既定) | そのまま同じ本を読み続けます |
| 次の本の1ページ目へ | 同じフォルダの次の本を開き、その1ページ目から始めます |
| 次の本へ | 同じフォルダの次の本を開きます(読書位置や環境設定「開始ページ」に従います) |
| 本を閉じる | このタブを閉じます(タブが1枚だけならウインドウごと閉じます) |
| ウェルカム画面へ戻る | ウインドウを残したまま、本だけ閉じます |
| 毎回確認 | 書き出しが終わるたびに、上の5つがシートで並びます |

意味と動作は、環境設定「閲覧中の動作」の「最後のページで」の同名の項目とまったく同じです([21. 前後の本への移動・同じフォルダのファイルを開く](#21-前後の本への移動同じフォルダのファイルを開く)参照。「次の本」を開くにはそのフォルダへのアクセス権が必要です)。

これらを組み合わせると、**新しい本を開く(自動でレイアウトされる)→ 右クリックから書き出す → 次の本が開く**、という流れを操作なしで繰り返せます。

---

## 16. 画像の書き出し

今ビューアが表示しているページの画像ファイルを、そのまま(または見開きを1枚に結合して)書き出せます。メニューバー「File」の「画像の書き出し」サブメニュー(ライブラリデータの読み込み・書き出しとEPUBの書き出しの間にあります)、または画像表示エリアを右クリックしたコンテキストメニューの「画像の書き出し」サブメニューから使います。

表示していないページも、ページ一覧(サムネイルグリッド)・サイドパネルの「ページ」モード・「本の中身ブラウザ」でサムネイル/行を右クリックし、「画像を書き出す…」から書き出せます(書庫・PDFの中のページのときだけ表示されます。[6.](#6-ページ一覧サムネイルグリッド)・[7.](#7-サイドパネル)参照)。

- **単ページ表示中**: 「このページを書き出す」の1件だけが表示されます。
- **見開き表示中(ファイルメニュー)**: 「右のページを書き出す」「左のページを書き出す」「見開きを結合して書き出す」の3件が並びます。
- **見開き表示中(コンテキストメニュー)**: 右クリックした位置(左半分/右半分)に応じて、「左のページを書き出す」または「右のページを書き出す」のどちらか一方と、「見開きを結合して書き出す」の2件が表示されます。
- 見開きの相方ページが実際には表示されていない場合(横長画像の自動単ページ化など)は、単ページ表示中と同じ扱いになります。

いずれの項目を選んでも、保存先のフォルダとファイル名を指定するウインドウ(NSSavePanel)が開きます。

- 「このページ/右のページ/左のページを書き出す」は、元の画像ファイルをそのまま複製します(再エンコードしないため画質は劣化しません)。既定のファイル名・形式は元のファイルと同じです。
  - PDFの本の場合は、独立した画像ファイルが無いため、そのページに埋め込まれている画像を取り出します。JPEGならそのまま、可逆形式ならPNGとして書き出し、既定のファイル名は「(PDFのファイル名)-(ページ番号).拡張子」になります。
- 「見開きを結合して書き出す」は、見開きを構成する2枚の画像を左右に結合し、新しい1枚の画像として書き出します。
  - 解像度は元の画像を維持しますが、左右で縦の解像度(高さ)が異なる場合は、高さが低い方に合わせて、もう一方をアスペクト比を保ったまま縮小します。
  - どちらを画面上の左右どちらに結合するかは、読み方向(右開き/左開き)に応じて、実際にビューアで表示されている並びのまま反映されます。
  - 書き出すファイル形式(jpg/pngなど)は元の画像に揃えます。左右で形式が異なる場合は、読み順で先のページ(前のページ)の形式に揃えます。
  - 既定のファイル名は「(前のページのファイル名)-(後のページのファイル名).拡張子」です。
- 画像の読み込みや結合、書き込みに失敗した場合は、エラー内容を示すアラートが表示されます。

---

## 17. Finderで開く/情報を見る

### Finderで開く

メニューバー「File」→「Show in Finder」、または画像表示エリアを右クリックしたコンテキストメニューの「Show in Finder」から、今開いている本をFinderで表示します。フォルダの本はそのフォルダを開き、ファイルの本(zip/cbz・rar/cbr・7z/cb7・PDF・EPUB)は親フォルダを開いてそのファイルを選択した状態にします。

今開いている本以外も、次の場所で右クリックすると同じ「Finderで開く」が使えます([6.](#6-ページ一覧サムネイルグリッド)・[7.](#7-サイドパネル)参照)。

| 右クリックする場所 | Finderで表示されるもの |
| --- | --- |
| サイドパネル「ブラウザ」上段(フォルダブラウザ)の行 | その行のフォルダ/ファイルそのもの |
| サイドパネル「ブラウザ」下段(本の中身ブラウザ)の行 | フォルダの本の画像はその画像。書庫の中のものは書庫ファイル自体 |
| サイドパネル「ページ」モードの行、ページ一覧のサムネイル | そのページの画像。書庫・PDFの中のページは書庫/PDFファイル自体 |
| サイドパネル「履歴」モードの行 | その履歴の本 |
| サイドパネル「ブックマーク」上段(お気に入り)の行 | そのお気に入りの本 |

書庫やPDFの中の画像は、Finderからは1つのファイルとして見えないため、入れ物のファイルを表示する動作になります。中の画像そのものが必要な場合は、同じ右クリックメニューの「画像を書き出す…」を使ってください([16. 画像の書き出し](#16-画像の書き出し)参照)。

### 情報を見る

画像表示エリアを右クリックしたコンテキストメニューの「情報を見る」から、今見ているページの画像ファイル情報を、Finderの「情報を見る」のようなパネルでビューアウインドウ内に確認できます。見開き表示中は、右クリックした位置(左半分/右半分)に応じて対象のページが決まります([16. 画像の書き出し](#16-画像の書き出し)のコンテキストメニュー版と同じ考え方)。パネルの外側をクリックすると閉じます。

パネルは以下の2グループと、対象ページのサムネイル(見開き中にどちらのページの情報かを識別するため)で構成されます。

- **一般情報**: 種類、サイズ、場所、名前、作成日、変更日
- **詳細情報**: 解像度、色空間、カラープロファイル、アルファチャンネル

いずれもヘッダーから読み取れる範囲の情報で、取得できない項目は表示されません。フォルダ内の画像・PDFファイルは実際のファイルの作成日・変更日を表示しますが、zip/cbz・7z/cb7アーカイブ内の画像は形式の都合上、更新日のみ(作成日は非表示)になります(rar/cbrは両方表示されます)。PDFのページの場合は、独立した画像ファイルではないため、色空間・カラープロファイル・アルファチャンネル・サイズは表示されず、種類・場所・名前・作成日・変更日(PDFファイル自体のもの)・解像度(ページサイズ、ポイント単位)のみになります。

---

## 18. スライドショー

ツールバーの再生アイコン、gキー、メニューバー「View」→「Slideshow」で開始/停止します。ページを自動的にめくる間隔は環境設定「閲覧中の動作」の「間隔」(0.5〜30秒、0.1秒刻み、既定5秒)で調整できます。スライダーで大まかに合わせてから、値の右の⬆⬇ボタンで0.1秒ずつ詰められます。最後のページまで到達すると自動的に停止します。

---

## 19. 実寸表示ウインドウ

見開き表示中の左右いずれかのページを、原寸大の別ウインドウで表示します。既定では左ページが q、右ページが w に割り当てられています。変更したい場合は環境設定「キーボード」から「左ページを実寸表示」「右ページを実寸表示」に好きなキーを割り当ててください(環境設定「マウス」からマウス操作を割り当てることもできます)。

---

## 20. 拡大鏡

マウスカーソル位置を中心に、画像の一部を円形に拡大表示します。

- **ONにする**: 既定 l キー、メニューバー「View」→「Loupe」、または右クリックメニューの「Loupe」。
- **OFFにする**: 同じ操作をもう一度行うか、画像の上で左クリック・右クリックのどちらかを行うか、esc キーを押します(見開きのどちらのページ上でもかまいません)。
- 表示中は、ツールバー・プログレスバー・サイドパネルの自動表示(マウスを画面端に近づけたときの一時表示)が無効になります。閲覧の妨げにならないよう、既に自動表示されていた場合は拡大鏡を開いた時点で隠れます。
- 割り当てキーは環境設定「キーボード」から変更できます。マウスでは既定で中ボタン(ホイールクリック)に割り当てられており、環境設定「マウス」から変更できます。拡大率・レンズのサイズは環境設定「画像の表示」の「拡大鏡」セクションで調整できます([25.](#25-環境設定)参照)。
- 画面表示用の画像よりも高解像度の画像データを参照して拡大するため、画面をそのまま引き伸ばすよりも精細に表示されます。
- 見開き表示中は、左右のページの境目をまたいでも途切れず拡大できます(2ページ分の高解像度画像を結合したものを参照するため)。

---

## 21. 前後の本への移動・同じフォルダのファイルを開く

ツールバーの上下矢印ボタン、既定 control+c(次)/control+d(前)キー、メニューバー「Move」→「Go to Previous/Next Book」で、今開いている本と同じフォルダ内にある前後の本へ直接移動できます(サブフォルダは対象外)。

メニューバー「File」→「Open File in Same Folder」では、同じフォルダ内のすべての本を一覧でき、選んで開けます。

### 並ぶ順番

既定ではどちらも**名前順**(Finderと同じ並び)です。前後の本への移動はこれに加えて、移動先が現在の本と同じ種類(アーカイブ/PDF/EPUBファイル同士、またはフォルダ同士)に絞られ、種類の異なる本を挟みません。

環境設定「一般」の「本の移動をブラウザの並び順に合わせる」(既定OFF)をONにすると、どちらもサイドパネル上段のフォルダブラウザとまったく同じ並びになります([7. サイドパネル](#7-サイドパネル)参照)。

- 並べ替えの基準(名前・サイズ・種類・作成日・変更日)と昇順・降順は、パネル上部の「並べ替え」ボタンで選んだものがそのまま使われます。フォルダをまとめて上に置くかどうか(環境設定「一般」の「並び順」)も反映されます
- 前後の本への移動は種類による絞り込みをやめ、**パネルに見えているとおりの順**でフォルダの本とファイルの本を混ぜて1冊ずつ辿ります
- 並べ替えを変えると、本を開き直さなくてもその場で反映されます
- 環境設定「一般」の「サイドパネルを有効にする」がOFFのときは、この設定自体がグレーアウトし、並び順は名前順のままになります(並べ替えを変える手段がパネル上部のボタンしか無いため)

最初/最後のページを超えてページ送りしようとしたときの動作は、環境設定「閲覧中の動作」の「最初のページで」「最後のページで」で**別々に**設定します。

ここでいう「前のページへ」「次のページへ」は物語的な向きのことで、右開きの本で画面右のページへ移動する操作なども含みます(読み方向によらず、その本を読み進める向きが「次のページへ」です)。

**最初のページで**「前のページへ」の操作をしたとき:

| 選択肢 | 内容 |
| --- | --- |
| 何もしない(既定) | 最初のページで止まる |
| ループする | 同じ本の最後のページへループする |
| 前の本の最後のページへ | 前の本を開き、必ず最後のページから読む |
| 前の本へ | 前の本を開く(どのページから始まるかは「開始ページ」の設定に従う) |
| 毎回確認 | そのつどシートで、上の4つから選ぶ |

**最後のページで**「次のページへ」の操作をしたとき:

| 選択肢 | 内容 |
| --- | --- |
| 何もしない(既定) | 最後のページで止まる |
| ループする | 同じ本の最初のページへループする |
| 次の本の最初のページへ | 次の本を開き、必ず最初のページから読む |
| 次の本へ | 次の本を開く(どのページから始まるかは「開始ページ」の設定に従う) |
| 本を閉じる | このタブを閉じる(タブが1枚だけならウインドウごと閉じる) |
| ウェルカム画面へ戻る | 本だけ閉じて、同じウインドウにウェルカム画面を表示する |
| 毎回確認 | そのつどシートで、上の6つから選ぶ |

「前の本の最後のページへ」「次の本の最初のページへ」は、その本を以前どこまで読んでいたか(読書位置)や「開始ページ」の設定より優先されます。「毎回確認」のシートは、Escキーやシートの外側のクリックでも閉じられ、その場合は「何もしない」と同じ結果になります。

サンドボックスの制約により、フォルダを開いた場合はこれらの機能がそのまま使えますが、アーカイブ/PDFファイル単体を直接開いた場合は、あらかじめそのフォルダへのアクセスを許可しておく必要があります(詳しくは[26. サンドボックスとアクセス権について](#26-サンドボックスとアクセス権について)を参照)。

画像ファイルを直接開いている場合、前後の本への移動は使えません。同じフォルダの画像をまとめて読みたい場合は、メニューバー「File」→「このフォルダの画像をすべて開く」でフォルダの本として開き直してください([2. 本を開く](#2-本を開く)参照)。開き直した後は前後の本への移動も使えます。

---

## 22. 複数ウインドウ・タブ

メニューバー「File」→「Open in New Window…」(新規ウインドウで開く…)「Open in New Tab…」(新規タブで開く…)から、新しいウインドウ/タブで別の本を開けます。タブバーの「+」ボタンからも、何も開いていない新しいタブ(ウェルカム画面)を追加できます。

本を指定せず、ウェルカム画面の新しいウインドウを開くこともできます。

| メニュー項目 | ショートカット | 開くもの |
| --- | --- | --- |
| 新規ノーマルウインドウ | Cmd+N | 通常の(記録が残る)ウインドウ |
| 新規シークレットウインドウ | Shift+Cmd+N | シークレットウインドウ(下記) |

この2つは、下記の「シークレットモードで起動」の設定に関わらず**常に両方とも表示されます**。どちらを選んだかがそのままウインドウの性質になるため、名前だけでどちらが記録の残るウインドウか分かります。Dock の qooViewer アイコンを右クリックしたメニューからも、同じ2つを選べます。

環境設定「本を開く」の「Finderから」(既に qooViewer が本を表示している状態で、Finder から別の本を開こうとしたときの動作)は次から選べます。

| 選択肢 | 内容 |
| --- | --- |
| 現在の本を閉じて新しい本を開く(既定) | 同じウインドウで新しい本に置き換える |
| 新規タブで開く | 現在のウインドウに新しいタブとして開く |
| 新規ウインドウで開く | 新しいウインドウとして開く |

複数のタブが開いているウインドウを閉じようとすると、環境設定「一般」の「複数タブのウインドウを閉じる前に確認」(既定ON)がONの場合、確認ダイアログが表示されます。確認を経て閉じると、そのウインドウのタブがすべて閉じます。

どの操作で確認が出るかは次のとおりです。

| 操作 | 確認 | 閉じるもの |
| --- | --- | --- |
| 赤い閉じるボタン | 出る | ウインドウ(タブすべて) |
| メニューバー「Window」→「Close Window」 | 出る | ウインドウ(タブすべて) |
| Cmd+W(File →「閉じる」) | 出る | ウインドウ(タブすべて) |
| タブ自体の×ボタン | 出ない | そのタブ1枚だけ |
| マウスに割り当てた「タブを閉じる」 | 出ない | そのタブ1枚だけ |
| マウスに割り当てた「ウインドウを閉じる」 | 出る | ウインドウ(タブすべて) |

マウスに割り当てられる「タブを閉じる」「ウインドウを閉じる」については[4. ページ送りの操作](#4-ページ送りの操作)の「マウス」を参照してください。タブ1枚だけを確認なしで閉じたい場合は、タブの×ボタンかこの「タブを閉じる」を使ってください。

環境設定「一般」の「最後のウインドウを閉じたら終了」(既定OFF)をOFFのままにしておくと、ウインドウをすべて閉じても他の多くの Mac アプリ同様 Dock に常駐したままになります。

### 一覧から開き方を選ぶ

本の一覧では、本を右クリックして開き方を直接選べます。使える場所は、サイドパネルのフォルダブラウザ・履歴・お気に入りの各一覧([7. サイドパネル](#7-サイドパネル)参照)と、「お気に入りの編集」「ブックマーク・レイアウトの編集」ウインドウの本の一覧です。どこでも同じ4項目が同じ並びで表示されます。

| 項目 | 開くもの |
| --- | --- |
| 開く | その一覧をクリック(またはダブルクリック)したときと同じ開き方 |
| 新規ノーマルウインドウで開く | 通常の(記録が残る)ウインドウ |
| 新規シークレットウインドウで開く | シークレットウインドウ(下記) |
| 新規タブで開く | 今のウインドウの新しいタブ |

「新規ノーマルウインドウ」「新規シークレットウインドウ」は、今のウインドウがどちらであっても、また下記の「シークレットモードで起動」の設定に関わらず、**選んだとおりの性質のウインドウ**で開きます。たとえばシークレットウインドウで読んでいる最中でも、「新規ノーマルウインドウで開く」を選べば記録の残るウインドウで開けます。

「新規タブで開く」だけは、**今のウインドウの性質をそのまま引き継ぎます**(シークレットウインドウからならシークレットのタブになります)。1つのウインドウのタブバーに記録の残るタブと残らないタブが混在すると、タイトルバーの「(シークレット)」表示はいちばん手前のタブのぶんしか出ないため、今読んでいる本が記録されるのかどうか見分けられなくなってしまうためです。

### 同じ本を二重に開かない

**同じ本を、同じ種類のウインドウ・タブで二重に開くことはありません。** 開こうとした本が既に開かれている場合は、新しく開く代わりに、その本を開いているウインドウ/タブがそのまま手前に出ます。**表示していたページはそのまま**で、先頭に戻ったり読書位置を読み直したりはしません。

上記の「新規ウインドウ/タブで開く」だけでなく、履歴・お気に入り・フォルダブラウザ・ウェルカム画面の一覧・「開く…」パネル、Finderやドックから渡された本など、本を開く操作全般に当てはまります。

ノーマルウインドウとシークレットウインドウは**別々に数えます**。シークレットウインドウで開いている本をノーマルウインドウで開こうとした場合は、記録の残るウインドウが必要なので新しく開きます(逆も同じです)。

次の3つだけは対象外で、これまでどおり今のウインドウの表示が切り替わります。ページ送りの延長や、そのウインドウの中で完結する操作だからです。

- 前の本/次の本への移動([21. 前後の本への移動・同じフォルダのファイルを開く](#21-前後の本への移動同じフォルダのファイルを開く)参照)
- サイドパネルのフォルダブラウザでフォルダを移動したときの自動表示([7. サイドパネル](#7-サイドパネル)参照)
- メニューバー「File」→「このフォルダの画像をすべて開く」([2. 本を開く](#2-本を開く)参照)

### シークレットウインドウ

メニューバー「File」→「新規シークレットウインドウ」(Shift+Cmd+N)、または Dock アイコンの右クリックメニューから、**開いた本の情報を qooViewer に一切残さない**ウインドウを開けます(Google Chrome のシークレットウインドウと同じ考え方です)。タイトルバーに「(シークレット)」と表示され、ウェルカム画面に説明が出ます。

シークレットウインドウ(および、そこから「新規ウインドウ/タブで開く」で開いたウインドウ/タブ)で本を開いても、次のものは記録・保存されません。

- 「最近開いた本」の履歴、「最後に開いていた本」(起動時の自動再開の対象)
- 読書位置・表示モードなどの読書状態([23. 本の続きから再開する](#23-本の続きから再開する)の対象になりません)
- ブックマーク・お気に入り・ページレイアウト・メタデータの登録や編集、および EPUB/PDF/ComicInfo.xml からのそれらの自動取り込み
- ページサムネイルなどのディスク上のキャッシュ(環境設定「キャッシュ」がONでも保存されません。[25. 環境設定](#25-環境設定)参照)。入れ子の書庫を含む本を2回目以降に速く開くための記録([2. 本を開く](#2-本を開く)の「読み込み中の表示」参照)も作らず、通常のウインドウで作られたものを読むこともしません

このため、シークレットウインドウがアクティブな間は、メニューバー「Edit」のお気に入り/ブックマークの追加、各「編集…」、レイアウト関連の項目と、ツールバー・コンテキストメニュー・サイドパネルの対応するボタンが無効になります。登録済みのデータを**読む**操作(保存済みブックマークへのジャンプ、保存済みレイアウトでの表示、お気に入り一覧から開く)は通常どおり使えます。

お気に入りを開こうとしてファイルが見つからなかった場合、通常のウインドウでは「お気に入りから削除」を選べますが、シークレットウインドウでは見つからなかったことをお知らせするだけで、削除は選べません(削除も保存データへの変更にあたるためです)。

履歴も表示しません。シークレットウインドウでは、ウェルカム画面の「最近開いた本」「最近のお気に入り」、サイドパネルの履歴モード、「File」→「Open Recent」が空になります(通常のウインドウに戻れば元どおり表示されます)。

ファイル自身に含まれる情報は利用できます。EPUB の目次・PDF のアウトライン・ComicInfo.xml のブックマークは一覧・ジャンプに使え、書誌メタデータはツールバーのタイトル表示に、読み方向・見開きの指定は表示に反映されます。いずれもそのウインドウの中だけで保持され、閉じると消えます。

Finder や Dock から開いた本は、シークレットウインドウがいちばん手前にあっても、通常のウインドウで開きます(次の「シークレットモードで起動」がONのときは逆になります)。シークレットウインドウは次回起動時に復元されません。

### シークレットモードで起動

環境設定「一般」→「起動時」の「シークレットモードで起動」(既定OFF)をONにすると、**どの経路で本を開いても既定でシークレットウインドウになります。**

- アプリを通常起動したときの最初のウインドウ
- Finder でのダブルクリック、右クリック→「このアプリケーションで開く」
- Dock の qooViewer アイコンへのドラッグ&ドロップ
- ウインドウへのドラッグ&ドロップ、「開く…」パネル、履歴やお気に入りから開く

記録が残るウインドウで読みたいときは、メニューバー「File」→「新規ノーマルウインドウ」(Cmd+N)、または Dock アイコンの右クリックメニューから開いてください。**「新規ノーマルウインドウ」で開いたウインドウと、そこから派生したタブ/ウインドウだけ**が通常のウインドウとして扱われ、読書位置やブックマークなどが記録されます。逆に、シークレットウインドウから「新規タブ/ウインドウで開く」で開いたものは、この設定に関わらず常にシークレットのままです。

この設定がONの間は、「前回読んでいた本を開き直す」は記録が増えないため意味を持たなくなり、グレーアウトします。すでに記録されていた本も開き直しません。

「シークレットウインドウとは何か」(何が記録されないか)は上の説明のままで、この設定で変わるのは**どちらが既定か**だけです。

---

## 23. 本の続きから再開する

qooViewer は本ごとに、最後に表示していたページ・表示モード(見開き/単ページ)・読み方向・拡大縮小モードを自動的に記憶します。以前開いたことのある本を再度開いたときの動作は、環境設定「本を開く」→「本を再度開いたとき」の「開始ページ」で設定します。

| 選択肢 | 内容 |
| --- | --- |
| 前回表示したページから再開する(既定) | そのまま前回の続きから表示する |
| 常に最初から表示する | 前回どこまで読んでいたかに関わらず、常に最初のページから |
| 前回最後のページを表示した場合は最初から表示する | 最後まで読み終えた本だけ、開き直すとまた最初から読める |
| 問い合わせる | そのつど「前回表示したページから再開しますか?」と確認する |

また「前回読んでいた本を開き直す」(環境設定「一般」、既定OFF)をONにすると、次回 qooViewer を起動したときに、前回終了時にアクティブだった画面が表示していた本を自動的に開きます。ファイルが削除・移動されていたり、前回開いたときから中身が変わっていたりする場合は、安全のため自動では開かず、ウェルカム画面のまま起動します。

なお、本ごとの読書状態・ブックマークを保持しておく本の数には上限があり、環境設定「一般」の「データを保持する本の数」(50〜2000冊、既定500冊)で調整できます。上限を超えると、最後に読んだ時刻が古い本のデータから自動的に削除されます。

---

## 24. フルスクリーン表示

メニューバー「View」→「Enter Full Screen」(macOS標準)、右クリックメニューの「Full Screen」、またはウインドウ右上の緑色のボタンでフルスクリーンにできます。フルスクリーン中はツールバー・プログレスバーが自動的に隠れ、画像を画面いっぱいに表示します。マウスカーソルを画面の上端/下端に近づけると、半透明の状態で一時的に再表示されます(環境設定「外観」の「表示までの時間」を設定している場合は、その時間だけ端に留まってから表示されます)。

環境設定「一般」の「フルスクリーンで起動」(既定OFF)をONにすると、起動直後に自動的にフルスクリーンになります。

---

## 25. 環境設定

環境設定ウインドウ(Cmd+,)は、左のサイドバーで画面を選び、右にその中身を表示する2ペイン構成です(macOSのシステム設定と同じ形式)。サイドバーの12項目は次の4つに区切られています。

| 区切り | 画面 |
| --- | --- |
| (区切りなし) | 一般 / 外観 |
| 本 | 本を開く / 画像の表示 / 閲覧中の動作 / レイアウト |
| 操作 | キーボード / マウス / 表示モード別の操作 |
| 詳細 | キャッシュ / フォルダのアクセス権 / リセット |

最後に開いていた画面は記憶され、次に環境設定を開いたときも同じ画面から始まります。ウインドウはリサイズでき、大きさも記憶されます。

各行は「セクションの見出し(いつの話か) → 項目名(何を決めるか)」の2段構成です。項目名だけでは伝えきれない補足がある項目には、項目名の右に ⓘ が付きます。カーソルを合わせると吹き出しで、クリックするとポップオーバーで補足が表示されます。

値を選ぶドロップダウンは、macOS 26 以降ではシステム設定と同じ描き方(値の文字の右にシェブロンのボタンが付く形)になります。macOS 15 では、押せることが分かるように枠線付きで表示されます。

### 一般

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| 表示言語 | アプリの表示言語(システムに従う/日本語/English) | システムに従う |
| 前回読んでいた本を開き直す | 起動時に、前回終了したときに読んでいた本を開き直す。[23.](#23-本の続きから再開する)参照 | OFF |
| フルスクリーンで起動 | [24.](#24-フルスクリーン表示)参照 | OFF |
| シークレットモードで起動 | ONにすると、どの経路で本を開いても既定でシークレットウインドウになる。[22.](#22-複数ウインドウタブ)参照。ONの間は上の「前回読んでいた本を開き直す」がグレーアウトする | OFF |
| 最後のウインドウを閉じたら終了 | [22.](#22-複数ウインドウタブ)参照 | OFF |
| 複数タブのウインドウを閉じる前に確認 | 複数のタブを開いているウインドウを閉じるときに確認する。[22.](#22-複数ウインドウタブ)参照 | ON |
| データを保持する本の数 | [23.](#23-本の続きから再開する)参照(50〜2000冊) | 500冊 |
| 履歴の保存件数 | 「最近開いた本」の履歴として保持する本の数(10〜200件)。ファイルメニューの「Open Recent」と、サイドパネルの「履歴」モード([7.](#7-サイドパネル)参照)に反映される | 30件 |
| 最近開いた本を表示する | ウェルカム画面での表示。[9.](#9-お気に入り)参照(最大10件) | ON |
| 最近のお気に入りを表示する | ウェルカム画面での表示。[9.](#9-お気に入り)参照(最大10件) | ON |
| 並び順をFinderに揃える | [2.](#2-本を開く)の「ページの並び順」参照。本のページとサイドパネル下段の本の中身ブラウザを、Finderと同じ名前順にする。OFFのときは文字コード順(大文字で始まる名前がすべて先に来る)。切り替えると開いている本もその場で並び替わるが、レイアウトを設定した本は当時の並びのまま残る | OFF |
| サイドパネルを有効にする | [7.](#7-サイドパネル)参照。OFFにするとサイドパネル自体、およびメニューバー「View」→「Hide Side Panel」が非表示になる。OFFの間は下の4項目がグレーアウトする | ON |
| パネルの位置 | [7.](#7-サイドパネル)参照。サイドパネルをウインドウの左右どちらに表示するか。左/右 | 左 |
| 開く・フォルダへ移動をダブルクリックにする | [7.](#7-サイドパネル)参照。サイドパネルの「開く」「移動する」操作をダブルクリックに統一する | OFF |
| 並び順 | [7.](#7-サイドパネル)参照。サイドパネルのフォルダ・ファイルの並び順(上段・下段共通)。並べ替えの基準(名前・サイズ・種類・作成日・変更日)と昇順・降順は、上段のパネル上部にある「並べ替え」ボタンで選ぶ。フォルダを上に/名前順に混在 | フォルダを上に |
| 本の移動をブラウザの並び順に合わせる | [21.](#21-前後の本への移動同じフォルダのファイルを開く)参照。前後の本への移動とメニューバー「File」→「Open File in Same Folder」を、サイドパネル上段のフォルダブラウザと同じ並びにする。OFFのときは名前順 | OFF |

### 外観

アプリの見た目に関する設定をまとめた画面です。アプリ全体のライト/ダーク(外観モード)と、ビューアの背景色、そして「すりガラス」で描かれている6つの面(ウェルカム画面を含む)の見え方を扱います。

この画面は「レイアウト」画面と同じく**2階層**になっています。最初に開くのは「アプリ全体」「ビューア」と、6つの面が並ぶ「パネル」の一覧です。面の行を押すと、**その面の設定だけを載せたページ**へ進みます。面のページには、まずどの面にも共通の「背景」(すりガラスの濃さ・背景色・文字の影など)があり、その下に**その面だけが持つ設定**が続きます ―― ページ一覧パネルならサムネイルの大きさ・間隔・余白・枠の色など、プログレスバーならカーソルを合わせたときに並ぶサムネイルの見え方です。1つのパネルの見た目に関わる設定は、必ず同じページに揃っています。

- 一覧へ戻るには、ウインドウのタイトルバー左端の「‹」を押すか、Cmd+[ を押します。「‹」はどの画面でも表示されていて、戻る先が無いときは押せません。
- 一覧の各行の右側には、その面の「すりガラス 100% · 背景色 0%」といった現在の値と色の見本が出ます。ページを開かなくても、6つの面を見比べられます。
- 環境設定ウインドウを開き直したときは、必ず一覧から始まります。

ツールバー・プログレスバー・サイドパネル・ページ一覧パネルは、その空きスペースを右クリックして「調整…」を選ぶと、この画面を**その面のページを開いた状態**で開けます([3. 画面の基本構成](#3-画面の基本構成)・[6. ページ一覧](#6-ページ一覧サムネイルグリッド)参照)。

| セクション | 項目 | 内容 | 既定値 |
| --- | --- | --- | --- |
| アプリ全体 | 外観モード | アプリ全体をライト外観・ダーク外観のどちらで表示するか(システムに従う/ライト/ダーク)。下記参照 | システムに従う |
| ビューア | 背景色 | ビューアの背景色(黒/ダークグレー/白/セピア/カスタム)。「カスタム」を選ぶと色を指定するダイアログが開く(下記参照) | 黒 |
| パネル | ページ一覧パネル / ツールバー / プログレスバー / サイドパネル / ウェルカム画面 / その他のパネル | 6つの面の一覧。行を押すとその面のページへ進む(下記「すりガラスの面」以降を参照) | ― |

#### 外観モード

macOSのシステム設定(「外観モード」のライト/ダーク/自動)とは**別に**、qooViewerだけの外観を決められます。表示言語の設定と同じ考え方で、システム全体はダークのまま qooViewer だけライトにする(またはその逆)といった使い方ができます。

- **既定は「システムに従う」**で、これまでどおりシステムの設定に合わせて切り替わります。
- 「ライト」「ダーク」を選ぶと、**選んだ瞬間に切り替わります**(再起動は不要です)。ウインドウ・ツールバー・パネル・メニュー・環境設定ウインドウ・ダイアログまで、アプリの中のすべてに反映されます。
- メニューバーそのものや Dock などmacOSが描く部分は、システムの設定のままです(アプリの外側のためです)。
- **ビューアの背景色は、この設定とは独立しています。**「ライト」にしても、上の「ビューア」→「背景色」で選んだ色(既定は黒)のまま絵を表示します。
- システム設定「アクセシビリティ」→「ディスプレイ」→「コントラストを上げる」をONにしている場合は、ライト/ダークを選んでも、その高コントラストの見た目のままになります。

#### すりガラスの面

ページ一覧パネル・ツールバー・プログレスバー・サイドパネル・ウェルカム画面、およびそれ以外の浮かぶ表示 ――「その他のパネル」(「情報を見る」パネル・操作のフィードバック・拡大率の表示)―― は、背後がうっすら透ける「すりガラス」で描かれています(ウェルカム画面だけは、下記の「ウインドウの背後を透かす」をONにしたときにすりガラスになります)。「パネル」の一覧から面を選ぶと、そのページの「背景」セクションに、6つの面で共通の同じ4項目が並びます。ツールバー・プログレスバー・サイドパネル・ウェルカム画面には「ウインドウの背後を透かす」が、ツールバー・プログレスバー・サイドパネルにはさらに「表示までの時間」が加わります(いずれも下記)。

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| すりガラスの濃さ | すりガラスの層そのものの濃さ(0〜100%)。下げるほど背後が透けて見え、0%にするとぼかしが完全に無くなる | 100% |
| 背景色 | すりガラスの上に重ねる色。「変更…」ボタンで色を指定するダイアログが開く | 黒 |
| 背景色の濃さ | 上記の色をどのくらい濃く重ねるか(0〜100%)。100%にすると色で塗りつぶされ、すりガラスは完全に隠れる | 0% |
| 文字の影 | 文字とアイコンに輪郭を付けて、背後が何であっても読めるようにする(なし / 1〜5)。数字が大きいほど輪郭が太くなる | なし |
| ウインドウの背後を透かす | 常に表示している状態やウェルカム画面でも、ウインドウの背後(デスクトップや他のウインドウ)をうっすら透かす。**ツールバー・プログレスバー・サイドパネル・ウェルカム画面にだけある項目**(下記) | OFF |
| 表示までの時間 | 隠しているときに、ウインドウの端へカーソルを近づけてから表示されるまでの待ち時間(0〜2.0秒、0.1秒刻み)。**ツールバー・プログレスバー・サイドパネルにだけある項目**(下記) | 0.0秒 |

**既定値では、これまでの見た目とまったく同じです**(すりガラス100% + 色0% + 影なし + 背後を透かさない = 何も重ねない)。上の3項目(すりガラスの濃さ・背景色・背景色の濃さ)の組み合わせで、「今より透けさせる」「好きな色に染める」「すりガラスをやめて単色にする」のいずれもできます。

#### ページ一覧パネルのサムネイル

「パネル」→「ページ一覧パネル」のページで、「背景」の下にあるセクションです。ページ一覧([6. ページ一覧](#6-ページ一覧サムネイルグリッド)参照)だけに効く設定をまとめてあります。

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| サムネイルのサイズ | [6.](#6-ページ一覧サムネイルグリッド)参照(80〜320pt)。パネル上部のスライダーと共通 | 120pt |
| 横の間隔 / 縦の間隔 | サムネイル同士の間隔(0〜40pt) | 10pt |
| 左右の余白 / 上下の余白 | ウインドウの端からパネルまでに残す余白(画像表示領域に対する片側の%、0〜40%) | 10% / 5% |
| サムネイルの下の表示 | ページ番号 / ファイル名 / 表示しない | ページ番号 |
| 文字の大きさ | 上記の文字の大きさ(8〜20pt)。「表示しない」のときは操作できない | 11pt |
| 表示中のページの枠の色 | アクセントカラー / 赤 / 黄 / 緑 / 白 / 黒 / カスタム | アクセントカラー |
| カーソルを合わせたら拡大プレビューを表示する | [6.](#6-ページ一覧サムネイルグリッド)参照。ページ一覧だけに効く。パネル上部のスイッチと共通 | ON |
| ホイール1ノッチのスクロール行数 | [6.](#6-ページ一覧サムネイルグリッド)参照。ページ一覧の上で物理マウスのホイールを1ノッチ回したときに動く行数(0.5〜5.0行、0.1行刻み)。トラックパッドのスクロールには効かない | 1.0行 |

#### プログレスバーのサムネイル

プログレスバーにカーソルを合わせたときに並ぶサムネイル([3. 画面の基本構成](#3-画面の基本構成)参照)の見え方をまとめたセクションです。「パネル」→「プログレスバー」のページで、「背景」の下にあります。

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| カーソルを合わせたページをプレビュー | [3.](#3-画面の基本構成)参照。OFFにするとサムネイルを出さず、ページ番号だけのシンプルな表示になる。OFFの間は下の6項目がグレーアウトする | ON |
| サムネイルの数 | 一度に並べる枚数(3〜15枚)。バーの幅いっぱいに並ぶため、**少なくするほど1枚が大きくなる** | 9枚 |
| サムネイルの下の表示 | ファイル名とページ番号 / ページ番号 / ファイル名 / 表示しない。カーソル位置のページ番号だけはこの設定に関わらず常に表示される(下記) | ファイル名とページ番号 |
| 文字の大きさ | 上記の文字の大きさ(8〜20pt) | 10pt |
| カーソル位置以外を暗くする | カーソル位置以外のサムネイルを暗くして、カーソル位置のページを目立たせる | ON |
| カーソル位置の強調色 | アクセントカラー / 赤 / 黄 / 緑 / 白 / 黒 / カスタム。カーソル位置のサムネイルの枠・光彩・ページ番号に使う色 | アクセントカラー |
| 強調の太さ | カーソル位置のサムネイルを囲む枠線の太さ(1〜8pt) | 3pt |

- **サムネイルの数**: サムネイルはバーの幅いっぱいに等間隔で並ぶので、枚数と大きさは別々には決められません。**枚数を減らすと、そのぶん1枚が大きくなります**(前後を広く見渡したいなら増やす、1枚1枚をはっきり見たいなら減らす)。1枚が大きくなりすぎて画面を突き抜けないよう、大きさには上限があり、上限に達すると余った幅はサムネイル同士の間隔に回ります。
- **サムネイルの下の表示**: 枚数を増やすとファイル名は潰れて読めなくなるため、出す情報を選べます。**カーソル位置のページ番号(「現在のページ / 総ページ数」)は、この設定に関わらず常に表示されます** ―― プログレスバーにカーソルを合わせる目的そのものが「いまどのページを指しているか」の確認だからです。「ファイル名」を含まない表示にすると、書庫の中のフォルダにある画像でサムネイルの上に出る場所の行([画像が本の中のどこにあるかの表示](#画像が本の中のどこにあるかの表示)参照)も出なくなります。
- **カーソル位置以外を暗くする**: OFFにすると、すべてのサムネイルが同じ明るさで並びます。暗いページの中身まで読み取りたいときに使います。OFFでも、カーソル位置のページは枠・光彩・ページ番号で分かります。
- **カーソル位置の強調色 / 強調の太さ**: カーソル位置のサムネイルを示す枠・光彩・ページ番号の色と、枠線の太さです。「カスタム」を選ぶと、背景色と同じ色指定ダイアログが開きます(下記)。
- **いま開いているページ**には、上記とは別に**白い破線の枠**が付きます。カーソル位置(実線)とは線の種類が違うので、強調色に何を選んでいても見分けられます。設定項目はありません。

サムネイルの枠は、その本のページの縦横比に合わせて自動的に決まります(最初に読み込めた数ページの中央値。ページ一覧と同じ仕組みです)。そのため、ページの左右や上下に余白が残らず、サムネイルが枠いっぱいに表示されます。

#### ウインドウの背後を透かす

すりガラスがぼかして見せるのは、通常は**同じウインドウの中で背後にあるもの**(ページの画像)です。そのため、画像の上に重ならない部分 ―― 隠していない(常に表示の)状態のツールバー・プログレスバー・サイドパネルと、ウェルカム画面 ―― は、これまで透け感の無い不透明な描かれ方でした。「ウインドウの背後を透かす」をONにすると、これらの部分が**ウインドウの背後(デスクトップや他のウインドウ)**をぼかして透かすすりガラスになります。

- **既定はOFFで、これまでの見た目とまったく同じです。** ONにするかどうかは4つの面で別々に選べます。
- ONにした面には、同じページの「すりガラスの濃さ」「背景色」「背景色の濃さ」「文字の影」がそのまま効きます。
- OFFのままでも、常に表示中のツールバー・プログレスバーに「背景色」「背景色の濃さ」が効く点はこれまでどおりです(「すりガラスの濃さ」だけは、OFFの間は浮かべているときにしか効きません)。
- **隠しているときに画像の上へ浮かぶ帯・パネルには、この設定は影響しません。** そちらは従来どおり、背後のページをぼかすすりガラスのままです。
- ページ一覧パネルと「その他のパネル」にはこの項目がありません。どちらも常にページの上へ重ねて表示される面のためです。

#### 表示までの時間

ツールバー・プログレスバー・サイドパネルを隠す設定にしている場合(およびフルスクリーン中のツールバー・プログレスバー)、これらはウインドウの端にマウスカーソルを近づけると一時的に表示されます。「表示までの時間」は、**端に近づけてから実際に表示されるまでの待ち時間**です。

- 既定の**0.0秒**では、これまでどおり端に触れた瞬間に表示されます。
- 0.1〜2.0秒にすると、その時間だけカーソルが端に留まっていたときにだけ表示されます。**待っている間にカーソルを端から離した場合や、メニューバー・他のアプリのウインドウ・ウインドウの外へ抜けた場合は、表示されないまま終わります。** 別のウインドウやメニューバーへカーソルを動かしたいだけなのに、通りすがりで隠していた部分が出てきてしまうのを防ぐための設定です。
- **3つの面で別々に設定できます。** たとえばツールバーだけ1.0秒にして、プログレスバーとサイドパネルは今までどおりすぐに出す、といった使い分けができます。
- **隠れるほうには効きません。** カーソルが端から離れたときは、これまでどおりすぐに隠れます。
- ツールバーとプログレスバーは、これまでどおり**上端・下端のどちらにカーソルを近づけても両方**が表示されます。この設定で変わるのは、それぞれが何秒待ってから出てくるかだけです。
- ページ一覧パネルと「その他のパネル」にはこの項目がありません。どちらもカーソルを端へ近づけて出すものではないためです。

#### 文字の影

「背景色の濃さ」を上げて面を濃い色で塗りつぶすと、**文字やアイコンの色と近くなって読めなくなる**ことがあります。文字の色はアプリの外観(上記の「外観モード」。既定ではmacOSに従います)に従うため、たとえばライト外観では文字が黒なので、背景色を黒・濃さ100%にすると黒地に黒になります。ダーク外観で白・濃さ100%にすると、その逆のことが起きます。

「文字の影」を1以上にすると、文字とアイコンの形に沿った細い輪郭が**反対色**で付き(ライト外観なら白、ダーク外観なら黒)、背後が何色でも読めるようになります。輪郭はぼけません。文字そのものの色は変わりません。

数字は輪郭の太さで、1がいちばん細く(グレーのふちにぎりぎり沿う程度)、5がいちばん太くなります。細い線でできたアイコンは1〜2、本文は3以上が読みやすい傾向にあります。

輪郭が付くのは、面の上に直接置かれている文字とアイコンです。次のものには付きません。

- **検索欄・スライダーなど、自前の背景を持つ部品**。ページ一覧パネルのサムネイルサイズのスライダーだけは、つまみが背景に溶けないよう、輪郭の代わりに薄い溝が付きます。
- **サムネイル画像**。
- **コンテキストメニュー・システムのダイアログ・ポップオーバー**(サムネイルにカーソルを乗せたときの拡大プレビューを含む)。これらは macOS が不透明に描いているため、そもそもこの設定の影響を受けません。

あわせて、**選択中のモードボタンと選択行のハイライトにも縁が付きます**。背景色をアクセントカラーに近い色にすると、これらが地の色に溶けてどれが選ばれているのか分からなくなるためです。

ツールバーとプログレスバーは、メニューバー「View」の「Hide Toolbar」「Hide Progress Bar」がONのとき(およびフルスクリーン中)だけ、画像の上に浮かぶ半透明の帯として描かれます。常に表示している場合は、「ウインドウの背後を透かす」がOFF(既定)ならすりガラスではないため、**「すりガラスの濃さ」は浮かべているときにだけ効きます**(ONにすれば常に表示中にも効きます。上記の「ウインドウの背後を透かす」参照)。「背景色」「背景色の濃さ」はどちらの状態でも効きます。

#### 色を指定するダイアログ

背景色の「カスタム」、枠の色の「カスタム」、すりガラスの面の「背景色」の「変更…」は、いずれも同じダイアログを開きます。

- **上段(パレット)**: グレースケールと色相ごとの色見本が並んでいます。押すと、その色が下段のRGBに読み込まれます。
- **下段(RGB)**: 赤・緑・青のそれぞれをスライダーまたは数値(0〜255)の直接入力で調整できます。右側に調整中の色のプレビューと16進表記(#RRGGBB)が表示されます。
- **「元に戻す」**: ダイアログを開いた時点の色に戻します。
- **OK**で確定、**キャンセル**で破棄します。「カスタム」を選んで開いた場合、キャンセルすると選択もダイアログを開く前の状態に戻ります。

指定した色を後から変更したいときは、「カスタム」をもう一度選んでください(同じダイアログが開きます)。プリセット(黒など)に戻しても指定した色は保存されたままなので、再び「カスタム」を選べばその色に戻ります。

### 本を開く

| セクション | 項目 | 内容 | 既定値 |
| --- | --- | --- | --- |
| 本を再度開いたとき | 開始ページ | [23.](#23-本の続きから再開する)参照。前回のページ/常に最初から/読み終えた本のみ最初から/毎回確認 | 前回のページ |
| 別の本を開く | Finderから | [22.](#22-複数ウインドウタブ)参照。この本を閉じて開く/新規タブ/新規ウインドウ | この本を閉じて開く |
| 別の本を開く | お気に入りから | [9.](#9-お気に入り)参照。「Finderから」と同じ3択 | この本を閉じて開く |
| 見開き表示中のブックマーク | 対象ページ | [8.](#8-ブックマーク)参照。既定の側(読み方向に合わせた側に固定)/毎回確認 | 既定の側 |

「別の本を開く」の2項目は、すでに本を開いているときだけ適用されます。ウェルカム画面から開く場合は、この設定に関わらず常に現在のウインドウで開きます。

### 画像の表示

| セクション | 項目 | 内容 | 既定値 |
| --- | --- | --- | --- |
| 表示 | 既定の表示モード | [5.](#5-表示モード)参照。本を初めて開いたときに使われ、あとから変更した場合はその本にだけ適用される | 画面内に収める |
| 表示 | 画面より小さい画像を拡大する上限 | 画像が画面より小さいとき、最大何%まで拡大して表示するか(100〜800%) | 200% |
| 表示 | トラックパッドのピンチ拡大の上限 | [5.](#5-表示モード)参照。トラックパッドのピンチで拡大できる上限(100〜800%)。100%にするとピンチ拡大が無効になる | 400% |
| 画質 | 補間品質 | 拡大縮小時のなめらかさ(高/標準)。「高」は縮小時にモアレが出にくい | 高 |
| 拡大鏡 | 拡大率 | [20.](#20-拡大鏡)参照(100〜800%) | 250% |
| 拡大鏡 | 直径 | [20.](#20-拡大鏡)参照。レンズの直径(200〜600pt) | 400pt |
| 見開き表示 | 単ページ扱いの閾値(横÷縦) | [5.](#5-表示モード)参照。横÷縦がこの値以上の画像は見開きに組まず1ページとして表示する(0.5〜3.0) | 1.0 |

ビューアの背景色は、アプリの見た目に関する設定をまとめた「外観」画面(上記)にあります。

### 閲覧中の動作

| セクション | 項目 | 内容 | 既定値 |
| --- | --- | --- | --- |
| ページ送り | 最初のページで | [21.](#21-前後の本への移動同じフォルダのファイルを開く)参照。ループする/前の本の最終ページへ/前の本へ/何もしない/毎回確認 | 何もしない |
| ページ送り | 最後のページで | [21.](#21-前後の本への移動同じフォルダのファイルを開く)参照。ループする/次の本の1ページ目へ/次の本へ/本を閉じる/ウェルカム画面へ戻る/何もしない/毎回確認 | 何もしない |
| ページ送り | トラックパッドのフリックでページを送る | [4.](#4-ページ送りの操作)参照 | ON |
| スクロール | 2本指スクロールを反転 | [4.](#4-ページ送りの操作)参照。トラックパッドの2本指スクロールで画像が動く向きを上下・左右とも逆にする | OFF |
| サムネイルプレビュー | 表示までの時間 | 拡大プレビューが出るまでの待ち時間(0〜1.00秒)。ページ一覧・サイドパネル・ブックマーク編集・書き出しウインドウで共通 | 0.35秒 |
| サムネイルプレビュー | プレビューの大きさ | [6.](#6-ページ一覧サムネイルグリッド)参照。拡大プレビューの一辺(440〜800pt)。上と同じくすべての場所で共通 | 440pt |
| スライドショー | 間隔 | [18.](#18-スライドショー)参照(0.5〜30秒、0.1秒刻み) | 5秒 |
| ポインタ | 動かしていないあいだポインタを隠す | しばらく操作がないとマウスポインタを隠す | ON |
| ポインタ | 隠すまでの時間 | 上記がONのときの待ち時間(0.5〜10秒) | 2.0秒 |

**ページ一覧だけに効く設定は、すべて「外観」画面の「パネル」→「ページ一覧パネル」のページにまとめてあります**(サムネイルのサイズ・間隔・余白・サムネイルの下の表示・枠の色、拡大プレビューの表示、ホイールのスクロール行数。先読みだけはメモリに関わるため「キャッシュ」画面)。この画面に残しているのは、拡大プレビューの待ち時間と大きさのように**すべての場所に共通で効くもの**だけです。

同じ理由で、**プログレスバーのサムネイルだけに効く設定も「外観」画面の「パネル」→「プログレスバー」のページにまとめてあります**(表示のON/OFFを含む)。以前はON/OFFだけがこの画面にありました。

### レイアウト

ページレイアウトの自動計算と、本を1冊まるごと書き出すときの既定の振る舞いをまとめた画面です([10. ページレイアウト](#10-ページレイアウト)・[表示している本をその場で書き出す](#表示している本をその場で書き出す)参照)。

この2つが同じ画面にあるのは、書き出しがレイアウトの行き先だからです。このアプリで付けた見開き・ページ順・除外を、他のアプリでも読める形にして持ち出すのが書き出しにあたります。「新しい本を開く → 書き出す → 次の本を開く」という一続きの作業の設定が、この画面にまとまっています。

この画面は「外観」画面と同じく**2階層**になっています。最初に開くのは「レイアウトの保存データが無い本を開いたとき」「形式ごとの書き出し」(EPUB / PDF / CBZ の3行)「表示している本を書き出したとき」の3つで、形式の行を押すと、**その形式の書き出し設定だけを載せたページ**へ進みます。

- 一覧へ戻るには、ウインドウのタイトルバー左端の「‹」を押すか、Cmd+[ を押します(「外観」と同じです)。
- 一覧の各行の右側には、その形式の**保存先**が出ます(「毎回確認」、または決めてあるフォルダ)。ページを開かなくても3つの形式を見比べられます。
- 環境設定ウインドウを開き直したときは、必ず一覧から始まります。

| セクション | 項目 | 内容 | 既定値 |
| --- | --- | --- | --- |
| レイアウトの保存データが無い本を開いたとき | 自動レイアウト | [10.](#10-ページレイアウト)参照。何もしない/1ページ目を単ページに/1ページ目を見開きに | 何もしない |
| 形式ごとの書き出し | EPUB / PDF / CBZ | 3つの形式の一覧。行を押すとその形式のページへ進む(下記) | ― |
| 表示している本を書き出したとき | そのあと | 何もしない/次の本の1ページ目へ/次の本へ/本を閉じる/ウェルカム画面へ戻る/毎回確認 | 何もしない |

#### 形式ごとの書き出し

形式の行を押すと開くページです(見出しは「EPUBで書き出すとき」のように形式名が入ります)。中の並びは書き出しの時間順です(どこへ → 何を書くか → そのあと元の本をどうするか)。3つの形式で同じ項目が並びますが、**値は形式ごとに別々に決められます**。

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| 保存先 | 書き出した本をどこへ置くか。毎回確認/保存先を設定。「保存先を設定」を選ぶとフォルダを選ぶダイアログが開き、その下に選んだフォルダのパスを表示する行(「変更…」ボタン付き)が現れる。**File メニューの書き出しウインドウと、右クリックの「本の書き出し」の両方に効きます** | 毎回確認 |
| 画像ファイルを連番にリネームする | 書き出しオプションの既定値。**EPUB・CBZのページにだけある項目**(PDFはページごとの画像ファイル名という概念を持たないため) | EPUB: OFF / CBZ: ON |
| 除外ページを含める | 書き出しオプションの既定値 | OFF |
| 巻数を ComicInfo の Volume 要素にも書き出す | 書き出しオプションの既定値。**CBZのページにだけある項目**。[15.](#15-cbzの書き出し)参照 | OFF |
| 保存データ | 書き出しが終わった本のページレイアウト・ブックマーク・メタデータ・読書位置をどうするか。何もしない/削除(お気に入りは残ります。取り消せません) | 何もしない |
| 履歴 | 書き出しが終わった本を履歴からどうするか。何もしない/削除(本そのものには触れません) | 何もしない |

書き出しオプションの3項目(連番リネーム・除外ページ・Volume要素)で決めた値は、**書き出しウインドウの「書き出しオプション…」と、右クリックの書き出しシートを開いた直後の値**になります。それぞれの画面のトグルはこれまでどおり残っていて、その1回限りの変更として使えます(既定値そのものは変わりません)。

一覧ページの末尾にある「初期設定に戻す」は、3つの形式のページの設定もまとめて戻し、決めておいた保存先フォルダの記憶も一緒に消します。

### キーボード

[4. ページ送りの操作](#4-ページ送りの操作)を参照してください。

- **キーボード**: 操作ごとにキーを割り当てます。1つの操作に複数のキーを割り当てられますが、1つのキーを割り当てられる操作は1つだけです。すでに他の操作で使われているキーを選ぶと、どの操作に割り当て済みかを知らせる警告が出て、割り当ては行われません(元の操作から無言で奪うことはありません)。
- **初期設定に戻す**: キーボードとマウスの割り当てをすべて既定値に戻します。ほかの設定には影響しません。

この画面の割り当ては、どの表示モードでも同じように働きます。表示モードごとに変えたい操作は「表示モード別の操作」で設定します。

アップデートで操作が増えたときは、その既定のキー/トリガーが**空いていれば**自動的に割り当てられます。すでに別の操作へ割り当てているキーが奪われることはありません。また、既定のキーを自分で外した操作(ほかのキーを割り当てている場合)にも、外したキーが戻ってくることはありません。

### マウス

[4. ページ送りの操作](#4-ページ送りの操作)の「マウス」を参照してください。

- **マウス**: 操作ごとにトリガーを割り当てます。キーボードと同じく、1つの操作に複数のトリガーを割り当てられ、1つのトリガーを割り当てられる操作は1つだけです。＋ボタンを押すと、まず「ボタン・位置」「ボタン・ドラッグの向き」「ホイールの向き」のいずれかを選び、続けて修飾キーを選ぶ2段のメニューが開きます。
- 一覧の末尾にある「タブを閉じる」「ウインドウを閉じる」「qooViewerを終了」は**マウス専用**で、既定ではどれにも何も割り当てていません([4. ページ送りの操作](#4-ページ送りの操作)の「マウス」参照)。
- **初期設定に戻す**: キーボードとマウスの割り当てをすべて既定値に戻します。ほかの設定には影響しません。

この画面の割り当ては、どの表示モードでも同じように働きます。表示モードごとに変えたい操作は「表示モード別の操作」で設定します。

### 表示モード別の操作

[4. ページ送りの操作](#4-ページ送りの操作)の「表示モードごとの設定」を参照してください。

- **表示モード**: 設定する表示モード(横幅に合わせる/横幅に合わせる(単ページ)/拡大縮小しない)を選びます。「画面内に収める」は基本にあたるモードのため、ここには現れません(その設定は「キーボード」「マウス」が持ちます)。
- **キーボード** / **マウス**: 選んでいる表示モードでの割り当てです。ここで設定していない項目は「キーボード」「マウス」の割り当てがそのまま働きます。表示モードによって意味が変わる操作(スクロール系とページ送り系)だけを扱います。
- **スクロール**: 「上/下/左/右へスクロール」1回あたりの移動量(「1回のスクロール量」。既定20pt、5〜200pt)。
- **マウスホイール**: 「スクロールできるとき」で、ホイールを回したときの動作を選びます(スクロールのみ/スクロール(端で横へ)/スクロール+ページ送り/ページ送りのみ)。
- **初期設定に戻す**: 選んでいる表示モードについて、この画面のすべての設定(キーボード・マウス・1回のスクロール量・マウスホイール)を既定値に戻します(ボタン名は全画面共通で、効く範囲はカーソルを合わせると出る説明に書かれています)。ほかの表示モードや「キーボード」「マウス」には影響しません。

### キャッシュ

qooViewerがメモリとディスクに溜め込むもの(読書中のページ画像、入れ子の書庫、ページのサムネイル)を扱う画面です。

#### メモリに残しておくページ画像

読んでいるページの前後のページは、デコード済みのままメモリに残しておき、前後へ戻ったときに即座に表示できるようにしています。その上限を決めるのがこの設定です(100〜2000MB、既定300MB)。

- **開いている本1冊(ウインドウ/タブ1つ)あたり**の上限です。複数の本を同時に開いていれば、そのぶんだけ使います。
- 大きくすると、前後へ戻ったときのページ送りが速くなります。小さくするとメモリの使用量が減りますが、戻ったときに読み込み直す機会が増えます。下の「前後に先読みするページ数」が収まらないほど小さくすると、先読みが無駄になるため、下限は高解像度の見開きがいくつか収まる100MBにしてあります。
- 本を表示したまま変えられます。下げたぶんは、その場でメモリから解放されます。
- 上限は実際のメモリの使用量そのものです。白黒のページはカラーの1/4で数えるので、同じ上限でも白黒の本のほうが多くのページを残せます。

#### メモリに残しておく入れ子の書庫

書庫の中にさらに書庫が入っている本(章ごとに書庫化されているものなど)では、中の書庫は**そのページまで読み進めたときに初めて開き**、最近使ったものをしばらく手元に残しておきます。その置き場の大きさを決めるのがこの設定です(0〜1024MB、既定128MB)。

- 大きくすると、章をまたいで行き来したときの表示が速くなります。小さくすると、メモリと一時ファイルの使用量が減ります。
- **メモリに置けるのはZIP形式(.zip / .cbz)の書庫だけです。** RAR形式(.rar / .cbr)と7z形式(.7z / .cb7)は、読み出しに使っているライブラリがファイルとしての実体を必要とするため、一時ファイルへ書き出すほかありません。その一時ファイルをどれだけ残しておくかも、この設定から決まります。
- 0にすると、ZIP形式もメモリには置かず、常に一時ファイルを使います。
- 本を表示したまま変えられます。下げたぶんはその場で解放され、不要になった一時ファイルもその時点で削除されます。
- 入れ子の書庫を使っていない普通の本では、この設定は何も使いません。

#### 先読み

速くするためにメモリを前もって使う設定です。どちらも上の上限の中に収まる量を決めるものなので、ここにまとめてあります。

| 項目 | 内容 | 既定値 |
| --- | --- | --- |
| 前後に先読みするページ数 | 現在のページの前後それぞれ何ページ分を事前に読み込んでおくか(0〜10)。多いほどページ送りが速くなる代わりにメモリを多く使う | 3ページ |
| 表示中のサムネイルの拡大画像を先読み | [6.](#6-ページ一覧サムネイルグリッド)参照。ページ一覧で見えているサムネイルの拡大プレビュー用の画像を先にデコードしておく(メモリ・CPUを使う)。「外観」→「パネル」→「ページ一覧パネル」→「サムネイル」で拡大プレビューをOFFにしているときは操作できない | OFF |

#### ページのサムネイル

一度表示したページのサムネイルをディスクに保存しておくと、同じ本を開き直したときの表示が速くなります。特に効くのは「ブックマーク・レイアウトの編集」ウインドウのように、本を選び直すたびに読み込みをやり直す画面や、外付けドライブ・ネットワークボリューム上にある本です。そのぶんディスクを消費するため、**既定ではOFF**にしてあります。

| セクション | 項目 | 内容 | 既定値 |
| --- | --- | --- | --- |
| ページのサムネイル | ページのサムネイルをディスクに保存する | ONにするとサムネイルの保存を始める。OFFにすると保存をやめ、**溜まっているぶんもその場で削除する** | OFF |
| ページのサムネイル | 最大容量 | 保存しておく合計の上限(50〜2000MB)。上のトグルがOFFの間はグレーアウトする | 200MB |
| ディスクの使用量 | 現在の使用量 | いま実際に使っている容量。画面を開いたとき、設定を変えたとき、ほかのウインドウから戻ってきたときに測り直される | — |
| ディスクの使用量 | サムネイルのキャッシュを今すぐ削除 | 保存されているサムネイルをすべて削除する(確認ダイアログは出ません)。使用量が0のときは押せない | — |

- 上限を超えそうになると、**最後に見てから最も時間が経ったサムネイルから順に削除されます**。上限を下げたときは、その場で削除が行われます。
- 削除されるのはサムネイルだけです。お気に入り・ブックマーク・読書位置・ページレイアウト設定・メタデータには一切影響しません。消えたサムネイルは、次に必要になった時点でページから作り直されます。
- **この画面のどの操作も、本のファイル自体には触れません。**
- シークレットウインドウで開いた本のサムネイルは、この設定がONでも保存されません([22. 複数ウインドウ・タブ](#22-複数ウインドウタブ)参照)。
- サムネイルにカーソルを合わせたときの拡大プレビューの画像は、この設定がONでもディスクには保存されません(メモリにだけ残します)。1枚あたり数MBになるため、保存するとすぐ上限に達し、本来残しておきたい小さなサムネイルのほうが先に消えてしまうためです。
- サムネイルは macOS のキャッシュ領域に置かれるため、ディスクの空きが逼迫した場合はOSの判断で削除されることがあり、Time Machine のバックアップ対象にもなりません(消えても作り直せるものだけを置いています)。
- **初期設定に戻す**: この画面のすべての設定を既定値(ページ画像300MB・入れ子の書庫128MB、先読み3ページ・プレビューの先読みOFF、ディスクキャッシュOFF・200MB)に戻します。ディスクキャッシュがOFFに戻るため、保存されているサムネイルも削除されます。ほかの画面の設定、お気に入り・ブックマーク・読書位置には影響しません。

### フォルダのアクセス権

[26. サンドボックスとアクセス権について](#26-サンドボックスとアクセス権について)を参照してください。

### リセット

保存データを整理・削除するための画面です。日常的な整理のための「整理」と、データファイルごと作り直す緊急避難用の一括削除の2つがあります。

#### 整理

同じ形の2つのボタンが並びます。どちらも一覧のウインドウを開くだけで、この画面では何も削除されません。ファイル自体にはどちらも一切触れません。

- **保存データの削除…**: qooViewerが保存データを持っている本の一覧を開きます。
- **履歴の削除…**: 開いた本の履歴(ファイルメニューの「Open Recent」とサイドパネルの「履歴」モードに出るもの)の一覧を開きます。履歴が1件も無いときは押せません。

2つのウインドウは同じ形です。「メタデータの編集」「EPUB/PDF/CBZの書き出し」などと同じく、操作するものはタイトルバー(ツールバー)に置かれ、一覧はその下へスクロールして潜り、件数はウインドウ下部の中央に表示されます。

- **チェックボックスで複数まとめて選び、一度に削除できます。** 各行の左端のチェックボックスで選び、タイトルバーの「選択した項目を削除…」を押します。1件も選んでいないときは押せません。
- タイトルバーの**「すべて選択」**は、そのとき**一覧に出ている行だけ**が対象です(絞り込みや検索で隠れている行の選択には触れません)。すべて選択されている間はボタンが点灯します。
- **選択は絞り込みや検索を変えても残ります。** 別々の条件で絞り込みながらチェックを積み上げて、最後にまとめて削除できます。今いくつ選んでいるかは常に下部に表示され、確認ダイアログにも冊数が出ます。
- 各行の右端の「削除」ボタンで、その1件だけを削除することもできます。
- タイトルバー右端は標準の検索欄です(ウインドウを狭くすると虫眼鏡のアイコンに畳まれます)。列の幅は一覧に並ぶ文字列に合わせて自動的に調整され、タイトル行の区切り線のドラッグで自由に変えられます。
- 削除の実行前には必ず確認ダイアログが表示されます。

##### 保存データの削除

検証用に一度開いただけの本など、不要になった保存データを片付けるためのウインドウです。

- 各行には、その本のフルパスと形式のバッジ、元のファイルが今も存在するか、どの種類のデータが登録されているかが表示されます。
- **ファイル**: 「あり」「なし」「不明」の3つで表示されます。「不明」は、そのファイルの場所を確認する権限が無く判定できない場合です(環境設定「フォルダのアクセス権」でそのフォルダを許可するか、外付けボリュームを接続すると判定できるようになります)。外していたボリューム上の本を「消えた」と誤解して削除しないよう、「なし」とは区別しています。
  - ウインドウを開いた直後は「確認中…」と表示され、下部に進捗が出ます。ファイルの確認は一覧の表示とは別に進むため、パスや登録データはすぐ読めます。確認が終わった行から順に「あり」「なし」「不明」へ変わります。外付けドライブやネットワークドライブ上の本が多いと、確認に時間がかかることがあります。
- **保存データ**: お気に入り・ブックマーク・ページレイアウト設定・メタデータのうち、登録があるものだけがアイコンで色付き表示されます。
- タイトルバー左端の絞り込みで「見つからない本のみ」にしてから「すべて選択」→「選択した項目を削除…」とすると、元のファイルが無くなった本の保存データをまとめて片付けられます。
- 削除されるのは、その本についてqooViewerが保存しているデータ(お気に入り・ブックマーク・ページレイアウト設定・メタデータ・読書位置)だけです。

##### 履歴の削除

開いた本の履歴から、任意の項目だけを消すためのウインドウです。

- 各行には、その本のフルパスと形式のバッジが、履歴と同じ並び(新しい順)で表示されます。
- 削除されるのは履歴の項目だけです。その本の保存データ(お気に入り・ブックマーク・ページレイアウト設定・メタデータ・読書位置)は残ります。
- 履歴をすべて消したい場合は、「すべて選択」→「選択した項目を削除…」とします。同じ操作は、メニューバー「File」→「Open Recent」の末尾の「履歴をすべて削除」と、サイドパネルの「履歴」モードのゴミ箱ボタンからも行えます([7. サイドパネル](#7-サイドパネル)参照)。

#### すべてのお気に入り・ブックマーク・レイアウト設定・読書位置を削除

お気に入り・ブックマーク・読書位置(本ごとの最後に読んだページ・表示設定)・ページレイアウト設定・メタデータは、すべて同じ1つのデータファイルに保存されています。アプリの更新などでこのファイルが互換性を失い、開けなくなることに備えた緊急避難用の操作です。

- 画面には「削除されるもの」として、お気に入り(お気に入りフォルダを含む)・ブックマーク・ページレイアウト設定・読書位置・「最近開いた本」の履歴の5つが列挙されています。実行するとこれらがすべて完全に削除されます。
- 「最近開いた本」の履歴と、起動時の自動再開に使う「最後に開いていた本」の記録も一緒に消えます(これらは他とは別の場所に保存されているため、明示的に削除しています)。
- 非常に強力な(取り消せない)操作のため、実行前に必ず確認ダイアログが表示されます。誤ってクリックしてしまっても、確認ダイアログで「キャンセル」を選べば何も削除されません。
- 削除を実行すると、完了を知らせるダイアログの後、qooViewerは自動的に終了します。次に起動したときは、まっさらな状態から始まります。
- ページのサムネイル画像の一時保存(キャッシュ)も一緒に削除されます。これは表示を速くするために保存しているだけのもので、消えても必要になった時点で作り直されます(この保存のON/OFFと上限は「キャッシュ」画面にあります。上記参照)。
- 万一、アプリ自体が起動できなくなるほどデータの互換性が失われた場合は、この画面を開くこともできません。その場合は、起動時に代わりに案内ダイアログが表示され、そこで削除するかどうかを選べます(「削除して続行」を選ぶと、上記と同じ内容が削除された上でそのまま起動します。「削除せずに終了」を選ぶと、データを残したままqooViewerが終了します)。

---

## 26. サンドボックスとアクセス権について

qooViewer はサンドボックス化されたアプリのため、ユーザーが選択パネルやドラッグ&ドロップで直接選んだファイル/フォルダにしかアクセスできません。フォルダを開いた場合はその配下がまとめて許可されるため問題ありませんが、**アーカイブ(zip/cbz・rar/cbr・7z/cb7)やPDFファイル単体を直接開いた場合**、[21.](#21-前後の本への移動同じフォルダのファイルを開く)の「同じフォルダのファイルを開く」「前の本/次の本」機能は、まだアクセス許可のない兄弟ファイルを読もうとするため一覧が空になります。

これを避けるには、環境設定「フォルダのアクセス権」の「フォルダを追加…」から、漫画ファイルを置いているフォルダ(ホームフォルダ、外部ボリューム、ドライブのルートフォルダなど、どこでも指定できます)へのアクセスをあらかじめまとめて許可しておきます。一度許可すれば、以後そのフォルダ配下では次回起動後も含めて常に正しく機能します(セキュリティスコープ付きブックマークとして保存されるため)。

今開いている本のフォルダだけをその場で許可したい場合は、メニューバー「File」→「Open File in Same Folder」→「Grant Access to This Folder…」からも許可できます。

画像ファイルを直接開いた場合も同じ制約がかかります。渡された画像そのものにしかアクセス権が無いため、メニューバー「File」→「このフォルダの画像をすべて開く」を実行すると、必要に応じてそのフォルダへのアクセスを求めるパネルが表示されます。ここで許可したフォルダも、環境設定「フォルダのアクセス権」の一覧に加わります。

「最近開いた本」機能は、開いた時点でアクセス許可を自動的に保存するため、この設定をしなくても次回起動後に問題なく開けます。

環境設定「フォルダのアクセス権」では、許可済みフォルダの一覧確認・取り消し(「−」ボタン)もできます。

---

## 27. 表示言語の切り替え

環境設定「一般」の「表示言語」で、システムに従う/日本語/Englishを切り替えられます。macOS本体の言語設定とは独立して切り替えられ、メニューバー・ツールバー・環境設定画面などアプリ全体の表示に反映されます。

**注意**: ファイルが開けなかった場合などの一部のエラーメッセージは、アプリ内の表示言語設定ではなく、Macのシステム言語設定に従って表示されます(実装上の意図的な簡略化です)。

ライト外観・ダーク外観も同じように、macOS本体の設定とは独立して選べます(環境設定「外観」→「アプリ全体」→「外観モード」。[25. 環境設定](#25-環境設定)の「外観モード」参照)。

---

## 28. トラブルシューティング

**Q. 「同じフォルダのファイルを開く」「前の本/次の本」が空になる/機能しない**

A. アーカイブ/PDFファイルを単体で直接開いた場合に起きる、サンドボックスの制約による既知の挙動です。[26. サンドボックスとアクセス権について](#26-サンドボックスとアクセス権について)を参照し、あらかじめフォルダへのアクセスを許可してください。

**Q. 古い日本語のzipファイルで、ファイル名が文字化けする**

A. zip/cbz内のファイル名の文字コードは自動判定していますが、判定しきれない場合があります。判定精度に関する不具合と思われる場合はお知らせください。

**Q. トラックパッドでスワイプしても反応しない/意図しない方向にページが送られる**

A. 環境設定「閲覧中の動作」の「トラックパッドのフリックでページを送る」の設定と、Mac本体の「システム設定」→「トラックパッド」の「ページ間をスワイプ」の指の本数設定(2本指/3本指/4本指)の組み合わせを確認してください。詳細は[4. ページ送りの操作](#4-ページ送りの操作)を参照してください。

**Q. キーボードショートカットが反応しない**

A. 割り当てたいキーが、環境設定「キーボード」で実際に割り当てられているか確認してください。command キーを含む組み合わせや、Mission Control・入力ソース切替など macOS 標準の操作と衝突する組み合わせ(control+矢印キーなど)は選択肢に含まれていません。

**Q. 起動時に前回のウインドウが復元されない**

A. アプリを正常な手順で終了しなかった場合(強制終了など)は、次回起動時に前回のウインドウ構成は復元されず、通常どおり単一のウインドウから起動します。
