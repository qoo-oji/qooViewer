# macOS の標準との違いの監査(2026-09-26)

UI の作法が macOS の標準(HIG と、Finder・Safari・Preview・TextEdit の実際の振る舞い)と違う箇所の総点検の記録。
きっかけは、ファイルメニューの「閉じる」(⌘W)が、赤い閉じるボタンの差し替えに巻き込まれてタブをすべて閉じていたこと
(下の「この日に直したもの」)。メニューバー / ウインドウ・タブ・ダイアログ・環境設定 / ウインドウの中の操作、の 3 つに
分けてコードとコメント・docs を読んだ。

- **この表はコードを読んだ結果で、実機では確かめていない。** ✔ は、報告のあと手元でコードを読み直して事実を確かめたもの。
- 「意図」は、コードのコメントか docs に理由(ユーザーの判断・過去の不具合・プラットフォームの制約)が書いてあるもの。
  「理由なし」は見つからなかったもの ―― うっかり外れた可能性が高いが、直す前に経緯を確かめること。
- 実害・危険があるもの(1〜5)は同じ日に直した。残りは直すかどうかをまだ決めていない。直したら、その行を「直したもの」へ移す。

## この日に直したもの

| 何を | 以前 | 今 |
|---|---|---|
| ⌘W(ファイル ▸ 閉じる) | タブをすべて閉じ、複数タブなら確認が出た。`performClose` は閉じるボタンを押したのと同じ振る舞いで、差し替えた action(`forceCloseWindow`)が呼ばれていた(AppKit 単体で実測)。コメントは「⌘W はタブ 1 枚」のつもりだった | タブ 1 枚だけ、確認なし。差し替え先の `BookClosingWindowDelegate.closeButtonClicked` が「閉じるボタン上の本物のクリックか」を見分ける(docs/09) |
| 「ウインドウを閉じる」 | ショートカットなし。本を一度も開いていないウインドウではタブ 1 枚しか閉じなかった | ⇧⌘W(Safari と同じ)。どのウインドウでもタブをすべて閉じる |
| 「本を閉じる」という語 | 右クリックでは「ホームへ戻る」、環境設定(最後のページで・書き出し後)では「タブを閉じる」の意味で、同じ語が別の動作だった | 語をやめ、「ホームへ戻る」「タブを閉じる」「ウインドウを閉じる」の 3 つに分けた。名前と動作は `ViewerAction` の同名の操作と一致(docs/14「ホームへ戻る」) |
| 1. 保存データを開けないときの起動時の警告 | 既定のボタン(Return)が「削除して続行」で、Return 1 回で全データが消えた | 既定は「削除せずに終了」、削除のボタンは赤(`hasDestructiveAction`) |
| 2. 最小化したウインドウだけのときの Dock のクリック | `applicationShouldHandleReopen` が false を返すだけで、何も戻らなかった | 1 枚を自分で `deminiaturize`。見えているウインドウがあれば何もせず、何も無ければ新しいウインドウ(docs/09) |
| 3. 作業の途中の ⌘Q | 確認なしで終わり、コピー・圧縮・書き出しが切れた | `RunningWorkRegistry` に数えた作業があれば確認(既定と Esc は「キャンセル」)。ファイルブラウザの操作(並んでいるものも)・本の書き出し・表紙の書き出し・保存データの書き出しと読み込みを数える(docs/09) |
| 4. 「この本の内容が変わっている可能性があります」 | 取り消しの役のボタンが無く、SwiftUI が何もしない「キャンセル」を足していた(Binding の set を捨てているので閉じられない) | 「既存の設定を維持する」を `role: .cancel` にして Esc に当てた。破棄は Return の既定にしない |
| 5. 「開く…」⌘O | 本のウインドウが手前に無いと淡色 | 常に押せる。手前に無ければ新しいウインドウで開く |

## 見つかったもの ―― 実害・危険があるもの

5 件とも同じ日に直した(上の「この日に直したもの」の表の後半)。番号は欠番のまま残す(会話や履歴から番号で指せるように)。

## 見つかったもの ―― ウインドウ・タブ・ダイアログ

| # | 標準と違う点 | 標準 | 区分 |
|---|---|---|---|
| 6 | 「新規タブ」⌘T が無い(タブバーの「+」は、0.5 秒以内にタブへ入らない新しい main ウインドウを閉じる推測で受けている。`ContentView.swift`) | Safari・Finder にある | 理由なし |
| 7 | 起動時のウインドウ(`main`)・⌘N(`normal`)・「新規ウインドウで開く」/ Finder から(`book`)が別々の WindowGroup で、SwiftUI がグループごとに `tabbingIdentifier` を分けるため、「すべてのウインドウを結合」やタブのドラッグでまとめられない(2026-09-26 の実機検証でも結合できなかった)。「新規タブで開く」は `addTabbedWindow` を直に呼ぶので効く | 同じ種類のウインドウは結合できる | `private` を分けるのは意図。残りは大きさの扱いのための分割の副作用 |
| 8 | 「ウインドウを閉じる」⇧⌘W がウインドウメニューにある(`CommandGroup(before: .windowArrangement)`) | Safari・Finder はファイルメニューの ⌘W の下 | ショートカットは意図(2026-09-26)、置き場所は理由なし |
| 9 | ファイルメニューの ⌘W の名前が AppKit の「閉じる」のまま(今はタブ 1 枚を閉じる) | タブがあるときは「タブを閉じる」 | 理由なし |
| 10 | 複数タブを閉じる確認が `NSAlert.runModal()` でアプリ全体を止める。保存パネル・フォルダ選択も約 15 か所が `runModal()`(画像の書き出し、書き出しウインドウ、保存データ・表紙の書き出し/読み込み、メタデータ、自動リネーム、規則の編集、フォルダのアクセス権) | ウインドウに付くシート。ファイルブラウザだけはシート+アプリモーダルの予備にしてある(`FileBrowserOperationViews.swift`) | 理由なし |
| 11 | 使うときだけ開く道具のウインドウ約 13 個(ブックマーク・レイアウトの編集、保存データ・表紙の書き出し/読み込み、EPUB/PDF/CBZ の書き出し、保存データ・履歴の削除、メタデータ、ファイル名の解析、シリーズと巻数の抽出)が、ウインドウメニューに常に並ぶ。`.commandsRemoved()` はお気に入りと自動リネームだけ。保存データ・履歴の削除は「環境設定『リセット』からだけ開く」と書いてあるのに、ここからも開ける | 開いているウインドウだけが並ぶ | 一部は既知(docs/03)、意図とは食い違う |
| 12 | 赤い閉じるボタンの差し替えは本を一度開いたタブにしか付かないので、本を開いていないタブが前面なら確認なしで AppKit の既定の閉じ方になる。アクセシビリティ操作(AXPress)で閉じるボタンを押すと、クリックのイベントが無いのでタブ 1 枚だけ閉じる(`closeButtonClicked` のコメント) | どの経路でも同じ | 後者は既知 |
| 13 | ウインドウの状態の復元が切ってある(本のウインドウの 4 つの WindowGroup に `.restorationBehavior(.disabled)`)ので、⌘Q で開いていたウインドウとタブは戻らない。`applicationSupportsSecureRestorableState` のコメント(「true で大きさと位置が戻る」)は古い。道具のウインドウは切っていないので、そちらだけ戻る可能性がある(未確認) | 開いていたウインドウとタブが戻る | 意図(古い NSWindow の再利用で壊れたウインドウになる不具合の回避。docs/13) |
| 14 | 規則の編集(qooMeta 由来)のボタンが文の形("Reset to the default", "Put it back", "Discard the changes and move on" など)。色の選択・メタデータの編集は確定ボタンが「OK」。エラーの題が「エラー」だけ | 短い動詞。確定ボタンは何をするかの動詞 | 理由なし |
| 15 | 起動時にフルスクリーンにする設定が `NSApp.windows.first` を相手にしていて、違うウインドウを対象にする可能性がある(`ContentView.swift`) | ― | 理由なし |

## 見つかったもの ―― メニューバー

| # | 標準と違う点 | 標準 | 区分 |
|---|---|---|---|
| 16 | ✔ 「すぐに削除…」⌥⌘⌫ と「ここに移動」⌥⌘V が常に見えている。理由は「SwiftUI のメニューバーでは代わりの項目(isAlternate)を作れない」だが、移動メニューの「ライブラリ」は `.modifierKeyAlternate(.option)` で作れている(`FileBrowserGoMenu.swift`)。CLAUDE.md の同じ記述も古い | ⌥ を押している間だけ出る | 意図だが前提が崩れている |
| 17 | 編集メニューから「音声入力を開始」「絵文字と記号」「自動入力」を消している(`NSDisabledDictationMenuItem` ほか+`cleanUpEditMenu()`)。理由は「文字の入力欄が無いから」だが、今は名前の変更・検索・メタデータの入力欄がある(カット/コピー/ペーストは同じ理由で戻した経緯がある) | 残す | 意図だが前提が崩れている |
| 18 | ファイルメニューに「情報を見る」⌘I が無い(ファイルブラウザの右クリックにはある)。「標準のキーがあるものだけメニューに出す」という自分の決まり(docs/09)にも合わない | Finder は ⌘I | 理由なし(右クリックへの追加がメニューの割り振りより後だった) |
| 19 | 名前: 「Finderで開く」(Apple のアプリは「Finderで表示」。「開く」は「開く」コマンドとも紛れる)、移動メニューの「上の階層」(Finder は「内包しているフォルダ」 ―― docs/15 自身がその名で書いている)、「最近開いた本」(標準は「最近使った項目を開く」)、「EPUBの書き出し…」など名詞形が同じメニューの「このページを書き出す…」と混在、英語のメニュー名 "Move"(Finder・Preview は "Go") | 左の括弧内 | 理由なし |
| 20 | 「ツールバーを隠す」「サイドパネルを隠す」がチェック付きの「隠す」項目で、ショートカットも無い | 「表示/隠す」の名前を切り替える。⌥⌘T・⌃⌘S | 理由なし |
| 21 | ホームの表示の「リスト/アイコン」にショートカットが無く、並びも逆 | Finder はアイコン ⌘1 → リスト ⌘2 | 理由なし |
| 22 | 「取り消す」の差し替え(`CommandGroup(replacing: .undoRedo)`)が responder chain を通らないので、入力欄の編集中も「タイプ入力を取り消す」の名前が出ず、メタデータ以外のウインドウの UndoManager に届かない | 手前の欄・ウインドウの取り消しを出す | ファイル操作の取り消しは意図、入力欄の側は理由なし |
| 23 | 「最近開いた本」が空のとき淡色の「(なし)」を出す | AppKit は淡色の「メニューを消去」だけ | 理由なし |
| 24 | ホームの「拡大」が `.keyboardShortcut("+", .command)`。US 配列では ⇧⌘= が要るかもしれない(未確認) | ⌘= も受ける | 未確認 |
| 25 | 「よく使う項目に登録」にショートカットが無い | Finder の「サイドバーに追加」は ⌃⌘T | 理由なし |
| 26 | ヘルプブックが無い(`CFBundleHelpBookName` 無し)。ヘルプメニューの既定の項目は「ヘルプはありません」になるはず(未確認) | ― | 理由なし |

## 見つかったもの ―― ウインドウの中の操作

| # | 標準と違う点 | 標準 | 区分 |
|---|---|---|---|
| 27 | ✔ ファイルブラウザでスペースキーのクイックルックが無い(`QLPreviewPanel` がどこにも無い。docs/15 にも記載なし) | Finder の基本操作 | 理由なし |
| 28 | コレクションの一覧・中身の画面にキー操作が無い(矢印・⌘A・Return/⌘↓ で開く・type-select・Esc/⌘↑/⌘[ で戻る・⌘⌫)。一覧は焦点を持てず、中身は ⌘C のためだけに焦点を持つ。スマートライブラリとファイルブラウザにはある | キーで操作できる | 理由なし(Home メニューにショートカットが無いのは意図だが、格子の中のキーは議論が無い) |
| 29 | 本棚・コレクションの中身・スマートライブラリの格子・ページ一覧で、右クリックしても対象の項目に枠が出ない。対象の決め方は Finder と同じ(選択の外なら 1 件)なのに見えない。サイドパネルには `SidePanelContextMenuHighlight` がある | Finder は対象に枠を描く | 理由なし |
| 30 | コレクションのタイルは編集モード以外で右クリックメニューが出ない(コメントの理由「項目が空の contextMenu は付けない」は当たらない ―― 開く・本を追加・名前の変更などがある)。メニューバーからは編集モードでなくても同じ操作ができる | 常に出る | 理由が合っていない |
| 31 | ビューアに ⌘+ / ⌘- / ⌘0 が無い(拡大はピンチとスマートズームだけ。⌘± はホームの間だけ) | Preview にある | 理由なし |
| 32 | ページ一覧(`ThumbnailGridView`)が Esc で閉じず、矢印キーでも動かない(出ている間は切り替えのキー以外を無視) | Esc で閉じる | 余白のクリックで閉じるのは意図、Esc は理由なし |
| 33 | プログレスバーがクリックで飛ぶだけで、ドラッグで送れない。アクセシビリティの値・調整も無い | スライダーはドラッグで追う | 理由なし |
| 34 | 取り消しがファイルブラウザとメタデータにしか無い。コレクションの削除・本の除外は確認のみ、ブックマーク・履歴の削除は確認も取り消しも無い | ⌘Z で戻せる | 確認なしは意図(編集ウインドウのゴミ箱ボタンに合わせた)、取り消しは議論なし |
| 35 | ブックマークの編集で、名前のクリック 1 回で(選ばれていない行でも)シートの名前変更が開く。コメントは Finder と同じと言う | Finder は選ばれている項目の名前だけ、その場で編集 | 理由なし |
| 36 | ホームのドロップは窓全体で 1 つで、コレクションのタイルの上に落としてもそのタイルに入らない(いつも新しいコレクション)。タイルの強調も無い | Finder・写真はフォルダ・アルバムの上で入る | 理由なし(外へのドラッグがコピーだけなのは意図) |
| 37 | 文言: ダイアログを開くのに「…」の無い「名前を変更」(サイドパネル・お気に入りの整理。ホームは「名前を変更…」)。「N 冊をコレクションから外しますか?」「履歴を消去しますか?」の確定ボタンが「削除」 | ダイアログを開く項目は「…」。確定ボタンは実際の動作の動詞 | 理由なし |
| 38 | ブックマークの名前変更シートの取り消しに `.keyboardShortcut(.cancelAction)` が無く、Esc で閉じない可能性(未確認)。スマートライブラリとファイルブラウザのペインが `onHover` で `NSCursor.push()/pop()` を直に呼んでいて、docs/09 の `hoverCursor(_:)` の決まりに反する(途中で消えるとカーソルが残りうる)。メタデータの表で Tab が次の欄へ移らず編集を終える | ― | 理由なし |
| 39 | スマートライブラリの格子に範囲選択(マーキー)が無い | Finder にある | 理由なし |

## 意図的なもの(記録に理由があるもの)

変えるなら改めてユーザーの判断が要る。

| 何を | 理由の場所 |
|---|---|
| 表示モードの ⌘1〜⌘4(Preview は ⌘0/⌘9/⌘±、Finder は表示の形式) | cooViewer に倣う(`ScalingMode.menuShortcutKey`) |
| 「新規シークレットウインドウ」⌥⌘N(Safari は「新規プライベートウインドウ」⇧⌘N。⌥⌘N は Finder の新規スマートフォルダ)、⌘N は「新規ノーマルウインドウ」 | ⇧⌘N を新規フォルダ(Finder)へ。名前で記録の残るウインドウが分かるように(ユーザーの指示) |
| お気に入り・ブックマーク・レイアウト・メタデータが編集メニューにある。ブックマークの追加に ⌘D が無い | 独立のメニューを統合した(要望)。1046 行あたりのコメントは統合前の「ブックマーク」メニューのまま |
| 移動メニュー・表示メニューの中身が、ビューアとホームで入れ替わる。ショートカットも付け外しする | docs/09「メニューバーのホーム画面の項目」 |
| 編集メニューのレイアウトの群が、左右のページがあるときだけサブメニューになる。画像の書き出しの項目数が 1 か 3 | 読みやすさ(コメント) |
| ビューアのキー(a・g・t・矢印など)がメニューに出ない | キーは差し替えられ、メニューの項目と対応しない(docs/09) |
| 「履歴をすべて削除」(標準は「メニューを消去」)、「ホームフォルダ」(Finder は「ホーム」) | 用語表(docs/01)、「ホーム」メニューとの衝突(docs/09) |
| 環境設定がサイドバー型で大きさを変えられる。各ページに確認なしの「初期設定に戻す」。色の選択が独自のシート | タブが 8 つを超えて名前が省略され始めた(`SettingsView`) |
| 書き出し・読み込みが独立のウインドウ(Preview は書類のシート) | 本を開いているかに関わらず、まとめて書き出す(`LibraryExportWindow`) |
| ⌘O・Finder からの「開く」・お気に入りが今の本と置き換える(システムの「書類を開くときはタブで開く」を見ない) | 以前からの挙動、設定で変えられる |
| 新しいウインドウはキーウインドウの大きさを写して 48pt ずらす。`setFrameAutosaveName` は使わない | Dock の端の不具合(`ContentView`)、ユーザーの意見 |
| コレクションはクリック 1 回で開き、編集モードではクリックが選択の切り替え(iOS 風)。範囲選択は常に足す | 改善要望 5、docs/14「編集モードが決めるのは 3 つだけ」 |
| サイドパネルはクリック 1 回で開き、選択も焦点も持たない | 「クリック操作のみを想定」、ダブルクリックの設定はユーザーの要望 |
| 右クリックの項目は消さずに淡色(HIG は隠すことを勧める)。複数選んだときの「開く」は淡色 | docs/15「淡色の条件」、機能が OFF のときだけ消す(2026-09-23) |
| 検索欄の Esc は焦点を外すだけで文字を消さない | ユーザーの要望(消したければ×ボタン) |
| ファイルブラウザの Return は「開く」(Finder は名前の変更) | docs/15「移動の規則」 |
| ファイルブラウザの衝突の既定が「両方を残す」(Finder は「置き換える」)、「すぐに削除」の既定が取り消し | Return 1 回で既存の項目がゴミ箱へ行かないように(docs/15) |
| 最初/最後のページの「毎回確認」が独自のシート | macOS の確認ダイアログは 3 つを超えるボタンを落とす(`PageBoundaryChoiceSheet`) |
| ツールチップの遅れを 200ms に(`NSInitialToolTipDelay`) | コメント |

ひとつだけ背景が変わったもの: コレクションの「クリック 1 回で開く」と、スマートライブラリの「クリックで選択・ダブルクリックで開く」
(2026-09-22、Finder とファイルブラウザに揃えた)が並んだので、ホームの中で操作の形が 2 通りになっている。
