// このファイルは意図的にほぼ空にしてある。
//
// 以前はここに、ブックマーク一覧画面(BookmarkListView)専用の並べ替え基準
// (ページ番号/名前/追加日時、計6種類)を表す BookmarkSortOption という列挙を定義していた。
//
// その後、「ブックマークの編集」ウインドウをお気に入りの編集ウインドウと同じ2ペイン構成の
// 独立ウインドウへ作り直し、すべての本を横断してブックマークを編集できるようにした際、
// 並べ替え基準も「お気に入りの編集」画面と完全に揃える(名前・追加日時・更新日時の3種類、
// ページ番号ソートは廃止)ことになったため、専用の列挙を維持する意味が無くなった。
// 現在はFavoritesSortOption(FavoritesSortOption.swift)をそのまま流用している
// (BookmarkStore.swift参照)。
//
// このファイル自体を削除できれば削除するのが望ましいが、パッチの都合上ファイルの削除・
// リネームができないため、参照する型を持たない空のファイルとして残している。実際に
// プロジェクトへ適用する際は、このファイルを手動で削除して問題ない。
